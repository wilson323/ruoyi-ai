# 全局梳理 — 2026-09-17 续轮（给审计表补自动化检查）

> **owner 原指令**：基于剩余事项结合产品文档系统性梳理分析深度思考反思完整实现
>
> **本轮定位**：上一轮 `docs/全局梳理-2026-09-17.md` 已交付工程域四件事（DDL 门禁修补 / ai_doc_embeddings 补迁移 / R30+ CI 接入 / 治理清单过时快照登记）。本轮**继续向前一步**——读产品说明书 `docs/开发说明/开发说明书.md` 的合规章节，找出 R-NEW 反思报告没识别的一条合规盲点。

---

## 一句话总结

给审计表 `audit_logs` 的「业务账号只能加新行、不能改不能删」这条合规规矩，补了自动化检查。

## 为什么这件事重要

产品说明书 §12.1 写明这条规矩——`ipd_app` 对 `audit_logs` 只能 INSERT，UPDATE/DELETE 应被 SQL 错误码 1142 拒绝。违反它审计链就被破坏，是合规红线。

但仓库原本没有任何自动化检查验证它。9-17 那次 R-NEW 全局反思报告也没识别这条盲点——反思只盯工程类问题（实现 bug / 配置漂移），没盯合规类（审计/权限）。

## 本轮落地的三个文件（加起来解决了「没有自动化检查」这个问题）

| 文件 | 用途 |
|---|---|
| `docs/script/sql/update/2026-09-17-ipd-braud01-audit-grant-restrict.sql` | SQL 文件，给 DBA 在真库执行用：把审计表的库级写权限从业务账号手里收回来，含自校验和回滚预案 |
| `scripts/ci/check-braud01-audit-grant.sh` | 自动化检查脚本——扫仓库里有没有这条收回权限的 SQL，没有就报错 |
| `.github/workflows/braud01-audit-grant.yml` | CI 触发：以后改任何 SQL 文件都会自动跑这个检查 |

## 怎么验证脚本不是吓唬自己

把第一个 SQL 文件临时移走，脚本立刻报错说「缺 SQL」；加回来脚本立刻通过。**反向证明**——脚本真的会抓住违规，不是只看一眼说"通过"。

## 没碰的东西

业务代码（`src/main`）、maven 测试、服务器重启、前端仓、真库数据——都没碰。

## 留给下轮两件事及原因

1. **DBA 在维护窗口跑这个 SQL 文件**——真库执行权限不在我手上，我能给文件但决定不了什么时候跑。apply 后 DBA 要再跑一次检查脚本，确认真库里权限真的归零了。
2. **owner 拍板 DEF-5 看板卡翻完成**——那张卡现在 inreview 状态（task_id=`081fbd58-de76-4222-a7ae-22b59faa464f`），我只能推不能翻，得 owner 决定要不要从 inreview 推到 done。

## 三个教训

1. **R-NEW 反思报告只盯工程类问题**：没盯合规类（审计/权限）。下次全局梳理应该加一节「产品说明书合规线扫描」。
2. **bash 正则踩坑**：匹配单引号别用 `\x27` 十六进制转义——bash POSIX 不识别，直接写字面 `'` 就好（本轮踩了 3 次坑才反应过来）。
3. **治理报告里的 SQL 草稿不算闭环**：得正式化到 `docs/script/sql/update/` 体系 + 加自动化检查 + 接 CI 才算完整。光 SQL 文件没自动化检查，下一个会话可能随手把 SQL 删掉也没人发现。

## 与上一轮差异

| 维度 | 上一轮（工程域）| 本轮（产品域）|
|---|---|---|
| 真问题 | 4 项（DDL 门禁误报 / ai_doc_embeddings 漏迁移 / R30+ CI 缺失 / 治理清单过时）| 1 项（审计表合规线自动化检查缺口）|
| 深度 | 工程域微调 | **接通 DCL 草稿→生产 DDL→静态门禁→CI 闭环**，深度更深 |
| 反思盲点 | R-NEW §5.3「check_cross_repo_contract.sh 未接入 CI」实际 9-09 已闭环 | R-NEW 反思只覆盖工程域，未系统盘点产品说明书级红线 |

## commit 哈希（方便查证）

- `c86e20a5` — 业务红线落地（3 文件 / +330 行）
- `fab158e7` — 治理留档（log.md + 本汇总 / +184 行）