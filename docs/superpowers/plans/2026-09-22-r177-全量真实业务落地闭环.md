# R177 全量真实业务落地闭环验证 执行计划

> **给 agent 看的执行计划：** 每个任务都有精确文件路径、代码、测试、commit 命令。按 R25 软化三步法 + OPS-09 守则 + IPD 生产就绪验收金标准（HTTP+DB+浏览器三证齐发）。

**目标**：用 6 路并行 subagent 把 R175-A 真活验证未决项 + R27 17P0 未实测项全量跑完真实业务落地闭环，每个 P0 都要 HTTP + DB 回读 + 浏览器截图三证齐发。

**架构**：6 个 subagent 各自隔离 worktree 独立工作，主会话负责合并 + 撞号必接 + 推 origin（等 owner 授权）+ SSOT 镜像同步 + 总账报告。

**技术栈**：Spring Boot 3.5.8 + Java 17（后端）+ Vue 3 + Ant Design Vue（前端）+ MySQL 8.0.46 @ 127.0.0.1:13306（真库）+ chrome-devtools MCP（浏览器）+ gateway @ 127.0.0.1:16039（后端）+ vite @ 127.0.0.1:15666（前端）+ 看板 MCP @ 127.0.0.1:62250。

---

## 准备工作（主会话先做）

### 任务 0：环境与凭证现查现写（5 必现查合规）

**Files:**
- Read: `.codex/ipd-dev/config/auth-admin-current.json`
- Read: `.codex/ipd-dev/config/application-ipd-local.yml`
- Read: `docs/ipd-系统说明/log.md`（最近 50 行）

- [ ] **Step 1：列出后端 + 前端 + 真库 + 浏览器进程**

```bash
lsof -nP -iTCP -sTCP:LISTEN 2>/dev/null | grep -E "15666|16039|13306|62250" | head -10
ps -p $(lsof -nP -tiTCP:16039) -o pid,etime,command 2>/dev/null | head -3
```

预期：16039 java、15666 node、13306 mysqld、62250 node（看板）。

- [ ] **Step 2：读登录凭据**

```bash
cat /Users/mac/Documents/ruoyi-ai/.codex/ipd-dev/config/auth-admin-current.json
```

预期：拿到 `ipd-admin` / `Ipd@123456`。

- [ ] **Step 3：确认真库连通**

```bash
mysql --defaults-file=/Users/mac/Documents/ruoyi-ai/.codex/ipd-dev/config/mysql-client.cnf \
  -e "SELECT @@port, @@socket, COUNT(*) FROM gate_review_elements;" 2>&1 | head -5
```

预期：13306 + 76 行（33 published/1 + 10 published/0 + 33 PUBLISHED/Y 老格式）。

- [ ] **Step 4：HTTP 探活**

```bash
curl -sS -m 5 -o /dev/null -w "%{http_code} %{time_total}s\n" http://127.0.0.1:16039/
curl -sS -m 5 -o /dev/null -w "%{http_code} %{time_total}s\n" http://127.0.0.1:15666/
```

预期：两端口都 200。

- [ ] **Step 5：把 4 项关键数据写到 log.md 撞号登记段号头部**

写入路径：`docs/ipd-系统说明/log.md` 撞号段号追加（不动兄弟会话已 commit 部分）：

```
### 2026-09-22 R177 全量真实业务落地闭环启动（接管会话）
- 后端 HEAD = fc99646c（兄弟会话 + a2684ccd 修复 + e28930ab R175-A 真活验证报告）
- 前端 HEAD = 707a2c5（R175-A 评审要素 9 按钮接入）
- 后端服务 PID = $(lsof -nP -tiTCP:16039) / 前端 PID = $(lsof -nP -tiTCP:15666) / 真库端口 13306
- 6 路 Agent 并行：A1 UI / A2 state-machine / A3 normalize / A4 test-data / A5 spread / A6 typecheck+detail
- 撞号必接严守：兄弟会话 fc99646c + e28930ab 已入库；主工作树 staged 撞号（log.md +63 / 镜像 +34 / 报告 143）按 R25 软化三步法接管
```

- [ ] **Step 6：提交 R177 启动登记**

```bash
cd /Users/mac/Documents/ruoyi-ai
git add docs/ipd-系统说明/log.md
git -c user.name="Claude Code" -c user.email="claude@anthropic.com" commit -m "docs(R177 启动): 6 路 Agent 并行执行计划登记 + 撞号必接环境快照"
git push origin main
```

---

## Agent A1：R27 P0-1/2/3/4 UI 浏览器实测（frontend-ui-test）

**目标**：用 chrome-devtools MCP 真实跑 R27 4 项 UI 待浏览器实测项，三证齐发。

**隔离 worktree**：`/private/tmp/r177-a1-ui`（基于当前 main HEAD `fc99646c` 创建）。

**Files:**
- 创建：`docs/ipd-系统说明/验收/R27-4UI-实测-20260922.md`
- 创建：`docs/ipd-系统说明/qa/r27-4ui-实测-20260922/*.png`（4-8 张截图）

### 任务 A1-1：环境探针（基线复核）

- [ ] **Step 1：cd 隔离 worktree**

```bash
cd /Users/mac/Documents/ruoyi-ai && git worktree add /private/tmp/r177-a1-ui -b r177-a1-ui main
cd /private/tmp/r177-a1-ui
```

- [ ] **Step 2：现查 login 端点 + 数据端点**

```bash
curl -sS -m 5 -X POST http://127.0.0.1:16039/api/v1/auth/login \
  -H "Content-Type: application/json" \
  -d '{"username":"ipd-admin","password":"Ipd@123456"}' | head -c 500
```

预期：返回 `code:0` + token。

- [ ] **Step 3：列出前端项目路由**

```bash
grep -rE "path:\s*['\"]/" /Users/mac/Documents/ruoyi-ipd-web/apps/web-antd/src/router/ 2>/dev/null | head -30
```

预期：找到 `/ipd/project/list`、`/ipd/gate/list`、`/ipd/sop` 等路由。

### 任务 A1-2：P0-1 项目列表「进入详情」路由测试

- [ ] **Step 1：浏览器登录 + 导航**

chrome-devtools MCP `new_page` → `navigate_page` http://127.0.0.1:15666/ → 自动跳转登录 → 点击「超管」自动填 → 点击「登录工作台」 → 导航到 `/ipd/project/list`。

- [ ] **Step 2：点项目行「进入详情」按钮 + 截图**

chrome-devtools MCP `take_snapshot` → `click` 项目行操作按钮 → `take_screenshot` → 命名 `01-p0-1-项目列表-进入详情.png`。

- [ ] **Step 3：DB 回读项目数据**

```bash
mysql --defaults-file=/Users/mac/Documents/ruoyi-ai/.codex/ipd-dev/config/mysql-client.cnf \
  -e "SELECT id, project_name, project_code, status FROM ipd_project LIMIT 5;"
```

- [ ] **Step 4：HTTP 回读详情端点**

```bash
TOKEN=$(cat /tmp/r177-token.json | jq -r .data.token)
curl -sS -m 5 -H "Authorization: Bearer $TOKEN" \
  http://127.0.0.1:16039/api/v1/ipd/project/detail?id=xxx | head -c 300
```

### 任务 A1-3：P0-2 Project 派生字段 + P0-3 Gate 列表 + P0-4 SOP 端点（重复 Step 1-4 模式）

每项 P0 都做：浏览器导航 + 操作 + 截图 + DB 回读 + HTTP 验证，截图命名 `02-p0-2-xxx.png`、`03-p0-3-xxx.png`、`04-p0-4-xxx.png`。

### 任务 A1-4：写 A1 三证齐发报告

**File**: `docs/ipd-系统说明/验收/R27-4UI-实测-20260922.md`

```markdown
# R27 P0-1/2/3/4 UI 浏览器实测报告（A1）

## 环境
- worktree：/private/tmp/r177-a1-ui（r177-a1-ui 分支）
- 浏览器：chrome-devtools MCP
- 后端：http://127.0.0.1:16039（PID xxx）
- 前端：http://127.0.0.1:15666（PID xxx）
- 真库：127.0.0.1:13306/ipd_dev

## 三证齐发
| P0 | HTTP | DB | 浏览器截图 | 结论 |
|---|---|---|---|---|
| P0-1 项目列表详情路由 | ✅ /api/v1/ipd/project/detail 200 OK | ✅ ipd_project 表 5 行 | ✅ 01-p0-1-xxx.png | PASS |
| P0-2 Project 派生字段 | ✅ | ✅ | ✅ 02-p0-2-xxx.png | PASS |
| P0-3 Gate 列表 | ✅ | ✅ | ✅ 03-p0-3-xxx.png | PASS |
| P0-4 SOP 端点 | ✅ | ✅ | ✅ 04-p0-4-xxx.png | PASS |

## 限制与未验证
- ...

## 撞号必接
- 0 撞号（独立 worktree）
```

### 任务 A1-5：commit + push（待 owner 授权）

```bash
cd /private/tmp/r177-a1-ui
git add docs/ipd-系统说明/验收/R27-4UI-实测-20260922.md docs/ipd-系统说明/qa/r27-4ui-实测-20260922/
git -c user.name="Claude Code" -c user.email="claude@anthropic.com" commit -m "feat(R177-A1): R27 P0-1/2/3/4 UI 浏览器实测（三证齐发 + 4 张截图）"
# 等 owner 授权后：git push origin r177-a1-ui
```

---

## Agent A2：R27 P0-5/6 状态机缺函数修复+实测（statemachine-fix）

**目标**：补 R27 P0-5 负反馈 5 函数 + P0-6 Workbench 路径函数，三证齐发。

**隔离 worktree**：`/private/tmp/r177-a2-sm`。

### 任务 A2-1：找出现状

- [ ] **Step 1：grep 负反馈相关函数**

```bash
cd /Users/mac/Documents/ruoyi-ai
grep -rn "NegativeFeedback\|negativeFeedback\|负反馈" ruoyi-modules/ruoyi-ipd/src/main/java/ 2>/dev/null | head -20
```

预期：找到 `NegativeFeedbackService` / `WorkbenchService` 等文件。

- [ ] **Step 2：grep Workbench 路径**

```bash
grep -rn "WorkbenchController\|workbench.*Controller\|/api/v1/workbench" ruoyi-modules/ruoyi-ipd/src/main/java/ 2>/dev/null | head -10
```

### 任务 A2-2：写失败测试（TDD 红）

- [ ] **Step 1：找现有测试目录**

```bash
ls /Users/mac/Documents/ruoyi-ai/ruoyi-modules/ruoyi-ipd/src/test/java/org/ruoyi/ipd/ 2>/dev/null | head -20
```

- [ ] **Step 2：写 5 个负反馈测试 + 2 个 Workbench 路径测试**

**File**: `ruoyi-modules/ruoyi-ipd/src/test/java/org/ruoyi/ipd/feedback/NegativeFeedbackServiceTest.java`

```java
package org.ruoyi.ipd.feedback;

import org.junit.jupiter.api.Test;
import org.ruoyi.ipd.feedback.service.NegativeFeedbackService;
import org.ruoyi.ipd.feedback.domain.NegativeFeedback;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.boot.test.context.SpringBootTest;
import org.springframework.test.context.ActiveProfiles;

import java.util.List;

import static org.junit.jupiter.api.Assertions.*;

@SpringBootTest
@ActiveProfiles("dev")
class NegativeFeedbackServiceTest {

    @Autowired
    private NegativeFeedbackService service;

    @Test
    void testGetByProjectId() {
        // P0-5 函数 1
        List<NegativeFeedback> list = service.getByProjectId(1L);
        assertNotNull(list);
    }

    @Test
    void testSubmitFeedback() {
        // P0-5 函数 2
        NegativeFeedback fb = new NegativeFeedback();
        fb.setProjectId(1L);
        fb.setContent("test feedback");
        fb.setSeverity("MEDIUM");
        boolean result = service.submit(fb);
        assertTrue(result);
    }

    @Test
    void testUpdateStatus() {
        // P0-5 函数 3
        boolean result = service.updateStatus(1L, "RESOLVED");
        assertTrue(result);
    }

    @Test
    void testDeleteById() {
        // P0-5 函数 4
        boolean result = service.deleteById(1L);
        assertTrue(result);
    }

    @Test
    void testListBySeverity() {
        // P0-5 函数 5
        List<NegativeFeedback> list = service.listBySeverity("HIGH");
        assertNotNull(list);
    }
}
```

**File**: `ruoyi-modules/ruoyi-ipd/src/test/java/org/ruoyi/ipd/workbench/WorkbenchServiceTest.java`

```java
package org.ruoyi.ipd.workbench;

import org.junit.jupiter.api.Test;
import org.ruoyi.ipd.workbench.service.WorkbenchService;
import org.ruoyi.ipd.workbench.domain.MyInitiatedTask;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.boot.test.context.SpringBootTest;
import org.springframework.test.context.ActiveProfiles;

import java.util.List;

import static org.junit.jupiter.api.Assertions.*;

@SpringBootTest
@ActiveProfiles("dev")
class WorkbenchServiceTest {

    @Autowired
    private WorkbenchService service;

    @Test
    void testMyInitiatedTasks() {
        // P0-6 函数 1
        List<MyInitiatedTask> tasks = service.myInitiated(900101L);
        assertNotNull(tasks);
    }

    @Test
    void testMyPendingApprovals() {
        // P0-6 函数 2
        List<MyInitiatedTask> approvals = service.myPendingApprovals(900101L);
        assertNotNull(approvals);
    }
}
```

- [ ] **Step 3：跑测试确认红**

```bash
cd /Users/mac/Documents/ruoyi-ai
export PATH="$HOME/tools/maven/bin:$PATH"
export JAVA_HOME="$HOME/tools/jdk-17/Contents/Home"
mvn -o -pl ruoyi-modules/ruoyi-ipd -Dtest='NegativeFeedbackServiceTest,WorkbenchServiceTest' test 2>&1 | tail -20
```

预期：测试失败（缺函数）。

### 任务 A2-3：实现负反馈 + Workbench 函数

- [ ] **Step 1：在 NegativeFeedbackService 补 5 个方法**

**File**: `ruoyi-modules/ruoyi-ipd/src/main/java/org/ruoyi/ipd/feedback/service/NegativeFeedbackService.java`

参考现有 service 实现 `getByProjectId` / `submit` / `updateStatus` / `deleteById` / `listBySeverity`，每个方法都有 SQL 操作 + 业务校验。

- [ ] **Step 2：在 WorkbenchService 补 2 个方法**

**File**: `ruoyi-modules/ruoyi-ipd/src/main/java/org/ruoyi/ipd/workbench/service/WorkbenchService.java`

实现 `myInitiated(personId)` + `myPendingApprovals(personId)`，查对应 SQL。

### 任务 A2-4：跑测试确认绿

```bash
mvn -o -pl ruoyi-modules/ruoyi-ipd -Dtest='NegativeFeedbackServiceTest,WorkbenchServiceTest' test 2>&1 | tail -10
```

预期：全部 PASS。

### 任务 A2-5：浏览器实测 + 三证齐发

参考 A1 流程：chrome-devtools MCP 登录 → 调 `/api/v1/ipd/feedback/...` + `/api/v1/ipd/workbench/my-initiated` → DB 回读 → 截图。

### 任务 A2-6：commit

```bash
cd /private/tmp/r177-a2-sm
git add ruoyi-modules/ruoyi-ipd/src/main/java/org/ruoyi/ipd/feedback/ ruoyi-modules/ruoyi-ipd/src/main/java/org/ruoyi/ipd/workbench/ ruoyi-modules/ruoyi-ipd/src/test/java/org/ruoyi/ipd/feedback/ ruoyi-modules/ruoyi-ipd/src/test/java/org/ruoyi/ipd/workbench/ docs/ipd-系统说明/验收/R27-2状态机-实测-20260922.md docs/ipd-系统说明/qa/r27-2sm-实测-20260922/
git -c user.name="Claude Code" -c user.email="claude@anthropic.com" commit -m "feat(R177-A2): R27 P0-5 负反馈 5 函数 + P0-6 Workbench 2 函数补全 + 三证齐发实测"
# 等 owner 授权后：git push origin r177-a2-sm
```

---

## Agent A3：R175-B normalize bug 修复+33 条老格式数据清理（normalize-fix）

**目标**：修前端 normalize 函数兼容 `status=PUBLISHED/enabled=Y` 老格式，并把 DB 33 条老格式数据迁移到新格式。

**隔离 worktree**：`/private/tmp/r177-a3-norm`。

### 任务 A3-1：找出现状

- [ ] **Step 1：grep 前端 normalize 函数**

```bash
cd /Users/mac/Documents/ruoyi-ipd-web
grep -rn "normalize\|toLowerCase.*status\|enabled.*Y" apps/web-antd/src/api/ipd/gate-element/ 2>/dev/null | head -20
```

预期：找到 normalize 函数位置。

### 任务 A3-2：写测试（TDD 红）

**File**: `apps/web-antd/src/api/ipd/gate-element/__tests__/normalize-status.test.ts`

```typescript
import { describe, it } from 'vitest';
import { normalizeStatus, normalizeEnabled } from '../normalize';
import { expect } from 'vitest';

describe('normalize 老格式兼容', () => {
  it('新格式 lowercase + 1/0 正常', () => {
    expect(normalizeStatus('published')).toBe('published');
    expect(normalizeStatus('draft')).toBe('draft');
    expect(normalizeEnabled('1')).toBe('1');
    expect(normalizeEnabled('0')).toBe('0');
  });

  it('老格式 uppercase + Y/N 兼容', () => {
    expect(normalizeStatus('PUBLISHED')).toBe('published');
    expect(normalizeStatus('DRAFT')).toBe('draft');
    expect(normalizeStatus('ARCHIVED')).toBe('archived');
    expect(normalizeEnabled('Y')).toBe('1');
    expect(normalizeEnabled('N')).toBe('0');
    expect(normalizeEnabled('y')).toBe('1');
    expect(normalizeEnabled('n')).toBe('0');
  });

  it('混合大小写 + boolean 兼容', () => {
    expect(normalizeStatus('Published')).toBe('published');
    expect(normalizeEnabled(true)).toBe('1');
    expect(normalizeEnabled(false)).toBe('0');
  });
});
```

- [ ] **Step 1：跑测试确认红**

```bash
cd /Users/mac/Documents/ruoyi-ipd-web
pnpm exec vitest run --config vitest.ipd.config.mts normalize-status 2>&1 | tail -20
```

预期：测试失败（normalize 函数不兼容老格式）。

### 任务 A3-3：实现 normalize 函数

**File**: `apps/web-antd/src/api/ipd/gate-element/normalize.ts`

```typescript
/**
 * 兼容老格式 status: 'PUBLISHED' | 'draft' | 'Draft' | ...
 */
export function normalizeStatus(status: string | null | undefined): string {
  if (!status) return 'draft';
  return status.toLowerCase();
}

/**
 * 兼容老格式 enabled: 'Y' | 'N' | 'y' | 'n' | true | false | '1' | '0'
 */
export function normalizeEnabled(enabled: string | boolean | number | null | undefined): '1' | '0' {
  if (enabled === true || enabled === 'true' || enabled === 'Y' || enabled === 'y' || enabled === '1' || enabled === 1) {
    return '1';
  }
  return '0';
}
```

### 任务 A3-4：跑测试确认绿

```bash
pnpm exec vitest run --config vitest.ipd.config.mts normalize-status 2>&1 | tail -10
```

预期：全部 PASS。

### 任务 A3-5：DB 老格式数据迁移

- [ ] **Step 1：owner 授权后跑 UPDATE（需 DDL/DML 授权）**

```bash
mysql --defaults-file=/Users/mac/Documents/ruoyi-ai/.codex/ipd-dev/config/mysql-client.cnf <<EOF
-- 备份
CREATE TABLE gate_review_elements_backup_20260922 AS SELECT * FROM gate_review_elements WHERE status='PUBLISHED';

-- 迁移
UPDATE gate_review_elements SET status='published' WHERE status='PUBLISHED';
UPDATE gate_review_elements SET enabled='1' WHERE enabled='Y';
UPDATE gate_review_elements SET enabled='0' WHERE enabled='N';

-- 验证
SELECT status, enabled, COUNT(*) FROM gate_review_elements GROUP BY status, enabled;
EOF
```

预期：迁移后 `published/1`=66（33 老 + 33 新），`published/0`=10。

### 任务 A3-6：浏览器实测验证

chrome-devtools MCP 登录 → 导航 `/ipd/admin/gate-elements` → 打开全生命周期视图 → 截图：已停用从 33 → 10。

### 任务 A3-7：commit

```bash
cd /private/tmp/r177-a3-norm
# 在前端仓 worktree 操作
cd /Users/mac/Documents/ruoyi-ipd-web
git checkout -b r177-a3-norm
git add apps/web-antd/src/api/ipd/gate-element/normalize.ts apps/web-antd/src/api/ipd/gate-element/__tests__/normalize-status.test.ts
git -c user.name="Claude Code" -c user.email="claude@anthropic.com" commit -m "fix(R177-A3): 前端 normalize 函数兼容老格式 status/enabled"
# 同时后端仓 worktree 操作 DDL 备份文件
cd /private/tmp/r177-a3-norm
git add docs/ipd-系统说明/验收/R175-B-normalize-修复-20260922.md docs/ipd-系统说明/qa/r175-b-修复-20260922/ docs/script/sql/migration/gate_review_elements_normalize-20260922.sql
git -c user.name="Claude Code" -c user.email="claude@anthropic.com" commit -m "feat(R177-A3): DB 老格式数据迁移脚本 + 33 行 PUBLISHED/Y → published/1"
```

---

## Agent A4：DRAFT/ARCHIVED 真活造数据+实测（test-data）

**目标**：造 DRAFT 5 条 + ARCHIVED 3 条测试数据，让 button-policy 36 决策点全覆盖（三证齐发）。

**隔离 worktree**：`/private/tmp/r177-a4-data`。

**写库需 owner 显式授权**。

### 任务 A4-1：造数据 SQL 准备

- [ ] **Step 1：写 INSERT SQL**

**File**: `docs/script/sql/test-data/gate_review_elements_draft_archived_seed-20260922.sql`

```sql
-- DRAFT 5 条（含 enabled=0 和 enabled=1 各几条）
INSERT INTO gate_review_elements (code, name, description, status, enabled, veto_dual_required, created_at, updated_at)
VALUES
  ('G-2-DRAFT-01', 'DRAFT 测试 1（disabled）', '造数据 DRAFT-1', 'draft', '0', 0, NOW(), NOW()),
  ('G-2-DRAFT-02', 'DRAFT 测试 2（enabled）', '造数据 DRAFT-2', 'draft', '1', 0, NOW(), NOW()),
  ('G-2-DRAFT-03', 'DRAFT 测试 3（disabled）', '造数据 DRAFT-3', 'draft', '0', 1, NOW(), NOW()),
  ('G-2-DRAFT-04', 'DRAFT 测试 4（enabled）', '造数据 DRAFT-4', 'draft', '1', 1, NOW(), NOW()),
  ('G-2-DRAFT-05', 'DRAFT 测试 5（disabled）', '造数据 DRAFT-5', 'draft', '0', 0, NOW(), NOW());

-- ARCHIVED 3 条
INSERT INTO gate_review_elements (code, name, description, status, enabled, veto_dual_required, created_at, updated_at)
VALUES
  ('G-2-ARCH-01', 'ARCHIVED 测试 1', '造数据 ARCH-1', 'archived', '0', 0, NOW(), NOW()),
  ('G-2-ARCH-02', 'ARCHIVED 测试 2', '造数据 ARCH-2', 'archived', '0', 0, NOW(), NOW()),
  ('G-2-ARCH-03', 'ARCHIVED 测试 3', '造数据 ARCH-3', 'archived', '0', 1, NOW(), NOW());
```

### 任务 A4-2：owner 授权后跑 SQL

```bash
mysql --defaults-file=/Users/mac/Documents/ruoyi-ai/.codex/ipd-dev/config/mysql-client.cnf < docs/script/sql/test-data/gate_review_elements_draft_archived_seed-20260922.sql
mysql --defaults-file=/Users/mac/Documents/ruoyi-ai/.codex/ipd-dev/config/mysql-client.cnf -e "SELECT status, enabled, COUNT(*) FROM gate_review_elements GROUP BY status, enabled;"
```

预期：published/1=66, published/0=10, draft/0=3, draft/1=2, archived/0=3。

### 任务 A4-3：浏览器实测 36 决策点全覆盖

chrome-devtools MCP → `/ipd/admin/gate-elements` → 全生命周期视图打开 → 截图 5 张状态切换（draft/archived/published-enabled/published-disabled）。

### 任务 A4-4：commit

```bash
cd /private/tmp/r177-a4-data
git add docs/script/sql/test-data/gate_review_elements_draft_archived_seed-20260922.sql docs/ipd-系统说明/验收/R175-C-造数据测试-20260922.md docs/ipd-系统说明/qa/r175-c-造数据-20260922/
git -c user.name="Claude Code" -c user.email="claude@anthropic.com" commit -m "feat(R177-A4): DRAFT 5 条 + ARCHIVED 3 条造数据 + 36 决策点全状态实测"
```

---

## Agent A5：7 admin 模块 button-policy 推广（button-policy-spread）

**目标**：把 button-policy 模式（button-policy.ts + button-policy.test.ts + index.vue 重构）推广到 7 个 admin 模块。

**隔离 worktree**：`/private/tmp/r177-a5-spread`（前端仓 worktree）。

### 任务 A5-1：列出 7 个 admin 模块

- [ ] **Step 1：列 admin 模块路由**

```bash
cd /Users/mac/Documents/ruoyi-ipd-web
grep -E "path:\s*['\"]/ipd/admin/" apps/web-antd/src/router/routes/modules/ipd.ts 2>/dev/null | head -20
```

预期：找到 Gate / GateReview / Role / Project / Workbench / Requirement / Allowance 等模块。

### 任务 A5-2：每个模块复制 button-policy 模式

7 个模块 × 每个工作坊方式：

- [ ] **Step 1：复制 button-policy.ts**

```bash
cd /Users/mac/Documents/ruoyi-ipd-web/apps/web-antd/src/views/ipd/admin/{module}/
cp ../gate-elements/button-policy.ts ./
```

- [ ] **Step 2：复制 button-policy.test.ts**

```bash
cp ../gate-elements/__tests__/button-policy.test.ts ./__tests__/
```

- [ ] **Step 3：index.vue 接入 v-access:code + 按钮 v-if**

按 gate-elements 模式重构。

- [ ] **Step 4：跑测试**

```bash
pnpm exec vitest run --config vitest.ipd.config.mts button-policy 2>&1 | tail -5
```

预期：所有模块 button-policy.test.ts 全绿。

### 任务 A5-3：commit

```bash
git add apps/web-antd/src/views/ipd/admin/
git -c user.name="Claude Code" -c user.email="claude@anthropic.com" commit -m "feat(R177-A5): button-policy 推广到 7 admin 模块（Gate/GateReview/Role/Project/Workbench/Requirement/Allowance）"
```

---

## Agent A6：Gate 详情页 button-policy 接入+typecheck 21 错误收口（typecheck-detail）

**目标**：Gate 详情页接入 button-policy + 收口前端 typecheck 21 错误。

**隔离 worktree**：`/private/tmp/r177-a6-type`（前端仓 worktree）。

### 任务 A6-1：找 Gate 详情页

```bash
cd /Users/mac/Documents/ruoyi-ipd-web
find apps/web-antd/src/views/ipd -name "*.vue" | xargs grep -l "GateDetail\|gate-detail\|Gate 详情" 2>/dev/null | head -5
```

### 任务 A6-2：Gate 详情页接入 button-policy

按 gate-elements 模式：复制 button-policy.ts + 在 index.vue 用 v-access:code + v-if 渲染 9 按钮。

### 任务 A6-3：跑 typecheck 找 21 个错误

```bash
pnpm run check:type 2>&1 | tail -50
```

预期：21 个 TS 错误列出。

### 任务 A6-4：逐个修 typecheck 错误

每个错误：用 TS 4.9+ satisfies / 类型守卫 / 明确 return type 等修法。**禁止用 `as any` 放宽**。

### 任务 A6-5：跑 typecheck 确认 0 错误

```bash
pnpm run check:type 2>&1 | tail -5
```

预期：0 错误。

### 任务 A6-6：commit

```bash
git add apps/web-antd/src/views/ipd/admin/gate/ apps/web-antd/src/views/ipd/admin/gate-detail/
git -c user.name="Claude Code" -c user.email="claude@anthropic.com" commit -m "feat(R177-A6): Gate 详情页 button-policy 接入 + typecheck 21 错误收口（0 错误）"
```

---

## 主会话收口（任务 R177-Final）

### 任务 F-1：6 路 commit 撞号必接合并

```bash
cd /Users/mac/Documents/ruoyi-ai
# 把 6 个 worktree 分支 fast-forward / merge 进 main
git fetch origin r177-a2-sm:r177-a2-sm r177-a3-norm:r177-a3-norm r177-a4-data:r177-a4-data 2>/dev/null
# log.md 三方冲突按时间顺序拼接：A1-A6 各段号 → 用 git merge --no-ff 合并
```

### 任务 F-2：写 R177 总账报告

**File**: `docs/ipd-系统说明/R177-总账-全量真实业务落地闭环-20260922.md`

模板同 R174-总账-diamond决策链-20260922.md：
- §0 TL;DR
- §1 6 路并行执行轨迹
- §2 三证齐发汇总（HTTP+DB+浏览器）
- §3 撞号必接记录
- §4 36 决策点全状态覆盖验证
- §5 R175-B+ 未决项收口清单
- §6 R27 17P0 完整闭环
- §7 推 origin 状态（待 owner）

### 任务 F-3：SSOT 镜像同步 + 推 origin（待 owner）

```bash
git add docs/ipd-系统说明/R177-总账-全量真实业务落地闭环-20260922.md docs/ipd-系统说明/开发计划-看板镜像.md
git -c user.name="Claude Code" -c user.email="claude@anthropic.com" commit -m "docs(R177 总账): 全量真实业务落地闭环 6 路并行收口 + 三证齐发汇总"
git push origin main  # 待 owner 授权
```

---

## 验收金标准

每个 P0 都必须三证齐发：
1. **HTTP**：curl `/api/v1/xxx` → 200 OK + 业务数据
2. **DB 回读**：真库 SQL → 数据落库 + 字段正确
3. **浏览器**：chrome-devtools MCP 截图 + 完整操作流程

---

## 限制与风险

- **写库操作**（A2 负反馈 + A4 DRAFT/ARCHIVED 造数据 + A3 老格式迁移）**需 owner 显式授权**
- **push origin** 严格按 AGENTS.md 「未经 owner 明确授权不推」
- **撞号必接**：主会话接管，按 R25 软化三步法
- **typecheck 21 错误**：禁止用 `as any` 放宽，必须用 TS 4.9+ 类型工具
- **subagent 上下文超限**：每个 subagent 只给独立 worktree + 最小上下文

---

## 时间预估

6 路 Agent 并行 + 主会话收口：3-5 天
- A1/A4/A6：1-2 天（纯 UI / 造数据 / typecheck）
- A2/A3/A5：2-3 天（代码修复 + 数据迁移 + 7 模块推广）