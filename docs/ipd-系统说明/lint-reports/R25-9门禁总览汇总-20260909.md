# R25 九门禁总览汇总（2026-09-09）

> 本报告汇总 R25 全局治理 9 个自动门禁脚本的最终执行结果。
> 脚本全部位于 `scripts/`，报告明细位于 `docs/ipd-系统说明/lint-reports/`。
> 治理根因分类见 `docs/ipd-系统说明/治理/R25根因反思与防复发建议-20260909.md`。

## 汇总表

| # | 脚本 | 覆盖根因 | 最终结果 | 状态 |
|---|---|---|---|---|
| 1 | `scan_dead_code.sh` | RC-4 死代码 | 0 HIGH / 0 MEDIUM / 35 LOW（均为 Spring 自发现注解类，需人工复核） | ✅ 跑通 |
| 2 | `check_cross_repo_contract.sh` | RC-2 契约对账 | 3 真白屏（auth/platform-token 致命 + 2 边缘）+ 194 孤儿端点 + 21 文档未实现 | ✅ 跑通 |
| 3 | `check_dynamic_loadable.sh` | RC-5 动态加载 | 0 dynamic-loadable（前端用静态路由+动态菜单混合，import.meta.glob 模式未展开到动态路径） | ✅ 跑通 |
| 4 | `check_tenant_excludes_apply.sh` | RC-3 配置先行 | 0 致命 / 50 表名重叠待复核（真库未连，--skip-db 模式） | ✅ 跑通 |
| 5 | `check_entity_complete.sh` | RC-3 实体完整性 | 22 Mapper 缺 Service / 18 Service 缺 Controller（含命名变体误判可能，需人工抽查） | ✅ 跑通 |
| 6 | `check_i18n_unused_keys.sh` | RC-8 i18n 死键 | **清零完成**：148 keys → 70 keys，78 死键全清（zh-CN 5 个 json 已 backup + 已删） | ✅ 跑通 |
| 7 | `check_doc_link.sh` | RC-7 文档死链 | 270 链接 / 182 死链 / 2 v1 残留（死链集中在旧治理文档，不影响主规格） | ✅ 跑通 |
| 8 | `check_deletion_consistency.sh` | RC-1 删除对账 | --auto 模式无 HIGH 项退出 0（当前无待删除项） | ✅ 跑通 |
| 9 | `check_duplicate_ssot.sh` | RC-6 重复 SSOT | BE 205 / FE 86 / DOC 13 常量；同名比对 0/0（真无重复）；文档副本 md5 相同 1 个（开发说明书.md） | ✅ 跑通 |

## 本轮已执行的修复

### 2 要：i18n 死键清理（78 个全清）

- **动作**：`check_i18n_unused_keys.sh --delete`，zh-CN 5 个 json（component / demos / menu / page / pages）中 78 个死键全部删除。
- **fresh 复核**：删除前逐类 grep 复核过——menu.* 35 个是 admin 框架菜单残留；component.upload.* 21 个是 upload 组件硬编码中文不走 i18n；page.workbench.taskType.* 16 个走 TS 中文字面量常量（WORKBENCH_TASK_TYPE_TEXT）不用 i18n；demos.* / pages.common.* 是 demo 残留。全部确认真死。
- **回滚备份**：`/tmp/zh-CN-backup-20260909/`（6 个 json 原样）。
- **en-US 未动**：其他语言文件的孤儿翻译属另一张卡（owner 决策）。

### 3 要：CI workflow 落地

- **新建**：`.github/workflows/r25-root-cause-lint.yml`（201 行）。
- **触发**：PR 修改 `ruoyi-modules/ruoyi-ipd/src/main/**` / `apps/web-antd/src/**` / `docs/ipd-系统说明/**`；每周日 03:00 Asia/Shanghai；手动 dispatch。
- **行为**：9 门禁全跑，**默认 WARN 不阻断**（保护兄弟会话在途）；配 secret `STRICT_R25_LINT=1` 后升级为阻断（owner 30 天观察期后启用，参考 STRICT_DOCS_LINK 同款模式）。

### 1 修白屏：已登记（未动手改代码）

- **状态**：`ROOT-R6-D1-WHITE` 卡号已登记于 `docs/ipd-系统说明/治理/R25根因反思与防复发建议-20260909.md` §9（第 444 行）。
- **原因**：修复需改前端 `auth.ts`（兄弟会话在途持有 26+/5-）或后端 Java（OPS-09 单写者），本会话不越界。
- **应急方案**：§9 已写方案 A（后端补 IpdAuthController.platformToken 端点，推荐）/ 方案 B（前端 fallback 复用 login token）。

## 门禁脚本本轮修复记录

| 脚本 | 修了什么 | 为什么 |
|---|---|---|
| `scan_dead_code.sh` | 加 framework 类白名单 ~90 个 + extends/implements 双向匹配 + @Service 标 LOW | 消除 RuntimeException / NotificationChannel / @Service 零注入三类误报 |
| `check_cross_repo_contract.sh` | strip_ts_comments 剥注释 + PATH_RE 严格化 + 4 函数扫描（requestIpd/ipdGet/ipdPost/ipdPut/ipdDelete/ipdPatch）+ bare @GetMapping() 无 value 映射到 class_mapping | 白屏从 66 误报 → 1 → 3 真值；补漏 deletion-requests |
| `check_entity_complete.sh` | 放宽 Service / Controller 命名匹配（含 Ipd 前缀 / Impl 变体） | 减少命名变体误判 |
| `check_i18n_unused_keys.sh` | 遍历 zh-CN/*.json + namespace 前缀 + awk 多行转义 + jq any(paths) 精确匹配 + delpaths + --delete/--dry-run/--namespace= 三个开关 | key 数从 5 → 148（漏扫 5 个 namespace）；删除逻辑修通 |
| `check_duplicate_ssot.sh` | sed 分隔符 `/` → `,`（文件路径含 / 打破 sed）+ 前端扫描范围从 2 目录扩到全 src/ + 加文档副本 md5 检测 | FE 常量 0 → 86；补上蜂群 C 发现的文档副本维度 |

## 遗留待 owner 拍板

1. **白屏修复**：等兄弟会话或 owner 领 ROOT-R6-D1-WHITE 卡（方案 A 后端补端点，1-2 人日）。
2. **194 孤儿端点**：后端有端点但前端从未调用——部分是业务预留，需 owner 圈定哪些可下线。
3. **21 文档未实现**：文档写了端点但后端未实现——属规格超前，需 owner 确认优先级。
4. **50 租户白名单重叠**：tenant.excludes 与 @TableName 重叠的表名，需连真库跑一轮（不带 --skip-db）。
5. **en-US 孤儿翻译**：zh-CN 已清 78 死键，en-US 对应键未清，属另一张卡。
6. **文档副本删除**：`ZK-IPD/开发说明_副本/开发说明书.md` 与主路径 md5 相同，可直接删副本（owner 授权后）。
7. **commit / push：✅ 已落地（2026-09-09 R26 授权延伸）**：后端 9 脚本+CI+治理文档+lint 明细 76 文件 commit `cc201b5e` 已 push；前端 zh-CN 5 json 死键清理 commit `83846b8`（ruoyi-admin 仓，连带 F4 `bb74f4c`）已 push。验证：后端全量 2087 绿；前端 vitest 900 passed（唯一失败 ipd-error-text 系兄弟在途文件全量并发态，单跑 39/39 绿）。其余 6 项仍待 owner。

## 重跑全套

```bash
cd /Users/mac/Documents/ruoyi-ai
for s in scan_dead_code check_cross_repo_contract check_dynamic_loadable \
         check_tenant_excludes_apply check_entity_complete check_doc_link \
         check_deletion_consistency check_duplicate_ssot check_i18n_unused_keys; do
  echo "==== $s ===="
  bash scripts/$s.sh || true
done
```

CI 版本：`.github/workflows/r25-root-cause-lint.yml`（每周日 03:00 Asia/Shanghai 自动跑）。
