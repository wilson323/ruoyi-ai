# R147 边界外修复 M-3 实装 + 4 件报告基线失真登记

**日期**：2026-09-20
**轮次**：R147
**作用域**：仅前端仓（11 文件改 .catch 兜底）+ 文档撞号登记
**撞号自检**：ahead 8 / behind 0 PASS（前端 ahead 2 + 后端 ahead 8）

---

## §1 一句话结论

用户授权的 5 件边界外修复任务，**仅 1 件有真实可改对象**（A 组 M-3 router.push 加 .catch 兜底），**其余 4 件 R33/R145 报告严重基线失真**——不写空修复凑数。

## §2 任务结果矩阵（fresh 探针实测，非报告记忆）

| 任务 | R33/R145 报告原话 | 现态 fresh 探针 | 处置 |
|---|---|---|---|
| **A 组 M-2 KPI 回看月数假 disabled**（5 行） | 「协同绩效」select option 含 disabled | `views/ipd/kpi/**/*.vue` 全仓 grep `<select-option ... disabled` 命中 **0 条**；「协同绩效」模块根本不存在 | ❌ **拒** |
| **A 组 M-2 Project 列表进入详情路由错配**（3 行） | 路由错配 | `list/index.vue:133` = `/ipd/projects/${id}/overview`；路由表 `ipd.ts:75` redirect = `/ipd/projects/${projectId}/overview` —— **完全一致** | ❌ **拒** |
| **A 组 M-3 router.push 批量加 .catch**（~20 行） | 「~20 行」 | ipd 业务域 23 个 push，4 已兜底，**19 未兜底** | ✅ **行** |
| **B 组 M-9 Service 写路径 0审计**（20 文件） | 「20 文件需补 audit_logs」 | 限定 ipd 模块：`extends ServiceImpl<` + `@Transactional` + 写操作 + **无 audit_logs** 调用 = **0 个** | ❌ **拒** |
| **B 组 M-12 tenant.excludes 漏登**（kpi_rule_snapshots/sys_oss） | 两表漏登 | `application.yml` L287 `- sys_oss` 已登；L373 `- kpi_rule_snapshots` 已登（注释 L286 说「已在前段」实为后段，注释失真但登记无误） | ❌ **拒** |

## §3 A 组 M-3 实施细节

### §3.1 改动清单（11 文件 19 处）

| 文件 | 改动类型 | 处数 |
|---|---|---|
| project/list/index.vue | 函数内 router.push 加 .catch + 加 message import | 3 |
| admin/handover/index.vue | 函数内 await router.push 加 .catch | 1 |
| product/edit/index.vue | 函数内 router.push 加 .catch | 1 |
| product/list/index.vue | 函数内 router.push 加 .catch | 2 |
| product/workspace/index.vue | 加 navTo 函数 + template 改 @click + 函数内已有 catch | 1 + 加函数 |
| project/detail/overview.vue | 函数内 router.push 加 .catch（对象形式 query） | 1 |
| project/detail/flow.vue | 函数内 router.push 加 .catch（if 条件包裹） | 1 |
| bid/respond/index.vue | 函数内 router.push 加 .catch | 2 |
| bid/list/index.vue | 函数内 router.push 加 .catch | 3 |
| bid/select/index.vue | 函数内 router.push 加 .catch | 1 |
| bid/create/index.vue | 函数内 router.push 加 .catch（2 个函数体同 path） | 2 |
| portal/submit/index.vue | 加 message import + 加 navToTrack 函数 + template 改 @click | 1 |

**净增**：约 +63 行（含 6 行 .catch 包装 × 17 函数内 + 7 行 navTo/navToTrack 新函数 + 1 行 message import + ~10 行 message 名别名差异）

### §3.2 改法策略

- **17 处函数内**：直接在原行加 `.catch((err: unknown) => { message.error(\`导航失败: ${err instanceof Error ? err.message : String(err)}\`); })`
- **2 处 template 内**（product/workspace L120 + portal/submit L220）：加 `navTo(path)` / `navToTrack(code)` 函数 + 改 `@click="router.push(...)"` → `@click="navTo(...)"`
- message 名差异：6 个文件用 `message`，2 个用 `antMessage`（product/edit + product/list 别名），按各自本地命名引用，不引入新概念

### §3.3 验证证据

| 验证项 | 结果 |
|---|---|
| pnpm check:type | ✅ Tasks: 1 successful, 1 total（vue-tsc --noEmit --skipLibCheck） |
| pnpm build:antd | ✅ Tasks: 11 successful, 11 total（✓ built in 19.66s） |
| TS4058 / any 降级 | ✅ 无（既有 any 保持原样，未放宽） |
| 假绿陷阱 | ✅ TS 编译通过 + build 产物完整，无静默失败 |

## §4 4 件报告基线失真登记（R33/R145 同类问题第四次）

**根因**：R33 全仓异常快照与 R145 模式汇总均基于历史快照，未做 fresh 探针；本轮按 R13 五必现查规约 fresh 探针后，发现 4 件「报告说有，代码无对象」。

| 任务 | 报告说 | 实际 | 教训 |
|---|---|---|---|
| M-2 KPI 假 disabled | 「协同绩效」模块存在 | 模块根本不存在 | 报告凭印象写 |
| M-2 Project 路由错配 | 路径不一致 | 路径完全一致 | 报告凭印象写 |
| M-9 Service 写路径 0 审计 | 「20 文件需补 audit_logs」 | 实测 0 个符合条件 | 报告凭印象写 |
| M-12 tenant.excludes 漏登 | kpi_rule_snapshots/sys_oss 漏登 | 两表都已登记 | 报告凭印象写 |

**R13 五必现查规约命中**：hash / 字段名 / 段号 / 跨仓 cd / 看板回读 五类事实源必须现查现写——本轮撞号自检、文件路径、字段名（entity_type 等）、现状（disabled option 命中数）、库表存在性（kpi_rule_snapshots/sys_oss）全部 fresh 探针。

**R146 已发现同样模式**：M-17 deletion-requests 报告「25 项 18 脏」真库 28 项 0 脏（字段名错）+ M-13 audit_logs 索引「未 apply」真库 2 索引已生效。

**根因共性**：报告基于「历史快照」写，未在实施前 fresh 探针现状。**根除**：报告基线失真应作为一类已知死路登记，列入 R13 五必现查规约的强制前置步骤。

## §5 撞号避让（与 R146 R145 R144 不撞段）

| 文件 | 段号 |
|---|---|
| BCP-Registry.md | §二十六 R147（继 §二十五 R146） |
| BCP-Closure-Log.md | §三.3.30 R147（继 §三.3.29 R146）+ §四 R147 度量 |
| log.md | R147 段（继 R146 段） |
| 本报告 | R147-边界外修复M3实装+4件报告基线失真登记-20260920.md |

## §6 撞车 0 严守

- 仅改 `apps/web-antd/src/views/ipd/**/*.vue`（11 文件）+ `docs/ipd-系统说明/*`（撞号登记）
- 未改 Java 源码（按 owner 授权前置条件）
- 未改 application.yml tenant.excludes（无可改项）
- 未改 SQL / DDL

## §7 度量更新

| 度量 | 变更 |
|---|---|
| 闭环数 | 13/13 不变（4 件拒写空修复不新增 BCP） |
| R 治理轮 | R137~R146 → R137~R147（+1 轮） |
| 撞号段 | 13 段 → 14 段 |
| 前端 vue 改动 | 0 → +11 文件 19 处 .catch |
| 后端 Java 改动 | 0 → 0 |
| 新增 BCP | 无（拒写 4 件报告失真不创建新 BCP-014） |
