# R156 收口报告 — R140 路径 B 执行：P141 4 commit + 2 测试修复合入 main

**日期**：2026-09-21
**依据**：R140-路径拍板包-20260921刷新.md 推荐路径 B
**HEAD**：后端 `43f6aebc`（领先 origin 0）

---

## 1. 执行总账

按 R140 §5.2 路径 B 执行步骤 1-9 完成。

| 步骤 | 内容 | commit | 备注 |
|---|---|---|---|
| 3 | merge fix/r155-eval-mapper | 5bab82e2 | paiban-07 39 Mapper @Mapper 注解 |
| 4 | merge fix/r155-eval-scriptname | c0076661 | paiban-10 scripts kebab-case |
| 5 | merge fix/r155-eval-transactional | cd058164 | paiban-09 7 类 @Transactional 下沉 |
| 6 | merge fix/r155-eval-exception + 2 测试修复 | 43f6aebc | paiban-08 + P182AcceptanceTest + AuditHashChainTest 断言同步 |
| 7 | 全量单测验证 | — | 2266 跑 / 1 FAIL（main baseline 债）= 零回归 |
| 8 | git push origin main | — | 7ebec8bd..43f6aebc 推送成功 |
| 9 | 收口报告（本 md）| — | 落档后 commit |

**全部 4 commit 合入 + 2 测试修复 + 0 冲突 + 0 回归**

---

## 2. 改动详情

### 2.1 paiban-07 Mapper @Mapper 注解（commit 5bab82e2）
- 39 文件 / +78 行
- `ruoyi-modules/ruoyi-ipd/src/main/java/org/ruoyi/ipd/mapper/*.java` 全加 `@Mapper`

### 2.2 paiban-10 scripts kebab-case（commit c0076661）
- 18 个脚本 rename snake_case → kebab-case
- `git rename` 18 + 修改引用 2 处
- 77/77 bash -n 100% 通过

### 2.3 paiban-09 @Transactional 下沉（commit cd058164）
- 7 文件 / +20 -7 行
- 写 `@Transactional(rollbackFor = Exception.class)` 14 处
- 读 `@Transactional(readOnly = true)` 6 处
- 与 paiban-05 接口化兼容（AOP 代理实现类 public 方法生效）

### 2.4 paiban-08 异常替换 + 2 测试修复（commit 43f6aebc）
- 主 commit：12 文件 / +34 -21 行
  - `KpiScoreCalculator.java` +20/-10
  - `ProviderRegistry.java` +4/-1
  - `InAppChannelHandler.java` +5/-2
  - `DefaultStateMachineGuard.java` +2/-1
  - `AuditHashChain.java` +4/-1
- 测试修复：2 文件 / +3/-3 行
  - `P182AcceptanceTest.algoTypeValidation`：断言 `ServiceException` → `IpdBusinessException`
  - `AuditHashChainTest.canonicalByVersionRouting`：断言 `IllegalArgumentException` → `IpdBusinessException`

---

## 3. 验证结果

### 3.1 编译
```
mvn -o -pl ruoyi-modules/ruoyi-ipd -am compile -q
```
✅ BUILD SUCCESS

### 3.2 单测
```
mvn -o -pl ruoyi-modules/ruoyi-ipd -Dgroups=dev test
Tests run: 2266, Failures: 1, Errors: 0, Skipped: 22
```
✅ 零回归（1 FAIL = main baseline 债 `ServiceBareClockGuardTest` 报 `RecoveryWarningService` 裸时钟，与 P141 无关，R149 batch2a 引入时漏白名单登记）

### 3.3 撞车 0 严守
- ✅ 不动兄弟会话 11 个 wt
- ✅ 不 push 兄弟会话 fix/p141-* 分支（用 R155 eval 4 新 wt 走合入路径）
- ✅ 兄弟会话 fix/p141-* 分支仍 ahead 1 / behind 28+（合入后实际 ahead 0 / behind 30+，但因不动 wt 仍 ahead 1）

---

## 4. 剩余 micro-debt（不阻断）

### 4.1 P141 micro-debt（与 4 commit 相关）
- **3 处 echo log 标签字符串**：脚本 micro PR，不阻断
  - `check-doc-line-refs.sh:48 [check_doc_line_refs]`
  - `check-demo-default-off.sh:22 [check_demo_default_off]`
  - `check-ddl-idempotent.sh:14 [check_ddl_idempotent]`

### 4.2 main baseline 债（与 P141 无关）
- `ServiceBareClockGuardTest.noBareClockOutsideWhitelist`：报 `RecoveryWarningService` 有 `LocalDate.now()` 裸时钟
- 根因：R149 batch2a (commit `aa091482`) 引入 `RecoveryWarningService` 时漏登记白名单
- 修法：在 `LEGACY_WHITELIST` 加 `RecoveryWarningService` 行（估时 0.5 hr）
- 阻塞：owner 拍板

---

## 5. 兄弟会话 wt 状态

合入 P141 后，4 个兄弟会话 fix/p141-* 分支与 main HEAD 关系：
- `fix/p141-mapper-annotation` @ 916b04ee — **合入后已过时**（ahead 1 / behind 30+）—— owner 可停手
- `fix/p141-exception-rename` @ 96c78aca — **合入后已过时**（ahead 1 / behind 30+）—— owner 可停手
- `fix/p141-script-naming` @ c1a0559c — **合入后已过时**（ahead 1 / behind 31+）—— owner 可停手
- `fix/p141-transactional-downgrade` @ 3f395d24 — **合入后已过时**（ahead 1 / behind 30+）—— owner 可停手

兄弟会话 R120/R121/R126/R127 wt 不受影响。

---

## 6. R157 启动建议

按 R140 路径 A 剩余步骤推进：

### 6.1 R157-D2 P1 第一批完成
- ✅ paiban-07/08/09/10 已合入 main（本次完成）

### 6.2 R157-D3 owner 拍板 P0
- kpi_rules 表方案 A/B/C（拍板包 `paiban-02-kpi-rules-20260920.md` 已落档）
- 后端真活 E2E（P0-1 阻塞已 1 天）
- 字符集整改（P0-5，DDL apply by SRE）

### 6.3 R157-D4 DDL apply by SRE
- 表名单数整改（P0-4，10 张表）
- 字符集整改（P0-5，66 表 / 506 字段级）

### 6.4 R157-D5 架构偏离（最大破坏）
- Service 接口化（P0-6，65 个 × 4 批 = 16 hr）
- @Autowired 改构造器（P1-10，48 处）
- Controller 去 Ipd 前缀（P1-11，4 个）
- DTO 后缀收口（P1-12，61 个）
- Entity BaseEntity 父类（P1-13，10 个）

### 6.5 micro-debt 闭环（AI 可做）
- 修 RecoveryWarningService 裸时钟（白名单登记 0.5 hr）
- 修 3 处 echo log 标签字符串（脚本 micro PR 0.5 hr）

---

## 7. 双仓状态

- **后端 main HEAD**：`43f6aebc`（领先 origin 0）
- **前端 main HEAD**：`9e733b3`（领先 origin 0，与本次路径 B 无关）
- 后端仓 4 commit merge + 1 docs commit = 5 commit 推进
- 工作区干净

---

## 8. 边界严守

- ✅ 不动兄弟会话 11 wt
- ✅ 不擅自动 DDL
- ✅ 不擅自动 P0 阻塞项
- ✅ 用 R155 eval 4 新 wt（不动兄弟会话 fix/p141-* 分支）
- ✅ pre-commit 未触发（merge 不跑 pre-commit）

---

## 9. owner 后续决策

P141 4 commit 已合入后，下一步阻塞清单：
1. **撞车窗口**：11 兄弟 wt 仍在，可考虑派 worktree 处理 P1 第二批（@Autowired / Controller 前缀 / DTO 后缀 / Entity BaseEntity）
2. **DDL apply**：撞车窗口到达后由 SRE apply 表名单数 + 字符集整改
3. **P0-3 kpi_rules 方案 A/B/C**：owner 拍板后即可实装 3 端点
4. **P0-1 后端真活 E2E**：阻塞 24h SLA 已过 1 天，建议撞车窗口到达后立即推进