# C11 docs-only ticket — 决策 #18 原稿对账表注解（BR vs ZK-IPD 仅 docs）

> **Ticket 性质**：docs-only（业务规则文字解释，不动 Java 源码 / SQL / DDL；不写新代码）
> **决策源**：[`业务决策确认-20260905.md` §1 行 18（L30）](../../../工程合同/业务决策确认-20260905.md)
> **对应工程合同**：[`DOC-01.md` §1 + 本目录 docs-only 落档（不写新代码）](../../../工程合同/DOC-01.md)
> **R149/R153 关联**：R149 §6 D 路 C11（仅 docs） + R153 域 D §2 D 路 C11 🟡 + §6 ③ C11 docs-only
> **写注解时间**：2026-09-21 R154-E 子任务

---

## 1. 业务规则名称

**/Documents/ZK-IPD 原稿参考 → BR 系统事实对账表（来源/设计/页面映射 + 可运行性证据）**。

业务上要求：决策 18 明文"实际的原稿参考文档路径下 /Documents/ZK-IPD 作参考"。本机按用户目录解析为 `/Users/mac/Documents/ZK-IPD` 并实际核对；只读参考，不因找到旧原型代码就认定它是当前获准修改的前端仓库；建立"来源 / 设计 / 页面映射 + 可运行性证据"对账表，避免后续工程子任务误把旧原型代码当真活前端。

---

## 2. 决策摘要

- **业务实质**：ZK-IPD 是旧原型项目（前端仓），DOC-01.md §1 已声明"仅参考；不回写代码"。本注解负责把"BR 当前事实 vs ZK-IPD 原稿事实"形成可读的对账矩阵——行是核心模块/页面/数据流，列是来源（ZK-IPD OR 业务决策 OR 工程合同）/设计稿/页面映射/可运行性证据。
- **当前实现**：ZK-IPD 目录作为只读参考；DOC-01.md §1 已说明"不回写代码"。但**对账矩阵尚未系统化落档**——目前仅 DOC-01 §1 文字声明，无独立文档把每个 BR 模块与 ZK-IPD 源对齐。
- **本次 docs-only 落档要点**：本注解本身即"对账表的标题与索引"——不写新代码、不复制 ZK-IPD 内容，只声明对账范围与权威源；实际对账工作在 owner 拍板后由独立 worktree 启动 P2 子任务完成（不阻塞主线）。

---

## 3. docs-only 注解正文

1. **对账范围（13 行预排）**：DOC-01 §3 公式 / §4 KPI 量表 / §4.2 多项目叠加 / DOC-02 审批流 / DOC-04 时间合同 / DOC-05 33 要素 / DOC-AUTH 权限矩阵 / BonusPoolService 奖金终算 / GateEngine G2-G4 / PermanentDeleteService / KpiSharedCollectionService 8 项 / RequirementStateMachine 7 态 / P0EscalationService 升级链。
2. **对账权威源**：BR 侧 = 工程合同 5 份（DOC-01/02/04/05/AUTH）+ 业务决策确认-20260905.md；ZK-IPD 侧 = 用户目录 `/Users/mac/Documents/ZK-IPD` 下原稿文件（只读）。
3. **对账列定义**：`来源（BR 决策 / ZK-IPD 原稿 / 二者皆有）/ 设计映射（页面 ID → ZK-IPD 路径）/ 可运行性证据（端点 / 测试 ID / commit hash）/ 差异点 / 是否阻塞当前 BR`。
4. **撞号避让**：本 ticket 不引入新对账表 doc——R153 §6 ③ 标注"决策 #18 原稿 BR vs ZK-IPD 对账表（C11 docs-only，1 hr）"是 owner 拍板后启动的独立 worktree（建议 `wt-r154-c11-mapping`）；本 ticket 仅落档"对账框架 + 范围 + 权威源声明"。
5. **不复制 ZK-IPD 内容**：本目录只放对账框架，ZK-IPD 原文 / 旧原型代码不进入 BR 仓库；如未来 owner 要求"复制 ZK-IPD 子集做对比"，须独立批准 + 单独 worktree，避免 git rlock。

---

## 4. 代码影响面（不改代码）

| 模块 | 影响 |
|---|---|
| 任何 Java / Vue / SQL 源文件 | 无影响（ZK-IPD 是参考，不回写） |
| 工程合同 DOC-01/02/04/05/AUTH | 不改（已落档） |
| ZK-IPD 目录 | 不动（只读参考，不复制进 BR 仓库） |
| `verify_contract.py` 等验证脚本 | 无需改（与对账表无关） |
| 对账表 docs 落地 | 本目录提供框架 + 13 行预排（未来 worktree 补全） |

---

## 5. 测试覆盖建议

- **可运行性证据**：每个 BR 端点列 commit hash + 测试 ID（如 `KpiSharedCollectionService:666` → commit `aa091482`）
- **页面映射验证**：ZK-IPD 旧原型页面路径 vs BR 当前 Vue 路由（如 `/gate/list` vs `/ipd/gate/index`）
- **阻塞判定**：差异点 → owner 拍板（不阻塞 R154 主线；标注 🟡 黄等 owner 决策）

---

## 6. 边界严守

- ✅ 仅 docs/ 落档（本 ticket + 本目录）
- ❌ 不复制 ZK-IPD 任何文件进 BR 仓库
- ❌ 不改 Java / Vue / SQL
- ❌ 不擅自启动独立 worktree 复制对账（须 owner 拍板）
- ❌ 不抢 owner 拍板权限

---

> **本 ticket 闭环判定**：C11 在 R153 §2 表格中为 🟡 黄（"BR vs ZK-IPD 原稿对账表 docs-only 未单独排 ticket"）。本注解落档后 🟢 绿-文档（提供框架 + 范围 + 权威源声明）；**实际对账补全**仍保留 🟡 黄（owner 拍板后启动独立 worktree，不在 R154-E 子任务范围）。
