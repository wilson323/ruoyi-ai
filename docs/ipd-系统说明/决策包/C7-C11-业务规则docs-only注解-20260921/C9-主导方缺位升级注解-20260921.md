# C9 docs-only ticket — 决策 #6 主导方缺位升级注解（已并入 R149-A5）

> **Ticket 性质**：docs-only（业务规则文字解释，不动 Java 源码 / SQL / DDL）
> **决策源**：[`业务决策确认-20260905.md` §1 行 6（L18）](../../../工程合同/业务决策确认-20260905.md)
> **对应工程合同**：[`DOC-05.md` §3 O07 + §3 表 SIGN-NONLEAD-TIMEOUT / SIGN-LEAD-MISSING / SIGN-BOTH-MISSING / SIGN-REJECT](../../../工程合同/DOC-05.md)
> **R149/R153 关联**：R149 §6 D 路 C9（已合并到 R149-A5） + R149-A5（决策 #12 审批人按节点配置 GROUP scope） + R153 域 D §2 D 路 C9 🟢
> **写注解时间**：2026-09-21 R154-E 子任务

---

## 1. 业务规则名称

**评审/签署流程主导方缺位升级规则（按事项/节点提前确定 + 锁定 + 缺位升级）**。

业务上要求：决策 6 明确"按事项/节点提前确定主导方并在发起时锁定；不能先提交者主导；主导方有有效意见且另一方超时才执行原规则；主导未签/双方未签保持未完成并升级；明确否决优先"。本注解声明：C9 的代码 / 测试实装路径已并入 R149-A5（决策 #12 审批人按节点配置 GROUP scope，R149-A5 重大优化复用 `ipd_business_config` GROUP scope 节省 ≥8 hr），不在本 ticket 独立实装。

---

## 2. 决策摘要

- **业务实质**：决策 6 是评审流程层面的"主导方"概念——一项评审/签署先按节点确定主导（PM/QA/超管等），发起时锁定到审批表；缺位（主导未签 / 双方未签）保持未完成并升级（不允许自动按"先提交"规则推进）。这与决策 12 的"审批人按节点配置 + 快照"是同一概念的两面——决策 12 解决"配置可定"，决策 6 解决"配置生效后缺位怎么办"。
- **当前实现**：R149-A5 已合并本规则——`BusinessConfigController` + V003 DDL（`ipd_business_config` GROUP scope）已实装；主导方在审批发起时从 GROUP scope 读取并快照写入审批表；缺位路径走"未完成 + 升级"分支（不自动过）。R152 业务回归 D 场景（审批配置）全绿。
- **本次 docs-only 落档要点**：本注解记录"决策 6 与决策 12 的合并路径"——避免后续读 R153 §2 时误以为 C9 是"未实装"；明确**C9 🟢 绿（已并入 R149-A5）**。

---

## 3. docs-only 注解正文

1. **合并声明**：C9 决策 #6 主导方缺位升级 = R149-A5 决策 #12 审批人按节点配置 GROUP scope；两者本质同一概念（"按节点配置 + 锁定 + 缺位升级"）。R149-A5 重大优化采纳了 R148.1 §3.3 建议复用 `ipd_business_config` GROUP scope，避免新建独立主导方表。
2. **业务规则位置**：决策 6 已在 [`业务决策确认-20260905.md` L18](../../../工程合同/业务决策确认-20260905.md) 登记（"按建议：按事项/节点提前确定主导方并锁定，不能先提交者主导"）。
3. **代码位置锚定**：主导方读取路径在 `BusinessConfigController` + `BusinessConfigService.findLeadByNodeStage()`；缺位升级路径在 `ApprovalService.rejectOnMissingLead()`。
4. **测试覆盖**：`P2-5.4`（已存在，BASE-14 用例覆盖 SIGN-NONLEAD-TIMEOUT / SIGN-LEAD-MISSING / SIGN-BOTH-MISSING）；R152 业务回归 D 场景全绿。
5. **撞号避让**：不在本 ticket 重复实装；如未来业务上要"主导方角色可配置化"或"超时阈值可配"，须由 owner 拍板并落新 ticket，本注解不延伸。

---

## 4. 代码影响面（不改代码）

| 模块 | 影响 |
|---|---|
| `BusinessConfigController.java` | 无需改（R149-A5 已实装） |
| `BusinessConfigService.findLeadByNodeStage()` | 无需改（已实装 GROUP scope） |
| `ApprovalService.rejectOnMissingLead()` | 无需改（已实装缺位升级） |
| `BaseAcceptanceTest.java`（P2-5.4） | 无需改（已覆盖） |
| `ipd_business_config` 表 GROUP scope | 无需改（V003 DDL 已落） |
| DOC-05 §3 O07 | 不改（已落档） |

---

## 5. 测试覆盖建议

- **P2-5.4**：SIGN-NONLEAD-TIMEOUT / SIGN-LEAD-MISSING / SIGN-BOTH-MISSING / SIGN-REJECT（已存在）
- **R152 业务回归 D**：审批配置 4 场景（已 PASS，2026-09-21）
- **撞号回归**：主导方 GROUP scope 默认值（GROUP_LEADER vs SUPER_ADMIN）— 等 owner 拍板，R153 §6 ② 已登记

---

## 6. 边界严守

- ✅ 仅 docs/ 落档（本 ticket 标"已并入 R149-A5"）
- ❌ 不动 R149-A5 已合并的代码 / 测试 / DDL
- ❌ 不擅自做"主导方超时阈值可配"等扩域（须 owner 拍板）
- ❌ 不抢 owner 拍板权限

---

> **本 ticket 闭环判定**：C9 在 R153 §2 表格中已为 🟢 绿（已并入 R149-A5）。本注解落档后 🟢 绿-文档；C9 在 R154-E 不再产生新代码工作量。
