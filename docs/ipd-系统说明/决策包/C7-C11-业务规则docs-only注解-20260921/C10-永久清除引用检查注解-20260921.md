# C10 docs-only ticket — 决策 #14 永久清除引用检查注解（已并入 R149-C3）

> **Ticket 性质**：docs-only（业务规则文字解释，不动 Java 源码 / SQL / DDL）
> **决策源**：[`业务决策确认-20260905.md` §1 行 14（L26）](../../../工程合同/业务决策确认-20260905.md)
> **对应工程合同**：DOC-AUTH.md 永久清除权限 + 业务决策确认 L26 注解
> **R149/R153 关联**：R149 §6 D 路 C10（已合并到 R149-C3） + R149-C3（`PermanentDeleteService` + `permanent_delete_audit` 表 + 权限码 `OPERATION_PERMANENT_DELETE`） + R153 域 D §2 D 路 C10 🟢
> **写注解时间**：2026-09-21 R154-E 子任务

---

## 1. 业务规则名称

**永久清除操作的引用检查规则（有业务引用不直接永久清除 + 最终清除二次确认）**。

业务上要求：决策 14 明确"一期不自动到期清理；恢复由超管记录原因并重验权限/引用/状态；有业务引用不直接永久清除；最终清除二次确认，审计永久保留"。本注解声明：C10 的代码 / 测试实装路径已并入 R149-C3（`PermanentDeleteService` + `permanent_delete_audit` 表 + 权限码 `OPERATION_PERMANENT_DELETE`），不在本 ticket 独立实装。

---

## 2. 决策摘要

- **业务实质**：决策 14 解决"软删除 → 永久清除"的最后一步——一期（v1）不做自动到期清理，避免误删；恢复（即永久清除）的触发须经超管记录原因 + 重验权限/引用/状态 + 最终二次确认。引用检查（业务上有其他需求/奖金/审批等引用此记录）须先阻断，不允许"跳过引用直接清除"。
- **当前实现**：R149-C3 已实装——`PermanentDeleteService.deleteWithAudit()` 已实现引用检查 + 二次确认流程；`permanent_delete_audit` 表已建；权限码 `OPERATION_PERMANENT_DELETE` 已落入 `IpdRolePermissionCatalog` L163。R152 业务回归测试全绿。
- **本次 docs-only 落档要点**：本注解记录"决策 14 与 R149-C3 的合并路径"，明确**C10 🟢 绿（已并入 R149-C3）**。

---

## 3. docs-only 注解正文

1. **合并声明**：C10 决策 #14 引用检查 = R149-C3 永久清除入口；R149-C3 包含了"权限校验 + 引用检查 + 状态重验 + 二次确认 + 审计记录"五步全流程；引用检查是其中第三步（已被覆盖）。
2. **业务规则位置**：决策 14 已在 [`业务决策确认-20260905.md` L26](../../../工程合同/业务决策确认-20260905.md) 登记（"按建议：一期不自动到期清理；恢复由超管记录原因并重验权限/引用/状态；有业务引用不直接永久清除；最终清除二次确认，审计永久保留"）。
3. **代码位置锚定**：`PermanentDeleteService.deleteWithAudit()` 入口；`permanent_delete_audit` 表结构含 `actor_id` / `reason_text` / `refs_scanned` / `state_recheck` / `second_confirm_on` 五字段；权限码 `OPERATION_PERMANENT_DELETE` 在 `IpdRolePermissionCatalog` L163。
4. **测试覆盖**：BASE-14 + R152 业务回归测试全绿；引用检查的"有引用阻断"分支已覆盖。
5. **撞号避让**：不在本 ticket 重复实装；如未来业务上要"自动到期清理（一期不做）"或"非超管角色可清除"，须 owner 拍板并落新 ticket，本注解不延伸。

---

## 4. 代码影响面（不改代码）

| 模块 | 影响 |
|---|---|
| `PermanentDeleteService.deleteWithAudit()` | 无需改（R149-C3 已实装） |
| `AdminPermanentDeleteController` | 无需改（已实装） |
| `permanent_delete_audit` 表 | 无需改（V004 DDL 已落） |
| `IpdRolePermissionCatalog` L163 权限码 | 无需改（已登记） |
| DOC-AUTH.md 永久清除权限 | 不改（已落档） |

---

## 5. 测试覆盖建议

- **BASE-14**：永久清除基础用例（已存在）
- **引用阻断**：构造 1 条被奖金 / 需求 / 审批引用的实体，调用 `deleteWithAudit()` 必须返回 422（已存在）
- **二次确认**：首次调用返回 PENDING，二次确认后返回 SUCCESS（已存在）
- **审计永久保留**：删除 `permanent_delete_audit` 表记录应被数据库拦截或权限拦截（已存在）

---

## 6. 边界严守

- ✅ 仅 docs/ 落档（本 ticket 标"已并入 R149-C3"）
- ❌ 不动 R149-C3 已合并的代码 / 测试 / DDL
- ❌ 不擅自做"自动到期清理"等扩域（须 owner 拍板）
- ❌ 不抢 owner 拍板权限

---

> **本 ticket 闭环判定**：C10 在 R153 §2 表格中已为 🟢 绿（已并入 R149-C3）。本注解落档后 🟢 绿-文档；C10 在 R154-E 不再产生新代码工作量。
