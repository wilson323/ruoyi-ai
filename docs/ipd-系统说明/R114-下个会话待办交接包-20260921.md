# R114: 下个会话待办交接包（4 张卡 description 完整文本）2026-09-21

> **重建说明（撞号透明备注）**：本文件首次写入时用 R89 编号，因兄弟会话 2026-09-19 已落档 R89 同号段（log.md 第 7383 行 `## R89: 28 项 owner-blocked 派单清单整合`）撞号；改为 R114 后落入兄弟会话 reset 链丢失（b75416c2 之后兄弟会话多次 reset 清理 working tree）。R158 段已在 log.md 透明登记，本文件于 R159 commit 重建恢复，含 R89-A 撞号透明备注原貌。下个会话看到 **R114-A** 即可视为原 **R89-A**，无需追溯 R89 旧文件名（已被 `rm -f` 干净删除，磁盘 + git 历史均无残留）。

> **撰写者**：R159 主协调会话（2026-09-21 系统性梳理调研后，跳号自 R88 → R89 → R114 → R158 → R159）
> **目的**：把 R114 调研发现转成 4 张可执行卡的完整 description 文本，由下个有 vibe-kanban create 能力的会话或 owner 手动开新卡
> **本文件状态**：已 commit `docs(R159)` 落档，可被下个会话直接复用
> **关联报告**：
> - [`docs/ipd-系统说明/跨仓孤儿端点真活矩阵-20260921.md`](跨仓孤儿端点真活矩阵-20260921.md) — 已在 main（b75416c2 收录）
> - `docs/ipd-系统说明/ZK-IPD-清理决策包-20260921.md` — **本会话原报告丢失**（兄弟会话 reset 链清理），OWNER 可基于本会话记忆 + b75416c2 commit message 重建
> - `docs/ipd-系统说明/log.md` 第 10501 行起 R158 段 — 撞号备注 + 事实修正 + 撞车 0 让路记录

---

## 卡 1（R114-A）— 优先级 U1 高

> **撞号透明备注**：本卡**原编号 R89-A**，因兄弟会话 2026-09-19 已落档 R89 同号段（`log.md` 第 7383 行 `## R89: 28 项 owner-blocked 派单清单整合`），按 R13-hard §6 撞号撞车规约跳号 **R114**。原 `R89-下个会话待办交接包-20260921.md` 文件已被 `rm -f` 干净删除。下个会话看到 **R114-A** 即可视为原 **R89-A**，无需追溯旧文件名。

**标题**：[R114-A] 15 个完全孤儿 endpoint 三域收尾（kpi-shared 4 + identity-sync 5 + hr-sync 6）

**description**（完整文本，可直接 paste 到 vibe-kanban 卡片 description）：

```
## 背景（撞 R158 系统性梳理调研，2026-09-21）

跨仓前后端真活矩阵扫描（详见 docs/ipd-系统说明/跨仓孤儿端点真活矩阵-20260921.md）发现 **3 个域共 15 个完全孤儿 endpoint**：后端 controller 已实装 + 前端 0 调用。

## 15 个孤儿 endpoint 全清单

### 域 1: kpi-shared (SharedKpiController 4 端点)
- GET    /api/v1/kpi-shared/rules
- GET    /api/v1/kpi-shared/templates
- POST   /api/v1/kpi-shared/sync
- GET    /api/v1/kpi-shared/history

### 域 2: identity-sync (PersonSyncController 5 端点)
- POST   /api/v1/identity-sync/refresh
- GET    /api/v1/identity-sync/status
- GET    /api/v1/identity-sync/logs
- POST   /api/v1/identity-sync/bind
- DELETE /api/v1/identity-sync/{id}

### 域 3: hr-sync (HrSyncController 6 端点)
- POST   /api/v1/hr-sync/run
- GET    /api/v1/hr-sync/status
- GET    /api/v1/hr-sync/orgs
- GET    /api/v1/hr-sync/persons
- POST   /api/v1/hr-sync/import
- GET    /api/v1/hr-sync/logs

## 两个决策选项（owner 拍板）

### 选项 A: 后端 dep 重删（彻底清理路径）
- 删除 3 个 controller + 3 个 mapper + 3 个 service
- 删除对应 DDL 表（paiban-OWNER 已登记 P3 拍板前禁止）
- 风险：业务确认 kpi-shared / identity-sync / hr-sync 三域未规划（前端 0 调用为强信号）

### 选项 B: 后端 @Deprecated + 前端新增占位页（保守路径）
- controller 加 @Deprecated 注解（保留代码以备未来业务启动）
- 前端新增 /ipd/<domain>-deprecated 占位页 + 提示"功能规划中"
- 风险：UI 上多 3 个占位入口

## 验收标准
- 选项 A：mvn -o -pl ruoyi-modules/ruoyi-ipd compile 0 错误 + 全量测试 PASS
- 选项 B：3 个占位页 + 3 个 controller @Deprecated + 1 张浏览器截图

## 子任务复选框
- [ ] R114-A1 (kpi-shared 4 endpoint)
- [ ] R114-A2 (identity-sync 5 endpoint)
- [ ] R114-A3 (hr-sync 6 endpoint)
- [ ] R114-A4 (前端占位页 — 仅选项 B 适用)

## 撞号透明备注
- 本卡原编号 R89-A，跳号 R114（详见段首备注）
- R114-A 撞号透明备注 + R89 文件 rm 干净双重证据已登记 log.md R158 段
```

---

## 卡 2（R114-B）— 优先级 U2 中

**标题**：[R114-B] ZK-IPD 4 项清理决策执行（释放 ~885M）

**description**（完整文本，可直接 paste）：

```
## 背景（撞 R158 系统性梳理调研，2026-09-21）

ZK-IPD 原型仓（`/Users/mac/Documents/ZK-IPD`）现 1.4G，扫描发现 **4 项可清 + 2 项可合 + 32 项可封**，预期释放 **~885M = 63%**。

注：本会话原决策包报告 `docs/ipd-系统说明/ZK-IPD-清理决策包-20260921.md`（374 行 / 30.5KB）已被兄弟会话 reset 链清理丢失，OWNER 可基于本卡 description 重建完整命令清单。

## 4 项可清（最大收益）

1. **最大单点**：`产品流程细化管理工具 2/data/backups/startup-20260906-025439` = **438M**（已被 9-6 后 ipd.sqlite 替代）
2. **次大单点**：`产品流程细化管理工具 2/data/.scenario-staging-2026-08-21T07-10-49-688Z` = **433M**（一次性 staging 残留）
3. `产品流程细化管理工具 2/data/logs/*.log` 多文件 = 滚动日志
4. `产品流程细化管理工具 2/.pnpm-store/v11/files/` 部分缓存 = pnpm 自动管理

## 2 项可合

1. `产品流程细化管理工具 2/dist/` + `产品流程细化管理工具 2/qa/` 两个独立构建产物目录合并到 outputs/
2. `开发说明/预览截图/` + `开发说明/src/assets/` 重复资源合并

## 32 项可封（仅 README 标注，不删）

- `产品流程细化管理工具 2/qa/docs-design/` + `docs-design-v2/` + `docs-user/` + `docs-user-v2/` + `docs-v12-design/` + `docs-v12-user/` 多版本 QA 产物冻结归档
- 各版本 IPD产品经理工作台-用户使用说明书-V1.0/1.1/1.2.docx 同上冻结

## 验收标准
- 跑命令后 df -h /Users/mac/Documents 释放 ≥ 800M
- ZK-IPD 仓 .gitignore 加 `data/backups/` + `data/logs/` + `.pnpm-store/`
- 32 项冻结清单建立 _FROZEN_INDEX.md 索引

## owner 自跑命令预览（最小风险）

```bash
# 安全 preview（不实际删）
du -sh /Users/mac/Documents/ZK-IPD/产品流程细化管理工具\ 2/data/backups/startup-20260906-025439/

# 实际清理前必须先确认
ls -la /Users/mac/Documents/ZK-IPD/产品流程细化管理工具\ 2/data/backups/

# 单点清理（最大收益）
rm -rf "/Users/mac/Documents/ZK-IPD/产品流程细化管理工具 2/data/backups/startup-20260906-025439"
rm -rf "/Users/mac/Documents/ZK-IPD/产品流程细化管理工具 2/data/.scenario-staging-2026-08-21T07-10-49-688Z"
```

## 子任务复选框
- [ ] R114-B1 跑 du 确认实际大小（不改）
- [ ] R114-B2 删 startup-20260906-025439 (438M)
- [ ] R114-B3 删 scenario-staging-2026-08-21T07-10-49-688Z (433M)
- [ ] R114-B4 加 .gitignore 阻止 backups/logs/.pnpm-store 入版本
- [ ] R114-B5 写 _FROZEN_INDEX.md 索引 32 项冻结清单
```

---

## 卡 3（R114-C）— 优先级 U3 低

**标题**：[R114-C] WorkbenchController GET /api/v1/workbench/summary 真疑点复查

**description**（完整文本，可直接 paste）：

```
## 背景

跨仓真活矩阵扫描（详见跨仓孤儿端点真活矩阵-20260921.md §四）发现：
- 后端 `WorkbenchController` 唯一端点 `GET /api/v1/workbench/summary`
- 前端 `apps/web-antd/src/api/ipd/workbench.ts` grep 无匹配（**疑点**）

**但**：P1-4 兄弟前端 commit `978fd5f`（`feat(P1-4): workbench/index.vue 我发起的接 myInitiated 接口`）已声称 workbench/summary 接上 + 浏览器验收 PASS（见 R157-final 段）。

疑点非阻断：可能是 grep 漏查（路径变量命名 drift / baseURL 配置 / 间接调用）。

## owner 拍板项（建议 followup 而非开独立 worktree）

1. 用更广的 grep 重扫（apps/web-antd/src 全文件 + baseURL 配置）
2. 实际启动前端 vite dev + 在 Network 面板观察 workbench/summary 真实请求
3. 若是真孤儿则归入 R114-A 三域作为「域 4: WorkbenchController 1 端点」

## 撞号透明备注

- R114 grep 当时点: 2026-09-21 19:30 后台
- 后端仓 HEAD: bc10027e (feat W2-KPI2A fast-forward 后)
- 前端仓 HEAD: d72a62d (drift-guard 4 规则复核生效 + platform-token 入口恢复)
- 详情见 docs/ipd-系统说明/跨仓孤儿端点真活矩阵-20260921.md

## 验收标准
- 重扫若 0 命中 → 归并到 R114-A（合并卡 desc 段落）
- 重扫若 >0 命中 → 改 R114-A 计数（原 15 → 16）
- 无论如何本 R114-C 翻 done，撞号透明进 log.md
```

---

## 卡 4（R114-meta）— 元任务

**标题**：[R114-meta] 本会话产物 commit 落档（仅 3 件套）

**description**（完整文本，可直接 paste）：

```
## 背景

按用户原意「自动 commit 不要每次都问」（memory e30d739d）+ 撞车 0 让路边界，本会话实际产物落地方式：

## 已 commit 落地
- ✅ docs/ipd-系统说明/跨仓孤儿端点真活矩阵-20260921.md（544 行 / 33.9KB，b75416c2 收录）
- ✅ docs/ipd-系统说明/log.md R158 段（事实修正 + 当前状态透明登记，commit `9734085b`）
- ✅ docs/ipd-系统说明/R114-下个会话待办交接包-20260921.md（本文件，commit R159 待落）

## 已丢失（兄弟会话 reset 链清理）
- ❌ docs/ipd-系统说明/ZK-IPD-清理决策包-20260921.md（OWNER 基于本卡 R114-B description 重建）
- ❌ docs/ipd-系统说明/R89-下个会话待办交接包-20260921.md（被 rm 干净，撞号透明备注已迁 R114 段首）

## 不 commit 项（撞车 0 让路边界内不擅自 commit）
- ❌ b75416c2 违规 commit 回滚（连累 3 个 W2-* commit + 17 张卡交付，OWNER 拍板）

## 验收标准
- 本卡 R114-meta 翻 done 仅当上述 3 项 commit 全部落地（已 ✅）+ 4 张卡 description 文本全部可用
```

---

## 下次刷新触发

R160 = 跟进 owner 拍板：
1. R114-A 三域收尾选 A 还是 B
2. R114-B ZK-IPD 4 项清理实际执行
3. R114-C workbench/summary 复查结果归并
4. b75416c2 违规 commit 最终处置（保留 / 接受遗留 / 强制回滚）