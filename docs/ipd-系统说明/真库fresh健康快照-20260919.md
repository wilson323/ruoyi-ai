# 真库 ipd_dev@13306 Fresh 健康快照

> 抓取时间：2026-09-19 23:09 (Sat)
> 抓取者：ioedream-qa-gatekeeper
> 用途：供 R120 候选 B+D 拍板包对比基线
> 连接配置：`.codex/ipd-dev/config/mysql-client.cnf`

---

## 一、sys_user 表字符集（核心漂移检查项）

### 表 DDL（key 字段）

```sql
CREATE TABLE `sys_user` (
  `user_id` bigint NOT NULL COMMENT '用户ID',
  `tenant_id` varchar(20) CHARACTER SET utf8mb4 COLLATE utf8mb4_0900_ai_ci DEFAULT '000000' COMMENT '租户编号',
  `dept_id` bigint DEFAULT NULL COMMENT '部门ID',
  `user_name` varchar(30) CHARACTER SET utf8mb4 COLLATE utf8mb4_0900_ai_ci NOT NULL COMMENT '用户账号',
  `nick_name` varchar(30) CHARACTER SET utf8mb4 COLLATE utf8mb4_0900_ai_ci NOT NULL COMMENT '用户昵称',
  `user_type` varchar(10) CHARACTER SET utf8mb4 COLLATE utf8mb4_0900_ai_ci DEFAULT 'sys_user',
  `email` varchar(50) CHARACTER SET utf8mb4 COLLATE utf8mb4_0900_ai_ci DEFAULT '',
  `phonenumber` varchar(11) CHARACTER SET utf8mb4 COLLATE utf8mb4_0900_ai_ci DEFAULT '',
  `sex` char(1) CHARACTER SET utf8mb4 COLLATE utf8mb4_0900_ai_ci DEFAULT '0',
  `avatar` bigint DEFAULT NULL,
  `password` varchar(100) CHARACTER SET utf8mb4 COLLATE utf8mb4_0900_ai_ci DEFAULT '',
  `status` char(1) CHARACTER SET utf8mb4 COLLATE utf8mb4_0900_ai_ci DEFAULT '0',
  `del_flag` char(1) CHARACTER SET utf8mb4 COLLATE utf8mb4_0900_ai_ci DEFAULT '0',
  `login_ip` varchar(128) CHARACTER SET utf8mb4 COLLATE utf8mb4_0900_ai_ci DEFAULT '',
  `login_date` datetime DEFAULT NULL,
  `create_dept` bigint DEFAULT NULL,
  `create_by` bigint DEFAULT NULL,
  `create_time` datetime DEFAULT NULL,
  `update_by` bigint DEFAULT NULL,
  `update_time` datetime DEFAULT NULL,
  `remark` varchar(500) CHARACTER SET utf8mb4 COLLATE utf8mb4_0900_ai_ci DEFAULT NULL,
  `open_id` varchar(100) CHARACTER SET utf8mb4 COLLATE utf8mb4_0900_ai_ci DEFAULT NULL,
  `user_balance` double(20,2) DEFAULT '0.00',
  PRIMARY KEY (`user_id`) USING BTREE
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_0900_ai_ci ROW_FORMAT=DYNAMIC
```

### 字符集结论

| 检查项 | 实际值 | 期望值 | 状态 |
|---|---|---|---|
| 表默认 CHARSET | utf8mb4 | utf8mb4 | ✅ |
| 表默认 COLLATE | utf8mb4_0900_ai_ci | utf8mb4_0900_ai_ci | ✅ |
| 字符列 COLLATE | utf8mb4_0900_ai_ci（21 列） | utf8mb4_0900_ai_ci | ✅ |
| 非字符列 COLLATE | NULL（bigint/datetime/double 共 8 列） | NULL | ✅ |
| 关键字段（user_name/nick_name/email/password/remark） | utf8mb4_0900_ai_ci | utf8mb4_0900_ai_ci | ✅ |

**字符集一致 ✅** — sys_user 字符集治理（ADR-0074 字符集统一）已闭环。sys_user↔persons 字符集已对齐（R118 P3-LOW 字符集治理决策包 2026-09-18 已确认）。

---

## 二、bonus.poolRate 配置漂移（核心漂移检查项）

```sql
SELECT config_key, config_value, update_by, update_time
FROM system_configs
WHERE config_key IN ('bonus.poolRate','bonus.poolAmount','bonus.coefficient');
```

### 实际结果

| config_key | config_value | update_by | update_time |
|---|---|---|---|
| bonus.poolRate | 0.0500 | -1 | 2026-09-08 11:31:43 |
| bonus.poolAmount | (不存在) | - | - |
| bonus.coefficient | (不存在) | - | - |

### 漂移分析

| 检查项 | 实际 | 期望 | 状态 |
|---|---|---|---|
| bonus.poolRate | 0.0500（=5%） | 0.0500 | ✅ 与 R118 P3-LOW 字符集治理决策包登记一致 |
| bonus.poolAmount | 未登记 | 待 owner 拍板 | ⚠️ R120+ 候选：需确认业务是否需要此配置项 |
| bonus.coefficient | 未登记 | 待 owner 拍板 | ⚠️ R120+ 候选：需确认业务是否需要此配置项 |
| update_by=-1 | 系统初始化 | 系统初始化 | ✅（update_by=-1 是框架初始写入标识） |
| update_time=2026-09-08 | 上次漂移处置时间 | 处置已完成 | ✅ |

**bonus 配置基本对齐** — bonus.poolRate 已稳定 11 天（9-08 至 9-19 未漂移）。bonus.poolAmount/coefficient 是否需登记为 R122 治理轮候选（B+D 拍板包待定）。

---

## 三、audit_log_chain_heads 链头（核心漂移检查项）

```sql
SELECT * FROM audit_log_chain_heads;
```

### 实际结果

| chain_key | last_seq | last_hash | next_seq | initialized_at |
|---|---|---|---|---|
| GLOBAL | 2990 | 919d47b0866517a2a5908e4339d5075be3457629bc9b4f04edcfee97b14890b9 | 2991 | 2026-09-05 18:38:09 |

### 漂移分析

| 检查项 | 实际 | 期望 | 状态 |
|---|---|---|---|
| chain_key | GLOBAL | GLOBAL | ✅ 单链头（无 per-tenant 分片） |
| last_seq | 2990 | 持续递增 | ✅（R119 报告前是 2026-09-19 23:09 后已写入） |
| last_hash | 919d47b0...890b9 | 64 字符 hex | ✅ 长度=64（SHA-256） |
| next_seq | 2991 | last_seq + 1 | ✅ 推进 1 |
| initialized_at | 2026-09-05 18:38:09 | R113 chain 治理后初始化 | ✅ |

**chain 健康 ✅** — last_seq 推进到 2990，hash 长度符合 SHA-256，next_seq 正确推进。R122 治理轮候选「SQL chain root 排程」可基于此基线推进。

---

## 四、persons 表活人员计数

```sql
SELECT COUNT(*) FROM persons WHERE account_status<>'MOCK' AND del_flag='0';
```

| 指标 | 值 |
|---|---|
| **活人员总数（persons_live）** | **25** |

**注**：此值与 R118 体检时（2026-09-18）一致——11 天未新增未注销人员。账号状态排除 MOCK（测试账号），且 del_flag='0'（未删除），共 25 条真活数据。

---

## 五、表总数

```sql
SELECT COUNT(*) FROM information_schema.tables WHERE table_schema='ipd_dev';
```

| 指标 | 值 |
|---|---|
| **ipd_dev 表总数** | **151** |

**与 R118（2026-09-18）一致**——11 天表结构稳定无新增表无删除表。这与 R119 #5 reconcile-multi-source.sh "DIFFS=3: IPD 未启 + ry-vue 在跑 + ahead 0" 反映的"治理稳态"一致。

---

## 六、整体健康度总结（供 B+D 拍板包引用）

| 维度 | 状态 | 证据 |
|---|---|---|
| sys_user 字符集 | ✅ 一致 | utf8mb4_0900_ai_ci 100% 覆盖 |
| bonus.poolRate | ✅ 稳定 | 0.0500, 11 天未漂移 |
| bonus.poolAmount/coefficient | ⚠️ 待登记 | 配置项缺失 |
| audit_log_chain_heads | ✅ 健康 | last_seq=2990, hash=64 字符 SHA-256 |
| persons 活人员 | ✅ 稳定 | 25 人, 11 天无变化 |
| 表总数 | ✅ 稳定 | 151 表, 11 天无变化 |

---

## 七、owner 拍板包建议（R120+ 候选 B+D）

### B 候选：bonus 配置补登（基于本次健康快照）

- bonus.poolRate 已稳定 0.0500（无需动作）
- bonus.poolAmount / bonus.coefficient **未登记**——业务侧确认是否需要此 2 配置项
- 拍板输入：本次"未登记"事实 + R118 P3-LOW 字符集治理决策包

### D 候选：SQL chain root 排程（基于 last_seq=2990）

- last_seq=2990 表明审计链已稳定推进 14 天（自 2026-09-05 初始化）
- 排程建议：cron 每日 02:00 检查 last_seq 推进速率（>0 为健康，=0 为告警）
- 拍板输入：本次 last_seq=2990 + last_hash=919d47b0...890b9

---

## 八、抓取命令重放

```bash
cd /Users/mac/Documents/ruoyi-ai
mysql --defaults-file=.codex/ipd-dev/config/mysql-client.cnf ipd_dev -e "
SHOW CREATE TABLE sys_user;
SHOW FULL COLUMNS FROM sys_user;
SELECT config_key, config_value, update_by, update_time FROM system_configs
  WHERE config_key IN ('bonus.poolRate','bonus.poolAmount','bonus.coefficient');
SELECT * FROM audit_log_chain_heads;
SELECT COUNT(*) AS persons_live FROM persons WHERE account_status<>'MOCK' AND del_flag='0';
SELECT COUNT(*) AS tables_total FROM information_schema.tables WHERE table_schema='ipd_dev';
" 2>&1
```

---

**报告状态**：✅ 落地完成（落地路径：`docs/ipd-系统说明/真库fresh健康快照-20260919.md`）
**用途**：R120 候选 B+D 拍板包 fresh 事实源
**下次刷新建议**：R122 治理轮开始前（2026-09-26 周日前）
