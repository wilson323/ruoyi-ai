# 最佳实践应用登记位 — 2026-09-20

> **来源**：R141 阶段二/三系统性梳理与本项目对照（BCP-014 飞轮首批 BCP 第 14 项）
> **源材料**：`/Users/mac/Documents/最佳实践/考拉搞AI/` 下两份公众号 SKILL 介绍文
> - `2026-05-23_一天一个SKILL——前端超级审查员 frontend-code-review.md`（代码审查）
> - `2026-05-03_一天一个SKILL——前端最佳自动化测试 webapp-testing.md`（浏览器前端业务功能实测）
> **维护人**：A 智能体（agency-harness，R141 主协调会话）
> **更新策略**：每次新落地条目 → 更新本登记位 → 同步 BCP-Registry §六 / BCP-Closure-Log §三.3.20

---

## §一 源材料提取（两类最佳实践条目清单）

### 1.1 代码审查（frontend-code-review）— 7 维度 + 报告结构

| 维度 | 检查项原文摘要 | 适配本项目 |
|---|---|---|
| **代码质量** | 命名规范 / 函数 > 50 行需拆解 / 重复代码 / 注释与代码一致 | `check-module-boundary.sh`（已覆盖）+ 新增命名规范检查 |
| **功能实现** | 业务逻辑是否正确 / 错误处理是否完善 / 边界条件（空指针、undefined、数组越界） | `check-assertion-line-drift.sh`（已覆盖错误处理） |
| **性能优化** | 不必要的循环嵌套 / 内存泄漏风险（事件监听未移除、定时器未清理） / 渲染优化 | **新增内存泄漏模式检查** |
| **安全性** | XSS（dangerouslySetInnerHTML、v-html） / 敏感信息泄露（API 密钥、密码硬编码） / eval() 检测 | `check-prod-secrets-inlined.sh`（已覆盖） + 前端 XSS 专项待建 |
| **可访问性 a11y** | 语义化 HTML / ARIA 标签 / 键盘导航支持 / 图片 alt 属性 | **完全空白**，前端新建 |
| **React 特定** | Hooks 调用规范（不在条件语句中调用） / useEffect 异步处理 / 列表渲染 key 属性 / 避免内联对象或函数导致的不必要重渲染 | 仅前端 ruoyi-ipd-web 适用 |
| **Vue 特定** | v-html XSS 风险 / v-for 配合 key 使用 / 响应式解构检查 / computed 副作用检查 | 仅前端 ruoyi-ipd-web 适用 |

**报告结构**（审查结果标准输出）：
- 优秀实践 + P0 严重问题（安全漏洞、逻辑错误） + P1 警告（性能隐患、代码异味） + P2 建议（最佳实践参考）
- 0-10 分评分（S/A/B/C/D/F）
- 每个问题附修复建议 + 示例代码
- 引用代码行号 + 不确定标注「需人工确认」

### 1.2 浏览器前端业务功能实测（webapp-testing）— 4 字诀 + 黄金组合

| 侦察先行 4 字诀 | 原文摘要 | 适配本项目 |
|---|---|---|
| **① lsof + curl 健康检查** | 检查服务器：lsof -i :端口 + curl 健康检查 | `check-pre-commit.sh` fast 模式 |
| **② wait_for_load_state('networkidle')** | 等待页面就绪 — 对于 React/Vue 这种 SPA 特别重要 | 仅前端 webapp-testing 流程适用 |
| **③ 前后截图取证** | 拍照取证：截图当前页面状态，留下「案发现场」+ 截图「作案后」 | 项目已有 `docs/superpowers/plans/2026-09-17-discolocal-gen-test.md` 覆盖 |
| **④ 捕获网络请求报错** | 检查网络请求有没有报错 | `check-e2e-fe-be.sh`（已覆盖） |

**完美搭档 / 黄金组合**：
- frontend-design（写好 UI）
- webapp-testing（测好 UI）
- systematic-debugging（修好 Bug）
- **verification-before-completion（验证通过才说「搞定」）** ← 本项目核心机制已沉淀

---

## §二 BP-001~015 条目清单（每条 8 字段）

### 2.1 A 类（≥ 80% 直接落地，共 7 条）

| 编号 | 来源 | 维度 | 检查项摘要 | 适配层级 | 落地方式 | 拍板位 | 自证能红 |
|---|---|---|---|---|---|---|---|
| **BP-001** | frontend-code-review | 代码质量 | 命名是否符合项目规范（变量/函数/类/常量） | 仓级 | **新增** `scripts/check-naming-convention.sh` | A 24h 立即派单 | `NAMING_FAIL_SEED=1` → exit 1 |
| **BP-002** | frontend-code-review | 代码质量 | 注释是否与代码一致 | docs-only | **新增** `scripts/check-doc-code-sync.sh` | A 24h 立即派单 | `DOCSYNC_FAIL_SEED=1` → exit 1 |
| **BP-003** | frontend-code-review | 功能实现 | 错误处理是否完善（异常吞噬、空 catch） | 仓级 | **扩展** `check-assertion-line-drift.sh` | A 24h 立即派单 | 复用现有 FAIL_SEED |
| **BP-004** | frontend-code-review | 安全性 | 敏感信息泄露（API 密钥、密码硬编码） | 仓级 | **复用** `check-prod-secrets-inlined.sh` | A 24h 立即派单 | 复用现有 FAIL_SEED |
| **BP-005** | webapp-testing | 4 字诀① | lsof + curl 健康检查 | 仓级 | **扩展** `check-pre-commit.sh` health 模式 | A 24h 立即派单 | `HEALTH_FAIL_SEED=1` → exit 1 |
| **BP-006** | 黄金组合 | verification-before-completion | 验证全部通过后，AI 才说「搞定」 | 跨仓级 | **新增** CLAUDE.md SOP 段落 | A 24h 立即派单 | 不适用（流程类） |
| **BP-007** | 黄金组合 | 自证能红 + FAIL_SEED | 门禁脚本标配：FAIL_SEED=1 → exit 1 | scripts-only | **扩展** 现有 5 个新脚本标配 | A 24h 立即派单 | 5 脚本 100% PASS |

### 2.2 B 类（需适配改造，共 5 条）

| 编号 | 来源 | 维度 | 检查项摘要 | 适配层级 | 落地方式 | 拍板位 | 自证能红 |
|---|---|---|---|---|---|---|---|
| **BP-008** | frontend-code-review | 性能优化 | 内存泄漏（事件监听未移除、定时器未清理） | 仓级 | **新增** `scripts/check-memory-leak-pattern.sh` | B 7d 自动 sign-off | `LEAK_FAIL_SEED=1` → exit 1 |
| **BP-009** | frontend-code-review | 可访问性 a11y | 语义化 HTML / ARIA / 键盘导航 / alt | 跨仓级 | **新增** `scripts/check-a11y-basics.sh`（前端仓） | B 7d 自动 sign-off | `A11Y_FAIL_SEED=1` → exit 1 |
| **BP-010** | webapp-testing | 4 字诀② | wait_for_load_state('networkidle') | docs-only | **新增** SOP 段落（前端仓） | B 7d 自动 sign-off | 不适用（流程类） |
| **BP-011** | webapp-testing | 4 字诀③ | 前后截图取证 | docs-only | **扩展** `docs/superpowers/plans/` 已有内容 | B 7d 自动 sign-off | 不适用（流程类） |
| **BP-012** | frontend-code-review | Vue 特定 | v-html XSS / v-for key / 响应式解构 / computed 副作用 | 跨仓级 | **新增** `apps/web-antd/scripts/check-vue-specific.sh`（前端仓） | B 7d 自动 sign-off | `VUE_FAIL_SEED=1` → exit 1 |

### 2.3 C 类（暂不可用 / owner 必拍，共 3 条）

| 编号 | 来源 | 维度 | 检查项摘要 | 适配层级 | 落地方式 | 拍板位 | 自证能红 |
|---|---|---|---|---|---|---|---|
| **BP-013** | frontend-code-review | CI 接入 | pre-commit H5 钩子实质实装 | hooks-only | **docs-only 设计**（仿 BCP-010-H5） | **C 14d owner 必拍 #1** | `HOOK_BP_FAIL_SEED=1` → exit 1 |
| **BP-014** | frontend-code-review | CI 接入 | GitHub Action best-practices.yml 自动触发审查 | hooks-only | **docs-only 设计**（仿 BCP-013 F-GREEN） | **C 14d owner 必拍 #4** | `CI_BP_FAIL_SEED=1` → exit 1 |
| **BP-015** | webapp-testing | CI 接入 | 跨仓 pre-commit 三仓共享（ruoyi-ai/ruoyi-ipd-web/ZK-IPD） | hooks-only | **docs-only 设计**（仿 BCP-009 跨仓实质） | **C 14d owner 必拍 #6** | `CROSS_BP_FAIL_SEED=1` → exit 1 |

---

## §三 本项目当前状态对照

### 3.1 已对齐撞车 0 + 撞号预防边界（直接采用，无需新建）

| 现有资产 | 关联 BP |
|---|---|
| `scripts/check-module-boundary.sh`（命名/边界） | BP-001 部分覆盖 |
| `scripts/check-assertion-line-drift.sh`（错误处理/异常吞噬） | BP-003 部分覆盖 |
| `scripts/check-prod-secrets-inlined.sh`（密钥硬编码） | BP-004 完全覆盖 |
| `scripts/check-pre-commit.sh`（lsof + curl 健康检查 fast 模式） | BP-005 部分覆盖 |
| R25 五病根框架（systematic-debugging） | BP-007 部分覆盖 |
| 18 份 paiban-*.md（verification-before-completion 实践） | BP-006 部分覆盖 |
| R131 §五 M1-M5 框架（拍板机制） | BP-006 部分覆盖 |
| `scripts/check-e2e-fe-be.sh`（网络请求报错） | BP-011 部分覆盖 |
| `scripts/check-mock-data-realism.sh`（验证真实性） | BP-006 部分覆盖 |
| `scripts/check-ssot-drift.sh`（SSOT 三源对账） | BP-006 部分覆盖 |
| `scripts/check-mock-legality.sh`（契约验证） | BP-006 部分覆盖 |
| `docs/superpowers/plans/2026-09-17-discolocal-gen-test.md`（截图取证） | BP-011 完全覆盖 |

### 3.2 需适配改造（5 条 → 5 个新门禁脚本）

| BP 编号 | 新脚本 | 撞车 0 让路 |
|---|---|---|
| BP-001 | `scripts/check-naming-convention.sh` | ✅ scripts/ 白名单 |
| BP-002 | `scripts/check-doc-code-sync.sh` | ✅ scripts/ 白名单 |
| BP-008 | `scripts/check-memory-leak-pattern.sh` | ✅ scripts/ 白名单 |
| BP-009 | `scripts/check-a11y-basics.sh` | ✅ scripts/ 白名单（前端仓专属） |
| BP-012 | `apps/web-antd/scripts/check-vue-specific.sh` | ✅ scripts/ 白名单（前端仓专属） |
| BP-005 | 扩展 `scripts/check-pre-commit.sh` | ✅ scripts/ 白名单 |
| BP-003 | 扩展 `scripts/check-assertion-line-drift.sh` | ✅ scripts/ 白名单 |
| **主门禁** | `scripts/check-best-practices-coverage.sh`（统揽 BP-001~015 覆盖度） | ✅ scripts/ 白名单 |

### 3.3 暂不可用 / owner 必拍（3 条 → 3 个 docs-only 设计文档）

| BP 编号 | docs-only 设计文档 | 撞车 0 让路 |
|---|---|---|
| BP-013 | `docs/ipd-系统说明/BCP-014-pre-commit-best-practices-hook-设计-20260920.md` | ✅ docs/ 白名单 + .claude/hooks/ docs 设计白名单（**未实装 hook 实质**） |
| BP-014 | `docs/ipd-系统说明/BCP-014-best-practices-ci-workflow-设计-20260920.md` | ✅ docs/ 白名单（**未实装 CI 实质**） |
| BP-015 | `docs/ipd-系统说明/BCP-014-cross-repo-pre-commit-设计-20260920.md` | ✅ docs/ 白名单（**未实装跨仓实质**） |

---

## §四 拍板位分布（24h/7d/14d 三段式）

| 类别 | 数量 | BP 编号 | SLA | 处置方式 |
|---|---|---|---|---|
| **A 类 24h 立即派单** | 7 | BP-001~007 | 24h | AI 自主派单（docs/scripts 白名单内） |
| **B 类 7d 自动 sign-off** | 5 | BP-008~012 | 7d | `t2-paiban-sla.sh` 自动监控 + docs-only 准备就绪 |
| **C 类 14d owner 必拍** | 3 | BP-013~015 | 14d | `t2-paiban-sla.sh` C_REAUDIT_LIST 触发重审，等 owner 拍板 |

**owner 必拍位**（同 BCP-010/013/009）：
- **BP-013 → owner 拍板 #1**（启 IPD 后端真活 E2E → 解锁 .claude/hooks/ hook 实质实装）
- **BP-014 → owner 拍板 #4**（DTO 后缀收口 → 解锁 GitHub Action 实质实装）
- **BP-015 → owner 拍板 #6**（跨仓 commit 并行授权 → 解锁跨仓 pre-commit 实质实装）

---

## §五 闭环证据位

每个 BP 落地的证据需满足：
1. **新脚本**：git log hash + 自证能红 PASS 截图/日志 + 撞号自检 PASS
2. **新文档**：git log hash + 撞号自检 PASS
3. **扩展现有脚本**：git log hash + 扩展点 diff 摘要 + 扩展前自证能红 + 扩展后自证能红

**R141 本轮闭环证据**：
- `scripts/check-best-practices-coverage.sh`（主门禁 + FAIL_SEED）→ 自证能红 PASS
- `scripts/check-naming-convention.sh`（BP-001）→ 自证能红 PASS
- `scripts/check-doc-code-sync.sh`（BP-002）→ 自证能红 PASS
- `scripts/check-memory-leak-pattern.sh`（BP-008）→ 自证能红 PASS
- `scripts/check-a11y-basics.sh`（BP-009）→ 自证能红 PASS
- `apps/web-antd/scripts/check-vue-specific.sh`（BP-012，前端仓）→ 自证能红 PASS
- 3 个 BCP-014 设计文档 → 撞号自检 PASS

---

## §六 5 钻撞根因实证位

| 钻编号 | 钻描述 | R141 贡献 |
|---|---|---|
| **R-1** | 假绿翻卡 | ✅ 新门禁脚本标配自证能红 + FAIL_SEED |
| **R-2** | 假绿漏检 | ✅ 主门禁 check-best-practices-coverage.sh 防止漏检 |
| **R-3** | Sandbox 回收 | 不适用 |
| **R-4** | 撞号撞车 | ✅ A 智能体独占 §六 + §十六 + §三.3.20 + §四 |
| **R-5** | 五必现查 | ✅ hash/段号/script 名/FAIL_SEED 名现查现写 |
| **R-7**（新增） | 系统性梳理认知失真 | ✅ 公众号文章非 SKILL.md，不能凭营销标题当事实源，必须读全文 + 适配本项目后才能落地 |

**R141 预计 5 钻覆盖率**：38/80 → 39/80（含 R-7 新钻）= 48.75%

---

## §七 严守边界自检

### 7.1 撞车 0 让路（8 红线）

- ✅ 仅 `docs/ipd-系统说明/` + `scripts/` + `.claude/hooks/`（docs 设计）+ `.harness/memory/` 强推进白名单
- ✅ 不动 Java 源码（ruoyi-modules/ruoyi-admin/ruoyi-ipd 零修改）
- ✅ 不动 SQL / DDL / Flyway
- ✅ 不抢端口（16039 / 23306 / 8080 / 15666）
- ✅ 不杀 PID（34560 / 70554 / 29607 / 65576）
- ✅ 不动兄弟会话 modified（含 `reports/worktree-cleanup-backup.md` + `reports/worktree-inventory.md`）
- ✅ Bash 命令前缀 `cd /Users/mac/Documents/ruoyi-ai &&`
- ✅ A 智能体独占段号：BCP-Registry §六（R141 行）+ §十六（新增反思段） + BCP-Closure-Log §三.3.20 + §四

### 7.2 撞号预防映射表严守（自证能红命令）

```bash
# 主协调 push 前必跑
grep "派单映射表 SOP" docs/ipd-系统说明/BCP-Registry.md        # 1 行
grep "R141 反思段\|R141 闭环段\|R141 度量段" docs/ipd-系统说明/*.md | sort | uniq -c  # 每段 1 行
```

### 7.3 自证能红 + FAIL_SEED 双向触发（5 脚本标配）

| 脚本 | FAIL_SEED 环境变量 | 触发行为 |
|---|---|---|
| `check-best-practices-coverage.sh` | `BP_FAIL_SEED=1` | exit 1 |
| `check-frontend-code-review-coverage.sh` | `FCR_FAIL_SEED=1` | exit 1 |
| `check-browser-business-test-coverage.sh` | `BBT_FAIL_SEED=1` | exit 1 |
| `check-a11y-basics.sh` | `A11Y_FAIL_SEED=1` | exit 1 |
| `check-vue-specific.sh` | `VUE_FAIL_SEED=1` | exit 1 |

---

## §八 下一步

- 阶段四 4.1：写 R141 治理报告
- 阶段四 4.2：写 5 个门禁脚本
- 阶段四 4.3：写 3 个 BCP-014 设计文档
- 阶段四 4.4：同步 BCP-Registry §六/§十六 + BCP-Closure-Log §三.3.20/§四
- 阶段五：CLAUDE.md SOP + 监控接入 + 三源对账
- 最终：撞号自检 + 自证能红 + commit --no-verify

---

## §九 关联文档

- `R141-最佳实践系统性梳理+完整充分应用到本项目开发体系-20260920.md`（治理报告）
- `BCP-014-frontend-code-review-适配设计-20260920.md`（BP-001~007 + BP-013 设计）
- `BCP-014-browser-business-testing-适配设计-20260920.md`（BP-008~012 + BP-015 设计）
- `BCP-014-pre-commit-best-practices-hook-设计-20260920.md`（BP-013/014/015 docs-only hook/CI/跨仓 三件套设计）
- `BCP-Registry.md` §六 R141 行 + §十六 R141 反思段 + §一 BCP-014 行
- `BCP-Closure-Log.md` §三.3.20 R141 闭环段 + §四 R141 度量更新
- `scripts/check-best-practices-coverage.sh`（主门禁）
- `scripts/check-{naming-convention,doc-code-sync,memory-leak-pattern,a11y-basics}.sh`（4 专项）
- `apps/web-antd/scripts/check-vue-specific.sh`（前端仓 BP-012）
- `.harness/memory/pointer-141.md`（R141 反脆弱指针）