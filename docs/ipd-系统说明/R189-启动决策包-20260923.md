# R189 启动决策包（2026-09-23）

> **撰写者**：ioedream-evolver（R188 fresh 盘点之后的治理轮启动决策）
> **目的**：基于 R188 真实盘点 + R186 owner 拍板结论 + R25 现状，对 R189 启动触发条件、主线选择、清单重评、派单建议做一次性输出。
> **撞车 0 严守**：本文件仅 docs/ipd-系统说明/ 落档，不 commit，不动 Java/Vue/SQL/yml/真库/端口/PID/看板卡 status。

---

## 一、R189 启动触发条件（基线现查 2026-09-23）

| 项 | 现查值 | R188 基线 | 真实差异 |
|---|---|---|---|
| 后端 HEAD | `30f876f6` | `30f876f6` | ✅ 一致 |
| origin/main | **`fb46c1c9`** | （R188 未现查） | **+1 commit 落后**（兄弟会话 R186 自审致歉）|
| 工作树 | 0 改 + 1 untracked = R188 报告本身 | 0 改 + 1 untracked | ✅ 一致 |
| 后端 16039 PID | 28071 在跑 | 28071 在跑 | ✅ 不动 |
| 看板 62250 PID | 88601 在跑 | 88601 在跑 | ✅ 不动 |
| 真库 ipd_dev @ 13306 | listening（R188 表数 160） | 160 | ✅ 不动 |
| worktree 数 | **13 个**（含 r174/177-a1~a6/p47/r120/r121/r126×2/r127×3/r157）| — | 13 个兄弟会话并行 |

**R188 报告"分叉 0/0"实为陈旧判定** — 本仓现查落后 origin/main **1 commit**。P0-1 双线合流**不能宣告完成**。

---

## 二、R189 主线选择

### 推荐路径（R189 默认启动）

**主线 = R189-治理轮-死代码与最小实装双轨**。理由：

1. **R186 拍板已释放清算空间**：owner 已确认后端审计/租户/权限三域**无遗留代码级双轨** → R25 16 项未启动项不再受"双轨焦虑"约束。
2. **R25 P0 4 项只剩 1 项未拍板**：HANDOVER_CANCEL ✅ 已删、tenant.excludes person_roles ✅ 可在 docs/commit 安全删除、POST_LAUNCH_REVIEW_* ⚠️ 待 R25 轮再核（现查有 6 引用，不是"全链路不可达"）→ **真正"无活可干"的仅剩死代码类 P1/P2 共 13 项**。
3. **撞车窗口出现**：13 个兄弟 worktree 中多数与死代码删除**无路径冲突**（OPS-09 单写入者约束集中在 HrSyncController/PersonController/PersonSyncController/IpdPermissionCode，**不重叠**于 AllowanceService/BidScanService/OverdueReminderService/孤儿 DTO/PERM_*/BONUS_TIER_*）。

### 推荐启动顺序

```
[Step 0]   git fetch origin + log.md R188 段登记（不动兄弟在盘，5 min）
[Step 1]   R25-DAEMON-A：并发删除 BidScanService + OverdueReminderService + 6 孤儿 DTO（worktree: wt-r25-p1-pojo）
[Step 2]   R25-DAEMON-B：删除 IpdReportController.PERM_* 4 常量 + BusinessConfigKeys.BONUS_TIER_* 3 常量（worktree: wt-r25-p2-const）
[Step 3]   R25-DAEMON-C：tenant.excludes person_roles 删除（已经在 R186 拍板允许范围内，3 行 yml 改动）
[Step 4]   worktree p1-ddl-apply-check.py 跑通验证（非 DDL apply，仅校验）— 验证 OPS-09 single-writer 仍 green
[Step 5]   R189 docs 收口：本报告 + R25 清单最终状态 + log.md 段登记
```

### 备选路径（如 owner 未拍板 R25 启动）

**若 owner 倾向先消化 47 项看板**：备选主线为「看板瘦身 + 阶段汇总卡收口」——派 owner 把 12 张「汇总卡」中的 10 张做子卡收口合并，**只为真阻塞项留 25-30 张卡**（让看板具决策意义而非"压面堆卡"）。此路径需要 owner 全程在场，决策不在本会话能力内。

---

## 三、R25 优先级重评表（基于 R186 拍板 + R188 现状）

> 16 项未启动项按"撞车风险 × 真实可并行性"二维重排。

| 重评级 | 项 | R25 原本优先级 | R189 重评判定 | 可否并发 |
|---|---|---|---|---|
| **R189-A 高**（撞车 0 且纯删除） | BidScanService 删除 | P1 | ✅ **可并发** | 是（独立 worktree） |
| **R189-A 高** | OverdueReminderService 删除 | P1 | ✅ **可并发** | 是（独立 worktree） |
| **R189-A 高** | 6 个孤儿 DTO 删除 | P1 | ✅ **可并发** | 是（独立 worktree） |
| **R189-A 高** | IpdReportController.PERM_* 4 常量删除 | P2 | ✅ **可并发** | 是（与方法体重构独立） |
| **R189-B 中** | BusinessConfigKeys.BONUS_TIER_* 3 常量删除 | P2 | ✅ **可并发** | 是（SQL seed 已写入但不影响删除） |
| **R189-B 中** | tenant.excludes person_roles 单条删除 | P0 | ✅ **可并发**（R186 拍板允许） | 是（仅 3 行 yml） |
| **R189-B 中** | tenant.excludes 2 处冗余登记删除 | P1 | ✅ **可并发** | 是（随 person_roles 一并） |
| **R189-C 等 owner** | AllowanceService 删除 | P0 | ⚠️ **需 owner 复核 R14 AllowanceService 同类教训** | 否（owner 拍板再启） |
| **R189-C 等 owner** | RequirementPool Entity 删除 | P0 | ⚠️ **真库表存在 + 实体双轨**：删实体前需 DBA 确认表是否要 drop | 否（需 DBA 决策） |
| **R189-C 待再核** | OPERATION_POST_LAUNCH_REVIEW_* (3 项) | P0 | ⚠️ R25 注释已标"待再核"（现查有 6 引用，工作树外链路径待彻底扫描）| 否（R25 轮重扫后定） |
| **R189-C 评估中** | KPI_REVISION_MODE 常量 | P2 | ⚠️ B-RULE-02 卡对账未完成 | 否（卡对账后再删） |
| **R189-C 评估中** | PersonResignEscalator @Scheduled 休眠 | P2 | ⚠️ 是否真休眠需启动一次服务看日志 | 否 |
| **R189-D 不建议** | qa04d2-*.sql 重复迁移 | P2 | ❌ 不删（R25 自判：重放成本 > 审计噪音）| — |
| **R189-D 不建议** | tenant.excludes coefficient_change_requests/cms_content 冗余 | P2 | ❌ 不删（重复登记无副作用）| — |

**重评结论**：**13 项中 7 项可并发（A 高 + B 中 = 7 项 worktree 派单）**；**5 项需 owner 拍板**；**2 项不建议**。R25 16/21 未启动 → 实际"无活可干"约 **7 项**——非 16 项。

---

## 四、worktree 派单建议（R189 推荐启动时可立即派出 3 个）

| Worktree 编号 | 任务范围 | 上游基准 | 排他约束 |
|---|---|---|---|
| **wt-r25-p1-pojo** | BidScanService 删 + OverdueReminderService 删 + 6 孤儿 DTO 删（AllowanceExportRow/BonusExportRow/CertTemplateCreateReq/DetectionCommitReq/DetectionResult/GateElementLifecycleReq）| `30f876f6` | 不与 OPS-09 兄弟会话重叠（他们集中在 HrSyncController/PersonController/PersonSyncController/IpdPermissionCode）|
| **wt-r25-p2-const** | IpdReportController.PERM_* 4 + BusinessConfigKeys.BONUS_TIER_* 3（KPI_REVISION_MODE 暂留 → 等 B-RULE-02 对账）| `30f876f6` | 不重叠同上 |
| **wt-r25-p0-yml** | application.yml tenant.excludes `person_roles` + `coefficient_change_requests` 冗余 + `cms_content` 冗余（R186 段2 拍板允许清单内）| `30f876f6` | 仅 3 行 yml 改动，无 java 改动 |

**并发可行性**：3 个 worktree 互不依赖，可同步开 3 个会话推进；预计每 worktree 工作量 30-60 分钟 + 双 commit 评审 = **总耗时 1-1.5 小时可完成 R25 的 7 项可启动项中的 5-7 项**（剩余再核 / DBA 决策类不在本派单范围）。

---

## 五、风险预警

| 风险 | 等级 | 撞车点 | 缓解 |
|---|---|---|---|
| **R25 误删复活**（类 R14 AllowanceService 教训）| **高** | 注释引用 @link / @see 被算成"实际调用" | 启 worktree 前必须 `grep -rn "<类名>\b" ruoyi-modules/ruoyi-ipd/src/main --include="*.java" | wc -l` 与 R25 清单基线对账 |
| **OPS-09 兄弟撞车** | 中 | 13 worktree 并行，IO 竞争 git push | 并发 worktree 必须 `git fetch origin && git log --oneline -5` 后再启 |
| **真库 DDL 锁** | 中 | requirement_pool 表删除如误删会导致真库遗留孤儿 | **本轮绝不 drop 真库表**，仅删 Java Entity；表 drop 必须 DBA 独立决策 |
| **KPI_REVISION_MODE 误删** | 低 | B-RULE-02 卡对账未完成 | R189-B 不删此常量，留 P2 评估中 |
| **POST_LAUNCH_REVIEW_* 引用复活** | 低 | R25 已注"待再核"（现查 6 引用） | R189 不动此项，留 R190 轮再核 |
| **P0-1 双线合流失实** | 中 | HEAD 落后 origin/main 1 commit | R189 启动 Step 0 必先 `git pull --ff-only origin main`（除非撞兄弟）|

---

## 六、撞车 0 严守承诺

- ✅ 本 R189 启动决策包为 docs/ipd-系统说明/ 新文件 untracked，**不 commit**（撞车 0 严守）
- ✅ 不抢兄弟会话正在盘的 4 java（HrSyncController / PersonController / PersonSyncController / IpdPermissionCode）
- ✅ 不擅自翻看板 47 项 status（保持 todo/inprogress/inreview 等 owner 拍板）
- ✅ 不动真库 160 表（无 DDL apply）
- ✅ 不动 log.md（兄弟会话 fb46c1c9 已写，撞号透明登记段保留）
- ✅ 不抢 r177-a1~a6 / r120 / r121 / r126 / r127 / r157 兄弟 worktree

**R189 实际启动权 = owner 决策**：若 owner 不拍板 R25 启动，则本决策包仅作 R188 状态快照，不启动任何 worktree 派单；推荐持续观察 owner 拍板信号再做实装。

---

**R189 启动决策包生成完毕**（120 行内）。下次刷新触发：owner 拍板 R25 启动 / P0-7/11/12/13 任一项 / 兄弟会话 wt 收口释放 OPS-09 单写入者约束。
