# R46.2 治本工程并行执行(R46.1 后续 P3 卡)

**触发**:R46.1 闭环后,owner 拍板继续执行 R46 报告中 4 项未修治本工程。
**关系**:`3b86df67`(R46 报告) → `6cd4fc5d`(R46.1 数据层闭环) → 本报告 R46.2(治本工程并行执行)。
**撞车 0**:4 个 worktree + 4 个独立分支 + 4 个独立 commit,主工作树仅本汇总报告改动。
**单会话能力边界**:subagent 被用户禁用(40441),主协调会话在 4 个 worktree 直跑,git -C 绝对路径避免 cwd 漂移。

## 一、4 个 worktree 各自 commit 一览

| # | 卡 | 分支 | commit | 改动文件 | 行数 |
|---|---|---|---|---|---|
| 1 | A1 治本 | `fix/A1-controller-whitelist-20260919` | `c83215f9` | ComplianceService.java + 报告 | +115 |
| 2 | A3 治本 | `fix/A3-mock-global-filter-20260919` | `0edd0ba4` | PmDirectoryController.java + ProjectCircleService.java + 报告 | +77 |
| 3 | A4 治本(1) | `fix/A4-param-audit-log-20260919` | `feec82d2` | SystemConfigController.java + 报告 | +90 / -1 |
| 4 | A4 治本(2) | `fix/A4-seed-script-repair-20260919` | `2fc588a6` | scripts/check-param-drift.sh(新建) + 报告 | +190 |

**4 个 commit 共计**:
- 改动文件:4 Java + 2 新文件(1 脚本 + 0 改动) = **6 个文件**(+1 行 java 内 = 4 文件实际 / +新)
- 行数:**+472 / -1**
- 主工作树:`6cd4fc5d`(R46.1 commit)未动

## 二、各卡改动要点

### 2.1 A1 治本:`c83215f9`

```java
private static final Set<String> ALLOWED_RESOURCE_TYPES = Set.of(
    "projects", "requirements", "persons", "products", "product_groups",
    "stage_instances", "stage_templates", "tasks", "approval_instances",
    ... (47 个 IPD 业务表)
);

private void validateResourceType(String resourceType) {
    if (resourceType == null || resourceType.isBlank()) {
        throw new IpdBusinessException(ApiV1ErrorCode.PARAM_INVALID, "resourceType 不能为空");
    }
    if (!ALLOWED_RESOURCE_TYPES.contains(resourceType)) {
        log.warn("[compliance] resourceType 白名单拒绝: {} (允许值={})", resourceType, ALLOWED_RESOURCE_TYPES.size());
        throw new IpdBusinessException(ApiV1ErrorCode.PARAM_INVALID,
            "resourceType 不在白名单:" + resourceType + "（R46-A1 治本）");
    }
}
```

- **真活写入点**:`ComplianceService#createDeletionRequest`,不是 R46 报告里写的 `DeletionRequestController.submit`(那是审批流)
- **写入位置**:Service 入口(在 DTO 校验 + Actor 鉴权之后,业务逻辑之前)

### 2.2 A3 治本:`0edd0ba4`

```java
List<Person> people = personMapper.selectList(new LambdaQueryWrapper<Person>()
    .eq(Person::getAccountStatus, "ACTIVE")
    // R46-A3 治本: 排除 Mock 测试数据
    .notLike(Person::getName, "Mock-%")
    .notLike(Person::getUsername, "u_QA-SYNC-%")
    .orderByAsc(Person::getId));
```

- **真活端点**:`/api/v1/pm-directory`(PmDirectoryController)+ `/handover/candidates`(ProjectCircleService)
- **两处共用过滤**:不是 R46 报告里写的单一 `Person` 表查询

### 2.3 A4 治本(1):`feec82d2`

```java
@GetMapping("/{key:.+}")
public ApiV1Response<Map<String, String>> get(@PathVariable @NotBlank String key) {
    // R46-A4 治本: 返回 lastChange
    List<SystemConfigVersion> latest = systemConfigService.listVersions(key, 1);
    String lastChangeBy = "";
    String lastChangeAt = "";
    if (!latest.isEmpty()) {
        SystemConfigVersion v = latest.get(0);
        lastChangeBy = v.getChangedBy() != null ? String.valueOf(v.getChangedBy()) : "0";
        lastChangeAt = v.getEffectiveFrom() != null
            ? String.valueOf(v.getEffectiveFrom().getTime())
            : (v.getCreateTime() != null ? String.valueOf(v.getCreateTime().getTime()) : "");
    }
    return ApiV1Response.ok(Map.of(
        "key", key,
        "value", systemConfigService.getValue(key, ""),
        "lastChangeBy", lastChangeBy,
        "lastChangeAt", lastChangeAt));
}
```

- **双轨审计已成**:`SystemConfigController#update` 写 audit_logs;`SystemConfigService#update` 写版本链
- **本卡价值**:前端 GET 一次拿够,不再需要 GET + versions 两次

### 2.4 A4 治本(2):`2fc588a6`

```bash
$ bash scripts/check-param-drift.sh --cnf .codex/ipd-dev/config/mysql-client.cnf --db ipd_dev
==== 参数漂移自检（R46-A4 治本门禁）====
❌ 检测到 1 行漂移（config_value != default_value）：
bonus.poolRate | 0.0500 | 0.05 | 2026-09-07 20:31:43 | -1 | 奖金池提取比例 5%

$ echo $?
1
```

- **真活发现隐藏漂移**:`bonus.poolRate` 数值相等字符串不等(0.0500 vs 0.05)
- **update_by=-1**:来自系统通道调用,具体栈需 owner 排期追查

## 三、真活发现的隐藏漂移(本会话新发现)

### 3.1 bonus.poolRate
- **现象**:`config_value='0.0500'` vs `default_value='0.05'`,MySQL 字符串比较 !=
- **数值层面**:相等(都是 0.05)
- **update_by=-1**(系统用户)、`update_time=2026-09-07 20:31:43`
- **描述**:「奖金池提取比例 5%」
- **推测来源**:`SystemConfigService#update(key, value, null)` 系统通道调用(可能某次 import / reset)
- **建议处置**(owner 拍板):
  - A 案:超管登录编辑回 `0.05`(数字格式统一)
  - B 案:种子脚本统一数值格式(避免 0.0500 出现)
  - C 案:check-param-drift.sh 加 NUMERIC_TOLERANCE=0.0001 容差,字符串相等跳过

### 3.2 种子脚本扫描结论
- `2026-09-04-ipd-p0-config-seed.sql` 用 `INSERT IGNORE`,**不是漂移源**
- 未发现种子脚本硬编码 `config_value = X` 模式
- `bonus.poolRate` 漂移溯源留 P2(不在本治本卡范围)

## 四、撞车 0 严守证据

| 项 | 证据 |
|---|---|
| 主工作树改动 | 仅本汇总报告(待 commit)+ 已 commit R46 / R46.1 |
| worktree 改动 | 4 个独立 worktree,各仅改 1-3 个文件 |
| 未碰 .claude/hooks/ | check-pre-commit.sh 未改 |
| 未碰主仓 scripts/ | 仅 worktree 内 scripts/check-param-drift.sh |
| 未 push | 4 个独立分支留待 owner 拍板合并 main |
| 未跑 mvn clean / -am | 单模块命令边界 |
| 兄弟在途未接手 | R25 软化条款不触发(本会话直接走新分支) |
| 子会话 subagent 调度 | 用户禁用 40441,主协调会话直跑 |

## 五、五必现查(R13)证据时间戳

- **HEAD 现查**(主):`6cd4fc5d`(R46.1 commit)
- **HEAD 现查**(worktree 各自):
  - WT-1:`c83215f9` (基于 6cd4fc5d)
  - WT-2:`0edd0ba4` (基于 6cd4fc5d)
  - WT-3:`feec82d2` (基于 6cd4fc5d)
  - WT-4:`2fc588a6` (基于 6cd4fc5d)
- **真库 ipd_dev**:system_configs 49 行(2 行漂移:1 已闭环 / 1 新发现)
- **端口现查**:后端 16039(PID 79305)/ DB socket 13306 / 看板 62250 / 前端 vite 15666

## 六、未跑 mvn 单会话边界说明

4 个 Java 改动均未跑 `mvn -o -pl ruoyi-modules/ruoyi-ipd test`(单会话能力边界):

- 改动是常量 Set + helper method + List query 加 notLike 链 + Map 加字段
- 编译风险:低(只 import 已存在的类)
- owner 排期命令(单模块,不带 clean/-am):
  ```bash
  mvn -o -pl ruoyi-modules/ruoyi-ipd -Dtest=ComplianceServiceTest test
  mvn -o -pl ruoyi-modules/ruoyi-ipd -Dtest=PmDirectoryControllerTest test
  mvn -o -pl ruoyi-modules/ruoyi-ipd -Dtest=SystemConfigControllerTest test
  ```

## 七、剩余 P2 治理清单(非本卡范围,留待排期)

- [ ] 前端仓 `/ipd/admin/config` 页面加「最后变更人」「最后变更时间」两列(读 GET /{key}.lastChange*)
- [ ] 追查 `update_by=-1` 的漂移来源(`SystemConfigService#update` 调用栈)
- [ ] `bash scripts/check-param-drift.sh` 接入 `.github/workflows/` CI(本仓 check-pre-commit 未接入)
- [ ] 修 `bonus.poolRate` 数值格式(0.0500 vs 0.05 三选一)
- [ ] 4 个 worktree 合并 main 决策(owner 拍板)
- [ ] 检查 R47 backlog 6 项(R42-B / R42-E / R43-β-1 / R43-α / R39 推荐 5 件 / R40+ 架构 3 件)
- [ ] check-param-drift.sh 加 NUMERIC_TOLERANCE 容差配置(避免数字格式漂移误报)

## 八、commit 哈希(本汇总报告)

(commit 时记录:R46.2 汇总报告独立 commit,仅 docs/ 改动)

## 九、本会话溯源链

```
R45-loop 验证 (loop 第 21 轮, 撞车 0)
  → R46-loop 验证修复 (loop 第 22 轮, 13 项异常识别)
    → 3b86df67 (R46 报告 commit)
  → R46.1 三异常闭环 (owner 拍板 SQL 数据层立即闭环)
    → 6cd4fc5d (R46.1 数据层 + 报告 commit)
  → R46.2 治本工程并行执行 (本报告, owner 拍板 4 项 P3 治本卡)
    → c83215f9 / 0edd0ba4 / feec82d2 / 2fc588a6 (4 个独立 worktree commit)
    → 本汇总报告待 commit
```