# Paiban-14 决策包 — 前端修 2 端点错位

> 派单智能体：security-reviewer         拍板 owner：项目主理人
> 拍板 SLA：🟢 7d                    撞车 0 让路：让路（前端仓跨仓）
> 创建时间：2026-09-20                截止：2026-09-27
> 工作量：1 hr                         类别：B（低风险 / 7d 未决自动通过）

## 一、背景（200 字）
前端 2 个端点错位：`contribution` 应为 `/api/v1/kpi/contributions/{id}`（当前误调 `/score`）/`project-score` 应为 `/api/v1/projects/{id}/score`（当前误调 `/projects`）。R128 §四 推荐改路径，1 hr。属 B 类低风险。

## 二、3 个候选方案
| 方案 | 做法 | 优势 | 劣势 |
|---|---|---|---|
| A 修 2 端点路径 | 改 `requestIpd` 调用路径 | 一次性 | 跨仓改 |
| B 加路径别名兼容 | 保留旧路径 + 加 alias | 平滑 | 永久双轨 |
| C 暂不改 | 接受现状 | 零风险 | 阻塞功能 |

## 三、推荐方案
**A 修 2 端点路径**（R128 §四 推荐）：改 `contribution.ts` + `project-score.ts` 调用。
- 工作量：1 hr（改路径 30 min + 测试 30 min）
- 风险：低（路径修改可逆）
- 回滚 SOP：git revert wt + 还原路径

## 四、非 owner 拍板自动通过判定
**是**（B 类低风险 + 决策包完整 + 回滚 SOP 完整），7d 未决自动通过。

## 五、撞车 0 让路边界
- 写决策包 = **强推进**
- 前端仓跨仓改 = **让路**（#18 元规则）

owner 拍板位：✅ YES / ❌ NO / 🔄 再议   拍板日期：____
