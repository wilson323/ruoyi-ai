# OWNER 拍板登记 — P3 九项全部完整执行（2026-09-21）

> 本文件为 owner 对 P3 九项的**一次性拍板凭据**，供各执行单元引用。
> 登记人：主协调会话（响应 owner 直接指令）

## 拍板原文

owner 于 2026-09-21 直接下达指令：

> **「P3 九项（KPI 公式、平台票、字符集等）全部完整执行」**

即全部九项 = **YES 批准执行**，含原本需单独签署的 C 类（DDL 破坏类）项目。

## 九项拍板映射

| # | 项 | paiban 文档 | 原拍板要求 | 本次拍板 |
|---|---|---|---|---|
| 1 | KPI 端点（/kpi/rules） | paiban-02 | owner 必拍 | ✅ 执行 |
| 2 | KPI 8 项功能指标（A2 P1+P2） | R148.1 §2.2 | 8 项业务规则输入 | ✅ 执行（默认值由执行单元从 DOC-01 取或合理默认并标注） |
| 3 | platform-token 入口恢复 | —（P3 表） | 明确是否重开 | ✅ 执行（恢复入口） |
| 4 | 单数表名整改（3 表） | paiban-03 | owner 必拍 + DDL SRE | ✅ 执行（**含 application.yml tenant.excludes 3 行 + DDL 存档 + 测试断言同步的完整影响面**） |
| 5 | 字符集整改（66 表/571 字段） | paiban-04 | 最大破坏拍板 | ✅ 执行（4 批，ipd_dev 真库） |
| 6 | Service 接口化（69 个） | paiban-05 | owner 必拍 | ✅ 执行 |
| 7 | Controller 去 Ipd 前缀（4 个） | paiban-11 | owner 必拍 | ✅ 执行（Auth/Sse 撞 bean 名 → 保原名 + 显式 bean 名登记，见 §例外） |
| 8 | @Autowired 构造器注入（48 处） | paiban-10 | B 类低风险 | ✅ 执行——**前提核验归零**：实测 0 处必填字段注入（12 构造器级合规 + 29 optional 刻意设计），关单为「无需改造」 |
| 9 | 评审要素发布/归档/复制/恢复按钮 | —（P2-5.x） | 点名做哪几个 | ✅ 全部四个按钮执行 |
| 10 | 前端 drift-guard 挂载 | R153 §3.1 | 允许改前端仓 | ✅ 执行——**复核发现 R154-A 已闭环**，补齐三层自证能红验证 |

## §例外与边界

1. **paiban-10 前提失真**：W1-JAVA 核验证明「48 处 @Autowired 字段注入」为 grep 计数未区分语义的误判；实际 0 处必填字段注入。**关单**，不做强制改造（避免引入 12 处启动失败 + 29 处注入失效）。
2. **paiban-11 Auth/Sse**：`ruoyi-admin` 已有 `AuthController`、`ruoyi-common-sse` 已有 `SseController`，去前缀会制造 bean 名冲突（启动期 ConflictingBeanDefinitionException）。处置：**保原名**（`IpdAuthController`/`IpdSseController` 保留），Report/Menu 两个已去前缀。
3. **DDL 范围授权**：paiban-03/04 的 DDL apply 对象为**本机开发库 ipd_dev @ 127.0.0.1:13306**（非生产），执行单元须先备份（SHOW CREATE TABLE 存档），提供反向回滚 SOP。
4. **KPI A2 P2 的业务参数**：8 项输入（PPM 目标/量表/期间/返工率分母）owner 未给具体值 → 执行单元从 DOC-01 §4.1 取既定口径；文档无约定的项用**显式标注的合理默认**（例：期间=上市后 6 个月、返工率分母=期间内总任务数），并在代码注释 + 验收报告标注「默认值待业务复核」。

## 引用方式

执行单元在 commit message / 验收报告中引用：

```
Refs: OWNER-拍板登记-20260921.md（P3 九项全部完整执行）
```
