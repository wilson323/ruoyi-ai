# Paiban-13 决策包 — 前端补 3 端点调用

> 派单智能体：ioedream-pm             拍板 owner：项目主理人
> 拍板 SLA：🟢 7d                    撞车 0 让路：让路（前端仓跨仓）
> 创建时间：2026-09-20                截止：2026-09-27
> 工作量：1 hr                         类别：C（owner 必拍 / 依赖后端）

## 一、背景（200 字）
前端缺 3 端点调用：`/projects/{id}/stages` / `/persons/active` / `/kpi/rules`。依赖后端拍板 #2/#5。R128 §四 推荐前端先 mock 后真接，1 hr。

## 二、3 个候选方案
| 方案 | 做法 | 优势 | 劣势 |
|---|---|---|---|
| A 前端先 mock 后真接 | MSW mock + 后端就绪后切换 | 不阻塞前端 | 切换风险 |
| B 等后端拍完再写 | 串行依赖 | 一次到位 | 等 7d+ |
| C 暂不补 | 接受现状 | 零风险 | 阻塞 E2E |

## 三、推荐方案
**A 前端先 mock 后真接**（R128 §四 推荐）：MSW 拦截 + 后端拍板后切换真实端点。
- 工作量：1 hr（mock 30 min + 切换 30 min）
- 风险：低（mock 切换可逆）
- 回滚 SOP：删除 mock 路由 + git revert wt

## 四、非 owner 拍板自动通过判定
**否**（依赖后端拍板 + 跨仓 docs/ 写入让路），必须 owner 拍板 #18 跨仓 BCP。

## 五、撞车 0 让路边界
- 写决策包 = **强推进**（后仓 docs-only）
- 前端仓跨仓直写 = **让路**（#18 元规则）

owner 拍板位：✅ YES / ❌ NO / 🔄 再议   拍板日期：____
