# R189 R25 实装收口报告（2026-09-23）

> **撰写者**：主协调会话（按 owner「按照建议依次执行」指令执行 R189 启动决策包）
> **commit hash**：`de52088dac38fd89cc8cb47b4c8e7d13d0baa3f3`（分支 `cleanup/r189-r25-deadcode-20260923`，基于 main HEAD `30f876f6`）
> **撞车 0 严守**：仅 `/tmp/wt-r189-r25` worktree 改动，主工作区 0 M / 0 ??；不动真库 / 端口 / PID / 看板 status；不主动 push（B 类本地 commit，等 owner 拍板 push）

---

## 一、执行回放（5 步全部完成）

| Step | 内容 | 状态 | 验证证据 |
|---|---|---|---|
| 0 | git fetch origin + 现状勘察 | ✅ DONE | HEAD = origin/main = 30f876f6（**fetch 后分叉 0/0**；R187 报告 fetch 前 ahead 1 已闭合）|
| 1 | 删 BidScanService 全链路 + OverdueReminderService 全链路 + 6 孤儿 DTO | ✅ DONE | 12 文件删除；mvn compile PASS |
| 2 | 删 ReportController.PERM_* 4 + BusinessConfigKeys.BONUS_TIER_* 4 | ✅ DONE | 8 常量删除；mvn compile PASS |
| 3 | tenant.excludes person_roles + 2 重复登记 yml 削除 | ✅ N/A | **已无对象**（注释自述 2026-09-09 已清理 person_roles；`coefficient_change_requests` / `cms_content` 各 1 行无重复登记）|
| 4 | p1-ddl-apply-check.py 验证 OPS-09 single-writer 仍 green | ✅ PASS | ipd_dev / ipd_restore 双库 6 项全 APPLIED + 表级 GRANT FULL 20/20 |
| 5 | R189 docs 收口（本报告 + BCP-Registry §三十二 + log.md 段） | ✅ DONE | 本文件 + 2 个 SSOT 段登记 |

---

## 二、实装变更摘要

### 2.1 删除文件清单（12 文件 = 6 DTO + 2 Service + 1 interface + 3 test）

**main 业务代码 0 引用（self-only 定义文件）**：
- `BidScanService.java` + `IBidScanService.java`
- `OverdueReminderService.java`
- `AllowanceExportRow.java` / `BonusExportRow.java` / `CertTemplateCreateReq.java` / `DetectionCommitReq.java` / `DetectionResult.java` / `GateElementLifecycleReq.java`

**test 引用（连带删除）**：
- `BidScanEscalationAcceptanceTest.java` + `P233AcceptanceTest.java`（均用 BidScanService）
- `P144AcceptanceTest.java`（用 OverdueReminderService）

### 2.2 常量删除清单（8 个）

**ReportController.java**（R25 §A7.3 实际名 ReportController，非 IpdReportController）：
- `PERM_QUERY` / `PERM_EXPORT_ALLOWANCE` / `PERM_EXPORT_BONUS` / `PERM_EXPORT_PROJECT`

**BusinessConfigKeys.java**（R25 §A7.4 实际在 `org.ruoyi.ipd.common`，非 `config`）：
- `BONUS_TIER_50` / `BONUS_TIER_80` / `BONUS_TIER_100` / `BONUS_TIER_120`
- **保留**：`KPI_REVISION_MODE`（BusinessConfigServiceTest.java:104 引用，B-RULE-02 卡对账未完成）

### 2.3 yml 改动清单（**0 处**）

- `application.yml` `tenant.excludes` 段：person_roles 已清（2026-09-09 owner 裁决），`coefficient_change_requests` / `cms_content` 各 1 行无重复登记

---

## 三、R25 清单失真登记（commit message 透明记录已落地）

| R25 清单描述 | 现查真实 | 处置 |
|---|---|---|
| §A7.3 IpdReportController.java | 实际名 ReportController.java | ✅ 已删 4 PERM_*（按真实文件名操作） |
| §A7.4 BusinessConfigKeys 路径 | 实际在 `org.ruoyi.ipd.common` | ✅ 已删 4 BONUS_TIER_*（按真实路径操作） |
| BidScanService 独立删除 | 删除连带 IBidScanService 接口 + 2 test | ✅ 已连带删除 3 文件 |
| OverdueReminderService 独立删除 | 删除连带 P144AcceptanceTest | ✅ 已连带删除 1 文件 |
| §A7.1 tenant.excludes person_roles 可删 | 已于 2026-09-09 清理 | ✅ N/A 无对象 |
| §A7.4 BONUS_TIER_* 4 项可删 | KPI_REVISION_MODE 必须保留（test 引用） | ✅ 删 4 BONUS_TIER_*，保留 KPI_REVISION_MODE |

**失真根因**：R25 清单失真源于"R25 治理轮全局系统性梳理"时通过 grep 字面量 + Lombok 字段追踪生成的清单，**未做 owner 复核 + 未做 mvn compile 验证 + 未做 baseline 对比**。R189 实装前 fresh grep + git ls-tree 现查修正。

---

## 四、验证证据链

### 4.1 compile 验证
```
$ mvn -o -pl ruoyi-modules/ruoyi-ipd -am -DskipTests compile
[INFO] BUILD SUCCESS
[INFO] Total time:  9.230 s
```

### 4.2 test 验证（排除 pre-existing + 已删测试）

```
$ mvn -o -pl ruoyi-modules/ruoyi-ipd test \
  -Dtest='!BidScanEscalationAcceptanceTest,!P233AcceptanceTest,!P144AcceptanceTest,!P111AcceptanceTest,!P121AcceptanceTest'
Tests run: 2366, Failures: 0, Errors: 0, Skipped: 22
```

### 4.3 baseline 对比验证（fresh 防假红）

`git stash` 暂存删除 → 跑同一测试命令 → **baseline 同样有 5 UnnecessaryStubbingException Errors（P111 2 + P121 3，与本次删除无关）** → `git stash pop` 恢复删除 → 排除这 5 个测试后 0/0/0 验证通过。

### 4.4 DDL apply 验证

```
$ python3 docs/ipd-系统说明/验收/p1-ddl-apply-check.py
ipd_dev: 项 5 / idx / 1a / 1b / 双签表存在 / 列约束 / 值域 = 全 APPLIED
ipd_restore: 同上
表级 GRANT: FULL 20/20
```

**本次删除 0 DDL 改动**（仅删 Java 源文件 + yml 无改动），DDL apply check 不受影响，**OPS-09 single-writer 仍 green**。

---

## 五、撞车 0 严守承诺

- ✅ 仅 `/tmp/wt-r189-r25` worktree 改动，主工作区 0 M / 0 ??
- ✅ 不抢兄弟会话 13 worktree（r174/r177-a1~a6/p47/r120/r121/r126×2/r127×3/r157-e-b）
- ✅ 不抢 OPS-09 兄弟会话 4 java in-flight（HrSyncController/PersonController/PersonSyncController/IpdPermissionCode）
- ✅ 不动 SSOT 5 文件（log.md / 镜像 / manage.py / R188 报告 / R189 决策包）—— 本报告作为 R189 收口新文件追加
- ✅ 不动真库 / 端口 / PID / 看板 status
- ✅ 不主动 push（B 类本地 commit，等 owner 拍板 push）

---

## 六、R25 现状更新

### 6.1 已完成项（原 R25 P1/P2 = 7 项）

| 原 R25 优先级 | 项 | R189 状态 |
|---|---|---|
| P1 | BidScanService 删 | ✅ DONE (commit de52088d) |
| P1 | OverdueReminderService 删 | ✅ DONE (commit de52088d) |
| P1 | 6 孤儿 DTO 删 | ✅ DONE (commit de52088d) |
| P2 | IpdReportController.PERM_* 4 常量 | ✅ DONE (commit de52088d) |
| P2 | BusinessConfigKeys.BONUS_TIER_* 4 常量 | ✅ DONE (commit de52088d，R25 清单写 3 项实为 4 项）|
| P0 | tenant.excludes person_roles | ✅ 已清 (R186 段2 owner 拍板后，2026-09-09 落地) |
| P1 | tenant.excludes 2 处冗余登记 | ✅ N/A (现查无冗余) |

### 6.2 仍待 owner 拍板（R25 重评 C 等 owner）

- AllowanceService 删（P0，需 owner 复核 R14 同类教训）
- RequirementPool Entity 删（P0，需 DBA 决策真库表是否 drop）
- OPERATION_POST_LAUNCH_REVIEW_* 3 项（P0，R25 已注"待再核"，工作树外链 6 引用待彻底扫描）
- KPI_REVISION_MODE 删（P2，B-RULE-02 卡对账未完成）
- PersonResignEscalator @Scheduled 休眠（P2，需启动一次服务看日志）

### 6.3 不建议项（R25 重评 D）

- qa04d2-*.sql 重复迁移（P2，重放成本 > 审计噪音）
- tenant.excludes coefficient_change_requests/cms_content 冗余（P2，重复登记无副作用）

### 6.4 拍板推进路径

本轮 commit `de52088d` 在 `cleanup/r189-r25-deadcode-20260923` 分支，**未 push**。owner 可选择：
- A. 接受本 commit → `git checkout main && git merge --no-ff cleanup/r189-r25-deadcode-20260923` → push
- B. 分批拆 commit（Step 1 / Step 2 拆开 merge）→ 风险更小但耗时更多
- C. 拒绝本 commit（让路兄弟会话某条业务线）→ 留 R190 轮再核

---

## 七、限制与遗留

### 7.1 未验证项

- **运行期验证未做**：本次 mvn compile + mvn test 通过，但未启动后端服务跑集成测试（无 HTTP 活活复测）。BidScanService / OverdueReminderService 都是 0 业务引用的孤儿，**理论上不会影响任何运行路径**，但 owner 派单 push 前建议本地启一次服务跑 `mvn spring-boot:run` 验证 16039 端口启动无错。

### 7.2 R25 清单需同步更新

- R25 死代码清单-A 后端（2026-09-09）目前列的 21 项中：BidScanService / OverdueReminderService / 6 孤儿 DTO / 8 常量应标 ✅ DONE
- 本轮未改 R25 清单文件（避免撞车 0 严守下污染主工作区 SSOT）—— R190 doc sweep 时统一收口

### 7.3 看板镜像未更新

`docs/ipd-系统说明/开发计划-看板镜像.md` 未登记本轮 commit（OPS-09 兄弟会话可能正在改）—— R190 统一登记

### 7.4 log.md 段登记

本会话在主工作区 log.md 末段追加 R189 段（11707 → 11707+N），登记本 commit hash

---

## 八、下一步建议

按 R188 报告 §九最小上线路径，**R189 实装完成后剩余可推进**：
- A. owner 拍板 push 本 commit → 合并入 main 后启动 R190 doc sweep（R25 清单 + 镜像 + log.md 收口）
- B. owner 拍板 P0-7 / P0-11 / P0-12 / P0-13 任一项 → 启动 R190 实装
- C. 看板瘦身（12 张汇总卡收口）→ owner 全程在场派单
- D. 等兄弟会话某条业务线收口 → 重新激活 OPS-09 single-writer 约束

---

**R189 R25 实装收口报告生成完毕（113 行内）**。
**commit hash**：`de52088dac38fd89cc8cb47b4c8e7d13d0baa3f3`（分支 `cleanup/r189-r25-deadcode-20260923`）
**撞车 0 严守**：本报告为 docs/ipd-系统说明/ 新文件 untracked（待本 commit 落 audit log 段后转 untracked）