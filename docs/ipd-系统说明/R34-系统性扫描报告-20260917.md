# R34 系统性扫描报告(2026-09-17 22:50~23:50 PDT)

## 0. 摘要(大白话)

owner 在 R33 撞车接管验收中识别出 4 个真实业务异常:
- 异常 1:删除审批脏数据 `not_a_real_table #999999999`(23 条)
- 异常 2:KPI 月数 disabled
- 异常 3:`Mock-QA-SYNC-20260910B` 污染接任人下拉
- 异常 4:`allowance.L3` 当前值 1500 ≠ 默认值 2000

**owner 指令**:系统性梳理全局项目代码是否存在类似的异常。

**本轮执行**:dispatch 4 个专业智能体**并行**扫描 4 类异常模式(read-only)+ 我做 spot check + 真活验证 + 反转校正。

**核心发现**:4 个模式全部化后,远不止 R33 已识别的 4 个异常:
- Pattern A(脏数据污染): **5 P0 + 9 P1 + 6 P2 = 20 条**
- Pattern B(配置漂移): **2 P0 + 8 P1 + 27+ P2 = 37+ 条**
- Pattern C(API 契约错位): **1 P0 + 19 P1 + 5 P2 = 25 条**(其中 P0-1 反转)
- Pattern D(UI disabled + DTO 错位): **2 P0 + 9 P1 + 7 P2 = 18 条**

**总计:10 P0 + 45 P1 + 45+ P2 = 100+ 条同类异常**(4 个 Pattern 跨域合计,无去重)。

**最危险的 P0**:Pattern C P0-1 **`IpdPlatformAuthController.java` 主仓 main HEAD 源码树完全缺失**(仅存在于 8 个兄弟 worktree),意味着从 main HEAD fresh clone 会得到**功能残缺但能编译过**的后端,AI 平台桥(`/auth/platform-token` + `/chat` 跳转链路)直接 404,但当前运行 jar 仍能 HTTP 200。

---

## 1. owner 反转 / 撞车红线回顾

按 R25 OPS-09 软化条款 + 兄弟会话在途接手三步法执行:
- ✅ 评审:4 个 Agent 并行 read-only 扫描,独立 write 各自报告
- ✅ 登记:本报告 + log.md + 看板镜像 三件套同步
- ✅ 隔离落地:`/private/tmp/r34-takeover-ipd` worktree + `r34/takeover-20260917` 分支 + 主仓 main HEAD b1f443d8 未动

---

## 2. 4 个 Pattern 详细发现(概要 + 链接)

### 2.1 Pattern A — 脏数据污染(详细报告 `R34-pattern-A-findings.md`, 464 行)

| 等级 | 数量 | 代表性发现 |
|---|---|---|
| **P0** | **5 条** | (1) 3 名 SUPER_ADMIN 同时 ACTIVE;(2) products 表 27 条 ACTIVE 测试夹具;(3) projects 表 11 条 QA03/R30/R31/HARDWARE 探针(2026-09-17 23:55 PDT 勘误:原报 10,漏算 P132-HARDWARE-1788637764);(4) projects 表 4 条 status='G1' 异常枚举值;(5) ZK-GATE-TEST 跨表双写 |
| **P1** | **9 条** | (1) deletion_requests 51/52 条 create_by=-1;(2) handover_records ROLLED_BACK 探针;(3) stage_actions 47 条 NULL create_by;(4) bid_responses 3 条 responded_at='-1-';(5) Runner 写 ACTIVE 数据;(6) zk-scenario-seed.sql 无 profile 守卫;(7) 3 条 ARCHIVED 状态不一致;(8) status='LIFECYCLE' 1 条;(9) 1 条 SUSPENDED |
| **P2** | **6 条** | gate_review_elements 43/76 条 / unsupported_probe NULL create_by / SQL vs Runner create_by 不统一 / test 类写真库无 cleanup |

**3 条 root cause**:
1. **SQL 种子文件无 profile 守卫**(`2026-09-06-ipd-zk-scenario-seed.sql`):13 真实姓名人员 + 5 products + 5 projects 全 `level_source='MOCK' + employment_status='ACTIVE'` 进入 ipd_dev,导致 P0-2/P1-3/P1-6 的源头。
2. **测试夹具写真库 + 无 cleanup**(`Qa04MysqlConcurrencyTest` / `P131DatabaseIntegrationTest`):`@SpringBootTest` 默认连 ipd_dev 真库,test 类 `Connection.exec("INSERT INTO ...")` 直接写入 27+ 条 product fixture,`mvn test` 每跑一遍污染一次。
3. **超级用户治理脚本未生效**(`2026-09-07-ipd-person-super-admin-converge.sql` 设计了却没真跑):3 名 SUPER_ADMIN 仍 ACTIVE,违反 BR-ADM-02/03 单超管不变式。

### 2.2 Pattern B — 配置漂移(详细报告 `R34-pattern-B-findings.md`, 530 行)

| 等级 | 数量 | 主要内容 |
|---|---|---|
| **P0** | **2 类,共 7 条** | (1) `bonus.poolRate` / `gate.signDeadlineDays` 双表双写且已漂移;(2) `allowance.capMultiplier` 等 15+ 死配置;(3) `ipd_business_config_versions` 表存在但 0 行;(4) `audit_logs` 中 24 条 `entity_type='not_a_real_table'`;(5) `application-prod.yml` 含 17 处 justauth client-secret 字面量(生产密钥泄露) |
| **P1** | **8 条** | `bonus.salesSource` 5 分钟 flip 12 次;`gate_review_elements` 12 条软删/改无 audit;`coefficient_change_requests` 自己批自己;`product_groups` 测试组 leader 改 admin;`sys_menu` 15 条/`sys_dict_data` 5 条修改无版本审计;`persons` 10 行改级无 reason;`ipd_business_config.bonus.poolRate` 静默写 |
| **P2** | **27+ 条** | AllowanceService / KpiRecordService / ProjectScoreService / ContributionService / BonusPoolService 中 27+ 个 Java 字面量未配置化 |

**3 条缺审计最严重**:
1. **`system_config_versions` 覆盖率 9.1%**(5/55 keys) — 50 个 config_key 无任何版本历史
2. **`ipd_business_config_versions` 表完全空**(0/13) — 表结构完整但 0 行历史
3. **`gate_review_elements` 12 条修改/软删无 audit_logs 联动**

**R33 异常 4 反转结论**:`allowance.L3=1500` 不是异常,是 P0-3.3 验收套件高频震荡(2026-09-06~08 改了 19 次,1500↔2200↔2000 来回 11 次),`change_reason="P0-3.3 param update"` 操作者 `900101`,审计完整 27 条 `SYSTEM_CONFIG_UPDATE`,**没有任何变更丢失**。

**额外产出**: `application-prod.yml` 含 17 处 justauth client-secret 字面量(生产密钥泄露),maxkey/topiam/qq/weibo/gitee/dingtalk/baidu/csdn/coding/oschina/alipay_wallet 等 11+ 渠道密钥明文。

### 2.3 Pattern C — API 契约错位(详细报告 `R34-pattern-C-findings.md`, 193 行)

| 等级 | 数量 | 说明 |
|---|---|---|
| **P0** | **1 条**(反转校正) | `IpdPlatformAuthController.java` 主仓 main HEAD 源码树缺失(8 个兄弟 worktree 独有) |
| **P1** | **19 条** | §9.2 13 处 + §9.3 6 处 命名/路径错位 |
| **P2** | **5 条** | D.0.6 路径前缀(984/992 行)+ D.7.1(1162 行 × 2)+ 正文 1202 行 |
| **死代码** | **150+** | 后端有 + 前端无,分布 40+ Controller |

**Agent C 反转校正**:
- ❌ Agent C 初判:"`/auth/platform-token` 后端无,前端必 404" — **真活 HTTP 200 验证端点存在**
- ✅ 实际 P0:**`IpdPlatformAuthController.java` 源码不在主仓 main HEAD `b1f443d8`**(仅存在于 8 个 `.claude/worktrees/*` 路径),意味着从 main HEAD fresh clone 会得到一个**功能残缺但能编译过**的后端

**3 条契约错位重点**:
1. **P0 源码孤儿** — `IpdPlatformAuthController` 8 个 worktree 唯一版本,无 main HEAD 来源,owner 必须裁决
2. **P1 文档错位** — §9.2 第 559 行 `/api/v1/bonus/calculate` vs 后端 `/api/v1/bonus-pool/compute`
3. **P2 R33 修复连带** — D.0.6 第 984 行 `/api/v1/handoff-batches/*` vs 后端 `/api/v1/handovers/batch`

### 2.4 Pattern D — UI disabled + DTO 错位(详细报告 `R34-pattern-D-findings.md`, 528 行)

| 等级 | 数量 | 主要内容 |
|---|---|---|
| **P0** | **2 条** | (1) Controller `@RequestBody Domain` 而非白名单 record — 前端可注入 status='APPROVED' 绕过双签(RequirementChangeController.create());(2) 前端 disabled 硬编码(`admin/identity-sync/index.vue` 三按钮 + `workbench/index.vue` 任务教练按钮) |
| **P1** | **9 条** | 4 条 DTO 缺字段 + 4 条 UI disabled + 1 条前端类型不一致 |
| **P2** | **7 条** | 2 条 DTO 缺字段 + 5 条 UI disabled |

**R33 异常 2 反转确认**:`KPI 月数 6/12/24/36 disabled` 实为 `loading` 状态(`kpi/index.vue:129`),非永久禁用。R33 snapshot 抓帧时 loading=true,数据加载完成后(常见 <1s)select 恢复可交互。**不计入 P0/P1/P2**。

**R33 P3-8.1 教训**:NegativeFeedbackCreateReq 缺 source/content/severity,R29 service 层用 `.source("MANUAL").content(triggerEvidence).severity("MEDIUM")` 兜底 → **业务严重度永远固定为 MEDIUM**(CRITICAL 事故被记成 MEDIUM),**P1 业务漏洞待补前端表单 + DTO**。

**3 条 root cause**:
1. **Controller 直接用 `@RequestBody Domain`** — 应仿照 `ProjectCreateReq` / `ProductCreateReq` 白名单 record 模式,补 `RequirementChangeCreateReq`
2. **DTO 缺字段模式系统性问题** — 三个端点 `/projects`、`/gate-elements`、`/deletion-requests` 后端缺 `@Valid`,DTO @Pattern/@Size/@NotBlank 注解失效
3. **前端 disabled 硬编码 + 依赖后端数据无 loading 兜底** — 应在 ESLint 加规则:禁止 `disabled="true"` 字面量

---

## 3. 跨 Pattern root cause 联动

5 类病根框架(R25 全局复盘沉淀)在本轮体现:

| 病根类型 | Pattern A | Pattern B | Pattern C | Pattern D |
|---|---|---|---|---|
| ① 改主代码后测试没跟上 | 测试夹具写真库无 cleanup | — | — | DTO 缺字段导致 service 兜底后业务漏洞 |
| ② 提交不完整(fresh clone 必炸) | — | `IpdPlatformAuthController` 主仓缺失 | 同上 + 死代码 150+ | — |
| ③ 规则表与接线点靠人肉对账 | Runner / SQL / test 三轨写 | 9 张配置表 vs 27 字面量 | 文档 §9.2/9.3 命名 vs 后端 Controller | DTO/VO/domain 全量字段对照 |
| ④ 前后端契约无门禁 | 下拉污染(Mock-QA-SYNC-) | — | 19+5 条文档错位 | 117 条 disabled 命中需扫 28 文件 |
| ⑤ 多事实源无对账 | persons 创建者:Runner 写 -1,SQL 写 NULL | system_configs vs ipd_business_config 双体系 | 50 Controller vs 33 api 文件 vs 35 文档记录 | DTO vs VO vs domain vs 前端 type 4 方 |

---

## 4. owner 修复优先级建议(本轮 100+ 条汇总)

### 4.1 P0 必修(10 条,按修复成本排序)
1. **Pattern C P0-1**:`IpdPlatformAuthController.java` 缺失 — 8 个 worktree 选权威版本 → main HEAD(0.5d,owner 决策)
2. **Pattern A P0-1**:3 名 SUPER_ADMIN 同时 ACTIVE — 跑 `2026-09-07-ipd-person-super-admin-converge.sql`(0.1d)
3. **Pattern A P0-2**:products 27 条 ACTIVE 测试夹具 — cleanup SQL(0.5d)
4. **Pattern A P0-3**:projects 11 条探针 — cleanup SQL(0.5d)
5. **Pattern A P0-5**:ZK-GATE-TEST 跨表双写 — 双轨一致性修复(1d)
6. **Pattern B P0-5**:`application-prod.yml` 17 处 justauth 密钥泄露 — 立刻移到环境变量或 KMS(0.5d)
7. **Pattern B P0-1**:配置双体系 + 漂移 — 合并 `system_configs` + `ipd_business_config`(2d,owner 决策)
8. **Pattern D P0-1**:Controller `@RequestBody Domain` 注入漏洞 — 补白名单 record(1d,3 个 Controller)
9. **Pattern D P0-2**:前端 disabled 硬编码(`identity-sync` 三按钮 + `workbench` 任务教练) — 加 loading fallback(0.5d)
10. **Pattern A P0-4**:projects status='G1' 异常枚举值 — cleanup SQL(0.1d)

### 4.2 P1 应修(45 条,按 owner 时间窗口选做)
- Pattern A 9 条(cleanup + profile 守卫 + 状态对齐)
- Pattern B 8 条(配置审计覆盖率提升)
- Pattern C 19 条(文档 §9.2/9.3 批量勘误)
- Pattern D 9 条(DTO 补字段 + UI 假 disabled 修复)

### 4.3 P2 可选(45+ 条)
- 多数为文档勘误、字面量配置化、死代码清理、字段对齐

---

## 5. 留给 owner 的 3 个决策点

### 5.1 Pattern C P0-1:`IpdPlatformAuthController.java` 8 个 worktree 哪个是权威版本?
```
.claude/worktrees/agent-p322-postreview-fix-20260907-001/ruoyi-admin/src/main/java/org/ruoyi/ipd/controller/IpdPlatformAuthController.java
.claude/worktrees/agent-batch5-9-1788784989/ruoyi-admin/src/main/java/org/ruoyi/ipd/controller/IpdPlatformAuthController.java
.claude/worktrees/agent-batch5-2-1788784937/ruoyi-admin/src/main/java/org/ruoyi/ipd/controller/IpdPlatformAuthController.java
.claude/worktrees/agent-batch5-1-1788790980/ruoyi-admin/src/main/java/org/ruoyi/ipd/controller/IpdPlatformAuthController.java
.claude/worktrees/agent-p133-idor-fix-20260907-001/ruoyi-admin/src/main/java/org/ruoyi/ipd/controller/IpdPlatformAuthController.java
.claude/worktrees/agent-p133-sop-20260907-001/ruoyi-admin/src/main/java/org/ruoyi/ipd/controller/IpdPlatformAuthController.java
.claude/worktrees/r32-takeover/ruoyi-admin/src/main/java/org/ruoyi/ipd/controller/IpdPlatformAuthController.java
.claude/worktrees/agent-p322-20260907-001/ruoyi-admin/src/main/java/org/ruoyi/ipd/controller/IpdPlatformAuthController.java
```
推荐路径:`diff 8 个文件,选择内容最完整且与 R28.5 log 一致的版本`。

### 5.2 Pattern B P0-1:配置表双体系(system_configs + ipd_business_config)如何合并?
- 选项 A:保留 ipd_business_config,system_configs 标记 deprecated
- 选项 B:合并到 system_configs,改 BonusPoolService + GateReviewService 读 system_configs
- 选项 C:保留双表但加 audit 钩子保证同步

### 5.3 Pattern D P0-1:3 个 Controller 改白名单 record 工作量 vs owner 是否接受 status 注入风险?
- 选项 A:补 3 个白名单 record(`RequirementChangeCreateReq` + `PostLaunchReviewCreateReq` + `SwitchingAcceptanceCreateReq`)
- 选项 B:加 @PrePersist/@PreUpdate 钩子,domain 层防御 status 注入

---

## 6. 完成情况自评

- ✅ 4 个 Pattern 专业智能体并行扫描完成(1895 行结构化报告)
- ✅ Spot check 校正 Agent C 初判错位(反转 P0 案例)
- ✅ 100+ 条同类异常识别 + P0+P1+P2 优先级
- ✅ 0 个未授权修改(仅 docs/ipd-系统说明/ 下 append)
- ✅ 三件套同步(报告 + log.md + 看板镜像)
- ✅ 不动业务代码(按 R33 owner 选定「完全不修(只登记异常)」)
- ✅ 5 类病根框架视角(对照 R25 全局复盘)
- ✅ owner 决策点 3 条(P0-1 权威版本 / 配置双体系 / Controller 注入防御)

**本会话真实改动**:
- 新建 4 个 Pattern 详细报告(1895 行)
- 新建 1 个 R34 总报告(本文件)
- append log.md R34 节
- append 看板镜像 R34 卡
- commit 到 `r34/takeover-20260917` 分支

**留给 R35**:
- owner 决策 5.1 ~ 5.3
- R35 接受 owner 决策后开干
- 推荐先做 P0-1(Controller 孤儿)和 P0-5(密钥泄露)