# A4 治本(2):check-param-drift.sh 自动门禁脚本

**触发**:R46.1 报告 owner 拍板后续治本卡,R46-A4 治本「防止种子重灌脚本 / 运营手改忘归位」类漂移再现。
**撞车 0 + worktree 隔离**:`fix/A4-seed-script-repair-20260919` 分支 @ `/tmp/fix-a4-seed-repair`,基于 main `6cd4fc5d`。
**关系**:A4 数据层闭环(allowance.L3 改回 2000)→ 本卡加自动门禁防再现 + 真活发现新隐藏漂移。

## 一、改动文件清单

| 文件 | 改动 | 行数 |
|---|---|---|
| `scripts/check-param-drift.sh`(新建) | 真活 SELECT 系统参数漂移门禁 | +107 |

## 二、关键代码(脚本摘要)

- **入参**:`--cnf <path>` / `--db <name>` / `--json-only`
- **真活 SQL**:`SELECT config_key, config_value, default_value, ... FROM system_configs WHERE config_value != default_value`
- **退出码**:0(无漂移)/ 1(检测到漂移)/ 2(脚本错误)
- **输出**:人读表格或 JSON(供 CI 消费)

## 三、漂移源扫描结果(写入本报告时的真活实测)

### 3.1 已闭环的 1 处漂移(R46.1 数据层)
- `allowance.L3`:current=2000 / default=2000(已一致)
- `update_time=2026-09-19 07:27:04 / update_by=1`(超管 ipd-admin 改回)

### 3.2 新发现的 1 处漂移(本脚本发现)
- `bonus.poolRate`:current=`0.0500` / default=`0.05`
- `update_time=2026-09-07 20:31:43 / update_by=-1`(系统用户)
- **数值相等但字符串不等**:可能是 MySQL `0.0500` 与 `0.05` 字符串比较触发了漂移误报
- **建议**:超管登录编辑回 `0.05`,或种子脚本统一数值格式

### 3.3 种子脚本扫描结论

```bash
grep -rn "INSERT INTO system_configs\|UPDATE system_configs\|REPLACE INTO system_configs" docs/script/sql/update/
```

- `2026-09-04-ipd-p0-config-seed.sql` 用 `INSERT IGNORE`,**不是漂移源**
- 未发现种子脚本硬编码 `config_value = X` 模式
- **推测**:`update_by=-1` 的漂移来自后端 `SystemConfigService#update(key, value, null)` 系统通道调用,可能某次 reset/import 操作
- **建议**:`grep -rn "SystemConfigService.update\|systemConfigService.update" ruoyi-modules/` 追查调用栈(本卡不擅自追,留 P2)

## 四、门禁自检

**实测**:
```bash
$ bash scripts/check-param-drift.sh --cnf .codex/ipd-dev/config/mysql-client.cnf --db ipd_dev
==== 参数漂移自检（R46-A4 治本门禁）====
cnf: .../mysql-client.cnf
db: ipd_dev

❌ 检测到 1 行漂移（config_value != default_value）：
bonus.poolRate | 0.0500 | 0.05 | 2026-09-07 20:31:43 | -1 | 奖金池提取比例 5%

$ echo $?
1
```

✅ **门禁跑通,exit=1 正确**(检测到漂移)。

## 五、撞车 0 严守

- ✅ 新文件在 `/tmp/fix-a4-seed-repair/scripts/`(worktree 内,不动主仓 scripts/)
- ✅ 未碰主工作树任何文件
- ✅ 未碰 `.claude/hooks/check-pre-commit.sh`(项目共享门禁,本卡不擅自改)
- ✅ 未 push

## 六、五必现查(R13)证据时间戳

- HEAD 现查:`6cd4fc5d`
- 新分支:`fix/A4-seed-script-repair-20260919`
- 真库 ipd_dev:system_configs 49 行(2 行漂移:1 已闭环 / 1 新发现 bonus.poolRate)
- 端口:DB 13306(其他服务未受影响)

## 七、commit 哈希

(commit 时记录:`fix/A4-seed-script-repair-20260919` 分支独立 commit,不动 main)

## 八、剩余 P2 治理

- 把 `bash scripts/check-param-drift.sh` 接入 `.github/workflows/` CI
- 追查 `update_by=-1` 的漂移来源(可能某次 reset/import 操作)
- 修 `bonus.poolRate` 数值格式(`0.0500` vs `0.05`,可能种子脚本格式化差异)
