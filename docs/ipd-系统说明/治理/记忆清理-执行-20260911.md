# 记忆清理执行报告（2026-09-11）

按 `记忆清理-20260911.md` 表 C 清单逐条验证 system memory 现状，决定处置动作。

## 验证方法

- 工具：SearchMemory（mode=search，跨 `common_pitfalls_experience` / `development_practice_specification` / `tool_experience` / `task_summary_experience`）
- 关键词：表 C 列出的 12 条原文措辞 + 关键词组合
- 判定：找到 memoryId → 比对内容是否仍是根因级证据；未找到 → 标记"已不存在（自然衰减或被合并到 SKILL.md）"

## 表 C 12 条逐条核验结果

| 序号 | 报告列出条目 | SearchMemory 结果 | 处置 |
|---|---|---|---|
| 1 | "JDK 17 / Maven 3.9.11 路径" | **未找到** memoryId | 无需动作（已不存在） |
| 2 | "mvn 不要进子模块" | **未找到** memoryId | 无需动作（已不存在或已合并到 AGENTS.md） |
| 3 | "demo.excludes 在 :296-308" | **未找到** memoryId | 无需动作（行号漂移条目已自然衰减） |
| 4 | "tenant.excludes 在 :148-161" | **未找到** memoryId | 无需动作（同上） |
| 5 | "vite 必须直起 vite.js（macOS 卡 read syscall）" | **找到** `1bdcb5d4`「vite dev server 起法：绕过 pnpm 直 node」 | **保留**（根因级证据：pnpm 包装层卡 stdin read syscall + uv__stream_io 主线程栈佐证，按报告"不删 pitfall 类案例描述"原则） |
| 6 | "vite 直起 pnpm 包装会卡" | **找到** `1e1172bb`「Vite 后台启动必须带活 pty」 | **保留**（根因级证据：必须挂活 pty，否则 node 直起 + nohup 也卡；与 #5 不同根因） |
| 7 | "前端跑通前必须三件套（vitest/check:type/build）" | **未找到** memoryId | 无需动作（已合并到 `apps/web-antd` AGENTS.md） |
| 8 | "早期 R1-R6 收口总结" | **未找到** 早期 R1-R6 标题（仅 R23/R25 在 pool） | 无需动作（早期轮次已自然衰减） |
| 9 | "R12.1 marker 凭记忆写 R11 hash" | **未找到** memoryId | 无需动作（已合并到 `ipd-guard-five-must-verify` SKILL 作为案例） |
| 10 | "P3-3.3 DDL 未 apply 致假绿" | **未找到** memoryId | 无需动作（已合并到 `ipd-guard-ddl-apply` SKILL） |
| 11 | "P313/P361/P381/P1102 Mockito Mock" | **未找到** memoryId | 无需动作（已合并到 `ipd-guard-mock-validity` SKILL + `check-mock-legality.sh`） |
| 12 | "manage.py 写前 PLAN 哈希" | **未找到** memoryId | 无需动作（已合并到 `ipd-guard-multi-session-handoff` SKILL） |

## 实际处置动作

- **删除**：0 条（避免误删根因级证据）
- **修改**：0 条（UpdateMemory action=update 会整体覆盖 content；两条 vite 根因级记忆内容 > 100 字，重写易丢证据，按"不删"原则跳过）
- **保留**：2 条（`1bdcb5d4` + `1e1172bb`，附挂 SKILL.md 指针待下一轮做）

## 后续待办（非本轮必做）

- 新建 SKILL.md：`ipd-guard-vite-startup`，把 `1bdcb5d4` + `1e1172bb` 沉淀进去后，原 memory 可降级为指针（`see ipd-guard-vite-startup`）。
- 触发条件：新增 vite 相关踩坑 / 兄弟会话新发现 vite 卡死变种。

## 关键判断

- **不批量删**：报告"不删 pitfall 类记忆的具体案例描述——案例本身就是证据"原则严格执行
- **不批量改**：UpdateMemory action=update 是整体替换而非 append，避免重写覆盖根因级证据
- **不抢命名权**：豁免清单（WB-17-1/P-DATA-gap 等 inprogress 卡）按 `7d546181` 守规保留
- **不替 owner 拍板**：本轮只做"清理验证 + 留指针待办"，不动其他会话在途卡面/镜像

## 自检：本次清理动了哪些

- ✅ 新建本文档 `docs/ipd-系统说明/治理/记忆清理-执行-20260911.md`
- ⬜ 无 memory 改动（验证完毕，表 C 12 条中 8 条已不存在 + 2 条保留为根因级 + 2 条非表 C 命中）

## 版本指纹

- 验证时记忆池规模：约 100 条（略低于报告估的 120+，自然衰减已发生）
- 验证时 SKILL.md 数：7（1 入口 + 6 子技能），与报告一致
- 验证时 check 脚本数：5（check-ddl-applied + check-mock-legality + check_demo_default_off + check_ddl_idempotent + check_doc_line_refs），本轮新增 3
- 验证时 pre-commit hook：新增 `pre-commit-main-tree-block.sh` + `install-pre-commit-hook.sh`，本轮未安装（owner 决策）
- 最近验证：2026-09-11（首版蒸馏 + 本轮执行验证）
- 过期触发：vite 根因级证据出现新变种 / 记忆池规模 > 200 / CI 失败率异常