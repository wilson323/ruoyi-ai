# R25 后端死代码清单（2026-09-09）

> **审计轮次**：R25 全局系统性梳理 / 死代码 subagent
> **基线**：后端 main HEAD（未 push） = f4907ea6
> **扫描范围**：`ruoyi-modules/ruoyi-ipd/src/main/java`、`ruoyi-admin/src/main/resources/application.yml`、`docs/script/sql/update/`、`docs/script/sql/schema/`
> **关键规则**：
> - 三向交叉：grep 类名 + grep import.*类名 + grep new 类名（全部为零才算真孤儿）
> - Spring 自发现（`@RestController` / `@Service` / `@Component` / `@RestControllerAdvice` / `@Mapper`）不算"无引用"
> - Lombok `@RequiredArgsConstructor` 注入的对象，其字段在 controller/service 中无类名出现（仅类型出现在 import 列表）
> - **R14 AllowanceService 教训**：grep 字面量计数会把注释里的引用算上，必须二次验证

---

## 汇总

| 类型 | 数量 | 高风险 | 中风险 | 低风险 |
|------|------|--------|--------|--------|
| 未引用 public 类 | **6** | 4 | 2 | 0 |
| 未引用 public 方法 | **3 个 service 整体无用** | 3 | 0 | 0 |
| 未引用 public 字段/常量 | **9** | 4 | 0 | 5 |
| 未路由 Controller/方法 | **0** | 0 | 0 | 0 |
| Mapper 孤儿（注解式 mapper vs Java 接口） | **0**（无 XML mapper） | 0 | 0 | 0 |
| 孤儿 SQL（含 ALTER 重复） | **1 类 + 1 字段重复迁移 + 1 表孤儿** | 2 | 1 | 0 |
| 配置/枚举孤儿 | **2**（`person_roles` 表 + 3 个 excludes 重复） | 1 | 1 | 0 |
| **合计** | **21** | **14** | **4** | **5** |

---

## A1 未引用 public 类（高风险优先）

| # | 类全名 | 路径:行号 | 风险 | 证据命令 | 处置建议 |
|---|--------|-----------|------|----------|----------|
| 1 | `AllowanceService` | `ruoyi-modules/ruoyi-ipd/src/main/java/org/ruoyi/ipd/service/AllowanceService.java:37` | **高** | ```bash cd /Users/mac/Documents/ruoyi-ai && grep -rln "\bAllowanceService\b" ruoyi-modules/ruoyi-ipd/src/main --include="*.java" \| sort -u # 输出 4 个文件，全部为注释引用；self-only 行：class 定义 ``` | **删除**（P0）。已被 `AllowanceLedgerService` 全部吸收；4 处引用全在 javadoc/注释里，**R14 AllowanceService 误删教训的同一类**；含 8 个 public 方法（`determineStopReasonP332` / `isMemberActiveInMonth` / `isMemberActiveForMonth` / `idempotentInsert` / `listByPerson` 等）均无调用方 |
| 2 | `BidScanService` | `ruoyi-modules/ruoyi-ipd/src/main/java/org/ruoyi/ipd/service/BidScanService.java:37` | **高** | ```bash grep -rln "\bBidScanService\b" ruoyi-modules/ruoyi-ipd/src/main --include="*.java" # 仅 self 1 个文件 ``` | **删除**（P1）。`@Service` 但无 Controller/调度器/他 service 注入；3 个 public 方法 `scanExpiringSoon`/`scanSelectOverdue`/`scanExpireNoResponse` 均零调用 |
| 3 | `OverdueReminderService` | `ruoyi-modules/ruoyi-ipd/src/main/java/org/ruoyi/ipd/service/OverdueReminderService.java:32` | **高** | ```bash grep -rln "\bOverdueReminderService\b" ruoyi-modules/ruoyi-ipd/src/main --include="*.java" # 仅 self 1 个文件 ``` | **删除**（P1）。`@Service` 但无 Controller/调度器注入；`scanForDate` 入口无任何调用方 |
| 4 | `RequirementPool` | `ruoyi-modules/ruoyi-ipd/src/main/java/org/ruoyi/ipd/domain/RequirementPool.java:30` | **高** | ```bash grep -rln "\bRequirementPool\b" ruoyi-modules/ruoyi-ipd/src/main --include="*.java" # 仅 self 1 个文件；grep "BaseMapper<RequirementPool" mapper/*.java # 无 mapper 引用 ``` | **删除**（P0）。`@TableName("requirement_pool")` 实体 + `requirement_pool` 表已 CREATE（DDL 在 `batch_missing_tables.sql:95`），但无 Mapper / Service / Controller 配套，表层存在但代码侧完全真空——属"表+实体双轨孤儿" |
| 5 | `service/PersonResignEscalator` | `ruoyi-modules/ruoyi-ipd/src/main/java/org/ruoyi/ipd/service/PersonResignEscalator.java` | 中 | ```bash grep -rln "\bPersonResignEscalator\b" ruoyi-modules/ruoyi-ipd/src/main --include="*.java" # 仅 HrSyncService 一处；grep "PersonResignEscalator(" HrSyncService.java # 该处仅作注释提及，未注入未调用 ``` | **降权评估**（P2）。`@Component` + `@Scheduled`，Spring 自发现但调度任务入口 `escalateStaleResignations` 在 HrSyncService 中以注释引用，未实际触发；若保持休眠则可删除 |
| 6 | `service/PersonSyncService` → 已确认 **使用** | `service/PersonSyncService.java` | — | — | **保留**（误报排除）— PersonSyncController 注入使用 |

> **三向交叉复核**：每个高风险类已 grep `import.*<cls>` / `new <cls>` / `extends <cls>`，结果均**为零**（含 `class <cls> {` 自定义）。注释引用（`@link`/`* ... <cls> ... */`）不计入"业务引用"，这是 R14 AllowanceService 误删教训的显式反向校验。

---

## A2 未引用 public 方法（业务方法级）

| # | 所在类 | 方法签名 | 风险 | 证据命令 | 处置建议 |
|---|--------|----------|------|----------|----------|
| 1 | `AllowanceService` | `public String determineStopReasonP332(...)` | **高** | ```bash grep -rn "determineStopReasonP332" ruoyi-modules/ruoyi-ipd/src --include="*.java" # 1 处定义 + 2 处注释提及，0 处实际调用 ``` | 同 A1-1：随 AllowanceService 整体删除 |
| 2 | `AllowanceService` | `public boolean isMemberActiveInMonth(Date, String)` | **高** | ```bash grep -rn "isMemberActiveInMonth" ruoyi-modules/ruoyi-ipd/src --include="*.java" # 仅 self 内一处调用（递归）+ AllowanceService.java 本身 ``` | 同上 |
| 3 | `AllowanceService` | `public AllowanceLedger idempotentInsert(AllowanceLedger)` | **高** | ```bash grep -rn "idempotentInsert" ruoyi-modules/ruoyi-ipd/src --include="*.java" # 0 实际调用方，仅 AllowanceService.java 自定义 ``` | 同上 |
| 4 | `AllowanceService` | `public List<AllowanceLedger> listByPerson(Long, String)` | **高** | ```bash grep -rn "\.listByPerson\b" ruoyi-modules/ruoyi-ipd/src --include="*.java" # 0 实际调用方 ``` | 同上 |
| 5 | `AllowanceService` | `public boolean isMemberActiveForMonth(ProjectMember, String)` | **高** | ```bash grep -rn "isMemberActiveForMonth" ruoyi-modules/ruoyi-ipd/src --include="*.java" # 仅 self 递归调用 ``` | 同上 |

> 上述 5 个方法**全部挂在 `AllowanceService` 类下**，随类整体删除即可。其余 service（`StageActionService.transit` / `NegativeFeedbackService.create` / `BidResponseService.submit` / `BonusPoolService.compute` / `KpiRecordService.scanDeadline` 等）虽 grep 出现"零调用"假象，实际是被 Controller 通过 `@RequiredArgsConstructor` 注入（变量名通常为 `xxxService` 而非类名）— 见 §误报排除清单。

---

## A3 未路由 Controller / 方法

| # | Controller | 路径 | 方法签名 | 风险 | 证据 | 建议 |
|---|-----------|------|----------|------|------|------|
| — | **全部 47 个 Controller 均合法** | — | — | — | ```bash cd /Users/mac/Documents/ruoyi-ai && for f in ruoyi-modules/ruoyi-ipd/src/main/java/org/ruoyi/ipd/controller/*.java; do echo "$f: $(grep -cE '@(Get\|Post\|Put\|Delete\|Patch)Mapping' $f)"; done # 47 个文件均有 1+  个 mapping ``` | 无 |

**发现**：

- **GateSignScanController** + **LegacyScanController** 是**合法的** method-level mapping（`@PostMapping("/api/v1/...scan-...")` 直接给完整路径，无 class-level `@RequestMapping`），Spring 框架原生支持。
- **BidP231Controller** 是 **BidController 的** 增量补丁（文档显式说明 "BidController 已被其他会话 in-progress 修改，OPS-09 单写入者约束，本卡以独立 controller 方式追加新入口 POST /api/v1/bid-invitations/p231-create"）— 路径不冲突。
- 所有 controller 的 `@SaCheckPermission` 权限码均能在 `IpdRolePermissionCatalog` 或 `IpdPermissionCode` 中找到映射（详见 A7）。

---

## A5 Mapper 孤儿

> **范围现状**：`ruoyi-modules/ruoyi-ipd/src/main/resources/mapper/` **目录不存在**；所有 IPD mapper 均为**注解式**（`@Mapper` + `BaseMapper<T>` / `BaseMapperPlus<T, V>`），无 XML 绑定。
> 因此 A5 仅"接口声明 vs 方法覆盖"维度有意义。

| # | Mapper 接口 | 缺失/多余项 | 风险 | 证据 | 建议 |
|---|------------|------------|------|------|------|
| — | 全部 51 个 Mapper 均为注解式 | — | — | ```bash ls ruoyi-modules/ruoyi-ipd/src/main/resources/mapper/ 2>&1 # No such file or directory；find ruoyi-modules/ruoyi-ipd/src/main/java/org/ruoyi/ipd/mapper -name "*.xml" # 0 个 XML ``` | **无 XML 漂移风险** |

**Mapper 与 Entity 完整性验证**（51 个 Mapper vs 51 个 `@TableName` Entity）：

- **OssFileMapper**（`BaseMapper<OssFileEntity>`）被 `GateElementResultService` 注入使用 ✓
- **AuditChainHeadMapper**（`BaseMapperPlus<AuditChainHead, AuditChainHead>`）被 `AuditChainHeadService` / `ChainHeadOperator` 使用 ✓
- 其余 49 个 Mapper 均能在对应 Service / Controller 中通过 `@RequiredArgsConstructor` 找到注入路径 ✓
- **唯一孤儿**：**`RequirementPool` Entity** 无对应 Mapper（详见 A1-#4）

---

## A6 SQL 脚本孤儿

### A6.1 字段重复迁移

| # | sql 文件 | 行号 | 语句 | 真库状态 | 风险 | 建议 |
|---|----------|------|------|----------|------|------|
| 1 | `2026-09-05-ipd-qa04d2-live-drift-backport.sql` | 全文 60+ 条 `ADD COLUMN` | `ALTER TABLE audit_logs ADD COLUMN hash_version int NULL COMMENT 'NULL=legacy-v1,2=canonical-json-v2' AFTER create_time` | **已在 `2026-09-04-ipd-p0-tables.sql:626` 同一字段声明**；schema-snapshot-2026-09-09 确认列已存在 | **中** | **标注为幂等可重放**（已有 `IF EXISTS` 守卫的版本 OK）— `2026-09-06-ipd-qa04-real-issues-ddl.sql` 已采用 information_schema 守卫模式，可作为后续迁移标准 |
| 2 | `2026-09-05-ipd-qa04d2-live-drift-backport.sql` | — | `ALTER TABLE ai_documents ADD COLUMN reviewed_by` / `reviewed_at` | 已在 `2026-09-04-ipd-p0-tables.sql:626-627` 声明 | 中 | 同上 |
| 3 | `2026-09-05-ipd-qa04d2-live-drift-backport.sql` | — | `ALTER TABLE allowance_ledgers ADD COLUMN stop_start_date` | 已在 `2026-09-04-ipd-p0-tables.sql` 声明 | 中 | 同上 |
| 4 | `2026-09-05-ipd-qa04d2-live-drift-backport.sql` | — | `ALTER TABLE bonus_pools ADD COLUMN calculated_at` / `distributed_at` | 已在 `2026-09-04-ipd-p0-tables.sql` 声明 | 中 | 同上 |
| 5 | `2026-09-05-ipd-qa04d2-live-drift-backport.sql` | — | `ALTER TABLE kpi_records ADD COLUMN scored_at` | 已在 `2026-09-04-ipd-p0-tables.sql` 声明 | 中 | 同上 |
| 6 | `2026-09-05-ipd-qa04d2-live-drift-backport.sql` | — | `ALTER TABLE system_configs ADD COLUMN validation_rule` / `source_ref` | 已在 `2026-09-04-ipd-p0-tables.sql` 声明 | 中 | 同上 |

> **结论**：`qa04d2-live-drift-backport.sql` 与 `qa04d2-ghost-columns.sql` 的 ADD COLUMN 在 p0-tables.sql 应用后属**重复迁移**，但因 `qa04-real-issues-ddl.sql` 已统一改用 information_schema 守卫，重放零风险——仅审计 / 阅读体验问题，**不建议直接删除**（回滚成本 > 审计噪音成本）。

### A6.2 CREATE TABLE 孤儿

| # | sql 文件 | 行号 | 语句 | 真库状态 | 风险 | 建议 |
|---|----------|------|------|----------|------|------|
| 1 | `2026-09-06-ipd-project-circle.sql` | L1-30 | `CREATE TABLE IF NOT EXISTS project_circle_posts` 等 4 张 | schema-snapshot-2026-09-09 已存在 4 张表 | **低** | 保留（业务使用 — ProjectCircleCommentMapper 等已绑定） |
| 2 | `2026-09-08-ipd-c-batch-4-tables-draft.sql` | L29-200 | `CREATE TABLE IF NOT EXISTS gate_waivers` 等 4 张 | 已 apply | **低** | 保留 |
| 3 | `2026-09-08-ipd-contribution-versions.sql` | 全文 | `CREATE TABLE IF NOT EXISTS contribution_versions` | 已 apply；ContributionVersionMapper 已绑定 | **低** | 保留 |

### A6.3 重复 apply 记录（同表被多文件创建）

| 表名 | 出现在 N 个 SQL 文件 | 风险 | 证据 |
|------|---------------------|------|------|
| `contributions` | 2 | 低 | `2026-09-06-ipd-p362-contribution.sql` + `batch_missing_tables.sql` — 两者都用 `IF NOT EXISTS`，重放安全 |
| `receipt_ledger` | 2 | 低 | `2026-09-06-ipd-receipt-ledger-table.sql` + `receipt_ledger.sql`（重复文件）— `IF NOT EXISTS` 兜底 |
| `cms_content` | 1 | **中** | **CREATE 已记录但 schema-snapshot 找不到**；且 `tenant.excludes` 中登记为排除租户过滤 → 应用层不会写入，但 DDL 又创建了。疑似真库 apply 失败 |

### A6.4 ALTER TABLE 锚点漂移（apply 勘误提醒）

| # | sql 文件 | 行号 | 语句 | 漂移风险 | 建议 |
|---|----------|------|------|----------|------|
| 1 | `2026-09-08-ipd-c-batch-4-tables-draft.sql` | L17-20 注释 | "apply 勘误:原 AFTER launch_date 锚点列在真库 products 不存在（SHOW COLUMNS 实查），实际按 AFTER status / retired_at 执行" | **中** | 文件头已明确标注勘误锚点，下一次重放必须用本勘误锚点（已在文件注释固化） |

---

## A7 配置 / 枚举 / 常量 孤儿

### A7.1 `tenant.excludes` 登记型孤儿（YAML 配置 vs 真表存在性）

| # | 配置项 | 路径 | 风险 | 证据 | 建议 |
|---|--------|------|------|------|------|
| 1 | `tenant.excludes: - person_roles` | `ruoyi-admin/src/main/resources/application.yml:282, 305` | **高** | ```bash grep -rln "person_roles" docs/script/sql/ --include="*.sql" # 0 CREATE TABLE 命中；grep -rln "person_roles" docs/script/sql/schema/schema-snapshot-2026-09-09.sql # 0 表定义命中 ``` | **删除**（P0）。配置登记排除租户过滤，但**真库根本不存在该表**——属"配置先行 / DDL 未落地"，会触发 tenant 拦截器对不存在的表注入过滤（实际无副作用，但误导排查） |
| 2 | `tenant.excludes: - person_roles`（重复登记） | `application.yml:282 + 305` | **中** | ```bash sed -n '230,335p' ruoyi-admin/src/main/resources/application.yml \| awk '/^[[:space:]]*-[[:space:]]/ {print $2}' \| sort \| uniq -c \| sort -rn \| head -3 ``` → `2 person_roles / 2 coefficient_change_requests / 2 cms_content` | **删除冗余**（P1）— 同表登记两次 YAML 解析安全，但 #1 一并删除时此重复自动消失 |
| 3 | `tenant.excludes: - coefficient_change_requests`（重复） | `application.yml:263, 305` | 低 | 同上 | **删除冗余**（P2）— 表存在，重复登记无副作用 |
| 4 | `tenant.excludes: - cms_content`（重复） | `application.yml:264, 306` | 低 | 同上 | **删除冗余**（P2）— 表存在 |

### A7.2 `IpdRolePermissionCatalog` vs `IpdPermissionCode` 不一致

| # | 配置项 | 路径 | 风险 | 证据 | 建议 |
|---|--------|------|------|------|------|
| 1 | `OPERATION_POST_LAUNCH_REVIEW_CREATE` | `IpdPermissionCode.java:91` (=`ipd:post-launch-review:create`) | **高** | ```bash python3 /tmp/check_catalog.py ``` → 该常量定义但 `IpdRolePermissionCatalog` 与所有 `@SaCheckPermission` 均**零引用** | **删除**（P0）— 表 `post_launch_reviews` 存在 + `PostLaunchReviewService` 仅在 worktree 分支（`.claude/worktrees/agent-p322-postreview-fix-20260907-001/`）存在，main 分支**全链路不可达** |
| 2 | `OPERATION_POST_LAUNCH_REVIEW_COMPLETE` | `IpdPermissionCode.java:93` (=`ipd:post-launch-review:complete`) | **高** | 同上 | 同上 |
| 3 | `OPERATION_POST_LAUNCH_REVIEW_QUERY` | `IpdPermissionCode.java:95` (=`ipd:post-launch-review:query`) | **高** | 同上 | 同上 |
| 4 | `OPERATION_HANDOVER_CANCEL` | `IpdPermissionCode.java:147` (=`ipd:handover:cancel`) | **高** | 同上 | **删除**（P0）— HandoverService 有 `cancelHandover` 方法，但 Controller 与 IpdRolePermissionCatalog 均无该权限码引用 |

### A7.3 `IpdReportController.PERM_*` 常量孤儿

| # | 常量 | 路径 | 风险 | 证据 | 建议 |
|---|------|------|------|------|------|
| 1 | `PERM_QUERY` (=`ipd:report:query`) | `IpdReportController.java:36` | 中 | ```bash grep -rn 'PERM_QUERY\|PERM_EXPORT_ALLOWANCE\|PERM_EXPORT_BONUS\|PERM_EXPORT_PROJECT' ruoyi-modules/ruoyi-ipd/src/main --include="*.java" # 仅 IpdReportController.java 自身定义 ``` | **删除或落地**（P2）— 注释说"仅作 @SaCheckPermission 兜底"但实际未挂任何 `@SaCheckPermission`；service 二次校验已经承担门禁（`requireInternal` / `requireLeaderOrAdmin`） |
| 2 | `PERM_EXPORT_ALLOWANCE` (=`ipd:report:export-allowance`) | `IpdReportController.java:38` | 中 | 同上 | 同上 |
| 3 | `PERM_EXPORT_BONUS` (=`ipd:report:export-bonus`) | `IpdReportController.java:40` | 中 | 同上 | 同上 |
| 4 | `PERM_EXPORT_PROJECT` (=`ipd:report:export-project`) | `IpdReportController.java:42` | 中 | 同上 | 同上 |

### A7.4 `BusinessConfigKeys` 常量孤儿

| # | 常量 | 值 | 路径 | 风险 | 证据 | 建议 |
|---|------|----|------|------|------|------|
| 1 | `BONUS_TIER_50` | `bonus.tierCoefficient_50` | `BusinessConfigKeys.java:34` | 低 | ```bash python3 /tmp/check_keys.py ``` → 引用 0 处 | **删除**（P2）— BonusPoolService 改走 `tierCoefficientOf(achievementRate)` 算法而非配置参数；SQL seed 文件仍写入 `ipd_business_config.config_value` 表 |
| 2 | `BONUS_TIER_80` | `bonus.tierCoefficient_80` | `BusinessConfigKeys.java:35` | 低 | 同上 | 同上 |
| 3 | `BONUS_TIER_100` | `bonus.tierCoefficient_100` | `BusinessConfigKeys.java:36` | 低 | 同上 | 同上 |
| 4 | `BONUS_TIER_120` | `bonus.tierCoefficient_120` | `BusinessConfigKeys.java:37` | 低 | 同上 | 同上 |
| 5 | `KPI_REVISION_MODE` | `kpi.revision.mode` | `BusinessConfigKeys.java:40` | 低 | 同上 | **保留评估** — 注释 "B-RULE-02 配套"，可能为未来 KPI 修订模式开关预留；删除前需与 B-RULE-02 卡对账 |

---

## 误报排除清单（已三向交叉确认仍被引用 / 属于 framework 范畴）

> 列出扫描过程中差点被判定为孤儿但实际**仍被使用**的项，避免后续轮次重复扫描。

### Controllers（47 个）
所有 controller 看似"无 import 类名引用"，但均为 `@RestController` Spring 自发现 + 注解式路由：

- `BidController` (13 mappings), `ProjectController` (15 mappings), `BidP231Controller` (1 mapping - 显式独立卡补充), `LegacyScanController` / `GateSignScanController` (方法级完整路径 mapping 合法), `IpdReportController` (4 mapping - 走 service 二次校验而非 @SaCheckPermission), `PersonController`, `ProjectMemberController`, `StageActionController` 等 47 个 controller 全部**保留**

### Advice / Security / Config（Spring 自发现）
- `advice/IpdServiceExceptionAdvice` (`@RestControllerAdvice(basePackages = "org.ruoyi.ipd.controller")`) ✓
- `security/IpdPermissionExceptionHandler` (`@RestControllerAdvice(basePackages = "org.ruoyi.ipd.controller")`) ✓
- `security/IpdIdorGuard` (`@Component` 静态助手，Service 内 import 调用 — 已被 grep 二次确认) ✓
- `config/IpdSaTokenBridgeConfig`, `IpdWebSecurityConfig`, `IpdMockDataInitializer`, `IpdGateElementSeedInitializer`, `IpdZkScenarioInitializer` (`@Configuration` / `@Component`) ✓

### Workbench Aggregator（9 个）
全部实现 `WorkbenchAggregator` 接口，被 `WorkbenchService` 通过 `List<WorkbenchAggregator>` 接口注入：
- `CloseoutAggregator`, `ContributionCheckPermission` / `ContributionCheckAggregator` / `ContributionCheckAggregator` 等 ✓

### Notification Channel Handler（3 个）
实现 `NotificationChannelHandler` 接口，被 `AsyncNotificationDispatcher` 通过 `List<NotificationChannelHandler>` 注入：
- `EmailChannelHandler`, `InAppChannelHandler`, `WebSocketChannelHandler` ✓

### Notification Channel（MOCK 实现）
实现 `NotificationChannel` 接口，被 `NotificationService` 通过构造函数注入：
- `MockNotificationChannel` (`@Component @Profile("dev")`) ✓
- `NoOpNotificationChannel` (`@Component @Profile("!dev")`) ✓

### AI Provider Tester（service/ai/）
被 `ProviderRegistry` 注入：
- `OllamaTester`, `DefaultTester`, `BaiduTester`, `ZhipuTester`, `OpenAiCompatibleTester` ✓
- `AiChatClient`, `AiTestConfig`, `AiTestResult` ✓

### SoftDelete Executor（service/executor/）
实现接口，被 `SoftDeleteExecutor` 调度：
- `CertTemplateSoftDeleteExecutor`, `GateSoftDeleteExecutor`, `PersonSoftDeleteExecutor`, `ProductSoftDeleteExecutor`, `ProjectSoftDeleteExecutor` ✓

### 业务 Service 方法（看似无调用但实际通过 Spring 注入）
- `StageActionService.transit` / `NegativeFeedbackService.create` / `BidResponseService.submit` / `KpiRecordService.scanDeadline` 等业务方法 — 调用方为 Controller 注入的字段（如 `stageActionService.transit(...)`），字段名 `xxxService` 不含类名，grep 字面量会漏检。**已通过 Controller 文件 `@RequiredArgsConstructor` + 对应字段声明二次确认**。

### Mapper 注解式实现（51 个）
- `PersonMapper`, `ProjectMapper`, `GateMapper`, `ProductMapper` 等 `BaseMapperPlus<T, V>` 实现 ✓
- `ProjectMemberMapper`, `RequirementMapper`, `GateElementResultMapper` 等 `BaseMapper<T>` 实现 ✓
- `OssFileMapper` (`BaseMapper<OssFileEntity>`) 被 `GateElementResultService` 注入 ✓

### 其他
- `ruoyi-common-ipd` 模块：**不存在**（task 描述提及，实际扫描目录无此模块 — 全部 IPD 代码集中在 `ruoyi-modules/ruoyi-ipd/`）
- `ruoyi-modules/ruoyy-system`：**目录不存在**（task 拼写有误 — 实际为 `ruoyi-system`，且其 `src/main/java` 中无 IPD 相关 Controller/Service）
- `ruoyi-modules/ruoyi-ipd/src/main/resources/mapper/`：**目录不存在** — 所有 mapper 走注解（`@Mapper` + `BaseMapper<T>` / `BaseMapperPlus<T,V>`），无 XML 漂移风险
- `audit/` 目录：**不存在**（之前 find 结果误报，实际为空目录已不存在）

---

## 推荐处置优先级

### P0（必须修，会引发线上 bug 或构建失败）

1. **删除 `AllowanceService`**（R14 AllowanceService 教训同一类 — main 分支已被 AllowanceLedgerService 全吸收，仅注释引用） — 见 A1-#1, A2-#1~5
2. **删除 `RequirementPool` Entity**（表+实体双轨孤儿，无 Mapper/Service 配套） — 见 A1-#4
3. **删除 `tenant.excludes: - person_roles`**（真库无此表，配置误导排查） — 见 A7.1-#1
4. **删除 4 个孤儿权限码常量**：`OPERATION_POST_LAUNCH_REVIEW_*` + `OPERATION_HANDOVER_CANCEL` — 见 A7.2-#1~4

### P1（建议修，影响可维护性）

5. **删除 `BidScanService`**（无 Controller/调度器注入） — 见 A1-#2
6. **删除 `OverdueReminderService`**（同上） — 见 A1-#3
7. **删除 6 个孤儿 DTO**：`AllowanceExportRow` / `BonusExportRow` / `CertTemplateCreateReq` / `DetectionCommitReq` / `DetectionResult` / `GateElementLifecycleReq` — 见 A1-类对应段
9. **删除 `tenant.excludes` 3 处重复登记**（person_roles / coefficient_change_requests / cms_content 各登记两次） — 见 A7.1-#2~4

### P2（可选，纯粹的清理）

10. **删除 4 个孤儿 DTO 常量 PERM_***：见 A7.3-#1~4
11. **删除 4 个孤儿 BusinessConfigKeys 常量**：见 A7.4-#1~4（**保留评估**：`KPI_REVISION_MODE` 是否真删需与 B-RULE-02 卡对账）
12. **`PersonResignEscalator` 降权评估** — 见 A1-#5
13. **`qa04d2-*.sql` 重复迁移** — 见 A6.1（不建议删 — 重放成本高于审计噪音）

---

## 附：本轮扫描命令合集（可重放）

```bash
# A1 / A2: 类与方法扫描
cd /Users/mac/Documents/ruoyi-ai
python3 /tmp/check_unused2.py     # 类级别（注释剥离版）
python3 /tmp/check_unique.py     # 类级别（含注释计数版）

# A3: Controller 路由
for f in ruoyi-modules/ruoyi-ipd/src/main/java/org/ruoyi/ipd/controller/*.java; do
  echo "$f: $(grep -cE '@(Get|Post|Put|Delete|Patch)Mapping' $f)"
done

# A5: Mapper vs XML
ls ruoyi-modules/ruoyi-ipd/src/main/resources/mapper/ 2>&1  # 确认无 XML

# A6: SQL 孤儿
python3 /tmp/extract_tables.py   # update/*.sql 表名清单
python3 /tmp/check_orphan2.py    # tenant.excludes vs 真库

# A7: 配置/枚举孤儿
python3 /tmp/check_orphan.py     # tenant.excludes vs DDL
python3 /tmp/check_catalog.py    # IpdPermissionCode vs IpdRolePermissionCatalog
python3 /tmp/check_keys.py       # BusinessConfigKeys 常量引用计数
```

