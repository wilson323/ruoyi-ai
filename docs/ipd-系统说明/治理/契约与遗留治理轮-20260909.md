# 契约与遗留治理轮执行报告（2026-09-09）

> 接续《P0修复执行-17类问题首轮-20260909.md》§8 P2 轮。本轮按 owner 指令对三个事项完整执行：
> ① 津贴口径裁决材料备齐（已 R19 拍板落地：当月退出当月不发）② 43 项历史 finding 蜂群核查+关单+顺手修复（R17+R19 双批）③ 审计 AOP 改造盘点+设计（R18 落盘待批）。
> 4 路蜂群（A 安全核查 / B 性能核查 / C 审计盘点 / D 错误码三表比对）全部只读，结论均带当前代码证据。

## 0. 一句话总账

42 项历史 finding 逐项核完：**30 已修复（71.4%，R17 关 25 + R19 卫生 5 全清）/ 3 部分修复 / 8 仍在 / 1 误报**；本轮顺手新修 6 项（R17：后端 3 + 前端 3 组；R19：5 卫生项）；待 owner 裁决 3 项（config_value 列型、hash_version=2 历史行、40002 语义归属——津贴口径 R19 已结）。

## 1. 43 项 finding 关单核查明细

### 1.1 蜂群 A：SEC-AUD 2026-09-05（22 项）→ 15 已修 / 6 仍在 / 1 误报

| finding | 级别 | 判定 | 当前证据（文件 + 一句话） |
|---|---|---|---|
| HIGH-1 BCrypt cost=4 | HIGH | ✅已修 | IpdMockDataInitializer `gensalt(10)` + IpdAuthService hutool 默认 10，两处一致 |
| HIGH-2 密码字面量 | HIGH | ✅已修 | `INITIAL_PWD` 常量已删，`@Value("${ipd.security.initial-password}")` env 注入；prod 无默认 fail-fast |
| HIGH-3 handler 漏列 6 Controller | HIGH | ✅已修 | IpdPermissionExceptionHandler `@RestControllerAdvice(basePackages=...)` 全包覆盖 + PermissionAdviceCoverageTest CI 防漂移 |
| HIGH-4 P0-6.3 测试假绿 key | HIGH | ✅已修 | key 改 `deletion.leaderDeadlineDays` 与生产一致 + 源码文本断言锁死 |
| HIGH-5 config_value 列型漂移 | HIGH | ⛔仍在 | 仓库 DDL 仍 varchar(500)、写入无长度校验 → **owner 裁决列型后同步**（建议 text） |
| MED-1 verifyChain 无事务读误报 | MED | ✅已修 | 锚行悲观锁全局串行分配 seq，部分可见场景根因消除 |
| MED-2 rebuildChain 缺原子保护 | MED | ⛔仍在 | 无 FOR UPDATE/锚行互斥，重算后不推锚 last_hash → 并发 rebuild+append 人为断链。**审计链核心，挂专项** |
| MED-3 Withdraw 差分响应 | MED | ⛔仍在 | 不存在抛 404 / 非本人抛文案，侧信道原样保留 |
| MED-4 actuator `*` 暴露 | MED | ✅已修 | include 收窄 health,info,metrics + WHEN_AUTHORIZED |
| MED-5 RSA 私钥字面量 | MED | ✅已修 | env 注入，PEM 字面量已移除 |
| MED-6 requireJson 位置风险 | MED | ✅已修 | 重试循环整体删除，载体不复存在 |
| MED-7 prod 池 20 | MED | ✅已修 | maxPoolSize 80 + connectionTimeout 5000 |
| LOW-1 锚表未落地 | LOW | ✅已修 | AuditChainHead + Mapper 存在并被使用；tenant.excludes 已登记 |
| LOW-2 springdoc 暴露 | LOW | ✅已修 | prod api-docs/swagger-ui 均 false |
| LOW-3 jwt-secret 默认值 | LOW | ✅已修 | env 无默认，历史泄露密钥声明作废 |
| LOW-4 websocket `*` | LOW | ✅已修 | allowedOrigins 已收窄 |
| LOW-5 append 3 次重试不足 | LOW | ✅已修 | 锚行悲观锁零重试零 CAS |
| LOW-6 listArchive 缺 readOnly | LOW | ✅**本轮已修** | DeletionArchiveService.listArchive 补 `@Transactional(readOnly=true)` |
| INFO-1 dev 明文 root 密码 | INFO | ✅**R19 已修** | dev 密码改 `${DB_PASSWORD:root}` env 占位，默认 root 仅本地便利；生产/共享环境需显式设 DB_PASSWORD 覆盖 |
| INFO-2 gensalt(4) 同源 | INFO | ✅已修 | 同 HIGH-1 |
| INFO-3 rebuild 运维护栏 | INFO | ✅**R19 已修** | rebuildChain 响应加 serverBuild 字段 + currentBuildVersion helper（jar Implementation-Version + host），多实例共库运维可 curl 比对版本一致后再统一调用 |
| INFO-4 7 commit 纯文档 | INFO | ⚪误报 | 元观察非缺陷，无修复对象 |

### 1.2 蜂群 B：PERF-AUD 15 + QA 5（20 项）→ 10 已修 / 3 部分 / 7 仍在

| finding | 级别 | 判定 | 当前证据 |
|---|---|---|---|
| P0-1 删除升级 N+1 | P0 | ✅已修 | 单 SQL 批量 UPDATE + idx_del_status_leader 索引 |
| P0-2 招标过期 N+1 | P0 | ✅已修 | 单 SQL 批量 + idx_bi_status_expire |
| P0-3 audit 索引缺失 | P0 | ✅已修 | idx_al_operator_seq + 游标分页 + 5 万导出上限；16 并发 p95=239ms 实测 |
| P0-4 listByRdPm 缺索引不分页 | P0 | ✅**本轮已修** | idx_br_rd_pm 已 apply+校验（两列序 1/2）；listByRdPm/listResponses 补 LIMIT 500。注：listByRdPm 零生产 caller（仅测试） |
| P1-1 ReceiptLedger 分页 | P1 | ✅**R19 已修** | in_window SQL 切换已落地（.apply("in_window = 1")），Java 循环废；uk_receipt_project_month 已封顶 + 真库列已验存在 |
| P1-2 listResponses 不分页 | P1 | ✅**本轮已修** | LIMIT 500（生产路径 BidController L151） |
| P1-3 prod 池/p6spy | P1 | ✅已修 | p6spy false / 池 80 / timeout 5000 |
| P1-4 append 2 SQL+竞态 | P1 | ✅已修 | 锚行原子分配；100 并发 3948 行零断链 |
| P1-5 bootstrap 75+ SQL | P1 | ✅已修 | 2 次 insertBatch，80→3 SQL |
| P1-6 Coef/LDC 4 SQL | P1 | ⛔仍在 | audit 合并框架（Wave3 Batch-5 规划）未落地，低优先 |
| P2-1 升级串行 | P2 | ✅已修 | 随 P0-1 |
| P2-2 达成率内存过滤 | P2 | ✅**R19 已修** | listByProject 加 `.last("LIMIT 200")`，uk_receipt_project_month 约束每项目每月一行实测远小于上限 |
| P2-3 JVM 参数 | P2 | 🟡部分 | ZGC+HeapDump 已固定，Xms/Xmx 未显式 |
| P2-4 logback 无 profile | P2 | ✅**R19 已修** | logback-plus.xml → logback-spring.xml；springProfile 区分 prod 收紧 WARN、dev/local/test 保留 INFO；application.yml L57 切换 |
| P2-5 单 JVM 配置缓存 | P2 | ⛔仍在（知情搁置） | 注释明示单实例定位，多实例时升 Redis |
| QA05 P1-1 池 4× 放大 | U0 | ✅已修 | 基座 20（4×20=80≤151）+ 预算推导注释；dev/prod 80 属知情运维约束 |
| QA05 P1-2 verifyChain 权威 | U1 | ✅已修 | 应用契约口径 + gaps 分列 |
| QA05 P1-3 5s 取舍披露 | U1 | 🟡部分 | log.md 已披露，README 增补未做 |
| QA06-1 毫秒/seq 弱化 | — | ✅已修 | secondMillis 归零 + seq 锚行分配，跳号根因消除 |
| QA06-2 hash_version=2 | — | ⛔仍在 | canonicalOf 不感知版本，62 行历史不可验 → **owner 裁决**（补 v2 实现 / 标记 LEGACY_UNVERIFIABLE 单列） |

### 1.3 关单处置

- **25 项已修复**：证据在位，本报告即为关单登记，源文档（SEC-AUD / PERF-AUD / Wave3 规格包）对应条目状态由本表覆盖。
- **3 项部分修复**：残留项列入 §5 遗留清单，不关单。
- **13 项仍在**：其中 3 项本轮已顺手修复（LOW-6 / P0-4 / P1-2）随本报告关单；其余 10 项分级：裁决类 3（HIGH-5 列型 / QA06-2 hash_version / MED-3 差分——后者涉及业务语义）、专项类 2（MED-2 rebuild 互斥 / P1-6 audit 合并）、卫生类 5（P1-1 in_window 切换 / P2-2 同根 / P2-4 logback / INFO-1 / INFO-3）。
- **1 项误报**（INFO-4）：登记为非缺陷。

## 2. 本轮代码修复明细（双仓）

### 2.1 前端 ruoyi-ipd-web（4 文件）

| 文件 | 改动 |
|---|---|
| `views/ipd/_shared/ipd-error-text.ts` | IPD_COMMON_CODE_TEXTS 补 16 键（40006 + 50003~50017，文案与 auth.ts 同源）；portal 域删 40010 幽灵码；ai_document 域删 404 死键 |
| `views/ipd/_shared/ipd-error-text.test.ts` | 同步 3 处断言 + 新增 2 个测试（幽灵码走兜底 / 补齐键命中） |
| `api/ipd/auth.test.ts` | CODES 数组删 40010，补 50003/50007/50012/50017 抽检 |

**用户影响**：修复前 kpi/contribution/negative-feedback/allowance/handover 7+ 页面命中 50003~50017 时只显示「KPI 加载失败」类泛化文案，精确原因（如「贡献度评定入口仅在 G5 上市后 90 天复盘阶段开放」）不达用户；修复后直接显示。50009/50010 两码因 DTO `@Pattern` 先行校验被 10001 顶掉暂不可达，仍随批补入保持表-枚举镜像完整。
**验证**：定向 2 文件 124 用例全绿（04:31）。

### 2.2 后端 ruoyi-ai（4 文件）

| 文件 | 改动 |
|---|---|
| `service/DeletionArchiveService.java` | listArchive 补 `@Transactional(readOnly = true)`（LOW-6） |
| `service/BidResponseService.java` | listByRdPm 补 `.last("LIMIT 500")` + 零生产 caller 注记（P0-4） |
| `service/BidInvitationService.java` | listResponses 补 `.last("LIMIT 500")`（P1-2，生产路径） |
| `docs/script/sql/update/2026-09-09-ipd-perf04-bid-response-rdpm-index.sql` | 幂等索引脚本（照 perf01 模式），**已 apply 至 ipd_dev 并校验**：idx_br_rd_pm(rd_pm_id, create_time) 两列 seq 1/2 |

**验证**：`mvn -o -q -pl ruoyi-modules/ruoyi-ipd test -Dtest=...` exit 0；surefire 铁证（04:33）：BidResponseServiceTest 12 / P231 10 / P232 16 / BidScanEscalation 6 = 44 用例 0 失败。

## 3. 错误码三表一致性（蜂群 D）

- **真缺口 0**：auth.ts 33 条与后端 33 失败码一一对应。
- **展示表缺口 16**（§2.1 已修）。
- **幽灵码 2**（已清）：40010（后端无枚举，P4-1.1 验收文档曾声称登记 QUERY_CODE_INVALID——该声明失实）；404（查表键错位永不命中的死键）。
- **语义分叉 3**：40002 **真分叉待裁决**（后端 DUAL_SIGN_INCOMPLETE vs 通用表/bid 域「条件已变更」——是后端在 bid 场景复用 40002 传条件变更语义，还是前端文案历史错位，需人工定归属）；90001、30001 属带注释的有意分叉，备查不动。
- **HTTP 兜底弱点**：403 家族固定「权限不足」对 20002/20003/50011 不贴切（当前被 code 表全覆盖遮蔽）；409/429 无状态级兜底。低优先，随 P2-3 i18n 迁移一并处理。

## 4. 审计 AOP 改造设计（蜂群 C → 独立文档）

见《审计AOP改造设计-20260909.md》。要点：81 处分布 44 文件三类模式；注解化上限约 35-45 点（40%），六类场景永久保留手写；零依赖成本（ruoyi-common-log 等 4 切面已在 classpath）；**切面必须同步调用 append**（禁仿 LogAspect 异步事件，否则吞 requireJson 校验信号且与验链时序冲突）；P0 预备步=抽 4 份 Controller 逐字相同 audit() 拷贝为公共组件（不引 AOP 先消重）。

## 5. 津贴口径裁决材料（✅已拍板落地：当月退出不发）

**问题**：当月中途退出项目的人，当月津贴发不发？

**owner 拍板（2026-09-09）**：「当月退出不发」。已落地：isMemberActiveInMonth 退出侧改为月末在岗口径（退出 ≥ 次月月初才计），与主流程 isNull(exitDate) 对齐，双口径并存消除；P333AcceptanceTest 5 用例断言同步改，20/20 绿（04:54）。加入侧「月初在岗才计」不变。登记见 log.md R18。

**两套口径历史（留档）**：

| 口径 | 代码位置 | 语义 | 谁在用 |
|---|---|---|---|
| A 严格 | AllowanceService.calculateMonthlyAllowance L71 `.isNull(exitDate)` | 只要记录了退出日期就排除——**当月退出当月不发** | 生产主流程（津贴计算真实路径） |
| B 宽松 | isMemberActiveInMonth（旧版）：退出 ≥ 月初 ⇒ 当月仍 active | **当月退出当月照发，次月才停** | P333AcceptanceTest 7 处（P3-3.3 AC-INC 验收契约——旧断言已随拍板改） |

**拍板影响面**：
- 若定 A（严格）：改 P333 测试契约 + isMemberActiveForMonth/isMemberActiveInMonth 标注废弃或改语义；历史账无回溯（发放走 ledger 幂等）。
- 若定 B（宽松）：主流程 L71 过滤改「exitDate 月末之后才算退出」口径（一行 wrapper 条件），测试不动；当月中途退出的 PM 当月仍领全额（无按天折算——现系统无折算模型）。
- 关联：B 口径即规格 P3-3.3「退出次月停发」的字面义；A 口径是实现的简化。**建议 B**（与验收契约一致，改动最小）。

**另外三个待裁决**（已在上文登记）：HIGH-5 config_value 列型（建议 text）；QA06-2 hash_version=2 历史行（补实现 vs 标记不可验）；40002 语义归属（改后端复用 vs 改前端文案）。

## 6. 图工程更新

- `图/audit-flow.dot`：78→81 处刷新 + 写端重构为三类模式 cluster + traceId 报障链路注记。
- `图/finding-state.dot`（新增）：42 项核查状态总览 + 本轮 6 项新修标注。

## 7. 遗留清单（刷新）

| 类别 | 项 | 状态 |
|---|---|---|
| 待裁决 3 | HIGH-5 列型 / QA06-2 v2 历史行 / 40002 归属 | §5，owner 一句话各定（津贴口径 R19 已结） |
| 专项 2 | MED-2 rebuild 锚行互斥 / P1-6 audit 合并框架 | 审计链核心，挂审计专项（与 AOP P3 批合并） |
| 卫生 5 | P1-1 in_window 一行切换 / P2-2 同根 / P2-4 logback profile / INFO-1 dev 密码占位 / INFO-3 rebuild 护栏 | ✅R19 全清（b3336d48） |
| 架构 3 | AOP 改造四批（见设计文档）/ P2-3 i18n 迁移 / P2-1→并入 AOP | 按设计文档排期 |
| 契约 1 | 403 兜底语义 / 409 无状态兜底 | 随 i18n 一并 |
| 既有 | @EnableScheduling 启用（OPS-04 卡依赖）/ 8 审计端点 HTTP 真活验收 / 5 台纯登记+kpi_record 接线轮 | 前轮登记不变 |

## 8. 提交

- 后端 `fix/gov-contract-20260909`：本报告 + AOP 设计 + 2 图 + 3 Java + 1 SQL（待 worktree 提交，见提交记录）。
- 前端 `fix/gov-contract-20260909`：3 文件（error-text + 2 测试）。
- 全部 local-only 不 push（AGENTS.md 守则）。
