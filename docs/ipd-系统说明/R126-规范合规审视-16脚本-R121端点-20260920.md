# R126 规范合规审视报告 — 16 脚本 + R121 端点

| 项 | 值 |
|---|---|
| 报告 ID | R126 |
| 审视范围 | R119 新 5 脚本 + R120 修 R25 4 脚本 + R25 自证 5 脚本 + R30+ 治理 2 脚本（共 16）+ R121 真活 E2E 5 端点 |
| 审视基线 | R126 后端规范基线（前一个 agent 出） |
| 审视日期 | 2026-09-20 |
| 审视模式 | 只读探针 + 落审视报告 |
| worktree | `/Users/mac/Documents/ruoyi-ai-wt-r126-audit` @ `5e77c3a8` |
| 审视者 | 规范合规审视智能体（QA 只读） |

---

## 一、5 维度审视矩阵

### 维度 1：脚本命名规范合规（V1）

| # | 脚本 | 来源 | 风格 | 备注 |
|---|---|---|---|---|
| 1 | `check-test-coverage-by-domain.sh` | R119 新 | kebab-case | ✓ 符合 |
| 2 | `check-rule-wiring.sh` | R119 新 | kebab-case | ✓ 符合 |
| 3 | `reconcile-multi-source.sh` | R119 新 | kebab-case | ✓ 符合 |
| 4 | `check-e2e-fe-be.sh` | R119 新 | kebab-case | ✓ 符合 |
| 5 | `check-commit-completeness.sh` | R119 新 | kebab-case | ✓ 符合 |
| 6 | `check_tenant_excludes_apply.sh` | R120 修 R25 | snake_case | ⚠️ 与 R119 不同 |
| 7 | `check_dynamic_loadable.sh` | R120 修 R25 | snake_case | ⚠️ 与 R119 不同 |
| 8 | `check_duplicate_ssot.sh` | R120 修 R25 | snake_case | ⚠️ 与 R119 不同 |
| 9 | `check_deletion_consistency.sh` | R120 修 R25 | snake_case | ⚠️ 与 R119 不同 |
| 10 | `check_cross_repo_contract.sh` | R25 自证 | snake_case | ⚠️ 与 R119 不同 |
| 11 | `check_entity_complete.sh` | R25 自证 | snake_case | ⚠️ 与 R119 不同 |
| 12 | `check_i18n_unused_keys.sh` | R25 自证 | snake_case | ⚠️ 与 R119 不同 |
| 13 | `check_doc_link.sh` | R25 自证 | snake_case | ⚠️ 与 R119 不同 |
| 14 | `scan_dead_code.sh` | R25 自证 | snake_case | ⚠️ 与 R119 不同 |
| 15 | `pre-commit-main-tree-block.sh` | R30+ 治理 | kebab-case | ✓ 符合 |
| 16 | `check-mirror-vs-board.py` | R30+ 治理 | kebab-case | ✓ 符合 |

**统计**：kebab-case 7 个（R119 新 5 + R30+ 治理 2）/ snake_case 9 个（R120 修 R25 4 + R25 自证 5）

**底座惯例**：kebab-case 占主体（11 个原有脚本）/ snake_case 历史沉淀（16 个）。新脚本应统一为 kebab-case。

### 维度 2：R121 真活 E2E 端点合规（V2）

| # | R121 端点 | 后端 Controller | 端点签名 | 漂移状态 |
|---|---|---|---|---|
| 1 | `/api/v1/projects/{id}` | `ProjectController` | `@GetMapping("/{id}")` L73 | ✅ 对齐 |
| 2 | `/api/v1/projects/{id}/stages` | `ProjectController` | **不存在** /stages 子端点 | ❌ **P0 漂移** |
| 3 | `/api/v1/kpi/rules` | `KpiRuleController` | **Controller 不存在** | ❌ **P0 漂移**（R121-A 已发现）|
| 4 | `/api/v1/persons/active` | `PersonsController` | **Controller 不存在**；PersonController 仅 resign/rehire/wecom/unbind | ❌ **P0 漂移** |
| 5 | `/api/v1/deletion-requests` | `DeletionRequestController` | `@RequestMapping("/api/v1/deletion-requests")` L38 | ✅ 对齐 |

**结论**：R121 真活 E2E 5 端点 **2 对齐 / 3 漂移**（60% 失败率）。`scripts/check-e2e-fe-be.sh` 必 404 失败。

### 维度 3：硬编码路径合规（V3）

| # | 脚本 | 绝对路径 | /tmp | /etc | cnf | ENV override |
|---|---|---|---|---|---|---|
| 1 | check-test-coverage-by-domain.sh | 0 | 0 | 0 | 0 | 全相对路径 |
| 2 | check-rule-wiring.sh | 0 | 0 | 0 | 1 | 相对 cnf |
| 3 | reconcile-multi-source.sh | 0 | 1（/tmp/r*-backup/） | 0 | 1 | 相对 cnf |
| 4 | check-e2e-fe-be.sh | 0 | 0 | 0 | 1 | 相对 cnf |
| 5 | check-commit-completeness.sh | 0 | 0 | 0 | 0 | 全相对路径 |
| 6 | check_tenant_excludes_apply.sh | **1** L25 | 1（/tmp/mysql_13306.sock） | 0 | 1 | ✓ BACKEND_ROOT |
| 7 | check_dynamic_loadable.sh | **1** L23 | 0 | 0 | 0 | ✓ BACKEND_ROOT |
| 8 | check_duplicate_ssot.sh | **1** L25 | 0 | 0 | 0 | ✓ BACKEND_ROOT |
| 9 | check_deletion_consistency.sh | **1** L22 | 0 | 0 | 0 | ✓ BACKEND_ROOT |
| 10 | check_cross_repo_contract.sh | **1** | 0 | 0 | 0 | 待补 |
| 11 | check_entity_complete.sh | **1** | 0 | 0 | 0 | 待补 |
| 12 | check_i18n_unused_keys.sh | **1** | 0 | 0 | 0 | 待补 |
| 13 | check_doc_link.sh | **1** | 0 | 0 | 0 | 待补 |
| 14 | scan_dead_code.sh | **1** | 0 | 0 | 0 | 待补 |
| 15 | pre-commit-main-tree-block.sh | 0 | 0 | 0 | 0 | 全相对路径 |
| 16 | check-mirror-vs-board.py | 0 | 0 | 0 | 0 | 全相对路径 |

**统计**：硬编码 `/Users/mac/Documents/ruoyi-ai` 默认值的脚本 9 个（R120 修 R25 4 + R25 自证 5），全相对路径脚本 7 个（R119 新 5 + R30+ 治理 2），cnf 路径硬编码 4 个（均为相对路径）。

### 维度 4：真库表名一致性（V4）

| 真库表 | 行数 | 引用脚本 | 备注 |
|---|---|---|---|
| `projects` | 29（active） | check-e2e-fe-be.sh | ✓ R121 端点 1 |
| `project_stages` | 246 | 无直接 SQL | ✓ 表存在但 /stages 端点不存在 |
| `persons` | 25 active / 27 all | check-e2e-fe-be.sh | ⚠️ /active 端点不存在 |
| `deletion_requests` | 28 | 隐含 | ✓ R121 端点 5 |
| `kpi_records` | 2 | 隐含 | ✓ |
| `kpi_rule_snapshots` | 0 | 隐含 | ⚠️ kpi_rules 表不存在 |
| `kpi_shared_confirms` | 4 | 隐含 | ✓ |
| `audit_log_chain_heads` | 1 | check-rule-wiring.sh | ⚠️ 表存在但**无 del_flag 列**（头表非实体） |
| `audit_logs` | 1556 | check-rule-wiring.sh | ✓ |
| `system_configs` | 55 | check-rule-wiring.sh | ✓ |
| `system_config_versions` | 86 | check-rule-wiring.sh | ✓ |
| `kpi_rules` | **不存在** | R121 端点 | ❌ R121-A 已确认 |

**关键确认**：`kpi_rules` 表 **不存在**（R121-A 评估结论有效）。

### 维度 5：运行态合规（V5）

| 项 | 状态 | 证据 |
|---|---|---|
| 后端 16039 LISTEN | ❌ 未监听 | `lsof -nP -iTCP:16039 -sTCP:LISTEN` exit=1 |
| 前端 15666 LISTEN | ✅ 监听中 | node 70554 LISTEN 15666 |
| Java RuoYi 进程 | ❌ 未启动 | ps aux 无 RuoYiAiApplication |
| 唯一 Java 进程 | ruoyi-snailai-server.jar @ 23306（ry-vue 库） | 非 ipd 后端 |

**结论**：`scripts/check-e2e-fe-be.sh` 即便端点全对齐，当前也无法跑通真活 E2E——后端 16039 未启动。**R121 真活 E2E 必失败 = 双 P0**（端点漂移 + 后端未启）。

---

## 二、违规清单（按优先级）

### P0（必须立即修，6 项）

| # | 违规项 | 影响 | 修复 owner |
|---|---|---|---|
| P0-1 | `/api/v1/kpi/rules` 端点缺失（KpiRuleController 不存在） | R121 E2E 必 404 | 后端 agent |
| P0-2 | `/api/v1/projects/{id}/stages` 端点缺失（ProjectController 无 /stages 子端点） | R121 E2E 必 404 | 后端 agent |
| P0-3 | `/api/v1/persons/active` 端点缺失（PersonController 无 /active 子端点） | R121 E2E 必 404 | 后端 agent |
| P0-4 | 后端 16039 端口未 LISTEN（Java RuoYiAiApplication 未启动） | R121 E2E 整体失败 | SRE / 后端 agent |
| P0-5 | `kpi_rules` 真库表不存在 | 即便新建 Controller 也无表可查 | 后端 agent + DBA |
| P0-6 | R121 E2E 5 端点 60% 漂移率（3/5 漂移） | 真活 E2E 整体不可信 | QA + 后端 agent |

### P1（应当修，4 项）

| # | 违规项 | 影响 | 修复 owner |
|---|---|---|---|
| P1-1 | 16 脚本命名风格不统一（kebab 7 + snake 9） | 违反底座惯例（主体 kebab-case） | QA |
| P1-2 | R120 修 R25 4 脚本硬编码 `/Users/mac/Documents/ruoyi-ai` 默认值 | 不可移植（虽支持 BACKEND_ROOT ENV override） | QA |
| P1-3 | R25 自证 5 脚本硬编码绝对路径 | 同上 | QA |
| P1-4 | R119 与 R120 路径处理风格不一致 | 新旧脚本混杂，团队认知负担 | QA |

### P2（可接受现状，2 项）

| # | 违规项 | 影响 | 接受理由 |
|---|---|---|---|
| P2-1 | 4 脚本硬编码 `.codex/ipd-dev/config/mysql-client.cnf` 相对路径 | 不可 ENV override | 共享配置非环境差异，可通过 cnf 内容切换 |
| P2-2 | `reconcile-multi-source.sh` 引用 `/tmp/r*-backup/` 通配符 | 假定 backup 落在 /tmp | 当前 SOP 约定 |

---

## 三、整改建议（最小变更方案）

### P0 整改

#### P0-1/2/3：补 3 端点（最小变更）

```java
// ProjectController.java（新增）
@GetMapping("/{id}/stages")
public R<List<ProjectStageVO>> getProjectStages(@PathVariable Long id) {
    return R.ok(projectStageService.listByProjectId(id));
}

// PersonController.java（新增）
@GetMapping("/active")
public R<List<PersonVO>> listActivePersons() {
    return R.ok(personService.listByAccountStatusNot("MOCK"));
}

// 新建 KpiRuleController.java
@RestController
@RequestMapping("/api/v1/kpi/rules")
public class KpiRuleController {
    @GetMapping
    public R<List<KpiRuleVO>> list() { ... }
}
```

#### P0-4：启后端 16039

```bash
cd /Users/mac/Documents/ruoyi-ai
nohup java -jar ruoyi-modules/ruoyi-ipd/target/ruoyi-ipd.jar \
  --spring.profiles.active=local \
  > logs/ipd-16039.log 2>&1 &
```

#### P0-5：Flyway 迁移 kpi_rules 表

```sql
-- V20260920__create_kpi_rules.sql
CREATE TABLE kpi_rules (
  id BIGINT PRIMARY KEY AUTO_INCREMENT,
  code VARCHAR(64) NOT NULL UNIQUE,
  name VARCHAR(128) NOT NULL,
  formula TEXT,
  del_flag CHAR(1) DEFAULT '0',
  create_by VARCHAR(64),
  create_time DATETIME DEFAULT CURRENT_TIMESTAMP,
  update_by VARCHAR(64),
  update_time DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP
);
```

#### P0-6：E2E 整改验收

```bash
bash scripts/check-e2e-fe-be.sh
# 预期：5/5 端点 HTTP 200 + 业务契约字段通过
```

### P1 整改

#### P1-1：脚本命名统一

**方案 A（推荐）**：snake_case → kebab-case 重命名
- `check_tenant_excludes_apply.sh` → `check-tenant-excludes-apply.sh`
- `check_dynamic_loadable.sh` → `check-dynamic-loadable.sh`
- `check_duplicate_ssot.sh` → `check-duplicate-ssot.sh`
- `check_deletion_consistency.sh` → `check-deletion-consistency.sh`
- `check_cross_repo_contract.sh` → `check-cross-repo-contract.sh`
- `check_entity_complete.sh` → `check-entity-complete.sh`
- `check_i18n_unused_keys.sh` → `check-i18n-unused-keys.sh`
- `check_doc_link.sh` → `check-doc-link.sh`
- `scan_dead_code.sh` → `scan-dead-code.sh`

**方案 B（接受现状）**：保留 snake_case，加 README 标注"历史沉淀命名"。

#### P1-2/3：硬编码路径整改

```bash
# 现有
BACKEND_ROOT="${BACKEND_ROOT:-/Users/mac/Documents/ruoyi-ai}"
# 改为
BACKEND_ROOT="${BACKEND_ROOT:-$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)}"
```

#### P1-4：路径处理风格统一

所有脚本统一用 `$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)` 自解析，或用 `${BACKEND_ROOT}` ENV override。

### P2 整改（接受现状）

P2-1/P2-2 当前不构成业务阻塞，文档化即可。

---

## 四、Owner 拍板项

| 拍板项 | 选项 | 建议 |
|---|---|---|
| P0-1/2/3 端点补齐时机 | A. 立即补 / B. E2E 改 Mock / C. 改 R121 端点为后端实际端点 | **A**：补齐端点是治本 |
| P0-4 后端启动 | A. SRE 启 16039 / B. QA 自启 / C. 改 E2E 跳过健康检查 | **A**：SRE 启动 |
| P0-5 kpi_rules 表 | A. 建新表 / B. 重用 kpi_rule_snapshots | **A**：建新表 |
| P1-1 脚本命名 | A. snake→kebab 重命名 / B. 接受现状加 README | **A**：与底座主体一致 |
| P1-2/3 硬编码路径 | A. 改自解析 / B. 接受现状 | **A**：自解析更通用 |

---

## 五、底座规范引用

- **脚本命名惯例**：kebab-case 占主体（11/16 = 69%），snake_case 为历史沉淀
- **路径规范**：底座脚本多用相对路径，硬编码绝对路径属例外
- **端点契约规范**：Controller 类名应与端点路径单复数一致（`/persons` → `PersonController` 违反约定，属历史漂移）
- **真库表名规范**：表名应与 Controller 资源名对齐（`kpi_rules` 缺失说明端点规划在表迁移之前，未遵守表优先 SSOT）

---

## 六、审视元数据

- worktree 分支：`fix/r126-audit`
- worktree commit：`5e77c3a8`
- worktree 路径：`/Users/mac/Documents/ruoyi-ai-wt-r126-audit`
- 探针脚本：5 维度共 ~30 条 read-only 命令
- 报告路径：`docs/ipd-系统说明/R126-规范合规审视-16脚本-R121端点-20260920.md`
- 主仓分支 commit hash：见主仓 `fix/r126-audit` 分支最新 commit

---

**报告完成**。P0 违规 6 项 / P1 违规 4 项 / P2 违规 2 项。等待 owner 拍板 P0/P1 整改优先级。
