# R212 全局项目闭环梳理与全量计划矩阵 V2（增量轮，2026-09-24）

> **写给谁来用**：owner 全局视图 / 主协调派单 / 兄弟会话对齐基线
>
> **定位**：本轮是 R211（同日已完成、经 ioedream-qa-gatekeeper 独立复核 12/12 PASS 零漂移）的**增量轮**，不重做 R211 已验证通过的产品域级清单，只在其**自我声明的三大限制**处补齐：①端点级逐条对账（R211 §七"271×字段级不在本轮展开"）②65 路由浏览器全量遍历（R211b 仅人工覆盖 14 页）③测试计划多数条目的"实际结果"回填。
>
> **撞车 0 严守**：本轮仅 `docs/ipd-系统说明/` 落档 + 只读对账脚本（git / grep / mysql SELECT / 无 token HTTP GET / playwright 只读遍历）；未动 Java/Vue/SQL/yml/真库写操作/端口/PID/看板卡 status；看板仅按撞号透明协议**新建 1 卡**（不翻任何兄弟在途卡）。
>
> **明细挂载**：L1 端点 / L2 数据表 / L3 路由 三条逐条矩阵见姊妹篇 [`R212-全量对账明细矩阵-20260924.md`](./R212-全量对账明细矩阵-20260924.md)（564 行，285 端点 + 160 表 + 66 路由全展开），复算脚本与原始 JSON/日志存档 `验收/R212-可见性-20260924/`。

---

## 〇、基线现查（全部 2026-09-24 fresh，R212 时间戳）+ 与 R211 diff

| 事实源 | R211 记录值 | R212 现查值 | 差异定性 |
|---|---|---|---|
| 后端 main HEAD | `e683c770` → R211c 时 `ebcfa3d6` | **`ebcfa3d6`**（origin/main 一致）| R211c 已合入，无新漂移 |
| 前端 main HEAD | `8144910` → R211b 修复 `59f0a85` | **`59f0a85`**（工作树新增本轮遍历脚本，未 commit）| 路由闸 P0 修复已入库 |
| 后端 Controller | 59 文件 | **59**（ruoyi-ipd 模块）+ **1**（ruoyi-admin）= 60 作用域文件 | 口径澄清：R211 记 59 为模块数，含 admin 追加 1 |
| 方法级端点注解 | "≥271（234 带路径 + 裸 ~37）" | **285 唯一端点**（234 带路径 + 48 裸注解 + 去前缀补 /api/v1）| 裸注解实测 48（R211 记 ~37 为抓取正则口径漏），见 P2-5 统一口径 |
| 后端 @Test / 测试类 | 2239 / 271 | **2239 / 271** | 全吻合 |
| 前端 .vue（views/ipd）| 66 | **66** | 全吻合 |
| 前端 api/ipd .ts | "68（31 业务 + 37 测试）" | **60（36 业务 + 24 测试）** | **R211 §〇 失真项 D6**：68 为历史快照，现查 60；导出函数门禁实测 `fe_calls_non_test=179` |
| 前端路由 path | 65 | **66 条解析条目 / 63 leaf**（含父级布局节点）| 口径澄清，见附录 L3 |
| 真库 ipd_dev@13306 | 160 表 | **160 表**（0 行 86 张：框架/系统/备份/test 47 + IPD 业务 39）| 全吻合，本轮 L2 逐表展开 |
| 后端服务 16039 | 在跑 | **UP** | 无变 |
| 前端服务 15666 | R211b 已起 | **UP** | 无变 |
| MySQL 13306 | 在跑 | **UP** | 无变 |
| **Redis 6379** | R211c 临时拉起 | **DOWN**（已再度下线）| R211c 临时实例已回收；实测后端登录链仍正常（见交付一 Redis 探针）|
| 看板 ruoyi-ai 项目 | 470 → R211c 476 | **476 → 本轮建 1 卡 → 477**（done 367 / todo 24 / inprogress 25 / inreview 4 / cancelled 57）| todo 23→24（新增 landed-scenarios 契约缺陷卡 `0ddd9f67`）|
| 未提交残留 | — | `验收/ddl-apply-check-result-20260923.json`（M，仅 checkedAt 时间戳一行）、`.qoder-cn/`（??）| ddl json 为脚本重跑产物；两者均**不裹入本轮 stage**（见收口声明）|

**一句话总判**：代码层功能覆盖较 R211 的"约 85%"**维持**——本轮逐条对账未翻出实现层倒退，反而坐实孤儿端点收敛趋势（115→98）与路由可达性（65 条 0 白屏 0 死链）。真实用户可见性的两大压制点中，①"前端未起"已于 R211b 解除、本轮 playwright 全遍历进一步量化为"62 干净 + 1 预期拦截 + 2 有错（其中仅 1 为真缺陷）"；②兄弟在途卡仍 25 张未收口。**R211 声明的三大限制本轮全部补齐。**

---

## 一、交付一：fresh 基线差异复核（R211 §六复算命令重跑）

R211 §六全部复算命令本轮重跑，逐项对照见 §〇 表。三处新增探针结论：

### 1.1 Redis 6379 缺失对后端 16039 运行态的实际影响（只读）
- 现查：6379 DOWN，16039 仍 UP 且无 token GET 关键端点返回 **401**（鉴权闸活），带 token 业务链正常（遍历脚本 HTTP 登录兜底成功、`/api/v1/projects` 返回真实数据 `code=0`）。
- **定性**：Redis 缓存为**非阻塞路径**——本次会话内后端不依赖 6379 即可提供完整 HTTP 服务与登录链。与 R205"C6 Redis 多实例切流 26h"待拍板项不冲突（那是生产高可用议题，非 dev 运行阻塞）。**不重启、不拉起 Redis**（撞车 0）。

### 1.2 五连 HTTP 探针（无 token GET，复算 R211）
- 全吻合：鉴权端点 `401 401 404 401 404` 序列与 R211 记录一致（404 为未映射路径预期）。

### 1.3 脏数据三探针（真库只读）
- `dirty=0` / `dead_path=0` / `mock=1`（persons 表 R33 异常 3 残留 Mock 前缀账号 1 条，即 R211 P1-3，仍在）——三值全吻合。

### 1.4 未提交残留归属判定
- `ddl-apply-check-result-20260923.json`（M）：diff 仅 `checkedAt` 时间戳一行，为 ddl 校验脚本重跑产物，**非兄弟会话业务在途**，但属他人领域，本轮**登记不裹挟**（不加 -A，stage 时显式排除）。
- `.qoder-cn/`（??）：IDE 缓存目录，不入库。

---

## 二、交付二：逐条级全量矩阵（本轮主增量 → 详见姊妹附录）

三条明细表全部落 [`R212-全量对账明细矩阵-20260924.md`](./R212-全量对账明细矩阵-20260924.md)，此处仅汇总与关键结论。

### 2.1 L1 端点级（285 条逐条，`验收/.../r212_l1.py` + `check-api-contract-fe-be.mjs`）

| 状态 | 数 | 说明 |
|---|---|---|
| ✅ 双契约通 | **173** | 前后端 path-variable 归一后完全对齐 |
| 🔴 BE 孤儿 | **96** | 后端有端点、前端无调用（含 hr-sync/person-sync 运维域，R211 P1-1 延续）|
| ⚠️ 字段错位 | **16**（矩阵端点行；门禁按去重路径计 **15**）| 前端 placeholder 命名与后端不一致（如后端 `{id}` vs 前端 `{documentId}`，能跑通、属命名漂移非缺失，R211 §3.4 已定性）|

- **与 0921 矩阵 diff**（同门禁脚本对 `7b45572b` worktree 复跑，孤儿集合精确差集）：后端孤儿 **115（0921 template）→ 98（0924）**；净**消亡 7 条**全部落在 `gate-elements` 域（`/manage`、`/{X}/archive|copy|duplicate|publish|restore|revert`——该域已接前端）；**0 条新增孤儿**（0921 以来方法注解 +13 条均已双契约通）。趋势印证 R211"孤儿收敛"判断。
- 门禁与自制脚本仅剩 2 条口径差（`GET /api/v1/ai-copilot`、`GET /api/v1/resource` 裸注解归前缀），已核实为工具口径非真实差异。

### 2.2 L2 数据表级（160 表逐表，`r212_l2.py` 真库只读）

- 逐表列：表名 × 行数（0 行精确 COUNT 复核）× Entity × Mapper × Service × Controller(域) × 前端域 × tenant 登记状态。
- **0 行表 86 张**：框架/系统/备份/test 类 47（flow_* 13、sj_* 24、sys_* 6、*_BACKUP_20260923 3、test_* 3 等，非业务缺口）；**IPD 业务相关零行表 39 张**，其中 R211 已点名的重点候选全覆盖（`contribution_versions`、`gate_waivers`、`kpi_functional_metrics`、`project_scores`、`receipt_ledgers`、`requirement_changes`、`requirement_pools`、`post_launch_reviews`、`product_retirements`、`multi_project_capacity_approvals`、`rd_replacement(s/approvals)` 等）→ 数据回填波次（R211 N5）仍是主要待办。
- **有表无 Entity（真 IPD 业务缺口 5 张）**：`gate_waivers`、`multi_project_capacity_approvals`、`product_retirements`、`rd_replacement_approvals`、`rd_replacements`——与 E2 豁免未交付、RD 替补缺口互证（表建了但无实体/服务，功能未落地）。
- **bonus_pools 外键完整性扫描**（R211c 卡 15d5e689 遗留待扫项，本轮只读 SELECT 补全）：19 池中 **1 悬空**（`project_id=9140004` 在 projects 表无记录），**复现且仅止于** R211c 已建卡的那一条；`bonus_allocations` 台账（列名实测 `bonus_pool_id`）对 19 池引用 **0 孤儿**——发放台账外键链健康。

### 2.3 L3 路由级（静态 66 条 + 浏览器全遍历 65 条，`r212_l3.py` + `r212-route-visibility-traverse.mjs`）

**静态全解析**：66 条路由条目、63 个带 component 的 leaf、**0 缺失 vue 文件**，每条挂 `meta.access` 权限码、菜单隐藏标志、api/ipd 依赖列（附录全表）。

**浏览器全量遍历**（playwright channel:chrome，dev 账号 ipd-admin，只读，前端 15666 实测）：

| 指标 | 值 |
|---|---|
| total | **65** |
| ✅ 干净 | **62** |
| ⚠️ 白屏 | **0** |
| ❌ 404/死链 | **0** |
| 🔒 预期拦截（/ipd/no-access 本身）| 1 |
| 🟡 有错 | **2** |

**2 条有错逐条定性（关键）**：
1. `/ipd/projects/:projectId/change/:changeId` → `404 /api/v1/requirement-changes/1`：遍历脚本兜底 `changeId=1`，而 `requirement_changes` 表 0 行（无真实种子）→ **非页面缺陷**（页面正确显示空/错误态），根因是脚本参数 + L2 已知空表；不建卡。
2. `/ipd/scenarios/landed-scenarios` → `400 /api/v1/scenarios/landed`：后端 `LandedScenarioController.list()` 的 `projectId` 为必填 `@RequestParam Long projectId`（无 `required=false`），前端 `landed-scenarios.vue:147` 无参调用 `listLandedScenarios()` → **真实前后端契约缺陷（P1）→ 已按撞号透明协议建卡 `0ddd9f67`**（建卡前查询看板无 landed/scenario 相关卡，独立 GET 回读核验 status=todo、看板总数 476→477）。

---

## 三、交付三：业务链路与一致性标注刷新（仅刷变化项）

复用 R211 §二 三大闭环描述，本轮逐条现查后**只刷新有差异处**：

| 链路/机制 | R211 标注 | R212 刷新 | 依据 |
|---|---|---|---|
| E1 双 PM 五节点签署链 | 🔴缺（无 `/api/collaboration`）| **仍 🔴** | 0924 grep 复核无增量 |
| E2 Gate 豁免 waiver | 🔴缺（无 waiver Controller）| **仍 🔴 + 新证** | L2 现查 `gate_waivers` 表存在但**无 Entity** → 表建功能未落地，缺口坐实 |
| E4 产品目录 Excel 导入 | 🔴缺 | **仍 🔴** | 0924 grep 无 `import-*`；`legacy_imports` 0 行 |
| 奖金池 distribute（闭环 2）| 🕐待验 → R211c 已验 | **已验证** + 挂 2 缺陷 | R211c 三证（卡 d9bd1477 done）+ 2 缺陷卡 `15d5e689`（distribute 无贡献度必 500 + 池悬空外键）|
| bonus_pools 外键完整性（15d5e689 待扫项）| 待扫 | **已扫**：19 池 1 悬空（=已建卡那条）/ 台账 0 孤儿 | L2 只读 SELECT 补全 R211c 声明的待办 |
| RD 替补 / 产品下线 / 多项目组队门禁 | 🟡在途 | **表在 Entity 缺**（L2：`rd_replacement*`、`product_retirements`、`multi_project_capacity_approvals` 5 表无 Entity）| 功能未落地的新硬证据 |
| 落地场景 landed（新发现）| 未遍历到 | **🔴契约缺陷 P1**：landed-scenarios 页进入即 400 | L3 遍历 + 前后端参数核对，建卡 `0ddd9f67` |
| 工作台 taskType 8→17（WB-17-1）| 第 4 轮在途 | **仍 inprogress**（卡 `e30c86a2`，兄弟在途只标注不越权）| 看板现查 status=inprogress |

**与 ZK-IPD 原型对账**：本轮未新增原型漂移（`ZK-IPD/开发说明/spec/` 49 页抽查未改动），沿用蜂群 D 矩阵 + `zk-ipd-裁决-执行矩阵-20260907.md`，无变化项。

---

## 四、交付四：测试计划执行状态回填（R211 §四逐条 → 实际结果五元组）

本轮实际补做的验证：**L3 全量路由遍历（≈完成 T-V1）**、**交付一 HTTP 探针复跑（T-I 部分）**、**bonus_pools 外键只读扫描（T-I4 延伸）**。后端单测不重跑全量（R28 基线全绿 + dev tag 过滤假红风险，遵 R211 §4.0 纪律），仅按需单模块错峰复验本轮发现涉及的测试类。

| # | R211 期望 | R212 实际结果 | 证据路径 | 阻塞/下一步 |
|---|---|---|---|---|
| **T-V1** | 65 路由逐条可达（脚本化待建）| **✅ 本轮建成并跑通**：65 条 / 62 干净 / 0 白屏 / 0 死链 / 1 预期拦截 / 2 有错（1 真缺陷已建卡）| `验收/R212-可见性-20260924/traverse-result.json` + 前端仓 `scripts/r212-route-visibility-traverse.mjs` | 已补齐 R211b 只人工 14 页的缺口；4 角色矩阵（T-V2）仍待 |
| T-V2 | 菜单按角色显隐 4 视角矩阵 | 🕐 未执行（本轮单账号 ipd-admin）| — | 入口：`R212_USER=<role> node scripts/r212-route-visibility-traverse.mjs` 扩 4 角色 |
| T-V4 | 27 张 0 行表对应页空态 | 🟡 部分：L2 现查 IPD 业务零行 39 张；遍历中 landed-scenarios 空态调用触 400（真缺陷）| 附录 L2 + traverse-result | 建卡 `0ddd9f67`；其余空态页 62 干净已覆盖 |
| T-V5 | 工作台待办数字 = DB COUNT | 🕐 未执行（双证对照需专门造数）| — | 待派 |
| **T-I1~I3** | 登录/me/需求/项目/阶段链 | 🟡 复算探针（无 token GET `401 401 404 401 404` 全吻合）+ 遍历中登录链实测正常（`/api/v1/projects code=0` 返真实数据）| 交付一 §1.2 | 全链三证需隔离库造数（§4.5），本轮不污染 ipd_dev |
| **T-I4** | 激励链 distribute 全链 | ✅ distribute 已由 R211c 三证（卡 d9bd1477 done）；**bonus_pools 外键完整性本轮扫描：19 池 1 悬空 / 台账 0 孤儿** | 附录 L2 外键扫描 + 卡 `15d5e689` | 悬空池 + distribute 无贡献度 500 两缺陷待裁决修 |
| T-B1~B5 | Controller 契约/状态机/KPI 边界/审批链/审计链单测 | 🕐 未重跑（R28 基线 2104 全绿 + 假红风险，本轮无代码改动不需复验）| R211 §4.1 命令在位 | 按需单模块错峰 |
| T-F1~F4 | 前端三件套/api 包络/权限/drift 自证 | 🕐 未重跑（无 FE 源码改动）| — | drift-guard 门禁在位 |
| E2E 五路径 | 需求→上市 / 绩效季 / 负反馈 / 人员异动 / 删除治理 | 🕐 未执行（需 `ipd_qa04` 隔离库 + 全链 playwright，重跑成本高）| — | 进交付五 M4 门禁 |

**回填净结论**：R211 §四多数"无实际结果"条目中，T-V1 本轮从"待建脚本"→**建成并跑通**（最大增量），T-I4 补全外键扫描，其余为"未执行 + 已给入口命令"如实登记（不假绿）。

---

## 五、交付五：全量待办计划矩阵刷新（R211 W/C/N 全量 → R212 状态）

优先级 P0>P1>P2>P3；类型：〔AI〕可推 worktree /〔裁〕owner 拍板 /〔DBA〕〔FE〕〔DevOps〕外部 /〔验〕实测。

### 5.1 立即窗口 W1~W4 刷新

| # | R211 | R212 状态 | 依据 |
|---|---|---|---|
| W1 | 奖金池 distribute 全链真活 | ✅ **已完成**（R211c 三证，卡 done）+ 抓出 2 缺陷 | L2 外键扫描补全 15d5e689 待扫项 |
| W2 | T-I1~I3 集成链复跑扩面 | 🟡 **部分**（无 token 探针复算全吻合 + 登录链实测；全链三证需隔离库）| 交付四 |
| W3 | 起前端跑 T-V1/V2 可见性遍历 | ✅ **T-V1 基本达成**（65 路由全遍历）/ 🕐 T-V2 4 角色矩阵未做 | traverse-result.json |
| W4 | WB-17-1 taskType 8→17 收口 | 🕐 **仍兄弟在途**（卡 `e30c86a2` inprogress，只标注不越权）| 看板现查 |

### 5.2 未拍板残余 C1~C9 与 5.4 缺口 N1~N7

全部**保持待拍板**，本轮无越权代为决策。R212 增量：
- **N1（E1/E2/E4 业务裁决）**：L2 新证 `gate_waivers` 表建但无 Entity，E2 缺口从"无 Controller"升级为"无 Entity/Service"，佐证交付/降级裁决依据更充分。
- **N5（27 张 0 行表回填）**：L2 精确化为 **IPD 业务零行 39 张**（含框架后共 86），回填波次范围据此刷新。

### 5.3 R212 本轮新发现（已建卡 / 待建）

| # | 项 | 类型 | 状态 | 验收 |
|---|---|---|---|---|
| **R212-N1** | landed-scenarios 页进入即 400（`GET /scenarios/landed` projectId 后端必填 vs 前端无参）| 〔裁→FE〕| **已建卡 `0ddd9f67`（todo，GET 回读核验）**| 修法 A 前端带 projectId / B 后端 `required=false`，二选一后页面无 400 |
| R212-N2 | 5 张 IPD 表建了无 Entity（gate_waivers/multi_project_capacity_approvals/product_retirements/rd_replacement*）| 〔裁〕并入 N1/N4/N7 | 登记（不重复建卡，归 R211 缺口卡）| Entity+Service+端点+页四件套或明确降级 |
| R212-D6 | R211 §〇 "api/ipd 68（31+37）"失真 → 现查 60（36+24）| 〔AI docs〕| 勘误级，本篇 §〇 已刷新（R211 本体不动，待下轮勘误批）| AGENTS/R211 数字对齐现查 |

### 5.4 里程碑排序（R212 刷新）

```
M1（本周·验证窗口）：W3 剩余 T-V2 四角色矩阵 + R212-N1 landed 缺陷裁决修 + C1 翻卡
M2（两周内·拍板集中批）：N1（E1/E2/E4 + R212-N2 五表无 Entity 并入）/N2 + C2/C8/C9 + 5.2 汇总卡处置
M3（月内·外部窗口）：C4/C5/C6（Redis 高可用，非 dev 阻塞）/C7 + 字符集 + N5 数据回填波次（39 张）
M4（发布门禁）：§4.5 五路径 E2E 全绿 + 九门禁 PASS + 0 未解释失真 → 生产就绪复评
```

---

## 六、证据索引与复算命令（任何人可重做）

```bash
# L1 端点级（门禁权威 + 自制脚本）
node scripts/check-api-contract-fe-be.mjs --json            # 0924: 0 孤儿路径/98 孤儿端点/15 字段错位
python3 验收/R212-可见性-20260924/r212_l1.py                  # 285 端点逐条 → /tmp/r212_l1_table.md
# 与 0921 diff（worktree 复跑同门禁对 7b45572b + 前端 d72a62d）
git worktree add -f /tmp/be0921 7b45572b && git -C ../ruoyi-ipd-web worktree add -f /tmp/fe0921 d72a62d
IPD_FE_API_DIR=/tmp/fe0921/apps/web-antd/src/api/ipd node /tmp/be0921/scripts/check-api-contract-fe-be.mjs --json

# L2 数据表级（真库只读）
python3 验收/R212-可见性-20260924/r212_l2.py                  # 160 表 + 外键扫描
# 基线复算
ls ruoyi-modules/ruoyi-ipd/src/main/java/org/ruoyi/ipd/controller/*.java | wc -l   # 59
grep -r '@Test' ruoyi-modules/ruoyi-ipd/src/test --include='*.java' | wc -l          # 2239
find apps/web-antd/src/views/ipd -name '*.vue' | wc -l                                # 66 (前端仓)
ls apps/web-antd/src/api/ipd/*.ts | grep -vc '.test.ts'                                # 36 业务
# 端口
for p in 16039 15666 13306 6379; do nc -z 127.0.0.1 $p && echo "$p UP" || echo "$p DOWN"; done
# 看板
curl -s "http://127.0.0.1:62250/api/tasks?project_id=01dcf15c-86bb-4c7b-957c-8fe44bddd10d" | python3 -c "import json,sys,collections;d=json.load(sys.stdin);t=d.get('data',d)if isinstance(d,dict)else d;print(len(t),collections.Counter(x['status']for x in t))"  # 477
# L3 遍历（前端仓，只读）
R212_USER=ipd-admin R212_PASS=<dev-accounts.yaml> R212_PROJECT_ID=2098349974990389250 R212_PRODUCT_ID=2101316086891827201 node scripts/r212-route-visibility-traverse.mjs
```

---

## 七、状态、限制与撞车 0 声明

- **本轮完成**：交付一 fresh 差异复核（含 Redis 探针 + ddl 残留归属）✅ / 交付二 L1+L2+L3 三条逐条矩阵（564 行附录）✅ / 交付三业务链路刷新 ✅ / 交付四测试回填（T-V1 建成跑通）✅ / 交付五待办矩阵刷新 + 看板建 1 卡回读 ✅。
- **R211 声明的三大限制全部补齐**：端点级逐条（285）、路由全遍历（65）、测试结果回填。
- **仍未做（不得假绿）**：T-V2 四角色矩阵、全链 E2E 三证（需隔离库造数）、后端/前端单测重跑（无源码改动不需复验）。
- **撞车 0**：仅动 `docs/ipd-系统说明/`（本轮新增 2 个 md + 1 个验收目录）+ 前端仓新增 1 个只读遍历脚本；真库零写；端口/PID 零触碰；看板仅新建 1 卡（`0ddd9f67`）不翻任何兄弟卡；`ddl-apply-check-result-20260923.json`（M）与 `.qoder-cn/`（??）兄弟/IDE 在途文件**不裹入 stage**。

