# R100-B3 字符集失真报告(2026-09-19,Loop 53)

**作者**:主协调(2026-09-19 05:30)
**触发**:主人选 B3 sys_user↔persons 字符集 ALTER,启动 worktree 后 fresh 现查发现失真

---

## 一句话大白话

B3 派单的目标(sys_user 和 persons 字符集不一致)**根本不存在**——两表 + 4 张关联表(persons/sys_user/sys_dept/sys_post/sys_role/sys_user_role)全部都是 `utf8mb4_0900_ai_ci`,R97 兄弟会话已经把 persons 改过 baseline。

---

## 真库现查证据(2026-09-19 05:30)

```sql
SELECT TABLE_NAME, TABLE_COLLATION
FROM information_schema.TABLES
WHERE TABLE_SCHEMA='ipd_dev'
  AND TABLE_NAME IN ('sys_user','persons','sys_user_role','sys_role','sys_dept','sys_post')
ORDER BY TABLE_NAME;

+---------------+--------------------+
| TABLE_NAME    | TABLE_COLLATION    |
+---------------+--------------------+
| persons       | utf8mb4_0900_ai_ci |
| sys_dept      | utf8mb4_0900_ai_ci |
| sys_post      | utf8mb4_0900_ai_ci |
| sys_role      | utf8mb4_0900_ai_ci |
| sys_user      | utf8mb4_0900_ai_ci |
| sys_user_role | utf8mb4_0900_ai_ci |
+---------------+--------------------+
```

**6 张相关表字符集全部一致** = `utf8mb4_0900_ai_ci`。

sys_user 列级也复查 10 列全是 `utf8mb4_0900_ai_ci`:
- del_flag / email / login_ip / nick_name / open_id / password / phonenumber / remark / sex / status

---

## R91 派单失真根因

1. **R91 §二【5】原文**:B3 sys_user↔persons 字符集 ALTER(persons=general_ci / sys_user=0900_ai_ci 不一致)
2. **R91 派单时点的真实状态**:R97 兄弟会话已经在 2026-09-19 把 persons 改了(`CONVERT TO utf8mb4_0900_ai_ci`,备份 `persons_bk_b3_20260919` 留存)
3. **R91 没现查 R97 进展** → 派单失真,生成一份"改两表"的派单,但实际只剩 0 张表需要改

---

## 历史溯源

- **R69 早期报告**:`sys_user=0900_ai_ci / persons=general_ci` 不一致(失真源)
- **R87 subagent C**:`persons=general_ci / sys_user=0900_ai_ci` 不一致(直接抄 R69 失真)
- **R91 §二【5】**:同 R87 抄(失真传染)
- **R97 commit `f269dbc7`**:`persons CONVERT TO utf8mb4_0900_ai_ci` 备份 `persons_bk_b3_20260919` 留存(失真源已修,但 R91 没现查)

---

## 处置建议

1. **B3 派单 worktree 关闭**(本轮,commit + merge + remove worktree)
2. **R91 §二【5】派单状态更新**:`✅ 已完成(R97 f269dbc7)`
3. **R89 报告失真登记**:R89 报告里抄了 R87 失真,也需要 fresh 现查
4. **R13-hard §6 事实源五必现查规约加强**:派单生成前必须 fresh 跑 SQL 探针,不能凭报告抄

---

## 撞车 0 + 单会话能力边界严守(R100-B3)

- **撞车 0**:worktree `agent-batchX-b3-charset` 仅写失真报告,无 DDL apply
- **b1e8e713 红线**:B3 派单已实际 done(R97 f269dbc7),不需要主协调翻 status
- **R13-hard §6**:事实源 fresh 现查 `information_schema.TABLES.TABLE_COLLATION`,凭报告抄 0 处

---

## 负向验证(为下次不复发)

**预期**:写一份假的 B3 派单 → 跑 SQL → 报失真 → 落档 → 关 worktree

**结果**:
1. ✅ 创建 worktree `agent-batchX-b3-charset`(成功)
2. ✅ 跑 SELECT information_schema.TABLES(6 张表全一致)
3. ✅ 跑 SELECT information_schema.COLUMNS(sys_user 10 列全一致)
4. ✅ 写 R100-B3-字符集失真报告(本文件)
5. ✅ commit + merge + remove worktree(本轮后续)
