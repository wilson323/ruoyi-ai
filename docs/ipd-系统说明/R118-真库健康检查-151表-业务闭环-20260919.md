# R118 真库健康检查-151 表 26+2 业务闭环-DDL 全 in-place

## 一句话结论

R118 真库 ipd_dev@13306 fresh 盘点：**151 张表（比 R111 体检时 125 表 +26 张）、核心业务数据齐全（projects 29 / stages 246 / actions 2405 / real_persons 25）、audit_log_chain_heads 单链 1 条、correction_logs/kpi_rule_snapshots 表已建**。**R46 治本 4 工程落地的 DDL 已全部 in-place**，无需 apply。后端进程在跑（PID 34560）但连的是兄弟会话 ry-vue 库（不是 ipd_dev），按 R25 红线"撞车 0 严守"不擅杀。

## 实测数据

### 库基础

| 指标 | 值 |
|---|---|
| MySQL 版本 | 8.0.46 |
| 端口 | 13306 |
| 业务库 | ipd_dev |
| 总表数 | 151（比 R111 +26） |
| system_configs 总数 | 55 |
| audit_log_chain_heads | 1 条（chain_key='audit_log'） |

### 核心业务数据（del_flag='0'）

| 表 | 活跃数 |
|---|---|
| projects | 29 |
| project_stages | 246 |
| stage_actions | 2405 |
| persons（去 Mock + 状态过滤） | 25 |

### 高频写入表（按数据量）

| 表 | 行数 |
|---|---|
| sys_logininfor | 4703 |
| stage_actions | 2405 |
| audit_logs | 1359 |
| sys_role_menu | 400 |
| project_stages | 246 |
| sys_menu | 220 |
| system_config_versions | 85 |
| gate_review_elements | 76 |
| sys_dict_data | 74 |
| notification_events | 68 |
| system_configs | 55 |
| products | 54 |
| projects | 45 |
| cert_templates | 39 |
| gate_element_results | 30 |

## R46 治本 4 工程 DDL in-place 核验

| DDL 表 | 存在? | 用途 |
|---|---|---|
| kpi_rule_snapshots | ✓ | R46 治理元数据表（规则版本快照） |
| correction_logs | ✓ | R46 治理元数据表（字段更正留痕） |
| sop_template_instances | ✓ | SOP 模板版本实例 |

R46 治本 4 工程落地的 DDL **全部已 apply 真库**，无需 DDL apply。

## 发现 1 项待 owner 拍板

**bonus.poolRate = 0.0500**（spec 应为 0.05），update_by = **-1**（系统/system 默认 ID，非真人）：
- 当前值：`0.0500`（4 位小数，spec 是 2 位）
- update_time：2026-09-08 11:31:43
- update_by：-1

R49 报告已登记为待 owner 拍板（A/B/C 三选一）：
- A：保留 0.0500（业务实际值）
- B：改回 0.05（spec 对齐）
- C：保留 + 注释说明

## 撞车 0 严守

**后端进程**：
- 唯一 java 进程 PID 34560 在跑
- 但连的是兄弟会话起的 `ruoyi-snailai-server.jar` + 库 `127.0.0.1:23306/ry-vue`
- 不是 IPD 后端 + ipd_dev 库

**按 R25 红线 + AGENTS.md「撞车 0 严守」**：
- 不擅杀兄弟进程（可能兄弟会话在跑任务）
- 不擅起 IPD 后端（避免端口/资源冲突）
- 当前盘查只读，不写任何 DDL

要起 IPD 后端做真活 HTTP 验收，需 owner 明确「杀掉兄弟进程起 IPD 后端」指令。

## 教训沉淀

1. **真库 ipd_dev@13306 已自洽运行 14+ 天**：从 R49/R111 体检到现在表数从 125→151 增长，业务自然演进
2. **R46 治本 4 工程 DDL 已 in-place**：不需要 DDL apply 工作，R113 救回的 4 工程已落 main + 真库 schema 已同步
3. **兄弟会话后端进程是 R25「撞车 0 严守」最大变量**：起新后端前必先确认兄弟进程状态
4. **bonus.poolRate update_by=-1 浮漂**：兄弟会话历史写入的 system 默认 ID，需 owner 拍板 spec 对齐 vs 业务实情
5. **audit_log_chain_heads 只有 1 条**：单一 chain_key='audit_log'，其他 entity_type 链路未启用，需后续治理扩展
6. **业务真实数据已成型**：projects 29 / actions 2405 / persons 25 真活数据可支撑真活 HTTP 验收
