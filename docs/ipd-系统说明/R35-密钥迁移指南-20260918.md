# R35 密钥迁移指南 — 2026-09-18

**承接**:R34 系统性扫描报告 Pattern C + R34 大白话反思版 §6.2 任务 3
**owner 授权**:2026-09-18 loop 自动执行(主协调会话确认)
**基线 HEAD**:6b515ddb(main)

## 1. 范围

`ruoyi-admin/src/main/resources/application-prod.yml` 含 **16 处 justauth client-secret 真生产密钥**(1 处已被 `${SA_TOKEN_JWT_SECRET_KEY}` 替换)。本轮只交付**dry-run 工具**,不直接改 prod yml(撞车红线 + sensitive-field-guard 边界)。

## 2. 量化

| 类型 | 数量 | 处理方式 |
|---|---|---|
| client-secret(真密钥) | 16 | 本轮 dry-run,占位符化建议 |
| access-key-secret(占位 "您的accessKeySecret") | 2 | 不需迁移(已是占位) |
| jwt-secret-key(已 ${} 引用) | 1 | 已完成 |

## 3. 工具

`scripts/r35-migrate-prod-secrets.sh`(本轮 commit,R35 takeover 内)。

```bash
# dry-run,只看清单
bash scripts/r35-migrate-prod-secrets.sh

# 真写 .env.example.r35
bash scripts/r35-migrate-prod-secrets.sh --apply
```

## 4. 迁移步骤(owner 执行)

1. 跑 dry-run 确认 16 处平台清单正确
2. 加 `--apply` 写出 .env.example.r35
3. 把 .env.example.r35 重命名为 .env(本地开发用)
4. 在 .env 里把 ${YOUR_SECRET_HERE} 替换为真密钥(密钥不入版本库)
5. 修改 application-prod.yml,把每个 `client-secret: <明文>` 改为 `client-secret: ${JUSTAUTH_<PLATFORM>_CLIENT_SECRET}`
6. 重启应用,验证 justauth 第三方登录仍可用

## 5. 安全规约

- **禁止 commit 真密钥字面量**(sensitive-field-guard 已拦)
- **禁止改 application-prod.yml 直接写明文**(只允许改占位符)
- **禁止把 .env 提交**(已在 .gitignore)
- **生产环境密钥通过 CI/CD secret 注入,不落 .env 文件**

## 6. 三证律

- **DB 实证**:不涉及 DB(只配置文件)
- **HTTP**:owner 改完后,跑一次 justauth 登录验证
- **SQL**:不涉及 SQL

## 7. 留待 R36

1. owner 实际跑 --apply 并 review 清单
2. owner 修改 application-prod.yml 占位符化
3. 接入 CI 门禁:`scripts/check-prod-secrets-inlined.sh` 扫 prod yml 内是否还有明文密钥
4. R35 merge gate 加第 8 项:扫描 prod yml 内 client-secret 关键字面量