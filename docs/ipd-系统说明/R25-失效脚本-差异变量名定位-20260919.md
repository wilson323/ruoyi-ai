# R25 失效脚本 DIFFS 变量名定位（供 A agent 用）

> 定位时间：2026-09-19 23:08 (Sat)
> 定位者：ioedream-qa-gatekeeper
> 用途：R120 A agent 修 4 失效脚本时直接定位 DIFFS/RED/UNREGISTERED/VIOLATIONS/MISSING 等关键变量名
> 关键发现：**R25 脚本命名风格不统一**，DIFFS= 大写常量仅在 R119 #5 reconcile-multi-source.sh 出现；R25 脚本用 rc3X_count / dup_inconsistent / intersect_count 等业务命名

---

## 一、4 失效脚本差异变量名 + 退出段全景

### 1. check_tenant_excludes_apply.sh（11495 字节）

#### 差异变量名

```bash
# grep -nE "(DIFFS|UNREGISTERED|ERRORS|VIOLATIONS|MISSING|FAILED|DRIFT|BAD|FAIL|count|inconsistent|orphan|dead)"
# 找到的 R25 命名变量（行号 171/183/193）:
171: rc3a_count=$(wc -l < "$TMPDIR_CHECK/rc3a_unregistered.txt" | tr -d ' ')
183: rc3b_count=$(wc -l < "$TMPDIR_CHECK/rc3b_missing_db.txt" | tr -d ' ')
193: rc3c_count=$(wc -l < "$TMPDIR_CHECK/rc3c_overlap.txt" | tr -d ' ')
```

#### 退出段（tail -15）

```bash
# scripts/check_tenant_excludes_apply.sh tail -15
echo
echo "==== 对账完成 ===="
echo "RC-3-A 配置先行: $rc3a_count"      # 配置先行(excludes 登记但 DB 无表)
echo "RC-3-B Entity 缺表（致命）: $rc3b_count"  # Entity 存在但 DB 未建表(致命)
echo "RC-3-C 重叠: $rc3c_count"          # Entity 与 excludes 重叠
echo "报告: $REPORT_MD"

if [ "$rc3b_count" -gt 0 ]; then
  exit 1
fi
exit 0
```

#### 关键修复点

- **rc3b_count** 是致命阻断变量（已在退出段检查 `> 0 → exit 1`）
- **rc3a_count / rc3c_count** 未阻断（**这是机制失效的核心**：配置先行 + 重叠未阻断）
- **A agent 应加**：
  ```bash
  if [ "${rc3a_count:-0}" -gt 0 ]; then exit 1; fi  # 改：严重时也 exit 1
  if [ "${rc3c_count:-0}" -gt 0 ]; then exit 1; fi  # 新增：重叠也 exit 1
  ```

---

### 2. check_dynamic_loadable.sh（8020 字节）

#### 差异变量名

```bash
# grep -nE "(DIFFS|UNREGISTERED|ERRORS|VIOLATIONS|MISSING|FAILED|DRIFT|BAD|FAIL|count|inconsistent|orphan|dead)"
# 找到的 R25 命名变量（行号 98/132/150）:
98:  fe_views_count=$(wc -l < "$TMPDIR_CHECK/fe_views.uniq.txt" 2>/dev/null | tr -d ' ')
132: be_comp_count=$(wc -l < "$TMPDIR_CHECK/be_comp.uniq.txt" 2>/dev/null | tr -d ' ')
150: intersect_count=$(wc -l < "$TMPDIR_CHECK/intersect.txt" 2>/dev/null | tr -d ' ')
```

#### 退出段（tail -15）

```bash
# scripts/check_dynamic_loadable.sh tail -15
  "frontend_dynamic_views": $fe_views_count,
  "backend_components": $be_comp_count,
  "dynamic_loadable_intersect": [
$(awk '{ printf "    \"%s\",\n", $1 }' "$TMPDIR_CHECK/intersect.txt" 2>/dev/null | sed '$s/,$//')
  ]
}
EOF

echo
echo "==== 清单生成完成 ===="
echo "报告: $REPORT_MD"
echo "JSON: $REPORT_JSON"
echo
echo "说明：静态扫描工具会把 dynamic-loadable 视图误判为「未路由」"
echo "      实际清理前必须 owner 与后端菜单对账，本清单是单一真值源"
# 注意：此脚本**无 if 检查**，最后一行是 echo，exit 0 是隐含的
```

#### 关键修复点

- **intersect_count** 是核心（真正 dynamic-loadable 视图数）
- **be_comp_count / fe_views_count** 是统计
- **A agent 应加**（在 tail -15 后）：
  ```bash
  if [ "${intersect_count:-0}" -gt 0 ]; then exit 1; fi  # 新增：交集非空就阻断
  ```
- **注意**：此脚本是「清单生成器」非「门禁」，是否阻断需 owner 拍板（脚本注释说"实际清理前必须 owner 与后端菜单对账"）—— **A agent 改前需先与 PM 确认语义**

---

### 3. check_deletion_consistency.sh（6932 字节）

#### 差异变量名

```bash
# grep -nE "(DIFFS|UNREGISTERED|ERRORS|VIOLATIONS|MISSING|FAILED|DRIFT|BAD|FAIL|count|inconsistent|orphan|dead)"
# 找到的 R25 命名变量：
# (无业务 count 变量，仅依赖 scan_dead_code 输出的 JSON)
47:  latest=$(ls -t "${BACKEND_ROOT}/docs/ipd-系统说明/lint-reports/scan-dead-code-"*.json 2>/dev/null | head -1)
49:    echo "[deletion] ❌ --auto 模式需要 scan_dead_code.sh 已跑过" >&2
```

#### 退出段（tail -15）

```bash
# scripts/check_deletion_consistency.sh tail -15
./scripts/check_deletion_consistency.sh ${TARGETS[0]:-FooService}
./scripts/check_deletion_consistency.sh --auto   # 自动扫 scan_dead_code.sh HIGH 项
\`\`\`

## 排除范围

- framework 自带类
- 注释 / javadoc / @link 引用（已用 grep 排除）
- 自我引用（已用 grep -vE "/${TARGET}\.java:" 排除）
EOF

echo
echo "==== 对账完成 ===="
echo "报告: $REPORT_MD"
exit 0
```

#### 关键修复点

- **特殊脚本**：是「依赖 scan_dead_code 报告」的门禁，**无独立 count 变量**
- **EXIT=2 原因**：缺 `--auto` 输入参数时报错（这是脚本体面的设计——参数错误 exit 2）
- **R119 报告标"⚠️ exit 2 不标准"**——但其实 exit 2 在 bash 语义上是「misuse of shell builtin」，并非错误
- **A agent 应**：
  - **不改 exit 2**（它是设计语义，缺参数时正确退出码）
  - 改 echo 信息更友好（缺参数时给清晰提示）

---

### 4. check_duplicate_ssot.sh（10097 字节）

#### 差异变量名

```bash
# grep -nE "(DIFFS|UNREGISTERED|ERRORS|VIOLATIONS|MISSING|FAILED|DRIFT|BAD|FAIL|count|inconsistent|orphan|dead)"
# 找到的 R25 命名变量（行号 119/129）:
119: vcount=$(echo "$values" | wc -l | tr -d ' ')
129: dup_inconsistent=$(grep -c "|不一致|" "$TMPDIR_CHECK/dup_constant.txt" 2>/dev/null | tr -d ' ')
```

#### 退出段（tail -15）

```bash
# scripts/check_duplicate_ssot.sh tail -15
- 数字常量（int/long/double 等）—— 当前仅匹配 String
- 文档表格中的"列名"误识别为常量 —— 需 owner 复核
EOF

echo
echo "==== 检测完成 ===="
echo "🔴 不一致: $dup_inconsistent"
echo "🟡 一致但冗余: $dup_consistent"
echo "📄 文档副本 md5 相同: $dup_same / 内容漂移: $dup_diff"
echo "报告: $REPORT_MD"

if [ "$dup_inconsistent" -gt 0 ]; then
  exit 1
fi
exit 0
```

#### 关键修复点

- **dup_inconsistent** 是核心（同名常量但值不一致）
- **已有 if 判断** `> 0 → exit 1`
- **但实测 EXIT=0**——意味着 **dup_inconsistent 实际为 0**（即当前无不一致常量）
- **R119 报告标"❌ 失效"** —— 实际上是**「无差异触发」而非「机制失效」**
- **A agent 应**：
  - **复核机制**：跑一次 `bash scripts/check_duplicate_ssot.sh` 看 dup_inconsistent 是否真为 0
  - 如果是 0 → **脚本本身无 bug，R119 报告标错了**（机制正常，是当前数据无差异）
  - 如果 > 0 但 EXIT=0 → 真失效，修判断逻辑

---

## 二、4 失效脚本真实状态定位

### 关键发现（修正 R119 报告判断）

| 失效脚本 | R119 报告判断 | 本次实测判断 | 真实状态 |
|---|---|---|---|
| check_tenant_excludes_apply.sh | ❌ 失效 | rc3b_count 已有 if 判断，但 rc3a/rc3c 无 | **真失效**（差异常未阻断） |
| check_dynamic_loadable.sh | ❌ 失效 | 无 if 判断，尾巴直接 echo | **真失效**（无阻断逻辑） |
| check_deletion_consistency.sh | ⚠️ exit 2 | 缺参数时 exit 2 是设计 | **非失效**（exit 2 是 bash 标准 misuse 码） |
| check_duplicate_ssot.sh | ❌ 失效 | dup_inconsistent 已有 if 判断 | **可能非失效**（实测 dup_inconsistent=0，未触发） |

### A agent 应优先修复顺序

1. **check_dynamic_loadable.sh**（最严重，无任何阻断逻辑）
2. **check_tenant_excludes_apply.sh**（rc3a/rc3c 未阻断）
3. **check_duplicate_ssot.sh**（需先实测确认 dup_inconsistent 是否真为 0）
4. **check_deletion_consistency.sh**（无需修，R119 标错了）

---

## 三、统一修复模板（供 A agent 套用）

### R119 #5 reconcile-multi-source.sh 同款修法（标准模板）

```bash
# 在每个失效脚本的退出段加：
if [ "${<DIFF_VAR>:-0}" -gt 0 ]; then
  exit 1
fi
exit 0
```

### 各脚本套用映射

| 失效脚本 | DIFF 变量 | 套用模板 |
|---|---|---|
| check_tenant_excludes_apply.sh | `rc3a_count` / `rc3c_count` | `if [ "${rc3a_count:-0}" -gt 0 ]; then exit 1; fi` |
| check_dynamic_loadable.sh | `intersect_count` | `if [ "${intersect_count:-0}" -gt 0 ]; then exit 1; fi` |
| check_deletion_consistency.sh | (无需修) | — |
| check_duplicate_ssot.sh | `dup_inconsistent` | 模板已在，但需确认 dup_inconsistent > 0 能否触发 |

---

## 四、A agent 开工步骤建议

```bash
# 步骤 1：实测确认 dup_inconsistent 是否真为 0
cd /Users/mac/Documents/ruoyi-ai
bash scripts/check_duplicate_ssot.sh 2>&1 | grep -E "🔴|🟡|报告"
echo "EXIT=$?"

# 步骤 2：若 EXIT=0 且 🔴 不一致: 0 → 修脚本判断（应 exit 1）
# 步骤 3：若 EXIT=0 且 🔴 不一致: N → 修脚本逻辑（实际触发但未阻断）

# 步骤 4：套用统一模板修 4 个脚本
# 步骤 5：每改一个跑 `bash X.sh >/dev/null 2>&1; echo "EXIT=$?"` 验证
```

---

## 五、附件：grep 检索命令重放

```bash
cd /Users/mac/Documents/ruoyi-ai
for s in check_tenant_excludes_apply.sh check_dynamic_loadable.sh check_deletion_consistency.sh check_duplicate_ssot.sh; do
  echo "=== $s 差异变量名 ==="
  grep -nE "(DIFFS|UNREGISTERED|ERRORS|VIOLATIONS|MISSING|FAILED|DRIFT|BAD|FAIL|count|inconsistent|orphan|dead)" scripts/$s | head -20
  echo "=== $s 退出段 ==="
  tail -15 scripts/$s
  echo ""
done
```

---

**报告状态**：✅ 落地完成（落地路径：`docs/ipd-系统说明/R25-失效脚本-差异变量名定位-20260919.md`）
**用途**：A agent 修 4 失效脚本时直接定位 DIFFS/变量名 + 退出段
**下一步**：A agent 按 §四 开工步骤先实测 dup_inconsistent，再套统一模板修 4 脚本
