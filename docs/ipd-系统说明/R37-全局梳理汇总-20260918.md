# R37 全局梳理报告 — 前后端 21 模块用户可见性 + 契约 + DB + 测试头 — 2026-09-18

> 三证律( HTTP + DB + 浏览器) + 蜂群并行 4 Agent 扫描 + 致命 P0 修复 + 三轮实测闭环

## 1. 结论一句话

> **后端 + 前端 21 IPD 视图模块全部可达,0 真 P0 致命缺陷;但根因层的 R25 病根 ①(测试假绿)+ ④(契约无门禁)+ ⑤(多事实源无对账)三个仍待门禁化。**

## 2. 全局量化(实测,非估算)

| 维度 | 数量 | 状态 |
|---|---|---|
| 后端模块 | 6(ruoyi-aiflow / chat / generator / ipd / system / workflow) | OK |
| 后端 IPD Controller | **51 个** | OK |
| 后端全局 Controller | 25+ | OK |
| 前端 .vue 文件 | 61 | 100% 可达(修复后) |
| 前端 IPD 视图子模块 | **21 个** | 100% HTTP 200 SPA fallback |
| 前端 api/ipd/*.ts | 56 | OK |
| DB 表(ipd_dev) | **150 张** | OK |
| DB 抽样失真表 | **8 张表名漂移** | R25 病根 ⑤ |
| 测试类总数 | **238 个** | OK |
| `@Tag("dev")` 测试类 | **238 个(100%)** | **假绿陷阱已确认**(R25 病根 ①) |
| 前端孤儿路径(生产) | **0** | OK |
| 前端字段名错位 | **10 处** | 维护性问题,非阻塞 |
| 后端孤儿端点 | **101 个**(前端未调) | 评估价值 |
| 文档失真点 | **5 处** | R25 病根 ⑤ |
| 前端 P0 致命 | **0**(修复后) | OK |
| 前端 P1(非阻塞) | **3 处** | 非阻塞,可后续打磨 |

## 3. 致命 P0 修复时间线

| 时间 | 事件 |
|---|---|
| 03:49 | Agent A R1 报告:前端 vite dev server PID 84577 STAT=TN 挂死 6h+,端口 LISTEN 但 HTTP 100% 不响应 |
| 03:50 | kill -9 84577 + esbuild 子进程 |
| 03:50 | 启动 vite,**关键修复**:`vite.js apps/web-antd --config apps/web-antd/vite.config.mts`(CLI 必须带 apps/web-antd 路径作为 root,否则 vite 找不到 index.html 返回 404) |
| 03:50 | PID 20215 起来了但 404(size 0)— root 没指向 apps/web-antd |
| 03:52 | **正确启动** PID 21436:nohup + disown + monorepo 根 cwd |
| 03:52 | HTTP 200 size 1039,21 个 /ipd/* 路由全部 200 |
| 03:55 | Agent A R2 报告:**0 P0 + 3 P1(非阻塞)** |
| 03:58 | 21/21 模块可达,UI 控件静态覆盖率 58/58 |

## 4. R25 五病根反证命中

| 病根 | R37 反证 | 门禁方案 |
|---|---|---|
| ① 改主代码后测试没跟上 | **238 测试类 100% @Tag("dev"),Surefire 默认 profile=local 跑 0 个 → BUILD SUCCESS 是假绿** | 门禁:`mvn -o -pl ruoyi-ipd test -P dev`(必须显式 -P dev) |
| ② 提交不完整(untracked 引用) | 本轮未触发 | 沿用 R5 check-submission.cjs |
| ③ 规则表靠人肉对账 | 文档失真 5 处 = 多对账点 | 门禁:`scripts/check-doc-drift.sh`(新) |
| ④ 前后端契约无门禁 | 字段名错位 10 处 + R34 platform-token(已修)+ IpdPlatformAuthController 在 ruoyi-admin 但包名是 ruoyi.ipd(模块边界不一致) | 门禁:`scripts/check-api-contract-fe-be.mjs`(新,扫描范围必须含 ruoyi-admin) |
| ⑤ 多事实源无对账 | 开发说明书 vs 实际表名漂移 8 张(`gate_elements` 实际是 `gate_review_elements` / `audit_log` 单复数错 / `demand` 实际是 `requirements` / `change_requests` 实际是三张独立表) | 门禁:`scripts/check-doc-db-drift.sh`(新,每条失真都对应一个事实源过期) |

## 5. 蜂群派单结果

| Agent | 范围 | 报告 |
|---|---|---|
| Agent A R1 | 前端 21 视图 HTTP 探针 + UI 元素静态 grep | `R37-前端21视图实测-20260918.md`(致命 P0 vite 挂死) |
| Agent A R2 | 前端 21 视图修复后重测(SPA fallback + Python AST 静态分析) | `R37-前端21视图实测R2-20260918.md`(0 P0 + 3 P1) |
| Agent B | API 契约 diff(前端 56 vs 后端 51 Controller) | `R37-API契约diff-20260918.md`(生产 0 P0 孤儿 + 10 字段错位 + 101 孤儿端点) |
| Agent C | DB schema 抽样 20 张 + 测试头验证 + 文档失真 5 处 | `R37-DB与测试头扫描-20260918.md`(238 测试类 100% @Tag dev + 8 表名漂移 + 5 处文档失真) |

## 6. 修复清单(后续轮次)

### R38 必修(门禁化,不修就一直假绿/失真)
1. **`scripts/check-api-contract-fe-be.mjs`** — 扫描前端 56 api/*.ts + 后端 ruoyi-modules/ruoyi-ipd/ + **ruoyi-admin/ 的 org.ruoyi.ipd.controller**(避免再漏报 R34 类问题)
2. **`scripts/check-doc-db-drift.sh`** — 对照 docs/开发说明 vs INFORMATION_SCHEMA.TABLES,固化表名白名单
3. **Surefire `<groups>` 治理** — pom.xml 的 `${profiles.active}` 默认值改 local,但应同时支持 `mvn -P dev test` 跑 @Tag("dev") 类,避免假绿
4. **`docs/wiki/wiki-lint.cjs` 加表名校验**

### R39 推荐(后续打磨)
1. P1-1:`/ipd/timeline/index.vue` 加 message.error catch
2. P1-2:`/ipd/change/index.vue` modal close + v-model 竞态修复
3. P1-3:`/ipd/portal/portal-shell.vue` 加 error boundary
4. 后端 101 个孤儿端点评估:dead code / 未对接业务(Bid admin/select)
5. 前端 10 处变量名错位 → 统一为 `{id}`(后端风格)

### R40+(架构层)
1. `IpdPlatformAuthController` 从 `ruoyi-admin` 移到 `ruoyi-ipd` 模块(包名一致性)
2. vite config 加 `root: 'apps/web-antd'` 显式声明,避免 CLI 参数丢失
3. vite 服务挂死监控(STAT=TN 检测,自动重启)

## 7. 三证律自检

- **HTTP**:21/21 模块 200,前端 + 后端各跑一遍
- **DB**:20 张表全 SELECT 抽样(零 DDL)
- **浏览器**:Agent A R2 因无 browser MCP 改用 curl + Python AST 降级;PID 21436 已起来 HTTP 200,**真活运行时白屏仍需 browser MCP 补齐**

## 8. 红线遵守

- ❌ 未跑 `mvn -am clean` / `mvn test` / `mvn install`(避假红/假绿)
- ❌ 未改 Java / SQL / yml / 文档失真源(只改 docs/ipd-系统说明/)
- ❌ 无 git 操作(后续 commit 待确认)
- ❌ 无 DROP/TRUNCATE 等破坏性 SQL
- ✅ 改 vite.config.mts?未改(用 CLI 参数绕开)
- ✅ kill PID 84577(死进程,无副作用)
- ✅ vite 用正确启动命令恢复(21436)

## 9. 关键 hash 落地

- main HEAD:`2a789abb`(R36 docs commit)
- 领先 origin/main:11 commit
- R37 汇总报告 commit:待追加
- vite 新 PID:**21436**(挂在 15666,monorepo 根 cwd,apps/web-antd root)

## 10. 阻塞项

1. **owner 拍板 R38 门禁范围** — 是否同步把 `check-api-contract-fe-be.mjs` 和 `check-doc-db-drift.sh` 加入 CI 门禁
2. **browser MCP 接入** — Agent A R2 用降级方案,真运行时白屏需补齐
3. **vite 重启监控** — STAT=TN 6h 才被发现,需自动重启守护

---

**签名**:R37 治理轮 · 闭环
**协作**:Agent A(×2) + Agent B + Agent C + 主会话 5 必查现查 + 蜂群派单