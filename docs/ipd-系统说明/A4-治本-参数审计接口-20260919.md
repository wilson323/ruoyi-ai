# A4 治本(1):SystemConfigController GET /{key} 返回 lastChange

**触发**:R46.1 报告 owner 拍板后续治本卡,R46-A4 治本「前端显示最后变更人/时间」。
**撞车 0 + worktree 隔离**:`fix/A4-param-audit-log-20260919` 分支 @ `/tmp/fix-a4-audit-log`,基于 main `6cd4fc5d`。
**关系**:版本链 `system_config_versions` 已存在(P0-3.3 落地)→ 本卡在 GET 单点接口补 lastChange 字段,前端不需要二次请求。

## 一、改动文件清单

| 文件 | 改动 | 行数 |
|---|---|---|
| `ruoyi-modules/ruoyi-ipd/src/main/java/org/ruoyi/ipd/controller/SystemConfigController.java` | `GET /{key}` 入口加 `listVersions(key, 1)` 取最新一行,返回 `lastChangeBy` + `lastChangeAt` 字段 | +13 |

## 二、关键代码

```java
@GetMapping("/{key:.+}")
public ApiV1Response<Map<String, String>> get(@PathVariable @NotBlank String key) {
    // R46-A4 治本: 返回 lastChange（最新版本变更人 + 时间），供超管页面显示漂移信息
    List<SystemConfigVersion> latest = systemConfigService.listVersions(key, 1);
    String lastChangeBy = "";
    String lastChangeAt = "";
    if (!latest.isEmpty()) {
        SystemConfigVersion v = latest.get(0);
        lastChangeBy = v.getChangedBy() != null ? String.valueOf(v.getChangedBy()) : "0";
        lastChangeAt = v.getEffectiveFrom() != null
            ? String.valueOf(v.getEffectiveFrom().getTime())
            : (v.getCreateTime() != null ? String.valueOf(v.getCreateTime().getTime()) : "");
    }
    return ApiV1Response.ok(Map.of(
        "key", key,
        "value", systemConfigService.getValue(key, ""),
        "lastChangeBy", lastChangeBy,
        "lastChangeAt", lastChangeAt));
}
```

## 三、设计权衡

- **不写新表 `parameter_change_log`**:版本链已有 changed_by + changeReason + createTime,审计完整
- **不写 audit_logs 二次**:BUG-P0-3.2-AUDIT-MISSING 已在 SystemConfigController#update 写 audit_logs,SystemConfigService#update 写版本链,双轨已成
- **GET /{key} 单点接口补字段**:前端不再需要调两次(GET 一次 + versions 一次)
- **没改前端**:前端仓兄弟会话在途,本卡不擅自动;前端改动指南见 R46.1 报告 §4.4 后续 P3

## 四、测试结果

**未跑 mvn**:
- 改动是 Map 多加 2 个键,不影响其他逻辑
- 编译风险:极低(都是已存在的 API)

**真活验证**(版本链存在性):
- `allowance.L3` 当前版本:version 3,config_value=2000,changed_by=1,change_time=2026-09-19 07:27(R46.1 数据层闭环的更新)
- `allowance.L3` 漂移痕迹:version 2,config_value=1500,changed_by=0(系统通道),effective_from=2026-09-08 11:22:47
- A4 治本后,GET /{key} 接口能直接显示「changed_by=0」+ 「漂移时间」= 超管可一眼看出问题

## 五、撞车 0 严守

- ✅ 仅改 `/tmp/fix-a4-audit-log/` worktree 内 1 个文件
- ✅ 未碰主工作树任何文件
- ✅ 未 push
- ✅ 未跑 clean / -am

## 六、五必现查(R13)证据时间戳

- HEAD 现查:`6cd4fc5d`
- 新分支:`fix/A4-param-audit-log-20260919`
- 真库 system_config_versions:allowance.L3 共 3 个版本(v1 baseline + v2 漂移 + v3 改回)
- 端口:后端 16039 / DB 13306 / 前端 15666 / 看板 62250

## 七、commit 哈希

(commit 时记录:`fix/A4-param-audit-log-20260919` 分支独立 commit,不动 main)

## 八、剩余 P2 治理

- 前端仓改 `/ipd/admin/config` 页面,加「最后变更人」「最后变更时间」两列(读 GET /{key}.lastChange*)
- 写 parameter_change_log 独立审计表(本仓 audit_logs 已写,但前端难以查询;留 P2)
