# 任务图：资料库与 IPD 全部功能关系梳理与完整实现

**日期**：2026-09-23
**作者**：ioedream-pm 智能体（主协调会话 OPS-09 单写者）
**状态**：ACTIVE — 待主协调会话按 Batch 顺序执行
**范围**：第一批（阶段 1-3，低风险），不含阶段 4-5
**源方案文档**：`docs/ipd-系统说明/资料库与IPD全部功能关系梳理与完整实现方案-20260923.md`（324 行）

---

## 总览

| 维度 | 数值 |
|---|---|
| Batch 数 | 7 |
| 任务卡数 | 18 |
| 子阶段覆盖 | 阶段 1（RAG 真活触发）/ 阶段 2（AiCopilotService 接 RAG）/ 阶段 3（retrieveContext 分 docType） |
| 阶段 4-5 | 不在本批次（owner 拍板 + 撞车 0 让路 worktree） |
| 单一写入者 | OPS-09 主协调会话独占 commit/push 权限 |
| 验证门禁 | Batch 6：mvn test + mysql 现查 + HTTP 三证 |

---

## 任务依赖图

```
Batch 1 (前置准备) ──┐
                     ├──> Batch 2 (DDL) ──┐
                     │                    ├──> Batch 3 (代码) ──┐
                     │                    │                     ├──> Batch 4 (单测) ──┐
                     │                    │                     │                      ├──> Batch 5 (阶段 1 验证) ──┐
                     │                    │                     │                      │                          ├──> Batch 6 (验证) ──┐
                     │                    │                     │                      │                          │                    ├──> Batch 7 (收口)
                     │                    │                     │                      │                          │                    │
                     │                    │                     │                      │                          │                    │
                     │                    │                     │                      │                          │                    │
                     │                    │                     │                      │                          │                    │
                     └──> T2.1 INSERT cfg ┘                     │                      │                          │                    │
                                                                   │                      │                          │                    │
                                                  T3.5/T3.6 传 docType ← T3.4 入参加 docType ← T2.2 加索引           │                    │
                                                                   │                                                          │                    │
                                                                   └──> T4.1 AiCopilot 单测 <── T3.1~T3.3                  │                    │
                                                                                                                                 │                    │
                                                                                                                                │                    │
                                                                                                                                └────────────────────┘
```

**关键依赖**：
- T2.2（加索引）必须先于 T3.4（retrieveContext 改 docType 过滤）
- T2.1（配置 RAG 开关）必须先于 T5.1（review 触发 embedAsync）
- T3.1~T3.3 必须先于 T4.1（AiCopilot 单测）
- Batch 5 是阶段 1 验收关卡
- Batch 6 是阶段 2+3 验收关卡

---

## Batch 1：前置准备（主会话执行）

### T1.1 SHOW CREATE TABLE 粘表结构

| 字段 | 内容 |
|---|---|
| **id** | T1.1 |
| **objective** | 拿到 ai_model_configs / ai_documents / ai_doc_embeddings 三张表完整 DDL，作为后续变更基线 |
| **action** | mysql `--defaults-file=.codex/ipd-dev/config/mysql-client.cnf -e "USE ipd_dev; SHOW CREATE TABLE ai_model_configs\\G; SHOW CREATE TABLE ai_documents\\G; SHOW CREATE TABLE ai_doc_embeddings\\G;"` |
| **tool_or_agent** | mysql client（主协调会话直接执行） |
| **preconditions** | (1) 本机 mysql 客户端可用；(2) `.codex/ipd-dev/config/mysql-client.cnf` 存在且权限 OK |
| **success_condition** | 三张表 CREATE TABLE 输出完整捕获（含索引、约束、外键、tenant_id 默认值） |
| **validation** | 输出保存到主协调会话本地剪贴板 + log.md 末段登记三表结构指纹 |
| **failure_path** | (a) `.cnf` 文件读不到 → 改用 IP/账号硬编 (b) 权限拒绝 → 联系 DBA；(c) 表不存在 → 查 Flyway 迁移日志确认 schema 是否落地 |
| **parallel_batch** | Batch 1（同 Batch 内与 T1.2 并行，但 T1.1 应先于 T1.2 完成） |

### T1.2 建备份表

| 字段 | 内容 |
|---|---|
| **id** | T1.2 |
| **objective** | 为三张目标表建 BACKUP_xxx_20260923 快照，防止误改可回滚 |
| **action** | mysql `-e "USE ipd_dev; CREATE TABLE BACKUP_ai_model_configs_20260923 AS SELECT * FROM ai_model_configs; CREATE TABLE BACKUP_ai_documents_20260923 AS SELECT * FROM ai_documents; CREATE TABLE BACKUP_ai_doc_embeddings_20260923 AS SELECT * FROM ai_doc_embeddings;"` |
| **tool_or_agent** | mysql client |
| **preconditions** | T1.1 已确认三表存在 |
| **success_condition** | 三张 BACKUP_xxx_20260923 表已创建且行数与源表一致（SELECT COUNT(*) 比对） |
| **validation** | `SHOW TABLES LIKE 'BACKUP_%20260923';` 返回 3 行 |
| **failure_path** | (a) 表已存在 → DROP 后重建；(b) 磁盘不足 → 清理 tmp 后重试；(c) 权限 → DBA |
| **parallel_batch** | Batch 1 |

---

## Batch 2：DDL（主会话执行）

### T2.1 INSERT ai_model_configs 配置 RAG 开关

| 字段 | 内容 |
|---|---|
| **id** | T2.1 |
| **objective** | 让 `embedAsync` 不再静默跳过（`AiDocEmbeddingService.java:90-92`：`if (cfg == null) return;`） |
| **action** | `INSERT INTO ai_model_configs (model_name, model_type, is_active, config_json, tenant_id, create_time, update_time) VALUES ('RAG-EMBED-DEFAULT', 'EMBEDDING', 1, '{"embedEndpoint":"http://localhost:16039/api/embedding","embedModel":"text-embedding-ada-002"}', '000000', NOW(), NOW());` |
| **tool_or_agent** | mysql client |
| **preconditions** | (1) T1.2 备份完成；(2) 参考 `AiModelConfig.fromJson` 解析逻辑确认字段名；(3) embedEndpoint 本机可达 |
| **success_condition** | `SELECT * FROM ai_model_configs WHERE is_active=1` 返回 ≥1 行 |
| **validation** | log.md 登记 INSERT SQL + 返回的 row id |
| **failure_path** | (a) 唯一键冲突 → UPDATE；(b) embedEndpoint 不可达 → 改用 mock endpoint；(c) AiModelConfig.fromJson 字段名不一致 → 读源码校正 |
| **parallel_batch** | Batch 2（与 T2.2 串行于不同表，无锁冲突；建议先 T2.1 再 T2.2） |

### T2.2 CREATE INDEX idx_emb_doctype

| 字段 | 内容 |
|---|---|
| **id** | T2.2 |
| **objective** | 给 `ai_doc_embeddings.doc_type` 加索引，避免后续 docType 过滤全表扫描 |
| **action** | `CREATE INDEX idx_emb_doctype ON ai_doc_embeddings(doc_type);` |
| **tool_or_agent** | mysql client |
| **preconditions** | SHOW INDEX 已确认 doc_type 无索引 |
| **success_condition** | `SHOW INDEX FROM ai_doc_embeddings` 返回 `idx_emb_doctype` 一行 |
| **validation** | log.md 登记索引 DDL + 表大小（行数 + 索引大小） |
| **failure_path** | (a) 索引已存在 → DROP 后重建；(b) 锁等待超时 → 业务低峰再跑；(c) 字段不存在 → 查 schema-snapshot 确认列名 |
| **parallel_batch** | Batch 2 |

---

## Batch 3：代码改动（主会话执行）

### T3.1 改 AiCopilotService 构造函数加 docEmbeddingService 参数

| 字段 | 内容 |
|---|---|
| **id** | T3.1 |
| **objective** | 让 AiCopilotService 能调 `retrieveContext` |
| **action** | 修改 `AiCopilotService.java` line 62-74 构造函数，加 `AiDocEmbeddingService docEmbeddingService` 参数（nullable） |
| **tool_or_agent** | 主协调会话（OPS-09 单写者） |
| **preconditions** | 阶段 1 已 commit（AiCopilotService 现有签名不能破坏） |
| **success_condition** | 构造函数参数已加；调用方（Spring 注入）已更新 |
| **validation** | `mvn -o -pl ruoyi-modules/ruoyi-ipd compile` 通过 |
| **failure_path** | (a) 编译失败 → 检查 nullable 注解；(b) Spring 启动报错 → 确认 nullable 注入配置；(c) 现有调用方漏改 → grep 全部 @Autowired 注入点 |
| **parallel_batch** | Batch 3（同 Batch 内 T3.1 必先于 T3.2/T3.3） |

### T3.2 改 AiCopilotService.chitchatPath 调 retrieveContext

| 字段 | 内容 |
|---|---|
| **id** | T3.2 |
| **objective** | 在 chitchatPath 里调 `docEmbeddingService.retrieveContext` |
| **action** | 在 line 181（composePrompt 调用前）加 `String ragCtx = docEmbeddingService != null ? docEmbeddingService.retrieveContext(req.projectId(), req.message(), null) : "";` |
| **tool_or_agent** | 主协调会话 |
| **preconditions** | T3.1 完成 |
| **success_condition** | retrieveContext 调用已加；docEmbeddingService 为 null 时降级返回空串 |
| **validation** | `mvn -o -pl ruoyi-modules/ruoyi-ipd compile` 通过 |
| **failure_path** | (a) 编译失败 → 检查方法签名是否需要调整；(b) null 安全未覆盖 → 加 `Objects.requireNonNullElse` |
| **parallel_batch** | Batch 3 |

### T3.3 改 AiCopilotService.composePrompt 加 ragCtx 参数

| 字段 | 内容 |
|---|---|
| **id** | T3.3 |
| **objective** | 把 RAG 上下文作为第三档注入 prompt |
| **action** | 改 composePrompt 方法签名加 `String ragCtx`，在 prompt 里加"【项目历史文档（RAG）】"块 |
| **tool_or_agent** | 主协调会话 |
| **preconditions** | T3.2 完成 |
| **success_condition** | composePrompt 新签名生效；prompt 模板含 RAG 块 |
| **validation** | 单元测试断言 prompt 含 RAG 块标识 |
| **failure_path** | (a) 模板拼接错 → 检查字符串模板；(b) ragCtx 为空时渲染多余空块 → 加 `if (!ragCtx.isBlank())` 判断 |
| **parallel_batch** | Batch 3 |

### T3.4 改 AiDocEmbeddingService.retrieveContext 入参加 docType

| 字段 | 内容 |
|---|---|
| **id** | T3.4 |
| **objective** | retrieveContext 支持 docType 过滤 |
| **action** | Read line 139-192，加 `String docType` 参数；SQL 加 `eq(AiDocEmbedding::getDocType, docType)` 条件（null 时不加，保持向后兼容） |
| **tool_or_agent** | 主协调会话 |
| **preconditions** | T2.2 索引已加（避免全表扫描） |
| **success_condition** | 方法签名变更；SQL 条件分支正确 |
| **validation** | `mvn -o -pl ruoyi-modules/ruoyi-ipd compile` 通过 |
| **failure_path** | (a) MyBatis-Plus 链式条件语法错 → 参考同文件其他 eq 调用；(b) null 短路逻辑漏 → 加 `if (docType != null)` 包裹 |
| **parallel_batch** | Batch 3（必须先于 T3.5/T3.6） |

### T3.5 改 AiGenerationService.generate 传 docType

| 字段 | 内容 |
|---|---|
| **id** | T3.5 |
| **objective** | AiGenerationService 调用 retrieveContext 时传 docType |
| **action** | 改 `AiGenerationService.java:128-132`，调 `retrieveContext` 时加 `docType` 参数（从 `AiGenerateReq.docType()` 获取） |
| **tool_or_agent** | 主协调会话 |
| **preconditions** | T3.4 完成 |
| **success_condition** | AiGenerateReq 含 docType 字段；retrieveContext 调用已传 |
| **validation** | `mvn -o -pl ruoyi-modules/ruoyi-ipd compile` 通过 |
| **failure_path** | (a) AiGenerateReq 无 docType 字段 → 补字段 + getter；(b) 编译错 → 检查 record 语法 |
| **parallel_batch** | Batch 3 |

### T3.6 改 AiCopilotService.chitchatPath 传 docType=null

| 字段 | 内容 |
|---|---|
| **id** | T3.6 |
| **objective** | AiCopilotService 调用 retrieveContext 时传 docType=null（不过滤，向后兼容） |
| **action** | 在 T3.2 的 retrieveContext 调用处加第三个参数 `null` |
| **tool_or_agent** | 主协调会话 |
| **preconditions** | T3.4 完成（retrieveContext 签名已变） |
| **success_condition** | chitchatPath 中 retrieveContext 调用带 docType=null |
| **validation** | `mvn -o -pl ruoyi-modules/ruoyi-ipd compile` 通过 |
| **failure_path** | (a) 缺第三个参数 → 补 `null`；(b) 重载错 → 确认 retrieveContext 是否需要重载 |
| **parallel_batch** | Batch 3 |

---

## Batch 4：单测（主会话执行）

### T4.1 写 AiCopilotService 单测

| 字段 | 内容 |
|---|---|
| **id** | T4.1 |
| **objective** | 验证 AiCopilotService 返回结果包含 RAG 上下文 |
| **action** | 新增 `AiCopilotServiceTest.java`：(1) mock docEmbeddingService.retrieveContext 返回非 EMPTY；(2) mock AiGateway 返回固定内容；(3) 断言 composePrompt 输出含 RAG 块标识 |
| **tool_or_agent** | 主协调会话（JUnit 5 + Mockito） |
| **preconditions** | T3.1~T3.3 已 commit |
| **success_condition** | `mvn -o -pl ruoyi-modules/ruoyi-ipd -Dtest=AiCopilotServiceTest test` PASS |
| **validation** | 测试报告含 RAG 上下文断言命中 |
| **failure_path** | (a) Mockito 静态方法难 mock → 用 Mockito-inline；(b) 断言词不匹配 → 检查 prompt 模板字面量；(c) 测试假绿 → 加 @Tag("dev") 标记 |
| **parallel_batch** | Batch 4 |

### T4.2 写 AiDocEmbeddingService 单测

| 字段 | 内容 |
|---|---|
| **id** | T4.2 |
| **objective** | 验证 retrieveContext 返回结果只包含指定 docType 的片段 |
| **action** | 新增 `AiDocEmbeddingServiceTest.java`：(1) mock 同 projectId 下 docType='PRD' 和 'MRD' 各 2 条；(2) 验证传 docType='PRD' 时只返回 PRD 的 2 条；(3) 验证传 null 时返回全部 4 条 |
| **tool_or_agent** | 主协调会话 |
| **preconditions** | T3.4 完成 |
| **success_condition** | `mvn -o -pl ruoyi-modules/ruoyi-ipd -Dtest=AiDocEmbeddingServiceTest test` PASS |
| **validation** | 断言行数与预期一致 |
| **failure_path** | (a) MyBatis-Plus chain 难 mock → 用 lambda 表达式 mock；(b) eq 条件未生效 → 检查条件组装顺序 |
| **parallel_batch** | Batch 4 |

### T4.3 写 AiGenerationService 单测

| 字段 | 内容 |
|---|---|
| **id** | T4.3 |
| **objective** | 验证 AiGenerationService 调用 retrieveContext 时传 docType |
| **action** | 新增 `AiGenerationServiceTest.java`：(1) mock docEmbeddingService.retrieveContext；(2) 用 ArgumentCaptor 捕获第 3 参数；(3) 断言等于 AiGenerateReq.docType() |
| **tool_or_agent** | 主协调会话 |
| **preconditions** | T3.5 完成 |
| **success_condition** | `mvn -o -pl ruoyi-modules/ruoyi-ipd -Dtest=AiGenerationServiceTest test` PASS |
| **validation** | ArgumentCaptor 捕获值与请求 docType 一致 |
| **failure_path** | (a) verify 模式错 → 改用 `verify(service).retrieveContext(eq(projectId), any(), eq(docType));`；(b) 假绿 → 加 @Tag("dev") |
| **parallel_batch** | Batch 4 |

---

## Batch 5：阶段 1 验证（主会话执行）

### T5.1 调 AiDocumentService.review 触发 embedAsync

| 字段 | 内容 |
|---|---|
| **id** | T5.1 |
| **objective** | 人工 REVIEW 一篇 AI 文档触发 `embedAsync` |
| **action** | (1) `SELECT id FROM ai_documents WHERE status='GENERATED' LIMIT 1;` 取 versionId；(2) 通过 HTTP POST `/ipd/ai/document/review` body={versionId, operatorId, decision: PASS}；(3) 后端日志确认 `embedAsync` 调用 |
| **tool_or_agent** | 主协调会话（HTTP 调用 + 日志查证） |
| **preconditions** | (1) T2.1 配置 OK；(2) T3 全部 commit；(3) ai_documents 至少 1 行 status='GENERATED' |
| **success_condition** | HTTP 200；后端日志含 `[AI-STRAT-1] embedAsync 调用` 字样 |
| **validation** | log.md 登记 versionId + HTTP 状态码 |
| **failure_path** | (a) ai_documents 无 GENERATED → INSERT 一行测试数据；(b) HTTP 接口未鉴权通过 → 拿 token；(c) embedAsync 未调 → 检查 T2.1 cfg 是否 is_active=1 |
| **parallel_batch** | Batch 5 |

### T5.2 mysql 现查 ai_doc_embeddings 行数 > 0

| 字段 | 内容 |
|---|---|
| **id** | T5.2 |
| **objective** | 验证 RAG 真活触发 |
| **action** | 等待 5-10 秒后 `SELECT COUNT(*) FROM ai_doc_embeddings;` 期望 > 0 |
| **tool_or_agent** | 主协调会话（mysql + sleep） |
| **preconditions** | T5.1 完成 |
| **success_condition** | COUNT(*) > 0 |
| **validation** | log.md 登记行数 + 抽样 1 行 doc_id/embedding |
| **failure_path** | (a) 仍为 0 → 检查日志 `[AI-STRAT-1] 向量化失败降级` 或 `向量化跳过（配置不可用）`；(b) 错误降级 → 排查 embedEndpoint；(c) 排队未执行 → 看守护池是否 alive |
| **parallel_batch** | Batch 5 |

---

## Batch 6：验证（主会话执行）

### T6.1 mvn test 验证

| 字段 | 内容 |
|---|---|
| **id** | T6.1 |
| **objective** | 错峰 + 单模块 + 不带 -am 不带 clean，跑全套 RAG 相关单测 |
| **action** | `mvn -o -pl ruoyi-modules/ruoyi-ipd -Dtest='AiCopilotServiceTest,AiDocEmbeddingServiceTest,AiGenerationServiceTest' test` |
| **tool_or_agent** | 主协调会话 |
| **preconditions** | Batch 4 全部单测已写 |
| **success_condition** | 三个测试类全部 PASS |
| **validation** | surefire 报告含 3 个 classes、tests count > 0、failures=0 |
| **failure_path** | (a) 假红 → 不带 -am 不带 clean 重跑；(b) 假绿 → 检查 @Tag("dev") 是否生效；(c) 测试间污染 → 加 @DirtiesContext |
| **parallel_batch** | Batch 6 |

### T6.2 mysql 现查验证（行数 + 索引）

| 字段 | 内容 |
|---|---|
| **id** | T6.2 |
| **objective** | 验证 RAG 真活 + 索引已加 |
| **action** | (1) `SELECT COUNT(*) FROM ai_doc_embeddings;` > 0；(2) `SHOW INDEX FROM ai_doc_embeddings WHERE Key_name='idx_emb_doctype';` 返回 1 行 |
| **tool_or_agent** | 主协调会话 |
| **preconditions** | T5.2 完成 |
| **success_condition** | 两项断言均通过 |
| **validation** | log.md 登记两项查询结果 |
| **failure_path** | (a) 行数仍 0 → 回查 T5 日志；(b) 索引未生效 → 重跑 T2.2 |
| **parallel_batch** | Batch 6 |

### T6.3 HTTP 验证（启动后端调接口）

| 字段 | 内容 |
|---|---|
| **id** | T6.3 |
| **objective** | 端到端真活验证：调 AiCopilotService 接口，确认返回含 RAG 上下文 |
| **action** | (1) `mvn -o -pl ruoyi-modules/ruoyi-ipd spring-boot:run`；(2) POST `/ipd/ai/copilot/chat` body={projectId, message:"总结本项目历史文档"}；(3) 查后端日志确认 prompt 含【项目历史文档（RAG）】块 |
| **tool_or_agent** | 主协调会话 |
| **preconditions** | T6.1/T6.2 PASS |
| **success_condition** | HTTP 200；日志含 RAG 块标识；响应含 RAG 上下文片段 |
| **validation** | 截图 + 日志段落写入 log.md |
| **failure_path** | (a) 启动失败 → 检查端口冲突；(b) 接口 404 → 查 controller 注册；(c) 响应无 RAG → 检查 chitchatPath 是否调 retrieveContext |
| **parallel_batch** | Batch 6 |

---

## Batch 7：收口（主会话执行）

### T7.1 更新 log.md

| 字段 | 内容 |
|---|---|
| **id** | T7.1 |
| **objective** | 登记三阶段实施事实 + 验证证据 |
| **action** | 在 log.md 末段新增条目：(1) 阶段 1 完成事实；(2) 阶段 2 代码 commit hash；(3) 阶段 3 代码 commit hash；(4) 测试报告；(5) 三证截图路径 |
| **tool_or_agent** | 主协调会话 |
| **preconditions** | Batch 6 全部 PASS |
| **success_condition** | log.md 末段新增 ≥ 5 段事实登记 |
| **validation** | grep 命令确认条目已落库 |
| **failure_path** | (a) 文件过大写入慢 → 用 sed 追加；(b) 编码错 → 检查 UTF-8 |
| **parallel_batch** | Batch 7 |

### T7.2 更新 SSOT 看板镜像

| 字段 | 内容 |
|---|---|
| **id** | T7.2 |
| **objective** | 阶段 1-3 全部翻 done 后更新镜像文档 |
| **action** | 更新 `开发计划-看板镜像.md`：标记 3 张子卡 done + 汇总卡 done |
| **tool_or_agent** | 主协调会话 |
| **preconditions** | T7.1 完成 |
| **success_condition** | 镜像文档与看板 4 张卡状态一致 |
| **validation** | diff 镜像文档前后版本 |
| **failure_path** | (a) 镜像版本漂移 → 回滚到最近一次 commit；(b) 看板不同步 → 手动翻 done |
| **parallel_batch** | Batch 7 |

### T7.3 git add + git commit + git push

| 字段 | 内容 |
|---|---|
| **id** | T7.3 |
| **objective** | 主协调会话统一 commit + push（R177 第 5 条） |
| **action** | (1) `git add -A`；(2) `git commit -m "feat(R183): 资料库与 IPD 全部功能关系梳理与完整实现 阶段 1-3"`；(3) `git push origin main` |
| **tool_or_agent** | 主协调会话 |
| **preconditions** | T7.1/T7.2 完成；git status 干净 |
| **success_condition** | commit hash 已生成；push 成功；origin/main HEAD 推进 |
| **validation** | `git log --oneline -1` 确认最新 commit |
| **failure_path** | (a) push 冲突 → rebase；(b) pre-commit hook 失败 → 修问题重提；(c) 撞号 → 透明披露（不 cherry-pick） |
| **parallel_batch** | Batch 7 |

---

## 边界声明

| 项 | 范围 |
|---|---|
| **本批次包含** | 阶段 1（RAG 真活触发）/ 阶段 2（AiCopilotService 接 RAG）/ 阶段 3（retrieveContext 分 docType） |
| **本批次不含** | 阶段 4（AuditLogEventListener）/ 阶段 5（KnowledgeAutoArchiver / RAG cron / EvoMap SDK） |
| **单写者** | OPS-09 主协调会话独占 Java 源码 commit/push 权限 |
| **撞号透明** | 开工前 git log -5 + 看板 in-progress + log.md 末段三查已由主协调会话完成，无需重复 |
| **撞车 0 守则** | 阶段 1-3 纯代码改动，撞车 0 风险低 |
| **假红陷阱** | 复核方一律错峰 + 单模块 + 不带 -am 不带 clean |
| **假绿陷阱** | 新测试加 `@Tag("dev")` |
| **demo.enabled** | 默认 false，不改 |

---

## 验收门禁

| 阶段 | 验收标准 | 验证方式 |
|---|---|---|
| **阶段 1** | `ai_doc_embeddings` 行数 > 0 | mysql 现查 |
| **阶段 2** | AiCopilotService 返回结果包含 RAG 上下文 | 单测 + 集成测试 |
| **阶段 3** | retrieveContext 返回结果只包含指定 docType 片段 | 单测 |
| **全批次** | mvn test PASS + git push 成功 | Batch 6 + Batch 7 |

---

## 关联文件路径

- 方案文档：`docs/ipd-系统说明/资料库与IPD全部功能关系梳理与完整实现方案-20260923.md`
- 进度报告：`docs/ipd-系统说明/进度报告-资料库与IPD全部功能关系梳理与完整实现-20260923.md`
- Java 源码：
  - `ruoyi-modules/ruoyi-ipd/src/main/java/org/ruoyi/ipd/service/AiDocumentService.java`
  - `ruoyi-modules/ruoyi-ipd/src/main/java/org/ruoyi/ipd/service/AiDocEmbeddingService.java`
  - `ruoyi-modules/ruoyi-ipd/src/main/java/org/ruoyi/ipd/service/AiGenerationService.java`
  - `ruoyi-modules/ruoyi-ipd/src/main/java/org/ruoyi/ipd/service/AiCopilotService.java`
- 数据库配置：`.codex/ipd-dev/config/mysql-client.cnf`

---

**签署**：ioedream-pm 智能体
**签署时间**：2026-09-23
**下一步**：等待主协调会话按 Batch 1→7 顺序执行
