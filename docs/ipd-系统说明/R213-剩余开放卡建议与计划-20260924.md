# R213 剩余开放卡建议与计划（2026-09-24）

> **性质**：只读盘点 + 建议计划。不改 Java/Vue/SQL/yml；不翻看板 status；不碰 git 写操作。  
> **看板现查**：MCP `user-zker_vibe_kanban` / project `01dcf15c-86bb-4c7b-957c-8fe44bddd10d`，`list_tasks` 分 status 全量（limit=200）。  
> **现查非终态**：todo **19** + inprogress **21** + inreview **4** = **44**（与「约 44」一致，无需纠偏）。  
> **本文件自包含**：故意不改 `log.md`（他会话可能在改）。

---

## 一、非终态卡主题归并（结论用主题，不用 44 行流水账）

| 主题 | 数量 | 代表卡（短 id） | 阻塞形态 |
|---|---:|---|---|
| **A. 需 owner 拍板** | 5 | `15d5e689` / `639de2c8` / `67ffc283` / `e30c86a2` / `ecdd3444` | 裁决悬空或「多环境」需人工窗口 |
| **B. 环境未重载即可收口** | 1 | `17324373`（inreview） | 代码已接线，缺 16039 进程重载真活 |
| **C. RAG / R183** | 4 | `3280f1e2` / `a1af61b7` / `5a2eedd3` / `53576dd5` | 进度报告滞后；代码大多已落地，差真活与索引断言 |
| **D. AI 大阶段 + 子卡** | 10+ | `PLAN-AI-FULL` / `AI-P1-1` / `AI-P2-1/2` / `AI-P3` / `P4-2.*` / `PLAN-KB-AUTO` | 卡面过大；依赖 SSE/Gateway；worktree 无人认领 |
| **E. 汇总卡等子卡/等翻卡** | 10 | `b9ab3425`(P4-4 子卡已全 done) / `728b1113` / `P1-3/4/6/10` / `P2-3` / `P3-2/7/8` / `P4-2/5` | 汇总纪律不假绿；部分子卡已齐 |
| **F. QA / SEC / OPS / AUD 长跑** | 7 | `QA-07/08` / `SEC-04` / `AUD-02` / `OPS-06` / `AUD-GOV` / `P4-5.1` | 依赖前端/服务层/外部；部分已有波次证据 |
| **G. 其它 inreview / 在途业务** | 若干 | `DB-02` / `P1-6.1` / `P1-4.2` / `P1-10.2` / `PLAN-ROOT-1` / `PLAN-AUDIT-FULL` | 待复核或批次追踪 |

依赖主轴（从左到右）：

```text
拍板(A) ──┬──► 奖金/字符集/WB 下一子项
          │
16039 重载(B) ──► 权限闸正修闭环 + 顺带复核奖金/工作台 HTTP
          │
R183 真活(C) ──► RAG 有向量 ──► AI 场景包/PLAN-AI 下一子卡(D)
          │
汇总翻卡(E) ←── 子卡真 done + owner 手动翻
```

---

## 二、五张拍板卡（现状 / 文档 / 选项代价 / 建议）

### 2.1 `15d5e689` — distribute 无贡献度 → 500（contribution_rate NOT NULL vs 注释）

| 维 | 事实 |
|---|---|
| **现状** | `BonusPoolService.writeBonusAllocations`（约 L1133–1183）在贡献度缺失时 **照写台账且 `contributionRate=null`**；单测 `BonusPoolAllocationWriteTest.distribute_contribution_missing_still_writes_with_null_rate` 锁此语义。DDL 快照 `docs/script/sql/schema/schema-snapshot-2026-09-09.sql` / `batch_missing_tables.sql`：`bonus_allocations.contribution_rate decimal(5,4) NOT NULL`。真活有贡献度时 distribute 已能写出台账（R211c 证据：双 PM 2 行）；**无贡献度路径撞 DDL → HTTP 500**。另附悬空 `bonus_pools.project_id=9140004`（只读扫库另卡）。 |
| **产品文档** | 主 Prompt v3 **BR-INC-08**：`每人成功奖金 = 奖金池 × 达成率阶梯系数 × 贡献度比例 × 绩效系数`——公式默认贡献度存在。A3 裁决（卡面 + `业务裁决提案-20260908`）：系数来源=贡献度五维，对象=双 PM；实施注记写明「贡献度缺失照写 rate=null」。 |
| **选项 A** | DDL 改列可空，对齐 Service 注释与 A3 实施。**代价**：台账可缺贡献率，弱化 BR-INC-08 可审计性；需 ALTER + 回归。 |
| **选项 B** | 无贡献度时跳过 insert 台账，对齐现 DDL。**代价**：与 A3「照写」+ 单测 + AC「行数=PM 数」冲突；池已 DISTRIBUTED 但无人员行，报表空洞。 |
| **建议** | **优先不在 A/B 二选一硬冲，而选「业务门禁」**：`distribute` 要求项目存在 `contributions.status=CONFIRMED`（及有效 `tierCoefficient`），否则 4xx 业务拒绝；**保持 `contribution_rate NOT NULL`**。理由：同时满足 BR-INC-08 与 DDL，且不推翻「有贡献度时写双 PM 台账」的已验路径。若 owner 明确允许「无贡献度也发钱」——再选 **A**（改可空），**不要选 B**（破坏台账完整与既有单测）。悬空 `9140004` 另开只读清洗卡，勿绑本拍板。 |

### 2.2 `639de2c8` — sys_user↔persons 字符集 collation

| 维 | 事实 |
|---|---|
| **现状** | 卡面真库：`sys_user.user_name=utf8mb4_0900_ai_ci`，`persons.username=utf8mb4_general_ci`；直接 JOIN 复现 ERROR 1267。IPD 登录走 `loginType=ipd` + persons，业务路径不 JOIN 两表（卡面与 `IpdAuthSession` 设计一致）。决策包与 SQL 草稿已在 `docs/ipd-系统说明/P3-LOW-字符集治理决策包-20260918.md`、`docs/script/sql/update/2026-09-18-p3low-collation-align.sql`。 |
| **产品文档** | 无「必须立即统一字符集」业务条款；属于运维/互通隐患。 |
| **选项** | **立即修 A1**（persons→0900_ai_ci）vs **推迟**。A1 代价：索引重建 + 建议重启 16039；推迟代价：跨系统 JOIN 持续 1267。 |
| **建议** | **推迟到「框架用户与 IPD 人员互通」窗口再执行 A1**。理由：卡面已自证「暂不阻塞」；撞车 0 下无业务收益。拍板矩阵勾选：推迟 / 若修则 A1 / 凌晨窗口 / 要回滚预案。 |

### 2.3 `67ffc283` — bonus_allocations.person_id 由谁填

| 维 | 事实 |
|---|---|
| **现状** | A3 已裁决：person_id = 双 PM（`project_members` MARKET_PM/RD_PM 且 `exit_date IS NULL`）。代码已接线：`writeBonusAllocations` 按成员 `personId` insert；单测 3 绿。R211c 真活已写出双 PM 行。卡仍 todo 因历史「JVM 未加载 / 待验」注记未清。 |
| **产品文档** | `_导航地图`「34 奖金池 → 35 贡献度评定」链；BR-INC-09 双 PM + 组长联合评定；与 A3「双 PM 两线」一致。 |
| **选项** | （历史）贡献度五维 vs KPI 月度；双 PM vs 全员。**已裁决，无需重拍系数来源。** |
| **建议** | **确认 A3 维持，不再重开「谁填 person_id」**。下一动作是验收收口：对「有 CONFIRMED 贡献度」的池重跑 distribute → 断言 `bonus_allocations` 行数=在册双 PM，并与 `15d5e689` 门禁拍板联动后翻卡。不要因旧标题里的「待业务决策」再次空转。 |

### 2.4 `e30c86a2` — 工作台 WB-17 字段映射

| 维 | 事实 |
|---|---|
| **现状** | 契约 `workbench-tasktype-契约登记.yaml`：**9 类 implemented**（stage_sign / key_gate / key_gate_arbitration / deletion_review / handover / contribution_confirm / strategic_change / kpi_fill / closeout），**8 类 PLANNED**。后端 `org.ruoyi.ipd.workbench.*Aggregator` 9 个；前端 DICT-1 17 类文案已落。B4 四项拍板（枚举/分组/stats/缺表暂缓）已接受。卡仍 inprogress：缺表 4 类等 apply；其余 4 类流程未贯通；2026-09-23 注记要求 **字段级 spec**（模板 `WB-17-1-tasktype字段级spec填写模板-20260923.md`）后才能扩聚合器。 |
| **产品文档** | `docs/开发说明/spec/batch-01-pages-01-12.md` 页03:165 权威 17 类；`pendingType` / `noOutputAlerts` 仍为 v3 缺口标注。 |
| **选项** | 一轮做完 8 类 vs **按批 2–3 类**；缺表先 apply C-batch 草稿 vs 继续暂缓。 |
| **建议** | **不要承诺一轮 17 类收口**。下一张可验收子卡建议：`receipt_review`（表 `receipt_ledger` 已存在，只差投递贯通）或 owner 填完模板后的 `bonus_lock`。缺表 4 类（waiver/rd_replacement/retirement/capacity）等 owner 对 `2026-09-08-ipd-c-batch-4-tables-draft.sql` 明确「apply」后再建聚合器。本卡维持 inprogress 合理。 |

### 2.5 `ecdd3444` — QA-06 多环境演练

| 维 | 事实 |
|---|---|
| **现状** | 本地恢复演练主线曾 PASS（`QA-06-业务部署安全与恢复演练-20260905.md`）；R101 落地 checklist + `docs/nginx/ipd.conf`（`验收/QA-06-部署演练checklist-20260919.md`）。§6 / checklist §1：**HTTPS、域名证书、真实多环境**仍显式未验。 |
| **产品文档** | TS14 / AC-ENV；允许「无真实生产发布、仅本地隔离」——但 HTTPS 与多环境属 owner 部署窗口。 |
| **选项** | 文档级视为 done vs **保留 inprogress 直到多环境演练**。 |
| **建议** | **维持 inprogress；owner 拍「演练窗口」**：填域名/证书/ENV，按 checklist §2–4 跑 compose + nginx -t + 回滚一次。AI 会话不得伪完成。前置依赖若仍卡 QA-03/SEC-04，先让路或平行登记。 |

---

## 三、RAG / AI / PLAN / 汇总 — 代码落点与依赖

### 3.1 R183（资料库 RAG）

| 卡 | 代码落点 | 判定 |
|---|---|---|
| **S1 真活触发** `3280f1e2` | `AiDocumentService.review` → `docEmbeddingService.embedAsync`；表 `ai_doc_embeddings` | **已有代码**；验收仍要：配置 embed 行 + review 后行数>0 + `retrieveContext` 非 EMPTY |
| **S2 Copilot 接 RAG** `a1af61b7` | `AiCopilotService` 注释已写「已接 RAG」；`chitchatPath`/`ragContextBlock` 调 `retrieveContext`；`AiCopilotServiceTest` 含 RAG 块断言 | **已有代码 / 只差集成真活**（进度报告仍写「未启动」=文档漂移） |
| **S3 docType** `5a2eedd3` | `retrieveContext(projectId, docType, query)` 三参；生成与副驾透传；单测 `ragContextBlockDocTypePassedThrough` | **已有代码**；**真缺口**：`idx_emb_doctype` 需 DB `SHOW INDEX` 断言（方案文档写过「验证时无索引」） |
| **Summary** `53576dd5` | 汇总 Stage1–3 | **被子卡堵住**（S1 真活 + S3 索引） |

依赖顺序：**S1 真活向量 →（S2 代码已齐可并行验）→ S3 索引现查 → Summary**。

### 3.2 AI / PLAN

| 卡 | 判定 | 下一可验收子项（不要一轮做完） |
|---|---|---|
| `AI-P1-1` SSE+promptType+重试 | 大卡；依赖 Gateway；worktree `agent-batch8-aip11` **无人认领** | 拆：仅 `promptType` 模板同步路径 + 单测 |
| `AI-P1-2` 按项目列表 | 标题已标 A 交付；真活/SOP 待验 | HTTP 列表 + 权限负向一单 |
| `AI-P2-1` Gate AI 预审 | 真缺口（场景包） | 单端点预审草稿 + 留痕，不做全要素 |
| `AI-P2-2` 招投标三件套 | 真缺口 | 只做「招标书起草」一条路径 |
| `AI-P3` 场景包七合 | 卡面过大 | 从 NL 查报表或需求查重拆 1 卡 |
| `PLAN-AI-FULL` | 方案文档已有；执行未拆完 | 先拆「Gate 要素建议」子卡 |
| `PLAN-KB-AUTO` | 阶段 4–5 边界外（R183 Summary 声明不含） | 等 R183 Summary 后再拆「审核事件→知识库」一卡 |
| `PLAN-ROOT-1` / `PLAN-AUDIT-FULL` | 统筹/审计覆盖长跑 | 每次只关一批可证条目，禁止整卡假绿 |

### 3.3 汇总卡

| 卡 | 判定 |
|---|---|
| `b9ab3425` P4-4 | 卡面：`check-done-gate-summary can_flip=True`，**只差 owner 手动翻 done** |
| `728b1113` 旧 P4-4 阻塞文案 | 与上重复语义；owner 复核后 cancelled 或并入 |
| 其余 P1/P2/P3/P4 汇总 | 等子卡；勿在子卡 PARTIAL 时翻 |

### 3.4 `17324373` /auth/me 权限码（inreview）

- 后端：`PersonView` + `IpdRolePermissionCatalog.permissionsOf`；测试 `IpdAuthMePermissionCodesTest`。  
- 前端：`parseIdentity` / `vbenCodesOf(..., permissionCodes)` / `refreshIdentity` 灌 `accessStore`。  
- **判定**：已有代码；**只差 16039 重载后** rd 登录 `/auth/me` 含 `ipd:` 码 + 无码角色负向拦页。本会话按指令不重启。

---

## 四、根因反思（本轮证据，不复述 R212 旧文）

1. **裁决悬空仍占坑**：`15d5e689` 把「A3 已允许 null」与「DDL NOT NULL」对立成 A/B，却未上升到「BR-INC-08 要不要门禁」——拍板问题选型偏窄，导致卡停在 todo。  
2. **卡面过大 + 派单无认领**：`AI-P1-1` / `PLAN-AI-FULL` / `AI-P3` / `WB-17` 仍是「多天工程」整卡；R44 worktree 名写了但 `has_in_progress_attempt=false`，状态 inprogress 变成僵尸。  
3. **文档事实源滞后**：R183 进度报告 Batch=0「未启动」，同时 `AiCopilotService` 已标注「已接 RAG」——看板与进度 md 未跟代码，制造「假缺口」。  
4. **环境未重载假阻塞**：`17324373` 明确「不重载则真活缺」；奖金/工作台历史多轮也卡在同一模式。  
5. **汇总纪律正确但堆积**：`b9ab3425` 已 can_flip 仍等 owner 手翻；其它汇总卡无人清点子卡矩阵。  
6. **兄弟在途 / 撞车 0**：字符集、多环境、C-batch apply 被正确让路，但缺少「拍板日历」导致长期 todo/inprogress 共存。

---

## 五、里程碑计划（按依赖排序）

### M0 — 本周可收口（不改业务规则，或只重载/翻卡）

| # | 动作 | 谁 | 路径 / 范围 | 验收 | 不做什么 |
|---|---|---|---|---|---|
| M0.1 | **重载 16039** 后收口 `/auth/me` | 运维/主协调（owner 授权杀旧进程） | 已有 `IpdAuthController` / 前端 `vben-identity` | rd：`permissionCodes` 含 `ipd:`；无码角色拦 `/ipd/projects` 浏览器证据 → 翻 `17324373` done | 不改权限目录；不关 demo |
| M0.2 | **owner 手翻** P4-4 汇总 | owner | 看板 `b9ab3425` | 子卡抽查 1 条真活仍绿后翻 done | 不重开报表代码 |
| M0.3 | R183 **S1 真活** | 后端会话 | `AiDocumentService.review` + `ai_model_configs` embed 行 | `ai_doc_embeddings` 行数>0；generation/copilot `retrieveContext` 非 EMPTY 一单 | 不改 Embedding 算法；不 push |
| M0.4 | R183 **S3 索引断言** | DBA/后端 | `CREATE INDEX` 若缺失；`SHOW INDEX` | `idx_emb_doctype` 存在；单测已绿可复跑 | 不改检索语义 |

### M1 — 拍板后才能动

| # | 拍板卡 | 拍板结论落地后下一动作 | 谁 | 验收 |
|---|---|---|---|---|
| M1.1 | `15d5e689` | 若采纳「门禁」：Service 前置 CONFIRMED + 单测红绿双向；若采纳 A：ALTER 可空 + 无贡献度 distribute 200 | 后端 | 无贡献度不再 500；有贡献度仍写 2 行台账 |
| M1.2 | `67ffc283` | 与 M1.1 同池验收后翻 done | 后端/QA | 台账 person_id=双 PM |
| M1.3 | `639de2c8` | 若「推迟」：卡面注记日期即可；若「立即 A1」：按草稿 SQL + 重启 | DBA | JOIN 不再 1267；IPD 登录回归 |
| M1.4 | `e30c86a2` | owner 填字段级模板中下一 2–3 类 **或** 声明 apply C-batch | 产品+后端 | 新类型至少 1 类：聚合器+契约 yaml+HTTP 1 卡 |
| M1.5 | `ecdd3444` | 域名/证书/ENV 确认后按 checklist 演练 | 运维 | checklist §2–4 勾选 + 新 JSON 证据 |

### M2 — 大阶段拆出的「下一张」可独立验收子卡（禁止整包承诺）

| 父卡 | 建议下一子卡标题 | 改哪些路径 | 验收 |
|---|---|---|---|
| R183-Summary | （等 M0.3–M0.4）后翻 Summary | 仅 docs/看板 | 三阶段证据链齐全 |
| AI-P1-1 | `[AI-P1-1a] promptType 模板-only` | `AiGenerateReq` + 文档页下拉 + 单测 | 选 PRD 时 system 模板命中；空值兼容 |
| PLAN-AI-FULL | `[AI-GATE-PRE] Gate 要素建议 MVP` | Gate 读接口 + Copilot 新 intent 一条 | 单 Gate 返回建议 JSON + 审计 aiAssisted |
| AI-P2-2 | `[AI-BID-1] 招标书起草-only` | 招投标域只读上下文 + 生成 | 一单流式/同步成功 |
| PLAN-KB-AUTO | 等 R183 后 `[KB-AUTO-1] REVIEWED→embed 已有则登记事件` | 审计/事件钩子最小 | 1 类事件可查，不做 EvoMap |
| WB-17 | `[WB-17-receipt] receipt_review 投递` | 新 Aggregator + 契约 yaml | summary 含该类且 DB 可溯源 |
| PLAN-AUDIT-FULL | 下一张「未审计表」清单中的 **1 表** | Listener/AOP 最小 | 该表写操作 audit_logs 有行 |

---

## 六、建议优先级一句话（给 owner）

1. **本周**：授权重载 16039（`17324373`）+ 手翻 `b9ab3425` + R183 S1/S3 真活断言。  
2. **拍板会（0.5h）**：`15d5e689` 选「贡献度门禁」或「列可空」；`639de2c8` 推迟；`67ffc283` 确认 A3 维持；`e30c86a2` 指定下一 2 类或 apply；`ecdd3444` 定演练日。  
3. **其后**：只派「下一张」AI/WB 子卡，停派整包 `PLAN-AI-FULL` / `AI-P3`。

---

## 七、现查卡号索引（便于跳转，非结论）

**todo (19)**：`15d5e689` `53576dd5` `a1af61b7` `5a2eedd3` `3280f1e2` `639de2c8` `b9ab3425` `67ffc283` `98db7e00` `fe6b6ac7` `728b1113` `98586804` `0d4b9ea4` `51738b8a` `7ad0b052` `076eba8b` `4912a562` `ae8636d3` `53ecc543`

**inprogress (21)**：`762f65c9` `65d3ad11` `0f4cc93b` `5e583809` `8d738f86` `17f362b6` `1b22479a` `2de46b46` `ef8dd03c` `e30c86a2` `a4657cec` `ae5bf623` `3ba1f028` `655f45e0` `ecdd3444` `0a293f3b` `10907a06` `6f725e41` `f42d37dd` `77341341` `fde68b8c`

**inreview (4)**：`17324373` `73fb9329` `6028cbed` `745b0141`

---

*R213 成文时间：2026-09-24；证据来源：看板 MCP 现查 + 后端/前端/产品文档只读。*
