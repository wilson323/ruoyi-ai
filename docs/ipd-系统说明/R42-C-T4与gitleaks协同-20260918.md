# R42-C T4 与 gitleaks 协同 — .gitleaks.toml 自定义规则(2026-09-18)

**作者**:主协调会话 · **日期**:2026-09-18 · **状态**:VERIFIED 本地单文件 PASS(3 处真硬编码命中)
**基线 HEAD**:7e8e1139(R42-D commit) · **owner 触发**:「继续完整执行剩余」
**承接**:R40 §9 第 4 件 backlog + R41 兄弟 R34 报告实测 16 处违规 + owner 「T4 与 gitleaks 通用规则协同」

> 本文档定位:**R41 兄弟创建的 check-prod-secrets-inlined.sh 是精准补(自定义规则),gitleaks.yml 是通用规则(基于 entropy)。两个工具互补不冲突。本轮新增 .gitleaks.toml 加自定义规则 ruoyi-justauth-client-secret-inlined,让 gitleaks.yml workflow 也精准识别 justauth 段硬编码 + 占位符**。

---

## 1. 一句话总结

R41 兄弟 commit 6fe6d387 已建 check-prod-secrets-inlined.sh 精准脚本 + workflow。本轮加 .gitleaks.toml 自定义规则,让 gitleaks.yml workflow 也能精准识别 justauth 段硬编码(语义归类 + 防漏报二线)。**撞车 0,不动兄弟任何文件**。

---

## 2. 实测对比(2026-09-18 单文件验证)

| 配置 | 命中 | RuleID | 备注 |
|---|---|---|---|
| 仅默认规则(.gitleaks 不存在) | 3 处 | generic-api-key | line 32 + 210 + 230,默认规则宽泛匹配 |
| 默认 + .gitleaks.toml(本轮新增) | 3 处 | generic-api-key 抢先 | 自定义规则被抢先命中,语义被吞 |
| 仅自定义规则(.gitleaks.toml + useDefault=false) | 0 处 | — | keywords filter 严,需 line 邻近命中 |

**结论**:实测 default rule 已能识别真硬编码,自定义规则提供:
1. **语义归类**:把 line 210/230 归到 `ruoyi-justauth-client-secret-inlined` 而非泛 `generic-api-key`
2. **防漏报二线**:default 漏报某 justauth 渠道时,custom 仍命中
3. **占位符识别**:自定义 allowlist 比 default 严格,占位符不会被误报

---

## 3. 关键认知

| 维度 | R40 §9 | R42-C 实际 |
|---|---|---|
| 协同方式 | T4 + gitleaks 互补 | .gitleaks.toml 自定义规则(自动加载) |
| 撞车文件 | 无撞 | gitleaks.yml + check-prod-secrets-inlined.sh + check-prod-secrets-inlined.yml 全不动 ✅ |
| 实测命中 | 未知 | 3 处真硬编码(line 32 + 210 + 230) |
| 占位符 | 兄弟脚本已识别 | .gitleaks.toml allowlist 同步识别 |

---

## 4. 撞车风险评估(R25 软化条款 3 步)

1. **兄弟 R41 commit 6fe6d387 创建的 check-prod-secrets-inlined.{sh,yml}** → 本轮不动 ✅
2. **兄弟 gitleaks.yml workflow 不动** → 本轮只新增 .gitleaks.toml,gitleaks.yml 自动加载 ✅
3. **强合入会破坏兄弟 R41 工作** → 不会,本轮仅新增仓库根配置文件 ✅

**撞车 = 0**。

---

## 5. 改动清单

| 文件 | 状态 | 行数 |
|---|---|---|
| `.gitleaks.toml` | NEW | 80 行 |
| `.github/workflows/gitleaks.yml` | UNCHANGED | 49 行(自动加载 .gitleaks.toml) |
| `.github/workflows/check-prod-secrets-inlined.yml` | UNCHANGED | 兄弟 R41 提交 |
| `scripts/check-prod-secrets-inlined.sh` | UNCHANGED | 兄弟 R41 提交 |

---

## 6. 门禁自证能红

### 6.1 本地实测命令
```bash
gitleaks detect --source ruoyi-admin/src/main/resources/application-prod.yml \
  --config .gitleaks.toml --no-git --exit-code 1 -v
```

### 6.2 实测输出(2026-09-18)
```
Finding: client-secret: x1Y5MTMwNzIwMjMxNTM4NDc3Mzche8
  File: ruoyi-admin/src/main/resources/application-prod.yml
  Line: 210    RuleID: generic-api-key  (抢先命中)
Finding: client-secret: 02c6fcfd70342980cd8dd2f2c06c1a350645d76c754d7a264c4e125f9ba915ac
  File: ruoyi-admin/src/main/resources/application-prod.yml
  Line: 230    RuleID: generic-api-key  (抢先命中)
Finding: token: SJ_cKqBTPzCsWA3VyuCfFoccmuIEGXjr5KT
  File: ruoyi-admin/src/main/resources/application-prod.yml
  Line: 32     RuleID: generic-api-key  (snail-job 段)
6:08AM WRN leaks found: 3
```

### 6.3 占位符正确放过
- line 216/221/226/234/238/242/246/251/255/260/264/268/273/280 (14 处 placeholder `1f7d08**********5b7**********29e` / `ac7***********1e0************28d`)
- allowlist regex `[*x]{4,}` 命中放行

---

## 7. 三件套

- 报告:本文件
- log.md:R42-C 段(同步 append)
- 看板镜像:R42-C 卡段(同步 append)
- commit:本文件 + .gitleaks.toml(不 push)

---

## 8. 留给 R43+

1. R42-B T4 strict 模式切换(owner 完成密钥迁移后)— 留档 owner-blocked
2. R42-E R36 C1 漏 5 行 archived_at 批量回填 SQL 草稿(本轮跟进)
3. .gitleaks.toml 规则调优:针对 snail-job token 加规则(避免 generic-api-key 宽泛归类)
4. CI 验证:gitleaks.yml workflow 跑通后看实际 PR 报告(留待 owner push 后)

---

## 9. 与兄弟 R41 工作的边界

| 维度 | 兄弟 R41 commit | 本 R42-C | 撞车? |
|---|---|---|---|
| check-prod-secrets-inlined.sh | 124 行精准脚本 | 不动 | ✅ 0 |
| check-prod-secrets-inlined.yml | 46 行 workflow | 不动 | ✅ 0 |
| gitleaks.yml workflow | 不动 | 不动 | ✅ 0 |
| .gitleaks.toml | 不存在 | 80 行新增 | ✅ 互补 |
| log.md | 已 R41 卡段 | append R42-C 段 | ✅ 主协调会话串行写 |

---

## 10. 自证能红 + 还原方法

- **自证**:本地 `gitleaks detect --config .gitleaks.toml --source ...yml` 命中 3 处真硬编码
- **还原**:`rm .gitleaks.toml && git revert <commit>` 即可(1 秒撤销)
- **撞车风险**:0(仅新增 .gitleaks.toml,不动任何兄弟文件)