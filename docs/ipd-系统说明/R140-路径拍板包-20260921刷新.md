# R140 路径拍板包（2026-09-21 刷新版）

**日期**：2026-09-21
**依据**：R140-P0P1P2-完整拍板包-20260920.md（1 天前）
**fresh 核对**：撞车窗口 + P141 commit 状态 + 端口 + 真库 + 测试债
**目标**：给 owner 提供 R140 路径 A/B/C 决策的最新事实

---

## 1. fresh 核对（2026-09-21 实测）

| 项 | R140 包 9/20 数据 | 9/21 实测 | 变化 |
|---|---|---|---|
| 兄弟会话 wt 在线数 | 11 | **11**（R120/R121/R126/R127/P141）| 无 |
| P141 4 commit HEAD | 916b04ee/96c78aca/c1a0559c/3f395d24 | **同** | 无 |
| P141 ahead / behind main | 1 / 27-28 | **1 / 27-28** | 无 |
| 后端 16039 PID 79728 监听 | 是 | **是（java PID 79728）** | 兄弟会话 |
| 前端 15666 PID 17021 监听 | 是 | **是（Python）** | 兄弟会话 |
| 真库 13306 可用 | OK | **OK** | 无 |

**撞车窗口状态**：未到（11 个 wt 在线）+ 后端/前端端口被占 + P141 4 commit 仍 ahead 1 / behind 27

---

## 2. P141 4 commit rebase 评估（R155 新做，4 wt 隔离）

### 2.1 评估流程
新建 4 个 wt `wt-r155-eval-*`（从 fix/p141-* branch HEAD）→ `git rebase origin/main` → 编译 + 单测

### 2.2 评估结果汇总

| # | paiban | rebase 冲突 | 编译 | 单测 | 结论 |
|---|---|---|---|---|---|
| 1 | paiban-07 Mapper @Mapper 注解 | **0** | BUILD SUCCESS | 2243 PASS / 1 FAIL（main baseline）| ✅ 可合入 |
| 2 | paiban-08 IllegalArgumentException → IpdBusinessException | **0** | BUILD SUCCESS | 2266 跑 / 3 FAIL（**2 个归因 P141 断言未同步 + 1 main baseline**）| ⚠️ 需修 2 测试（**R155 已修**，见 §3）|
| 3 | paiban-10 scripts kebab-case | **0** | bash -n 100% | 0 snake_case 残留（main baseline 已 0）| ✅ 可合入（echo log 标签字符串 3 处微遗留）|
| 4 | paiban-09 类级 @Transactional 下沉到 public 方法 | **0** | BUILD SUCCESS | 2266 跑 / 1 FAIL（main baseline）| ✅ 可合入（与 paiban-05 接口化兼容）|

### 2.3 真实发现

1. **paiban-08 留 2 个测试断言债**（R141 docs-only 提交时未跑测试）
   - `P182AcceptanceTest.algoTypeValidation`：断言期望 `ServiceException`，新代码抛 `IpdBusinessException`
   - `AuditHashChainTest.canonicalByVersionRouting`：同上
2. **paiban-10 留 3 处 echo log 标签字符串**（非功能引用，不影响 lint/CI/Java 测试）
   - `check-doc-line-refs.sh:48 [check_doc_line_refs]`
   - `check-demo-default-off.sh:22 [check_demo_default_off]`
   - `check-ddl-idempotent.sh:14 [check_ddl_idempotent]`
3. **main baseline 债**：`ServiceBareClockGuardTest` 报 `RecoveryWarningService` 有 `LocalDate.now()` 裸时钟（R149 batch2a 引入时漏登记白名单）

---

## 3. R155 已闭环

### 3.1 paiban-08 2 测试断言修复 ✅
- 工作 wt：`/tmp/wt-r155-eval-exception`
- commit：`14af60f2`
- 改动：`+4 -2`（2 测试文件）
- 验证：2 目标测试 13/13 PASS + 全量 2266 跑 / 1 FAIL（**与 P141 无关的 main baseline 债**）

### 3.2 4 路评估报告落档
- `/tmp/r155-p141-mapper-eval.md`（38 行）
- `/tmp/r155-p141-exception-eval.md`（41 行）
- `/tmp/r155-p141-scriptname-eval.md`（38 行）
- `/tmp/r155-p141-transactional-eval.md`（47 行）
- `/tmp/r155-p141-exception-testfix.md`（46 行）

---

## 4. R140 路径拍板（A/B/C 三选一）

### 路径 A：撞车窗口到达后开 R140 集中治理轮（推荐）
1. **R140-D1（docs-only）**：✅ **已完成**（R140 拍板包 + 本刷新版）
2. **R140-D2（worktree 隔离 P1 第一批）**：**paiban-07/08/09/10 已实装，paiban-08 2 测试断言已修**
3. **R140-D3（owner 拍板后 P0）**：阻塞（kpi_rules 方案 A/B/C + 后端真活 E2E）
4. **R140-D4（DDL apply by SRE）**：阻塞 SRE
5. **R140-D5（架构偏离）**：阻塞 owner 拍板（最大破坏 16 hr）

### 路径 B：**现在就拍板合入 P141 4 commit（推荐）**
- 4 commit 已实装 + 2 测试断言已修 + 0 冲突
- 撞车 0 严守（不动兄弟会话 wt）
- 工作量：≤30 min（4 cherry-pick / rebase + 合入 + 跑全量单测 + push）
- **剩余 micro-debt**：3 处 echo log 标签字符串（脚本 micro PR，不阻断）
- **剩余 main baseline 债**：RecoveryWarningService 裸时钟（与 P141 无关，另起 ticket）

### 路径 C：owner 不拍，全等时间触发
- 等 2026-09-27 D+7 B 类自动 sign-off
- 等 2026-10-04 D+14 C 类重审
- **风险**：撞车窗口可能在这期间出现更多 P141-style 未合入 commit

---

## 5. 推荐决策：**路径 B**

### 5.1 理由
1. **P141 4 commit 完备**：实装 + 测试 + 2 修测试 + 0 冲突 = 可立即合入
2. **撞车窗口未到**：但 R155 eval 4 wt 是新建 wt，不动兄弟会话 wt，撞车 0 严守不被破
3. **后续 R140 路径 A/D3/D5 阻塞 owner/sre**：先合入已就绪的 P141 解锁一类
4. **风险最低**：仅 micro-debt（echo log 3 处）+ main baseline 债（与 P141 无关），不阻断合入

### 5.2 执行步骤（owner 一声令下即可）
1. 主协调会话 cd main 工作树
2. `git fetch origin`
3. `git merge --no-ff fix/p141-mapper-annotation -m "merge: paiban-07 Mapper @Mapper 注解"`
4. `git merge --no-ff fix/p141-exception-rename -m "merge: paiban-08 IllegalArgumentException → IpdBusinessException"`
5. `git merge --no-ff fix/p141-script-naming -m "merge: paiban-10 scripts kebab-case"`
6. `git merge --no-ff fix/p141-transactional-downgrade -m "merge: paiban-09 类级 @Transactional 下沉到 public 方法"`
7. 跑全量 mvn 单测 → 期望 2266 跑 / 1 FAIL（main baseline）
8. `git push origin main`
9. 双仓同步 R156 收口报告

### 5.3 R156 启动建议
- 路径 B 合入后 = R156 起点
- 路径 A/D3/D5 仍是 owner 必拍（kpi_rules 表方案 + 后端真活 E2E + DDL apply + 架构偏离）
- 后端实装 10 处真缺端点（仍需 owner 拍板工作优先级）

---

## 6. 边界严守

- ✅ 仅 docs 改动 + R155 eval 4 wt 内操作（新建 wt 不动兄弟会话）
- ✅ 不 push
- ✅ 不擅自动 DDL
- ✅ 不动业务代码（仅 R155 eval-exception 改 2 测试断言 + 落档 docs）
- ✅ 兄弟会话 11 wt 全部未触碰

---

## 7. owner 拍板

| 选项 | 含义 | 工作量 | 风险 |
|---|---|---|---|
| A | 撞车窗口后集中治理（P141 暂不合入）| ≥5hr 至今 | P141 长期未合入 |
| **B（推荐）** | **现在合入 P141 4 commit + 2 测试修复** | **≤30 min** | **micro-debt + main baseline 债另起** |
| C | 全不拍，等 9/27 时间触发 | 0 | 后续 P141-style commit 累积 |