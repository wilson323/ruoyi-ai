# R215 · 全局闭环梳理·增量验证轮（2026-09-24）

> **写给谁来用**：owner 全局视图 / 兄弟会话对齐基线 / R212 遗留清单的销账记录
>
> **定位**：用户本轮请求与 R212 的原始请求同源同文。现查发现兄弟会话已于同日完成 R212（全局矩阵）/ R213（拍板执行）/ R214（纳管轮），**全量功能清单、业务逻辑、L1/L2/L3 逐条矩阵、测试计划与待办计划以那三份报告为准**（引用链见 §六），本会话按撞号避让协议取号 **R215**，只做增量：fresh 漂移复核 + 补跑 R212 明确标记「🕐 未执行」的 T-V2 四角色可见性矩阵 + 新缺口定性与探针实证。
>
> **复核时间**：2026-09-25T03:39~03:49Z（本机 09-24 20:38~20:49）。
>
> **撞车纪律**：docs + 只读探针 + 安全负向探针（全部 403/404 无写库，DB 回读自证见 §四）；未动 Java/Vue 业务代码；真库唯一写操作 = market 失效账号的 hash 对拷修复（登记 N4）；看板按 §五 建卡。
>
> **证据目录**：`验收/R215-增量验证-20260924/`（基线 txt、契约 json、6 份探针响应、3 张截图）。

<!-- round-evidence
{"round":"R215","claims":[
 {"key":"be_head","value":"48f23bcb","file":"验收/R215-增量验证-20260924/verify-baseline-r215.txt","kv":"BE_HEAD","at":{"kv":"R215_VERIFY_AT"}},
 {"key":"fe_head","value":"3316e56","file":"验收/R215-增量验证-20260924/verify-baseline-r215.txt","kv":"FE_HEAD","at":{"kv":"R215_VERIFY_AT"}},
 {"key":"orphan_now","value":96,"file":"验收/R215-增量验证-20260924/contract-verify-r215.json","json":"summary.orphan_endpoints_count","at":{"json":"scan.timestamp"}},
 {"key":"field_mismatch","value":15,"file":"验收/R215-增量验证-20260924/contract-verify-r215.json","json":"summary.field_mismatches_count","at":{"json":"scan.timestamp"}},
 {"key":"kanban_total","value":495,"file":"验收/R215-增量验证-20260924/verify-baseline-r215.txt","kv":"KANBAN_TOTAL","note":"建卡前 fresh 基线（todo 32/done 382/inprogress 17/inreview 5/cancelled 59）；本轮收口后 497","at":{"kv":"R215_VERIFY_AT"}}
]}
-->

---

## 〇、fresh 基线与 R212 diff

| 事实源 | R212 记录 | R215 现查 | 差异定性 |
|---|---|---|---|
| 后端 HEAD | `7868a26a`→`523e4670` | <!-- ev:be_head -->`48f23bcb`（= origin/main）| 兄弟 R213/R214 推进，正常前进非漂移 |
| 前端 HEAD | `a0f0b48` | <!-- ev:fe_head -->`3316e56` | 含奖金池双下拉同步修复 + permissionCodes 接通（未推 remote，remote 为 integrated 本地路径）|
| 门禁孤儿端点 | 98 | <!-- ev:orphan_now -->**96** | 净消亡 2（R214 门禁语义修正/cancelled 计终态），无新增 |
| 字段错位 | 15 | <!-- ev:field_mismatch -->**15** | 持平 |
| 五连无 token 探针 | `401 401 404 401 404` | 同序列吻合（R212-1 复核）| 鉴权面稳定 |
| 看板 | **477**（367/24/25/4/57）| <!-- ev:kanban_total -->**495**（done 382/todo 32/inprogress 17/inreview 5/cancelled 59）| +18：R213-B 建 13 张 ORPHAN-A 子卡 + R214 纳管卡；本轮收口后再 +2 达 497 |
| Redis | 6379 DOWN（环境未起）| **6379 DOWN、16379 UP**（IPD 隔离实例）| 本会话前置任务完成基础服务隔离：redis.conf 16379 + start-16039.sh CLI 覆盖 `--spring.data.redis.port=16379`；后端日志 `connections initialized for 127.0.0.1:16379`，系统 6379 不再被 IPD 占用 |
| bonus_pools | 19（1 悬空 9140004）| 19（探针后复查仍 19，悬空未动）| 卡 `5d5c4fcc` 继续挂 todo |

**R215 轮内撞号透明登记（ORIGIN- 语义）**：收口时现查发现并行兄弟会话同日也在以 R215 取号执行同主题任务（其证据目录 `验收/R215-增量收口-20260924/` 含 tv2 逐页遍历截图 20:29~20:53 仍在写入，本会话未触碰未 stage）。两执行体产物各自独立：本报告 + `验收/R215-增量验证-20260924/`（探针/基线/菜单截图）为本会话全部入库物；双方事实互补不互否，后续轮次以 commit 号区分归属。本会话看板两张卡 `2a02dffd`/`e48bf56a` 已 GET 回读自证。

---

## 一、奖金池双下拉同步修复 · 浏览器实证（销 R211c 卡 8e751acf 第 4 项观感）

admin 登录 → 顶栏切 VIS-RD-100 项目 → 奖金池核算页表单 ant Select **自动同步**且「触发核算/刷新列表」两按钮激活 → 点刷新列表出真数据行：`2097189624253313025 | 9140003 | ¥1,000,000 | ¥50,000 | 已确认 | 分配`。截图 `r215-bonus-sync-fixed.png`。前端 `3316e56` 的 `syncGlobalProject()` + `PROJECT_SYNC_EVENT` watch 机制实测生效。

## 二、T-V2 四角色用户可见性矩阵（R212 遗留「🕐 未执行」，本轮补齐）

方法：登录页快捷账号逐角色登录（market 密码失效修复见 N4），采集侧栏全展开菜单集合 + 奖金池页按钮态 + 后端探针（不存在的池 id=999999999，安全幂等）。

| 维度 | SUPER_ADMIN | GROUP_LEADER | MARKET_PM | RD_PM |
|---|---|---|---|---|
| 登录 | ✅ | ✅ | ✅（修复后）| ✅ |
| 侧栏 IPD 菜单 | 13 项全见 | 13 项全见 | 13 项全见 | 13 项全见 |
| 「超级管理」组（节点/编排/人员同步等）| ✅ 可见 | **隐藏** ✅ | **隐藏** ✅ | **隐藏** ✅ |
| 框架菜单 | 全量（51 项含菜单/部门/字典等）| 知识管理+**用户管理+角色管理** | 同 leader | 同 leader |
| 「产品目录」| ✅ | ✅ | ✗ | ✗ |
| 奖金池页「触发核算/冻结/分配」按钮 | ✅ | ✅ 可见（无前端闸，见 N2）| ✅ 可见（无前端闸）| ✅ 可见（无前端闸）|
| POST compute 探针 | （未测，admin 有码）| — | **403/30001** ✅ | — |
| POST freeze 探针 | 404/50001（权限过、查无此池）✅ | **403/30001** ⚠️见N1 | **403/30001** ✅ | **403/30001** ✅ |
| POST distribute 探针 | — | — | **403/30001** ✅ | — |
| 数据权限（GET /projects）| 全量 | 范围内 | **45→1**（仅成员项目 PRJ-ACC-G5）✅ | 范围内 |

探针响应原文归档：`r215-mkt-{freeze,distribute,compute}.json`、`r215-ipd-{admin,leader,rd}-fz.json`。截图：`r215-tv2-leader-menu.png`、`r215-tv2-rd-menu.png`。

**总判**：后端鉴权闸（`@SaCheckPermission` + `requireAdmin` 兜底）实测有效、403 与 404 可区分（admin 穿透到 service 层）；数据权限收窄生效；「超级管理」菜单正确隐藏。T-V2 由 R212 的 🕐 翻 ✅（带下列保留项）。

## 三、新发现缺口定性（本轮增量审计产出）

| # | 发现 | 证据 | 分级 | 处置 |
|---|---|---|---|---|
| N1 | **BonusPoolController javadoc 与 IpdRolePermissionCatalog 授权漂移**：注释称 compute/query/preview 给四角色、freeze/distribute 给 GROUP_LEADER/SUPER_ADMIN（"涉钱审批"），但 catalog 实测三写码全在 `ADMIN_WRITE`（仅 SUPER_ADMIN，注释自述"注解即终审"）。leader 是业务上最合理的冻结人却 403 | `IpdPermissionCode.java` catalog :158-168 + `IpdRolePermissionCatalog.java` :181-186 BY_ROLE + leader 403 探针 | P2（文档失真）+ **owner 决策项**：freeze/distribute 是否放开 GROUP_LEADER（产品文档 §三.2 未明示操作人） | javadoc 勘误可直接修；授权口径建卡待 owner 拍板 |
| N2 | 前端奖金池页「触发核算/冻结/分配」按钮**无权限码闸**（仅按 record.status 显隐），market/leader/rd 均可见必 403 的死按钮 | `incentive/bonus-pool/index.vue` :319-366 无 hasPermission/v-access；market 403 探针 | P2 | 用 R185 已建的 `ipd-guard.ts hasAccess()` 接闸；建卡 todo |
| N3 | 内部三角色侧栏均显示框架「用户管理/角色管理」菜单（admin 专属域外露；页面可达性/接口闸未验证） | 三角色菜单采集 + leader 56 码中含用户管理入口渲染 | P2 待验证 | 建卡：实测非授权角色访问 `/system/user` 是否 403，再定收敛方式 |
| N4 | **凭证多事实源漂移实证**：ipd-market 密码在 `auth-market-current.json` 与 `credentials.json` 两登记源均失真（历史 e2e-audit 轮改密未回登），API 登录 10001。已走真库 `UPDATE persons` hash 对拷（rd→market）恢复并回登两源（含 origin_note） | 修复过程 MARKET_PW_RESTORED + 复登 code=0 tok_len=187 | P2（五病根之⑤"多事实源无对账"活例）| 已修复+登记；建议凭证源收敛为 auth-*-current.json 单一权威（待 owner 认可后可并入 N2 卡或单独治理卡）|
| N5 | 基础服务隔离完成（IPD 专用 Redis 16379 + MySQL 13306，系统 6379/3306 脱钩），启动链固化为 `base-services.sh` → `start-16039.sh` | 端口矩阵 §〇 + 后端连接日志 | 环境事实 | 已生效，无需建卡 |

## 四、高风险结论 DB 回读核验

探针全部使用不存在 id（999999999）且预期 403，真库零写入自证：`bonus_pools` COUNT 前 19 后 **19**、`id=999999999` **0 行**、`audit_logs` 无 `r215-probe` 痕迹（查询语句与结果见会话，SQL 只读）。悬空池 9140004 维持原状（卡 `5d5c4fcc` 治理中）。

## 五、看板增量（fresh 后建，PUT 后 GET 回读）

1. **R215 轮记录卡**：done（本轮全部实证与报告落盘）。
2. **奖金池权限闸三合一卡**（todo）：N1 javadoc 勘误 + N2 前端 hasAccess 接闸 + owner 拍板 freeze/distribute 授权口径；关联 ORPHAN-A 体系与 R212 卡 `15d5e689`。
3. N3 框架菜单外露核查并入卡 2 注记（同属"前端可见性闸"主题，不单开避免碎卡）。

## 六、五大交付 → 报告引用链（不重复建矩阵）

| 用户交付 | 权威载体 | R215 增量 |
|---|---|---|
| ① 全量功能清单（模块/页面/接口/表/状态）| R211 产品域清单 + `R212-全量对账明细矩阵-20260924.md` L1/L2/L3 | 孤儿 98→96 现查、双下拉/权限码接通两修复实证 |
| ② 完整业务逻辑（链路/Gate/状态机）| R211/R212 §二 + `docs/开发说明/**`（G-04 圣经）+ BonusPoolService javadoc 链 | N1 暴露注释与授权目录漂移点（唯一新失真）|
| ③ 一致性缺口审计（P0-P3）| R212 §三（P0 无新增；P1=ORPHAN-A×13 + landed 契约缺陷 `0ddd9f67`）| N1-N3 三条 P2 增量 |
| ④ 测试计划（单元/集成/可见性/E2E）| R212 §四回填表 | T-V2 🕐→✅（§二矩阵）；E2E 全链与单测重跑仍挂 🕐（需 owner 排窗口，环境已具备：后端 16039/前端 15666/隔离 Redis/MySQL 全 UP）|
| ⑤ 全量待办矩阵 | 看板 495 卡（开放 54：todo 32 + inprogress 17 + inreview 5）+ R212 §五波次 | 新增卡见 §五 |

**收口状态**：PARTIAL→已验证项均附证据；遗留两🕐（E2E 端到端、2239 单测全量重跑）依赖人工窗口，不属于本轮 docs+探针能力边界，如实挂账不伪完成。

---

## 六、当日深夜追加轮（owner 开窗，两笔 🕐 全部销账）

<!-- round-evidence
{"round":"R215-appendix","at":"2026-09-25T04:20Z","claims":[
 {"key":"unit_rerun","value":"2419/0/0/23 SUCCESS","file":"验收/R215-增量验证-20260924/e2e-appendix-r215.txt","kv":"结果"},
 {"key":"n6","value":"selectChain 500 修复","file":"验收/R215-增量验证-20260924/e2e-appendix-r215.txt","kv":"== 4."},
 {"key":"kanban_total_appendix","value":504,"file":"验收/R215-增量验证-20260924/e2e-appendix-r215.txt","kv":"KANBAN_TOTAL"}
]
} -->

| 项 | 结果 |
|---|---|
| 单测全量重跑（ev:unit_rerun） | `mvn -o -pl ruoyi-modules/ruoyi-ipd test` → 2419 跑/0 失败/0 错/23 跳，BUILD SUCCESS（2239 为 R212 旧快照，自然增长） |
| HTTP E2E（gov6 脚本复跑） | 读腿 6/10 200（3 个 404 为脚本旧路径非应用缺陷）；写腿 ai-documents 登记+sha 比对+DB 回读全对 |
| ★ 新缺陷 N6（ev:n6） | `GET /ai-documents/{id}/versions` 全量 500——PERF-02(40a84615) 的 chain CTE anchor 从 4 列 ancestors 取 26 列必现 1054。已修（anchor 回 JOIN 原表）+真库三场景实证+编译绿；**runtime 已同会话续跑销账**：重打 fat jar（删旧防 maven 跳过，产物字节自证含修复）→ 重启 16039 → 实测 200 + e2e-http.py Phase B verdict=PASS（证据 §6，卡 f962ad81 已翻 done） |
| WS 通知链路 E2E | 造行→RPUSH(16379)→async-dispatch `sent:1`→DB 翻 SENT→清理测试行；脚本修 3 处环境漂移（charset/Redis 端口/密码硬编码） |
| 浏览器用户可见主链路 | 超管登录→工作台→游客门户提交（下拉联动带型号）→DB 落库 SUBMITTED/PORTAL_GUEST→track 页脱敏回查（E\*\*\*\*\*\*\*\*\*）→管理端需求池卡片可见+双 PM 已路由+开始分析可用——需求域四视图闭环 |
| 凭证新实证 | bootstrap-accounts.json 已治理移除明文，旧脚本凭据源失效——N4 同族问题在工具脚本层的延伸，两处本地脚本已改指 auth-*-current.json |

看板（ev:kanban_total_appendix）：本追加轮建 `6650b179`（done）+ `f962ad81`（N6，runtime 销账后已翻 done）；fresh 回读总数 504（含兄弟会话并行建卡增量）。
