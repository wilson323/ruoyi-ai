# R139 scripts/ 硬编码绝对路径修复（2026-09-20）

> **承接**：R128 §一 P1-17「4 脚本硬编码绝对路径改自解析」（0.5h 预估）
> **落地 commit**：`57d046be`（与「R139 清 lint-reports stale」捎带合并）
> **同步状态**：✅ origin/main 已同步（HEAD == origin/main）
> **撞车 0 严守**：✅ 只动 `scripts/*.sh`，未碰 Java/Vue/SQL/yml/.claude/hooks/、.git/hooks/

## 实际范围

R128 表格登记 4 个（工作量估算），§路径登记 9 个（事实），实际 `grep -lE '/Users/mac/Documents/ruoyi-ai' scripts/*.sh` 命中 **12 个**（后续新增 6 个跨仓检测脚本），**全部修复**。

## 12 脚本修复表

| # | 脚本 | 修复形式 |
|---|---|---|
| 1 | check-ssot-drift.sh | `REPO="..."` → `REPO="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"` |
| 2-9 | check_cross_repo_contract.sh / check_deletion_consistency.sh / check_doc_link.sh / check_duplicate_ssot.sh / check_dynamic_loadable.sh / check_entity_complete.sh / check_i18n_unused_keys.sh / check_tenant_excludes_apply.sh | `${BACKEND_ROOT:-/Users/...}` → `${BACKEND_ROOT:-$(...)}` |
| 10 | r35-merge-gate.sh | `MAIN_REPO` 同上 |
| 11 | r35-migrate-prod-secrets.sh | 拆为 `DEFAULT_PROD_YML` + `${PROD_YML:-$DEFAULT_PROD_YML}` |
| 12 | scan_dead_code.sh | `BACKEND_ROOT` 同 #2 |

## 验证 + 未修

- 验证：`bash -n` 12/12 PASS；自解析 smoke 12/12 PASS；`grep` 命中 0
- 未修：跨仓路径 `FRONTEND_ROOT`/`DOCS_ROOT`——`dirname` 对跨仓无效且不在 P1 #17 范围，保持 `${X:-...}` ENV 覆盖模式
