# R206 5 项阻塞拍板请示材料包（2026-09-24）

> **写给谁来用**：owner / OPS-09 / 业务 owner / DevOps / DBA 来了一签字就能推
>
> **目的**：把上一轮反问的 5 项阻塞全部打包成可签字的请示材料，撞车 0 严守 docs-only 落档
>
> **撞车 0**：本盘仅 docs/ipd-系统说明/ 落档，**不动 Java/Vue/SQL/yml/真库/端口/PID**

---

## 第 1 项 — owner 在场翻卡

**现状**：R198 §A5 已 docs-only 落档拍板矩阵（5 项 × 3 选项 + 推荐 4A+1B），R200 §A6 已落档 12 张汇总卡 4 步法操作模板。owner 来后按 R200 §A6 4 步法（GET 复核 → 标派单 → PUT → GET 回读核验）逐张翻 status。

**请示材料**：
- R200 §A6 4 步法模板（已落档）：`R200-§A6-看板12张汇总卡4步法操作模板-20260924.md`
- 12 张汇总卡清单 + 翻卡顺序（已落档）
- 翻卡后验证剧本（GET 回读核验）

**验证剧本**：
```bash
# 翻卡后立即 GET 回读核验 desc_len + marker content
curl -s http://127.0.0.1:62250/api/tasks/<task_id> -H 'project_id: 01dcf15c-86bb-4c7b-957c-8fe44bddd10d' | jq .data.status, .data.desc
```

**owner 在场要求**：必须真人到场，主协调不替翻 status。

---

## 第 2 项 — OPS-09 拍板动 Java

**现状**：R198 §A5 5 项中 R198-2 RequirementPool Entity（推荐 B = 改对齐真库）+ R198-4 KPI_REVISION_MODE（备选 B = 保留 + test 移除引用）需动 Java。撞车 0 严守下主协调不能动 Java。

**请示材料**：
- R198 §A5 拍板矩阵：`R198-§A5-R25-owner-blocked拍板矩阵-20260924.md`
- R198-2 改造方案：删 `@TableName("requirement_pool")` 对齐真库 `requirement_pool_v2` 表（DDL 已 apply）+ Mapper 同步改
- R198-4 改造方案：保留常量 + test 移除 `KPI_REVISION_MODE` import + 注释标注「保留作历史快照」

**OPS-09 在场要求**：必须由 OPS-09 单写入者拍板，主协调不替改 Java。

**撞车 0 兑现**：Java 改动在 OPS-09 授权后单 worktree 错峰执行，主工作区 git status 不污染。

---

## 第 3 项 — 业务 owner 拍 KPI 边界

**现状**：R201 §A7 #9 P0-2 KPI 文档化 = 主协调能力边界外，需业务 owner 拍「KPI 计算公式 + 阈值 + 异动审批链」。

**请示材料**：
- R201 §A7 拍板矩阵：`R201-§A7-拍板矩阵-20260924.md`
- KPI 计算字段草案（`kpi_records` 表 `score` / `weight` / `actual` / `target`）
- 异动审批链草案（KPI 异动 ≥ ±10% 触发二次审批）

**业务 owner 在场要求**：需业务 owner 答复 3 件事：
1. KPI 计算公式边界（加减乘除权重）
2. 异动阈值（绝对值 ±% 还是 ±分）
3. 异动审批链（PM 自审 / PMO 复审 / 业务 owner 终审）

**主协调备好**：答复后主协调立刻 docs-only 落档 KPI 边界文档（撞车 0 docs-only）。

---

## 第 4 项 — DevOps + 前端 owner 拍 docker

**现状**：R203 docker-compose 1XXXX 端口前缀调研已 docs-only 落档（端口前缀 1XXXX 约定 + 三套 compose 拆分 + 4 个 CI yml 补齐）。但「实装方向」= 阻塞 DevOps + 前端 owner 拍端口前缀 + compose 拆分边界。

**请示材料**：
- R203 调研：`R203-docker-compose-1XXXX端口前缀调研-20260924.md`
- 三套 compose 拆分草案（dev=13306/staging=15306/prod=17306）
- 4 个 CI yml 补齐清单（backend-ipd / backend-system / frontend-antd / e2e）

**DevOps + 前端 owner 在场要求**：需 2 人联合拍板：
1. 端口前缀 1XXXX 约定（dev/staging/prod 端口段）
2. compose 拆分边界（后端 / 前端 / DB / Redis / MinIO）
3. CI yml 部署顺序（4 个 yml 触发链）

**主协调备好**：拍板后主协调立刻 docs-only 落档 docker 实装方案（撞车 0 docs-only）。

---

## 第 5 项 — DBA 排维护窗口

**现状**：R201 §A7 #12 DBA 窗口合并推荐 A（2-3h 一刀） + R201 §A7 #13 DBA Redis 部署（dev=16379/staging=16380/prod=16390）。撞车 0 严守下主协调不动真库 + 不部署 Redis。

**请示材料**：
- R205 Redis 飞手计划：`R205-Redis多实例切流飞手计划-20260924.md`（4h DBA 单方行动）
- R194 §A7 §七 7.2 persons 字符集遗留（P3-LOW，1h）
- 维护窗口合并 4h：Redis 部署 1h + persons 字符集 1h + 验证 + 联调 2h

**DBA 在场要求**：需 DBA 拍板：
1. dev/staging/prod 维护窗口时间（推荐周末凌晨）
2. Redis 三套部署顺序（dev → staging → prod）
3. 回滚路径（切换前 git revert + 重启应用）

**主协调备好**：DBA 部署后主协调立刻 docs-only 落档部署回执（撞车 0 docs-only）。

---

## 第 6 项 — 收口建议（主协调内部）

**当前撞车 0 严守累计 13 轮**。docs-only 能做的全做完了。

**5 项阻塞全部 docs-only 备齐请示材料**——owner / OPS-09 / 业务 owner / DevOps / DBA 任一在场拍板，主协调立刻执行对应 docs-only 收尾。

**撞车 0 兑现**：本盘仅 docs 落档请示材料包，不动 Java/Vue/SQL/yml/真库/端口/PID。

**下一步**：等任一项阻塞拍板 → 主协调 docs-only 落档执行回执（撞车 0 严守）→ 进入 R207+ 实装阶段（撞车 0 严守外）。

---

## 撞号透明登记

- **已 push** `e0544be4 R205 Redis 切流飞手计划`（同周）
- **本盘** R206 = 5 项阻塞拍板请示材料包
- 按「撞号透明协议」+ R202 阶段 1 docs-only 收口，主题可分辨，与 BCP §四十三 R205 并列