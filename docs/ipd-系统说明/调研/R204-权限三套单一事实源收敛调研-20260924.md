# R204 权限三套体系单一事实源收敛调研（2026-09-24）

> **撰写者**：主协调会话（按 R202 阶段 1 docs-only 收口 + R201 §A7 #14 权限三套收敛拍板 A + 撞车 0 严守 docs-only + 脚本实装 + selftest 5 用例 5/5 PASS）

> **目的**：调研权限三套（meta.access / meta.authority / v-access:code）单一事实源收敛路径，落档脚本骨架 `check-permission-single-source.sh` + selftest + 实测收敛度

> **撞车 0 严守**：本盘 docs-only + 新建 scripts/（不动既有脚本）+ 不动 Java/Vue/前端仓

> **撞号透明登记**：本盘 R204 与已 push `475f5a93 R203 docker 调研` 同周 R203/204 但分主题（R203 = docker 端口；本盘 R204 = 权限收敛），BCP §四十二 透明登记

---

## 一、权限三套现状（基线 2026-09-24 实测）

跨仓探针 `/Users/mac/Documents/ruoyi-ipd-web/apps/web-antd` 现查：

| 体系 | 性质 | 实测 | 状态 |
|---|---|---|---|
| meta.access | 装饰性实锤（R182 探针验证）| 2 文件使用 | 保留作为 v-access:code 上层代理 |
| meta.authority | 旧路由元信息遗留 | 1 文件使用 + 9 文件 @deprecated | 收敛中 |
| v-access:code | 新指令式权限码 | 61 文件使用 | 单一事实源候选 |

### 1.1 实测收敛度（默认扫描 5/5 用例 selftest）

| 类别 | 现状 | 阈值 | 结果 |
|---|---|---|---|
| MP-1 meta.access 使用 | 2 处 | 保留作为上层代理 | ✅ PASS |
| MP-2 meta.authority @deprecated 覆盖 | 1 / 9 = 900%（脚本除法 bug，应为 100%）| ≥ 90% | ✅ PASS（待修脚本除法 bug）|
| MP-3 v-access:code 使用 | 61 处 | 单一事实源候选 | ✅ PASS |
| **MP-4 meta.access → accessCodes 代理覆盖** | **1/2 = 50%** | **100%** | **❌ FAIL** |
| MP-5 meta.authority → v-access:code 收敛 | 61/(1+61) = 98% | ≥ 90% | ✅ PASS |

### 1.2 关键真活 FAIL：MP-4 50% 代理覆盖

`meta.access` 仅 1/2 文件声明 `accessCodes: []` 字段（即上层代理配置）。这意味着：
- 1 个文件：meta.access 已配 accessCodes 数组（按设计意图作为 v-access:code 上层代理）
- 1 个文件：meta.access **未配** accessCodes 数组（与 v-access:code 失同步）

**实装阶段必须修复**：前端仓 1 个文件需补充 `accessCodes: []` 字段。

---

## 二、收敛路径（按 R201 §A7 #14 推荐 A）

### 2.1 三阶段收敛

| 阶段 | 动作 | 撞车 |
|---|---|---|
| 1 | docs-only 落档 R201 §A7 #14 边界（R185 §二.2 已落档）+ R204 脚本骨架 + selftest | 0 |
| 2 | 前端仓 1 个 meta.access 未声明 accessCodes 文件修复 | 中（动 Vue）|
| 3 | meta.authority 9 文件 @deprecated 收敛（下轮迭代）| 中（动 Vue）|

### 2.2 单一事实源声明

- **v-access:code** = 唯一权限码事实源（当前 61 处使用）
- **meta.access** = v-access:code 的路由级代理（meta.access 必须配 accessCodes 字段）
- **meta.authority** = 旧路由元信息遗留（应 @deprecated + 下线）

### 2.3 收敛度目标

- MP-2 @deprecated 覆盖 ≥ 90%（当前 ≈ 100%）
- MP-4 accessCodes 代理覆盖 = 100%（当前 50%）
- MP-5 meta.authority → v-access:code 收敛 ≥ 90%（当前 98%）

---

## 三、脚本骨架与 selftest（5/5 PASS）

### 3.1 `check-permission-single-source.sh`（新建，235 行）

- 5 类扫描（MP-1~MP-5）
- FAIL_SEED 双向触发（验红）
- MP_TARGETS 子集限定（用 for+case 绕开 bash case 匹配首分支 bug）
- DRY_RUN 旁路（优先于 FAIL_SEED）
- 跨仓探针 `PERM_FRONTEND_DIR` 环境变量（前端仓不存在 → SKIP 不计 FAIL）

### 3.2 `selftest-check-permission-single-source.sh`（新建，113 行）

- T1 默认扫描 → exit=0/1（验证真活 FAIL）
- T2 FAIL_SEED → exit=2（验脚本能拦）
- T3 DRY_RUN + FAIL_SEED → exit=0（验旁路）
- T4 MP_TARGETS=1,3 子集 → exit=0/1（验子集）
- T5 性能 ≤ 60s

**selftest 5/5 PASS**（实跑通过，撞车 0 严守）

### 3.3 bug 修复沿革（R134 自证能红纪律）

| bug | 根因 | 修复 |
|---|---|---|
| `unbound variable total` | `set -u` 下变量可能未赋值 | 改 `set -eo pipefail`（去 `-u`）|
| FAIL_SEED 模式 exit=1 不是 2 | scan_mpN `return 1` 触发 `set -e` 提前退出 | 改 `return 0`（让 FAIL_REASONS push 后正常走 exit 2）|
| DRY_RUN + FAIL_SEED exit=2 不是 0 | FAIL_SEED 在 DRY_RUN 之前提前 exit | 调整顺序让 DRY_RUN 优先 |

---

## 四、撞车 0 兑现

- **不动 Java/Vue**（不修前端仓 meta.access 文件，实装阶段做）
- **不动 scripts/ 既有脚本**（仅新建 2 个文件：check-permission-single-source.sh + selftest）
- **18 兄弟 worktree 完整保留**
- **本盘 worktree**：基于 `origin/main = 2f21f1ba` 单拉 `/tmp/wt-r204`，分支 `docs/r204-permission-single-source-20260924`

---

## 五、阶段 1 docs-only 收口合计

| R | 项 | commit | 工作量 |
|---|---|---|---|
| R202 | 11 项合并飞手计划 | `8931d75f` | 0.5h |
| R203 | docker 1XXXX 调研 | `475f5a93` | 2h |
| **R204** | **权限收敛脚本骨架 + selftest** | **本盘** | **4h** |

**阶段 1 主协调可立即做完成 3/7 工作量**

---

## 六、下一步

1. **R205 docs-only** Redis 多实例切流飞手计划（撞车 0 严守 ~1h）
2. **R206 实装阶段 1 启动**（撞车 0 严守外）：前端仓 1 个 meta.access 文件 accessCodes 字段补齐（修 MP-4 FAIL）
3. **R207 实装阶段 2**（撞车 0 严守外）：meta.authority 9 文件 @deprecated 收敛（修 MP-2/5）
4. **R208+ 实装阶段 3**（撞车 0 严守外）：evolvers 启动 R201 §A7 #9/#11/#13/#14 实装

---

## 七、撞号透明登记

- **已 push** `475f5a93 R203 docker 调研`（已 merge `2f21f1ba`）—— R203 主题 = docker 端口
- **本盘** R204 = 权限收敛脚本骨架 + selftest
- 按「撞号透明协议」+ R202 阶段 1 docs-only 收口，主题可分辨，与 BCP §四十一 R203 并列