# R201 §A7 拍板矩阵（6 子项 × 3 选项，2026-09-24）

> **撰写者**：主协调会话（按 e30d739d「自动推进不打断」+ R94「主协调不替 owner 拍板」+ R195 §六 14 项清单 #9-#14 落档 docs-only 拍板矩阵）

> **目的**：把 R195 §六 14 项拍板清单第 9-14 项 §A7 子项拍板点展开为 6 子项 × 3 选项矩阵 + 推荐 + 工作量 + 撞车风险 + 验证 + 回滚，等 owner 拍板后实装

> **撞车 0 严守**：仅 docs/ipd-系统说明/ 落档摆选项矩阵，不动 Java/Vue/SQL/yml/真库/端口/PID

> **撞号透明登记**：本盘 R201 §A7 与已 push `c8015da2 R200 §A6 看板模板` 同周 R200/201 但分主题（R200 §A6 = 看板操作模板；本盘 R201 §A7 = 拍板矩阵），BCP §三十九 透明登记

---

## 一、R195 §六 14 项 #9-#14 §A7 6 子项速览

| # | 项 | 来源 | 派给 | 阻塞 |
|---|---|---|---|---|
| 9 | P0-2 KPI 公式派单（业务边界） | R194 §A7 §三 H5/H16/H17 | 业务 owner | H5/H16/H17 三页解锁 |
| 10 | AllowanceService 删/留拍板 | R194 §A7 §三 H22 | owner 全程在场 | H22 解锁 |
| 11 | docker-compose 端口前缀 1XXXX | R194 §A7 §五 5.3 | DevOps + 前端 owner | 前端 Dockerfile + CI 启动 |
| 12 | DBA 窗口合并排期（2-3h 一刀） | R194 §A7 §六 P0-4/P0-10/DB-02 | DBA 单方 | DDL apply 落地 |
| 13 | DBA 部署 Redis（dev/staging/prod 三套） | R194 §A7 §七 7.1 | DBA 单方 + evolver 26h | P0-12 Redis 多实例切流启动 |
| 14 | 权限三套收敛顺序（meta.authority → v-access） | R194 §A7 §四 | 前端 owner + evolver | 前端权限收敛 |

---

## 二、6 子项 × 3 选项拍板矩阵

### 子项 #9 · P0-2 KPI 公式派单（业务边界）

| 选项 | 描述 | 工作量 | 撞车风险 | 验证 | 回滚 |
|---|---|---|---|---|---|
| **A** 业务 owner 全量拍 KPI 公式 + 联调 H5/H16/H17 三页 | 业务 owner 在场 30min 拍 8-12 个 KPI 边界公式 → evolver 启动 6h 三页联调 | 6.5h | **0**（仅前端联动）| mvn test + 浏览器实测 KPI 三页 | 删除 KPI 三页 PR |
| **B** 业务 owner 仅拍 H17 raw-records 边界，其余后置 | 5min 拍 raw-records + H5/H16 后置 | 5min | 0 | H17 单页验证 | 删除 H17 PR |
| **C** P0-2 KPI 公式整体后置到 P3 阶段 | 等业务 owner 全量整理 KPI 边界（2-4 周）| 0 | 0 | N/A | N/A |

**推荐 A**：业务 owner 在场拍 KPI 边界是 R195 §六 #9 推荐路径，evolver 启动三页联调立即解锁 H5/H16/H17。

### 子项 #10 · AllowanceService 删/留拍板

| 选项 | 描述 | 工作量 | 撞车风险 | 验证 | 回滚 |
|---|---|---|---|---|---|
| **A** 保留 AllowanceService（取消失真）| R198 §A5 已落档 owner 拍板 A=保留 | 0h | 0 | R25 清单更新 | N/A |
| **B** 删 AllowanceService + 改 test | 删 Service + 改 ~3 个 test 用例 | 1h | **低**（动 Java）| mvn test | git revert |
| **C** AllowanceService @Deprecated + 不删 | 标记 + Javadoc 警告 + 保留 | 0.5h | 0 | mvn test | 移除 @Deprecated |

**推荐 A**：R198 §A5 矩阵已拍 A，本盘仅翻历史层引用，不重新选。

### 子项 #11 · docker-compose 端口前缀 1XXXX（dev/staging/prod 三套统一）

| 选项 | 描述 | 工作量 | 撞车风险 | 验证 | 回滚 |
|---|---|---|---|---|---|
| **A** 不改维持 23306（dev 漂移）| 当前 docker-compose 写 23306，dev 真库走 13306 | 0 | 中（持续漂移）| N/A | N/A |
| **B** 全仓推 1XXXX 前缀（dev=13306/staging=15306/prod=17306）| docker-compose 三套独立 + `MYSQL_PORT` 显式 + 前端 `VITE_API_BASE_URL` 显式 16039 | 4h | **中**（动 .yml + 文档）| docker-compose up + 健康检查 | git revert |
| **C** 仅改 dev=13306，staging/prod 维持 | 单文件改 + dev 验证 | 2h | 低 | dev 启动验证 | git revert |

**推荐 B**：R195 §六 #11 推荐「全仓推 1XXXX 前缀」，dev/staging/prod 三套各自独立 compose 文件统一前缀约定 `1XXXX`。

### 子项 #12 · DBA 窗口合并排期（2-3h 一刀）

| 选项 | 描述 | 工作量 | 撞车风险 | 验证 | 回滚 |
|---|---|---|---|---|---|
| **A** 合并 P0-4/P0-10/DB-02 三项 2-3h 一刀 | DBA 单方排 2-3h 维护窗口，一次性 apply 3 项 DDL | 2-3h DBA | **低**（DBA 单方）| `p1-ddl-apply-check.py` 三项 PASS | git revert SQL |
| **B** 分三次小窗（每次 30min）| DBA 三次小窗分散 | 3×30min = 1.5h DBA | 中（多次切库）| 三次 PASS | N/A |
| **C** DBA 不动，等业务 owner 决策 P0-10 时机 | P0-4 先 apply，P0-10/DB-02 后置 | 不定 | 中（业务侧阻塞）| P0-4 PASS | N/A |

**推荐 A**：R195 §六 #12 推荐「2-3h 一刀」，降低多次切库风险。

### 子项 #13 · DBA 部署 Redis（dev/staging/prod 三套）+ evolver 26h 切流

| 选项 | 描述 | 工作量 | 撞车风险 | 验证 | 回滚 |
|---|---|---|---|---|---|
| **A** DBA 部署三套 Redis + evolver 26h 切流 | DBA 先部署 dev/staging/prod 三套独立 Redis 实例，evolver 启动 26h 实装（HrTokenClient + BusinessConfigService pub/sub + yml + IT + 联调）| 4h DBA + 26h evolver = 30h | **中**（动 Java + yml + DBA）| Testcontainers IT + mvn test + 浏览器实测多实例 | git revert |
| **B** 仅 DBA 部署 dev 一套，先单环境跑通 | DBA dev 一套 + evolver dev 26h | 1h DBA + 26h evolver = 27h | 低 | dev IT PASS | 删除 dev Redis 配置 |
| **C** Redis 不切，HrTokenClient 维持内存模式 | 不动，等 P2 阶段再切 | 0 | 0（性能瓶颈）| 性能监控 | N/A |

**推荐 A**：R195 §六 #13 推荐「DBA 部署三套」，与 #12 DBA 窗口合并（一起维护窗口）。

### 子项 #14 · 权限三套收敛顺序（meta.access / meta.authority / v-access:code）

| 选项 | 描述 | 工作量 | 撞车风险 | 验证 | 回滚 |
|---|---|---|---|---|---|
| **A** 先收 meta.authority 标记 @deprecated，再统一到 v-access:code | R194 §A7 §四 推荐路径：①meta.access 作 v-access:code 上层代理 ②meta.authority 标 @deprecated ③新增 `check-permission-single-source.sh` 门禁 | 8h evolver | **中**（动 Vue）| 浏览器实测权限收敛 + shell 脚本 PASS | git revert |
| **B** 仅 docs-only 标记边界（M-Root-4 范畴）| 仅落档 R185 §2.2 + R186 §八 文档化边界 | 0h | 0 | docs lint PASS | N/A |
| **C** 直接下线 meta.authority + meta.access | 强收敛，UX 显化改 v-access:code | 16h evolver | **高**（前端权限回归）| 浏览器全量权限验证 | git revert 强恢复 |

**推荐 A**：R195 §六 #14 推荐「先 meta.authority 后 v-access」，R185 §2.2 已落档边界，新增门禁脚本补 M-Root-4。

---

## 三、撞车 0 兑现

- **不动 Java/Vue/SQL/yml/真库/端口/PID**（仅 docs 落档摆 6 子项 × 3 选项矩阵）
- **17 兄弟 worktree 完整保留**（不抢不撞）
- **本盘 worktree**：基于 `origin/main = ae3671e4` 单拉 `/tmp/wt-r201`，分支 `docs/r201-a7-matrix-20260924`
- **不动看板卡 status**（仅 docs 摆拍板矩阵）

---

## 四、推荐汇总（owner 拍板参考）

| # | 项 | 推荐 | 阻塞下游 |
|---|---|---|---|
| 9 | P0-2 KPI 公式派单 | **A** 全量拍 KPI 公式 + 联调三页 | H5/H16/H17 三页解锁 |
| 10 | AllowanceService 拍板 | **A** 保留（R198 §A5 已拍）| H22 解锁 |
| 11 | docker-compose 端口 1XXXX | **B** 全仓推 1XXXX 前缀 | 前端 Dockerfile + CI 启动 |
| 12 | DBA 窗口合并 | **A** 2-3h 一刀 | DDL apply 落地 |
| 13 | DBA 部署 Redis + 切流 | **A** DBA 三套 + 26h 切流 | P0-12 Redis 多实例切流 |
| 14 | 权限三套收敛 | **A** 先 meta.authority 后 v-access | 前端权限收敛 |

**合计工作量**（如全部 A 推荐路径）：6.5h + 0h + 4h + 2-3h DBA + 30h = **~45h ≈ 5.5 人日**
**撞车点合计**：0（中/低撞车为单方权限，evolver 启动需错峰）

---

## 五、下一步

1. **owner 拍板 6 子项**：选定 A/B/C 后回写本盘推荐段
2. **R202 起按拍板实装**：A 路径 docs-only 落档 R202 实装计划 → R203+ 启动分阶段
3. **撞车 0 持续严守**：实装阶段仍 docs-only 先出飞手计划 + 错峰 + 单 worktree

---

## 六、撞号透明登记

- **已 push** `c8015da2 R200 §A6 看板模板`（commit 已 merge `ae3671e4`）—— R200 主题 = 看板操作模板
- **本盘** R201 §A7 = 拍板矩阵
- 按「撞号透明协议」+ R195 §六 顺序（#9-#14 = §A7），主题可分辨，与 BCP §三十八 R200 §A6 并列