# R207 5 项阻塞拍板后执行路线图（2026-09-24）

> **写给谁来用**：owner / OPS-09 / 业务 owner / DevOps / DBA 任一在场拍板后，主协调按此路线图执行
>
> **目的**：把上一轮 R206 5 项阻塞的「拍板 → 收尾 → 验收」路径 docs-only 落档成路线图，撞车 0 严守外错峰单 worktree 执行
>
> **撞车 0**：本盘仅 docs/ipd-系统说明/ 落档路线图，**不动 Java/Vue/SQL/yml/真库/端口/PID**

---

## 总览：5 项阻塞拍板后路线图

| 阻塞项 | 拍板者 | 拍板后收尾 | 验收剧本 | 撞车 0 兑现 |
|---|---|---|---|---|
| #1 翻卡 | owner | 主协调 docs-only 落档翻卡回执 | GET 回读核验 desc_len + marker content | 仅 docs |
| #2 动 Java | OPS-09 | 单 worktree 错峰改 Java + 主协调 docs-only 落档收尾 | 真库 13306 单测 + curl HTTP | docs + Java |
| #3 KPI 边界 | 业务 owner | 主协调 docs-only 落档 KPI 边界文档 | 文档评审 + 字段映射核验 | 仅 docs |
| #4 docker | DevOps + 前端 owner | 主协调 docs-only 落档 docker 实装方案 + 4 CI yml | compose up + curl 端口探活 | 仅 docs |
| #5 DBA | DBA | DBA 单方行动 + 主协调 docs-only 落档部署回执 | 真库 13306/16379 探活 + 应用重启 | docs + 真库 |

---

## #1 owner 翻卡路线图

**拍板后路径**：
1. owner 在场按 R200 §A6 4 步法（GET 复核 → 标派单 → PUT → GET 回读核验）逐张翻 status
2. 主协调 docs-only 落档「翻卡回执」（commit hash + 翻卡清单 + desc_len 前后值）
3. SSOT 同步：log.md + 看板镜像 + BCP-Registry §对应轮次

**撞车 0 兑现**：
- ✅ 仅 docs 落档
- ❌ 不替 owner 翻 status
- ❌ 不捎带兄弟会话 untracked json

**验收剧本**：
```bash
# 翻卡后独立 GET 回读核验
curl -s http://127.0.0.1:62250/api/tasks/<task_id> \
  -H 'project_id: 01dcf15c-86bb-4c7b-957c-8fe44bddd10d' \
  | jq '.data.status, (.data.desc | length), .data.desc'
```

**预计工作量**：0.5h（12 张汇总卡 × 2min/张）

---

## #2 OPS-09 拍板动 Java 路线图

**拍板后路径**：
1. OPS-09 在场拍 R198-2（R198-2 选 B = 改对齐真库） + R198-4（R198-4 选 B = 保留 + test 移除引用）
2. 主协调开单 worktree（基于 `origin/main = d0ce9501`）：`/tmp/wt-r208-ops09-java`，分支 `feature/r208-ops09-java-modify`
3. 错峰改 Java（不在主工作区，避免撞兄弟会话）
4. 单 worktree 内 mvn 单模块测试：`mvn -o -pl ruoyi-modules/ruoyi-ipd -Dtest=Xxx test`
5. 主协调 docs-only 落档「OPS-09 Java 收尾报告」（commit hash + 改动文件清单 + 单测结果）
6. 提交 PR（不直提 main，等 owner 合并）

**撞车 0 兑现**：
- ✅ docs + Java（撞车 0 严守外，OPS-09 授权后）
- ❌ 不捎带兄弟会话 modified
- ❌ 不直提 main（走 PR 等 owner 合并）

**验收剧本**：
```bash
cd /tmp/wt-r208-ops09-java
mvn -o -pl ruoyi-modules/ruoyi-ipd -Dtest=RequirementPoolServiceTest,KpiRecordServiceTest test
# 验证：单测全绿（Surefire 按 @Tag("dev") 过滤，避免假绿）
curl -s http://127.0.0.1:16039/api/v1/requirement-pool/list -H 'Authorization: Bearer <token>' | jq .
```

**预计工作量**：3h（R198-2 = 1h 改 Entity + 1h test；R198-4 = 0.5h 移除 import + 0.5h 注释）

---

## #3 业务 owner 拍 KPI 边界路线图

**拍板后路径**：
1. 业务 owner 答复 3 件事：KPI 计算公式 + 异动阈值 + 异动审批链
2. 主协调 docs-only 落档「KPI 边界文档」（含计算公式 + 字段映射 + 审批链流程图）
3. 撞车 0 docs-only，不动 Java 代码
4. SSOT 同步：log.md + 看板镜像 + BCP-Registry §对应轮次

**撞车 0 兑现**：
- ✅ 仅 docs 落档
- ❌ 不动 Java 代码（KPI 边界落档后再启动 R209 Java 改造）

**验收剧本**：
- 文档评审会（业务 owner + 主协调 + 测试人员）
- 字段映射核验（`kpi_records.score` / `weight` / `actual` / `target` vs 公式）
- 异动审批链流程图核验

**预计工作量**：2h（落档文档 + 评审）

---

## #4 DevOps + 前端 owner 拍 docker 路线图

**拍板后路径**：
1. DevOps + 前端 owner 联合拍板：端口前缀 1XXXX + compose 拆分 + 4 CI yml 顺序
2. 主协调 docs-only 落档「docker 实装方案」（含三套 compose yaml + 4 CI yml 草案）
3. 撞车 0 docs-only，不实际起 docker（DevOps 单方执行）
4. DevOps 按方案起 compose + curl 探活端口
5. 主协调 docs-only 落档「docker 部署回执」（端口探活结果）

**撞车 0 兑现**：
- ✅ 仅 docs 落档
- ❌ 不实际起 docker（DevOps 单方权限）

**验收剧本**：
```bash
# DevOps 起 compose 后探活
docker-compose -f docker-compose.dev.yml up -d
curl -s http://127.0.0.1:13306/ | grep -q 'mysql' && echo 'MySQL 13306 OK'
curl -s http://127.0.0.1:15666/ | grep -q 'antd' && echo 'Frontend 15666 OK'
```

**预计工作量**：4h（落档方案 2h + DevOps 起 compose 2h）

---

## #5 DBA 排维护窗口路线图

**拍板后路径**：
1. DBA 拍 dev/staging/prod 维护窗口时间
2. DBA 单方执行：Redis 三套部署 + persons 字符集迁移
3. 主协调 docs-only 落档「DBA 维护窗口执行回执」（含 4h 工作清单 + 部署结果 + 回滚路径确认）
4. 应用重启验证：mvn 单模块起后端 + curl HTTP 探活

**撞车 0 兑现**：
- ✅ docs + 真库（DBA 单方权限，主协调不擅自动真库）
- ❌ 不擅自 restart JVM / 应用（DevOps 单方）

**验收剧本**：
```bash
# DBA 部署后探活 Redis
redis-cli -h 127.0.0.1 -p 16379 ping  # dev
redis-cli -h 127.0.0.1 -p 16380 ping  # staging
redis-cli -h 127.0.0.1 -p 16390 ping  # prod（主从）

# 主协调起后端 + curl 探活
mvn -o -pl ruoyi-modules/ruoyi-ipd spring-boot:run -Dspring-boot.run.profiles=dev &
curl -s http://127.0.0.1:16039/api/v1/business-config/list -H 'Authorization: Bearer <token>' | jq .
```

**预计工作量**：6h（DBA 单方 4h + 主协调 docs-only 收尾 2h）

---

## 撞车 0 严守总兑现

**撞车 0 严守累计 14 轮**（R196-R206 共 11 轮 + 本盘 R207）。

**路线图执行总预计**：
- #1 翻卡：0.5h（仅 docs）
- #2 动 Java：3h（docs + Java，单 worktree）
- #3 KPI：2h（仅 docs）
- #4 docker：4h（docs + DevOps 单方）
- #5 DBA：6h（docs + DBA 单方）
- **合计**：15.5h ≈ 2 人日

**下一步刷新**：等任一项阻塞拍板 → 按对应路线图执行 → 主协调 docs-only 落档收尾报告 → 进入 R208+ 实装阶段。

---

## 撞号透明登记

- **已 push** `d0ce9501 R206 5 项阻塞拍板请示材料包`
- **本盘** R207 = 拍板后执行路线图
- 按「撞号透明协议」+ R202 阶段 1 docs-only 收口，主题可分辨，与 BCP §四十四 R206 并列