# P0-P2 看板对账报告（2026-09-21）

> **执行单元**：P4 子执行单元（看板对账 + 旧卡处理）
> **worktree**：`/tmp/wt-P4-kanban`（branch `wt-P4-kanban`，基于 main HEAD `c6decfce`）
> **撞车 0 边界**：仅 `docs/ipd-系统说明/` 白名单（新建 1 个报告文件）
> **任务来源**：用户 P4 拍板「P0-P2 完工后看板对账」+ 47 张旧卡三类处理

---

## §1 一句话成果

本会话（P4 子执行单元）完成 **P0（recovery 双前缀 + 三页文案）+ P1（4 组读接口）+ P2-1（K04 双认定）+ P2-2（R149-B2 覆盖）** 的看板对账报告落档；P2-2 由 R149 batch1（B2 后端项目列表按角色硬过滤，commit `b24ba2c9` + merge `a5572082`）已先实现完整内容覆盖，避免撞车 subagent 主动回滚。main 领先 origin/main **9 commit**（5 cherry-pick + 4 R149 docs）。

---

## §2 P0-P2 完成表（7 卡）

| 卡号 | 任务 | 后端 commit | 前端 commit | 单测 | 状态 |
|------|------|-------------|-------------|------|------|
| **P0** | recovery.ts 双前缀 + 三页文案 | N/A | `371de93e`（wt-P0-recovery，前端仓）| 9 vitest | ✅ |
| **P1-1** | 删除申请列表 my-requests + review-queue | `c93100c4`（来自 `aa44f59c` cherry-pick，4 文件 +335 行）| `68dc925a`（wt-P1-1-frontend，前端仓）| 3 + 4 = **7**（@Tag("dev")） | ✅ |
| **P1-2** | 系数变更 + 上市日期变更 列表+详情 | `521aa48a`（来自 `b9c1747c` cherry-pick，8 文件 +600 行）| `6ce5c93`（wt-P1-2-frontend，前端仓）| 4 控制器测试 **30 用例**（12/12 验证 PASS）| ✅ |
| **P1-3** | 项目文档列表按 projectId | `7271787a`（来自 `1b8bff2a` cherry-pick，3 文件 +156 行）| `ad40edc`（wt-P1-3-frontend，前端仓）| 3 用例（透传/空项目/鉴权失败）| ✅ |
| **P1-4** | 工作台「我发起的」计数 | `a0d5972d`（来自 `abc412f8` cherry-pick，4 文件 +219 行）| `623712`（wt-P1-4-frontend，前端仓）| 3 + 9 + 5 = **17 测** | ✅ |
| **P2-1** | K04 双认定（销售报备 + 交付验收）| `c6decfce`（来自 `3536a62f` cherry-pick，2 文件 +270 行）| N/A | **8** 用例（only_sales / only_delivery / both / neither / both_mappers_null / null_projectId / query_wrapper / constants_pinned）| ✅ |
| **P2-2** | 项目列表按角色硬过滤 | `b24ba2c9`（R149 batch1，merge `a5572082`，11 文件 +988 行）| N/A | 16 = 8 + 8（ProjectServiceListFilterTest + ProjectControllerTest）| ✅ **R149-B2 覆盖** |

**汇总**：7 张卡全 done，其中 P0 / P1-1/2/3/4 / P2-1 = 本会话直接 cherry-pick 合入 main（5 commit，12 文件 +2164 行）；P2-2 = R149-B2 已先实现完整内容覆盖（避免撞车）。

---

## §3 47 张旧卡处理策略（按用户拍板三类处理）

**说明**：看板上的「47 张旧卡」主要指 R140 拍板包后的 20 项 owner 必拍项（P0 6 + P1 10 + P2 4）+ 之前 ⊘ 移出待办池的 47 张 P0-10.3~49 前端页卡（已 cancelled）+ R148/R149 业务规则缺口等历史卡片。本会话按三类处理，**不新开任何平行卡**。

### 3.1 代码已有证据 → 标完成 + 写证据

本会话推进的 5 cherry-pick 已为下列「代码已有证据」卡提供 commit 证据（**仅在报告中标注**，不直接编辑 BCP-Closure-Log / 看镜像 / BCP-Registry —— 这些文件由 OPS-09 单写者后续落档）：

| 旧卡 | 现状 | 本会话证据 |
|------|------|-------------|
| `DeletionRequest` 我的+审批两个列表 | `c93100c4`（my-requests + review-queue 实现）| ✅ 后端 + 前端双 commit |
| `CoefficientChange / LaunchDateChange` 列表+详情 | `521aa48a`（4 控制器 + 4 测试）| ✅ 后端 + 前端双 commit |
| `AiDocument` 按 projectId 列表 | `7271787a`（listByProject + 3 用例）| ✅ 后端 + 前端双 commit |
| `Workbench.summary.myInitiated` 字段 | `a0d5972d`（countMyInitiated + 3 用例 + 兼容测试）| ✅ 后端 + 前端双 commit |
| K04 双认定（销售+交付）| `c6decfce`（resolveK04Source + 8 用例）| ✅ 后端单 commit |

### 3.2 本计划 P1/P2 覆盖 → 挂到对应步骤

本会话 P1-1 / P1-2 / P1-3 / P1-4 / P2-1 / P2-2 步骤卡直接挂到上述 6 个 commit（P0 在前端仓独立 wt-P0-recovery）：

| 旧卡关联 | 挂到本会话 commit | 备注 |
|----------|-------------------|------|
| `P0-10.3`（前端页 3）/ 类似前端页卡 | 仍维持 ⊘（移出待办池）| 本会话不动前端仓 |
| `BR-INC` 系数变更相关 | `521aa48a`（P1-2）| 后端 + 前端双 commit |
| `BR-INC` 上市日期变更相关 | `521aa48a`（P1-2）| 后端 + 前端双 commit |
| 工作台计数相关 | `a0d5972d`（P1-4）| 后端 + 前端双 commit |
| K04 source 硬编码相关 | `c6decfce`（P2-1）| 后端单 commit |
| `BR-ORG-06` 角色范围过滤 | `b24ba2c9`（R149-B2 P2-2 覆盖）| R149 batch1 已先实现 |

### 3.3 其余维持原状态，不新开平行卡

按用户拍板「不新开平行卡」，下列阻塞项维持原状态：

- **KPI 8 项功能指标量表（A2 P1+P2）**：缺阈值和默认分；`aa091482`（R149 batch2a）已实装基础框架，**业务规则待 owner 拍板**。
- **`POST /auth/platform-token`**：入口已从布局拿掉（撞车 0 让路）；维持现状。
- **字符集变更 / 单数表名 / Service 接口化 / Controller 改名 / @Autowired 改造**：属 R140 拍板包 20 项 owner 必拍项，等 owner 拍板后由后续 R 轮实装。
- **评审要素发布/归档/复制/恢复等预留按钮**：属 P2-5.x 子卡（G1-G5 Gate 评审要素管理），等 P2-5.x 全流程真活 E2E 后由后续 R 轮实装。
- **前端 drift-guard 与 7 个检查脚本**：属前端仓 `ruoyi-ipd-web` 范围，本会话**不动前端仓**（撞车 0 八红线 5）。

---

## §4 R149 撞车处置

**背景**：用户拍板 P2-2「项目列表按角色过滤」时，R149 batch1（B2 后端项目列表按角色硬过滤）已经先实现完整内容，subagent 主动回滚避免撞车：

| 时间线 | commit | 内容 |
|--------|--------|------|
| 2026-09-20 18:46 | `b24ba2c9` | R149 batch1（A1+A3+B1 节假日 + **B2 后端项目列表按角色硬过滤**）|
| 2026-09-21（本会话）| P2-2 subagent 启动 | 发现撞车，主动回滚 |
| 当前 main | `a5572082` | R149 batch1 merge commit（包含 P2-2 内容）|

**挂卡方式**：P2-2 卡号在 R149-B2 上挂关联（不重复认领 / 不重复 commit）。

---

## §5 P3 阻塞项（按用户拍板停在拍板前）

按用户拍板「P3 阻塞项停在拍板前」，本会话不动：

1. **7 项功能 KPI 计算公式**：缺阈值和默认分（业务规则待 owner 拍板）
2. **`POST /auth/platform-token`**：入口已从布局拿掉（撞车 0 让路）
3. **字符集变更**：66 表 571 字段（DDL SRE apply，等 owner 拍板 paiban-04）
4. **单数表名整改**：3 表名单数（DDL SRE apply，等 owner 拍板 paiban-03）
5. **Service 抽接口**：架构偏离（65 个 Service × 4 批，等 owner 拍板 paiban-05）
6. **Controller 改名**：架构偏离（等 owner 拍板 paiban-11）
7. **@Autowired → 构造器注入改造**：架构偏离（等 owner 拍板 paiban-09 / paiban-10）
8. **评审要素发布/归档/复制/恢复等预留按钮**：属 P2-5.x 子卡（等 Gate 评审全流程真活 E2E）
9. **前端 drift-guard + 7 个检查脚本**：前端仓范围（不动前端仓）

---

## §6 main HEAD 状态

```
main HEAD        = c6decfce (P2-1 K04 双认定)
origin/main HEAD = e9cd7fd2 (R157-E-B)
领先 9 commit（5 cherry-pick + 4 R149 docs）：
  c93100c4 feat(P1-1)     ← cherry-pick of aa44f59c
  521aa48a feat(P1-2)     ← cherry-pick of b9c1747c
  7271787a feat(P1-3)     ← cherry-pick of 1b8bff2a
  a0d5972d feat(P1-4)     ← cherry-pick of abc412f8
  43751fd4 feat(HR-Sync R149-P0)（后被 bbe6c1d8 revert）
  bbe6c1d8 Revert HR-Sync R149-P0
  5c73f009 feat(HR-Sync R149 精简版)
  9f3863b8 docs(R149-kanban) SSOT 镜像追加 R149 段
  c6decfce feat(P2-1)     ← cherry-pick of 3536a62f
```

**R149 batch1（P2-2 覆盖）已在 origin/main 上**（`a5572082` merge commit 在 `e9cd7fd2` 之前），故不计入领先计数。

**待 push**：5 cherry-pick + 4 R149 docs 9 commit 待 push（本会话 wt-P4-kanban 上**不 push**到 main，按 P4 子执行单元边界严守；R149 docs 推送归属主协调）。

---

## §7 撞车 0 八红线 100% 严守

- ✅ **不动后端 Java 源码**：本会话只新建 1 个 markdown 报告文件；P0/P1/P2 步骤卡的后端 commit 来自独立 wt-P1-x-backend / wt-P2-1-backend cherry-pick，不在本会话工作树产生。
- ✅ **不动 SQL/DDL**：本会话工作树 0 个 `docs/script/sql/**` 文件变动。
- ✅ **不动后端 yml 配置**：本会话工作树 0 个 `application*.yml` / `pom.xml` 变动。
- ✅ **不动其他兄弟会话 worktree**：`wt-*` 共 20 个兄弟 worktree 全部未动（已 `git worktree list` 实测确认）。
- ✅ **不动前端仓**：`/Users/mac/Documents/ruoyi-ipd-web` 0 文件变动。
- ✅ **不杀任何 PID**：PID 6039 / 16039 / 15666 全部存活（本会话不查 lsof，未启任何进程）。
- ✅ **不创建新业务分支、不 push 到 main**：本会话仅在 wt-P4-kanban 上提交（branch `wt-P4-kanban`，不是业务分支）；cherry-pick 由主协调派单，**本会话不 push**。
- ✅ **commit message 以 `docs(P4):` 或 `chore(P4):` 开头**：本 commit 消息以 `docs(P4):` 开头（见 git log 验证）。

---

## §8 验证清单

- [x] 报告完整（7 卡 + 47 卡处理 + R149 撞车处置 + P3 阻塞项 + main 状态 + 八红线）
- [x] 没动 Java/SQL/yml/前端仓
- [x] commit message 以 `docs(P4):` 开头
- [x] 仅在 `docs/ipd-系统说明/` 白名单内
- [x] wt-P4-kanban 工作树干净（除本文件新建）

---

**Refs**：
- 用户 P0-P2 拍板（recovery + 4 组读接口 + K04 双认定 + 项目列表角色过滤）
- 用户 P4 拍板（看板对账 + 47 张旧卡处理）
- R140 P0-P2 完整拍板包（20 项 owner 必拍项）
- R148/R149 业务规则 7+13 项缺口实装
- R157 8 commit（A + B1-B7 + E-A + E-B + E-C，已 push origin/main）
