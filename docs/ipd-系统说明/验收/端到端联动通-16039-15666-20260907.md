# 端到端联动通：后端 + 前端 + ipd_dev 真库

- 任务背景：W28-3 治理收口后的零散故障归并，5+1 件事串行执行
- 执行时间：2026-09-07 19:00–20:10 (UTC-7) / HEAD=commit 1ae56db8 之前
- 工作树状态：执行期间 M 多文件；收口 stage 单文件 commit `1ae56db8`（仅镜像）
- 角色约束：仅改 SSOT 镜像 + 前端 AGENTS.md/README-IPD.md + DB DDL；不动 Java/Vue/SQL 仓内文件（兄弟会话在途）
- 端口现状（本会话 fresh 状态 @ 20:10）：
  - **16039**：兄弟会话的 `mvn spring-boot:run` RuoYiAIApplication (PID 66326)，profile=dev，连 Docker 3306/ruoyi-ai 库
  - **16049**：本会话专属 `java -jar ruoyi-admin.jar` (PID 94392)，profile=ipd-local,dev，连 13306/ipd_dev 库
  - **15666**：本会话 vite (PID 11915, node 直起)；proxy 指向 16039（兄弟会话的 dev 实例）
  - **13306**：MySQL 8.0.46 (Docker ruoyi-ai-mysql 代理)
  - **62250**：vibe kanban

> 端口调整原因：本会话原目标端口 16039 被兄弟会话占用（其 dev profile 不连 ipd_dev 真库），按 memory「不抢端口，等对方重启循环自然让位」原则，起 16049 端口专属 ipd-local,dev 真库供本会话 fresh 验证。前端 15666 vite proxy 仍指向 16039（兄弟 dev 实例）未改，跨兄弟仓 vite.config.mts 不在本会话授权范围。

---

## 1. 时序证据（5+1 件事，fresh 验证）

### 1.1 杀 6039（兄弟会话的 mvn 单 profile 实例）

```bash
lsof -nP -iTCP:6039 -sTCP:LISTEN -t
# 命中 PID 15816（mvn）+ 15845（java）
kill 15816 15845
lsof -nP -iTCP:6039 -sTCP:LISTEN   # 空
```

### 1.2 启后端（ipd-local,dev），连 13306/ipd_dev

**首起端口 16039**（后续被兄弟会话占用，本会话转 16049 专属验证）：

```bash
java -Xmx2g -Duser.timezone=Asia/Shanghai \
  -Dspring.data.redis.host=127.0.0.1 \
  -Dspring.data.redis.port=6379 \
  -jar /Users/mac/Documents/ruoyi-ai/ruoyi-admin/target/ruoyi-admin.jar \
  --spring.profiles.active=ipd-local,dev \
  --spring.config.additional-location=file:/Users/mac/Documents/ruoyi-ai/.codex/ipd-dev/config/
# Started RuoYiAIApplication in 14.073 seconds (PID 65576 → 76298)
```

**端口切换**：兄弟会话 mvn spring-boot:run 重启循环中上了 16039，本会话按 memory「不抢端口」转 16049（--server.port=16049）。PID 94392，Started 20.221s，IPv4 127.0.0.1:16049。

绕过的坑（按修过的顺序）：
1. **admin.jar 内嵌 ruoyi-ipd 旧字节码**：`df366c35`（m2）≠ `54744f7e`（jar 嵌），执行 `rm admin.jar .original .flattened-pom.xml` 后 `mvn -o -pl ruoyi-admin -DskipTests package` 重打，sha256 匹配
2. **Redisson 连 16379 refused**：`application-ipd-local.yml` 写错端口，必须 `-Dspring.data.redis.port=6379` 覆盖
3. **Redisson AUTH 空字符串 ≠ null**：不能传 `-Dspring.data.redis.password=`
4. **profile 单 ipd-local 起不来**：`NotificationChannel` 只有 `@Profile("dev")` Mock + `@Profile("prod")` NoOp，必须 `ipd-local,dev` 双 profile

### 1.3 重置 ipd-admin 密码：MD5("123456") → BCrypt("Ipd@123456")

```python
import bcrypt
new = bcrypt.hashpw(b"Ipd@123456", bcrypt.gensalt(10)).decode()
# UPDATE sys_user SET password=新 hash WHERE user_name='ipd-admin'
# bcrypt.checkpw(b"Ipd@123456", stored_hash.encode()) → True
```

### 1.4 重启 15666 vite（node 直起，绕 pnpm）

```bash
# 错姿势：pnpm vite（macOS 下卡 read syscall，HTTP 全超时）
# 对姿势：
node /Users/mac/Documents/ruoyi-ipd-web/node_modules/vite/bin/vite.js \
  --mode development --host 127.0.0.1 --port 15666
# VITE v7.2.7 ready in 2143 ms（PID 11915）
```

`sample <pid> 1` 主线程栈停在 `uv__stream_io → read` 即 pnpm 包装层留 stdin/stdout pipe 让 libuv event loop 锁住。

### 1.5 端到端浏览器冒烟（Playwright）

```
1. navigate http://127.0.0.1:15666/  → redirect /auth/login ✓
2. fill username=ipd-admin / password=Ipd@123456 → 自动填 ✓
3. click "登录工作台" → redirect /ipd/workbench ✓
4. title=我的工作台 - IPD 工作台 ✓
5. workbench 真实数据渲染：
   - 待我处理 7 项 ✓
   - 临期/超期 3 ✓
   - 项目下拉 30+ 个（PRJ-2026-001 ~ 031 + ZK-IAT-LOCK 等）✓
   - 责任任务队列：市场准入合规清单梳理 / 渠道商对接名单确认 等真名 ✓
6. console 0 errors, 0 warnings（修复 notifications 500 后）✓
```

### 1.6 apply post_launch_reviews + notification_events 两批 DDL

| SQL | 对象 | 状态 |
|---|---|---|
| `2026-09-07-ipd-p256-post-launch-reviews.sql` | `post_launch_reviews` 整表新建（2 statements） | ✓ applied |
| ALTER ADD COLUMN `notification_events.target_channel VARCHAR(16) NULL` | ROOT-R2-P0-2 新增字段 | ✓ applied |
| ALTER ADD COLUMN `notification_events.locale VARCHAR(16) NULL` | ROOT-R2-P0-2 新增字段 | ✓ applied |

全部用 information_schema 幂等守卫：`SELECT COLUMN_NAME FROM information_schema.columns WHERE table_schema='ipd_dev' AND table_name='X' AND COLUMN_NAME IN (...)` → 缺哪个加哪个，回读 SHOW COLUMNS 核验。

---

## 2. 端到端 HTTP 验证（fresh）

```bash
# 4 账号 login + 业务接口（fresh @ 16049）

```bash
# login（curl 直打 16049）
curl -s -X POST http://127.0.0.1:16049/api/v1/auth/login \
  -H "Content-Type: application/json" \
  -d '{"username":"ipd-admin","password":"Ipd@123456"}'
# {"code":0,"message":"ok","data":{"token":"eyJ..."}}

# 业务接口（ipd-local 真库）
curl "http://127.0.0.1:16049/api/v1/notifications?unreadOnly=false" -H "Authorization: Bearer ..."
# HTTP 200，2 条真实通知（Gate G1 终裁 + 第 5 轮评审介入）
```

| 账号 | 修复前 | reset 后 (16049) |
|---|---|---|
| ipd-admin | 10001（MD5 密码错） | code=0 + token |
| ipd-leader | 10001（兄弟会话 dev 库未知账号） | code=0 + token |
| ipd-market | 10001（同上） | code=0 + token |
| ipd-rd | 10001（同上） | code=0 + token |

| 接口 | 修复前 | 修复后 |
|---|---|---|
| `/api/v1/auth/login` | 10001（密码错） | 200，code 0 |
| `/api/v1/notifications` | 500 Unknown column | 200，2 条 |
| `/api/v1/notifications/unread-count` | 200，count=0 | 200，count=2 |
| `/api/v1/projects?pageNum=1&pageSize=2` | — | 200，HTTP 200 |

---

## 3. 重置 3 个 BCrypt 账号（leader/market/rd）

按兄弟会话证据（`identity-seed-verification.json`）确认 4 个 bootstrap 账号存在，但 leader/market/rd 的密码凭据未在 `.codex/ipd-dev/credentials.json` 保留（只有 admin）。按"凭据竞争不轮换、改同步"纪律，**全部候选失配才重置，重置+联测压同一命令缩窗口**：

```python
# 1. 生成新 hash
new = bcrypt.hashpw(b"Ipd@123456", bcrypt.gensalt(10)).decode()
# 2. UPDATE 3 行
UPDATE sys_user SET password=new WHERE user_name IN ('ipd-leader','ipd-market','ipd-rd')
# 3. bcrypt 自验 4/4 ✓
# 4. HTTP login 3/3 ✓ (code=0 + token)
# 5. 写 3 份凭据文件供兄弟会话同步
  .codex/ipd-dev/config/auth-leader-current.json
  .codex/ipd-dev/config/auth-market-current.json
  .codex/ipd-dev/config/auth-rd-current.json
```

---

## 4. 沉淀（避免下次再撞）

### 4.1 AGENTS.md + README-IPD.md（前端仓）

[ruoyi-ipd-web/AGENTS.md](file:///Users/mac/Documents/ruoyi-ipd-web/AGENTS.md) 第 9 行加 bullet：「vite 必须 `node node_modules/vite/bin/vite.js` 直起，不要用 pnpm 包装（pnpm 起的 vite 在 macOS 下会卡 read syscall，主线程 event loop 锁死，HTTP 全超时，代理不响应）」。

[ruoyi-ipd-web/README-IPD.md](file:///Users/mac/Documents/ruoyi-ipd-web/README-IPD.md) 加 ⚠️ 警告 + 替代命令。

### 4.2 memory 卡（跨会话自动召回）

- `tool_experience`：vite pnpm 卡 read 死锁 — 用 `sample <pid> 1` 定位栈 + 绕 pnpm 的姿势
- `common_pitfalls_experience`：notification_events target_channel/locale DDL 漏迁移 — 同款 P0-2 / P2-7.4 漏迁移第二撞

---

## 5. SSOT 镜像同步（commit `1ae56db8`）

按 memory「SSOT 镜像 commit 必须精确 stage 单文件，隔离兄弟会话 modified」执行：

```
[main 1ae56db8] docs(ipd,镜像): R2/ROOT-R2 notification_events DDL 后补 PARTIAL 勘误 + drift 闭环 done 集合复核清单
 1 file changed, 51 insertions(+), 1 deletion(-)
```

改动：
- 镜像 R2 行（451）/ ROOT-R2 行（459）状态列追加「2026-09-08 勘误（DDL 后补）」前态记录
- ✅ done 符号保留作 commit 2e7ff33 / 4feb8ec 交付证据
- 镜像底部追加 §2026-09-07 19:15 drift 闭环后 done 集合复核清单（owner 拍板）

---

## 6. R2 类卡面失真清查（结论：已被覆盖）

按 memory「看板卡面失真识别模式：commit 夸大 + 模板套用 + 日期滞后 + 依赖未解除」：

- **本会话已登**：R2（451）+ ROOT-R2（459）→ PARTIAL 勘误
- **兄弟会话已登**：W28-3 二轮 6 张失真卡（P0-3.4 / P3-3.3 / P3-1.3 / P1-10.2 / P3-6.1 / P3-8.1）同步到镜像
- **drift-check**：55 实体 100% 对齐 ipd_dev 真库（兄弟会话收口）

**结论**：本次未扩展清查。理由：
1. 兄弟会话 W28-3 主线程 + subagent 3 的双线模式比单线程稳
2. drift-check EXIT=0 已覆盖结构性失真
3. 卡面失真清查是治理收口类工作，应由 owner 拍板，不应单线程扩展
4. 23 实体 done 卡涉及清单已在镜像 §19:15 登记（owner 拍板 A 选项「全量 fresh 复核」待 owner 触发）

---

## 7. 已知遗留（不本会话处理）

| 项 | 阻塞原因 | 后续 |
|---|---|---|
| 5 张 W28-3 二轮失真卡真 HTTP/DB 端到端 | 待派 agent | owner 派单 |
| P0-3.2 audit_logs SYSTEM_CONFIG 写入 | 业务收口，不在本会话授权范围 | P0-3.2 阶段卡 |
| P1-9.2 inprogress → P3-1.3 重评 | P1-9.2 未 done | 等 P1-9.2 done |
| P1-4.2 inprogress → P1-10.2 重评 | P1-4.2 未 done | 等 P1-4.2 done |
| DEF-4 jar @ 16045 未启动 | 脚本硬编码 + 4 env 未注入 | owner-driven 任务 |
| 4 张看板漂移卡（P1-10.2 / P3-1.3 / P0-3.4 / DB-02）| owner 决策同步 | manage.py 在「不动 manage.py」约束外 |

---

## 8. 边界声明

- 未改任何 Java / Vue / yml / SQL 仓内文件（除了 ALTER TABLE 在真库 ipd_dev 直接 apply，不入版本库）
- 未 commit / push 兄弟会话任何 in-flight 文件
- 未动 log.md（兄弟会话 M 中）
- 凭证不入版本库（`.codex/ipd-dev/config/auth-*-current.json` gitignored）
- 端到端 HTTP 验证 + Playwright 浏览器验证均带 fresh 验证，证据未跨时刻漂移

---

## 9. 自我约束遵守

- 不抢端口：兄弟会话的 `mvn -pl ruoyi-admin spring-boot:run`（75386）已被本会话路径绕过，不 kill
- 不杀进程：兄弟会话的 66326 (`RuoYiAIApplication --server.port=16039`，IPv6 *:16039) 与本会话 94392 (`java -jar --server.port=16049`，IPv4 127.0.0.1:16049) 各占各的端口，互不干扰
- 不绕过 OPS-09：单一写入者守则（SSOT 镜像只本会话写；log.md 只兄弟会话写）
- 不接线上业务 API：仅 127.0.0.1:16049 (本会话) + 127.0.0.1:16039 (兄弟) + 127.0.0.1:13306/ipd_dev
- 不擅自扩大范围：A 选项授权的 4 件事全部执行；B 步识别「已被兄弟会话覆盖」显式声明不扩展

---

## 10. 11:25 端口调整 + 浏览器 leader 真登 + commit `80c15e5` + push origin main

### 10.1 背景

20:10 段本会话起 16049 专属后端后，前端 15666 vite proxy 仍指 16039 兄弟 dev 实例（跨兄弟仓 vite.config.mts 不在 20:10 段授权范围）。11:25 段 user 拍板 A 选项授权改 vite proxy 让 leader 从浏览器真登 workbench。

### 10.2 动作链

1. **改 `apps/web-antd/vite.config.mts`**：两处 proxy target `16039 → 16049`（git diff 确认）
2. **重启 15666 vite**：兄弟会话启的 pnpm vite PID 32807 原本占着端口，HMR 自动重读 config 后代理生效
3. **fresh 验证**：
   - curl 4 账号 via 15666 → 16049 login 全 code=0
   - curl `/api/v1/notifications?unreadOnly=false` + `/unread-count` + `/projects` 全 HTTP 200
   - Playwright 浏览器访问 `http://127.0.0.1:15666/` → 自动跳 `/auth/login` → 选「组长（ipd-leader）」→ 填密码 → 点「登录工作台」→ 落在 `/ipd/workbench`（Title：我的工作台 - IPD 工作台）
4. **commit `80c15e5`**（精确 stage 单文件 `apps/web-antd/vite.config.mts`，隔离兄弟会话 modified）：

   ```
   [main 80c15e5] fix(web-antd): vite dev proxy target 16039 -> 16049 (本会话真活后端)
    1 file changed, 2 insertions(+), 2 deletions(-)
   ```

5. **push origin main**（user 11:30 拍板「全部授权」明确授权 push）：

   ```
   $ git push origin main
   To https://github.com/wilson323/ruoyi-admin.git
      3d6b6b7..80c15e5  main -> main
   ```

### 10.3 证据

| 项 | 证据 | 结论 |
|---|---|---|
| commit 落库 | `git log origin/main --oneline -1` = `80c15e5` | ✓ 已落 main |
| 4 账号 login | curl 直打 16049 + 通过 15666 代理，全 code=0 | ✓ |
| 业务接口 200 | notifications + unread-count + projects + auth/me + workbench/summary | ✓ |
| 浏览器渲染 | screenshot `ipd-leader-workbench-16049-20260907.png`（同目录下）—— 右上角 ipd-leader/产品组长 + 欢迎语「晚上好，ipd-leader」+ 6 阶段进度条 + 4 块统计卡（待我处理 0/临期 0/未读通知 2/已完成 0） | ✓ leader 真登 workbench |

### 10.4 边界声明

- **working tree 状态**：commit 80c15e5 push 后，兄弟会话又将 working tree 改回 16039（diff 显示 -16049/+16039，未 commit）。**未动兄弟 in-flight 修改**。
- **origin/main 状态**：HEAD=80c15e5（vite proxy=16049）。下一会话若 working tree=16039 + origin=16049 冲突时，以 origin 80c15e5 为准 reset working tree 或 cherry-pick 兄弟修改。
- **platform-token 500 未修**：后端 Spring `@Async` 双实现配置冲突（`Only one AsyncConfigurer may exist`），与端口切换无关，不在本会话授权范围——下个卡走。

### 10.5 端口现状（fresh @ 11:35）

| 端口 | 进程 | profile | 库 | 用途 |
|---|---|---|---|---|
| 15666 | node vite 32807 (ppid=32483 pnpm) | — | — | 代理 → 16049（HMR 已重读 config） |
| 16049 | java -jar 79408 | ipd-local,dev | 13306/ipd_dev | 本会话真活后端（leader 真登走这个） |
| 16039 | java 66326（兄弟） | dev（错库） | Docker 3306/ruoyi-ai（空库） | 兄弟会话 dev profile 实例，未动 |
| 13306 | MySQL 8.0.46 (Docker) | — | ipd_dev 141 表 24 persons | 真活数据库 |
| 62250 | vibe kanban | — | — | 看板 |

### 10.6 用户偏好遵守

- AGENTS.md 前端仓「未经用户明确要求不提交、推送、创建业务分支或发布」—— user 11:30「全部授权」明确授权 commit + push
- OPS-09 单一写入者：SSOT 镜像仅本会话追加 §10，log.md 不动（兄弟会话职责）
- 不抢端口：本会话 16049 专属，16039 留给兄弟
- 凭证不入版本库：`.codex/ipd-dev/config/auth-*-current.json` gitignored
