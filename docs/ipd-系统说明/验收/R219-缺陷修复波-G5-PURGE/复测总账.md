# R219 缺陷修复波 · G5字段错配 + purge守卫塌缩 · 复测总账

- 日期：2026-09-25/26 跨日（复测 ts 2026-09-26T06:47Z 起）
- 归属卡：`e697a401-d200-…`（[G5阶段字段错配]，lane4 D-3）+ `b494f56e-d200-4250-8b17-df6a63fa2ba8`（[purge守卫塌缩]，lane3 D-2）
- 修复实现（本会话直接落地，Java 单一写入者）：
  1. `ContributionService.requireG5Stage`：判定字段 `projects.status` → **`current_stage`**（状态机枚举无 LIFECYCLE/POST_LAUNCH 迁移，原判定令贡献度→distribute 链对新项目永久不可达）；规则 = `current_stage == "LIFECYCLE"` 且 `status != ARCHIVED`（补 ZK-IPD §二.10 归档只读守卫）。错误消息回显 `currentStage=`，字段错位可见。
  2. `DeletionArchiveService.purge` 原子守卫：裸 `.notLike(remark, PURGED_MARK)` → `.and(isNull OR notLike)`（SQL 三值逻辑令 remark IS NULL 的 DELETED 行 UPDATE 恒 0 行 → 首次 purge 恒 409；与已修的 listArchive/DEF-8 对齐）。
- 测试：P361 / P064 / ContributionServiceTest / ContributionVersionArchiveTest mock 改生产合法形态（status=ACTIVE + currentStage=LIFECYCLE，mock 合法性规约）+ P064 新增 purge wrapper「IS NULL OR NOT LIKE」回归锁；目标 54/54 绿；整模块 2519 例仅 2 条既有哨兵红（`TenantExcludesConsistencyTest` 3 表 / `ServiceBareClockGuardTest` DeliverableService——**干净 HEAD stash 后同红，兄弟在途造成，非本波引入**，已登记不捎修）。
- 部署：`install ruoyi-ipd` → `package ruoyi-admin -Dmaven.jar.forceCreation=true` → 解包比对内嵌 `ruoyi-ipd-3.1.0.jar` sha256 一致（`ec907c8a…`）→ kill 39098 → v2 基线脚本重启（新 PID **11914**，17.4s Started，计数法判就绪）。
- 真机复测（`retest_fix_wave.py` → `retest-ledger.json`）：**10 PASS + 1 PARTIAL**
  | # | 用例 | 结果 | 关键证据 |
  |---|------|------|----------|
  | N1 | 归档项目(LIFECYCLE+ARCHIVED)关闭 | PASS | msg=「项目已归档…不开放 G5」（新守卫生效） |
  | N2 | CONCEPT+DRAFT 项目拒绝 | PASS | msg 回显 **currentStage=CONCEPT**（旧码回显 status=DRAFT——判对字段铁证） |
  | P1 | LIFECYCLE 项目 G5 入口开放 | PASS | 50002「已确认的贡献度评定不可修改」= 过门控直达下游（修复前恒 50007） |
  | P2 | market-share 联动入口可达 | PASS | 50002「已确认…不可调整」（同上语义） |
  | P3/P4 | GET 回读 + 真库 contributions | PASS | status=CONFIRMED market_share=0.5500 |
  | A1 | 归档区列表含 NULL-remark 候选 | PASS | 27 候选含目标行 |
  | A2 | **首次 purge 200**（修复前恒 409） | PASS | remark=PURGED_BY_SUPER_ADMIN:900101@… |
  | A3 | 真库回读打标 + 审计 | PASS | `DELETE_ARCHIVE_PURGE` 审计 COUNT 0→2 |
  | A4 | 二次 purge 幂等拒绝 | PASS | 409「已清除，不可二次清除」 |
  | A5 | 非超管 purge 被拦 | **PARTIAL** | 拦下但 HTTP 500 裸 Spring 体——属 lane3 D-3「DEL 权限包络」既有缺陷（另卡口径），非本波范围 |
- 数据政策登记（R214）：测试写入留库——9150001 贡献度 comment 追加「R219修复波复测」（幂等覆盖，市场/研发比例未变 0.55/0.45→0.55/0.55? 见下条）；实际 P1/P2 均被下游拒绝**未改写**已确认记录；purge 打标 2 行（2103715748890382337 首轮、2103693441849405441 末轮）为验收痕迹数据。
  - 更正：首轮脚本 P2 判据误读时 market-share=0.55 探针**未生效写入**（50002 拒绝），P3 回读 0.5500 为该种子历史既有值，非本波写入。
- 遗留：A5 裸 500 归 lane3 D-3 权限包络卡（若未独立立卡则归 U3 台账卡 a946ab28 补记）；59 条 PARTIAL 修复腿、贡献度→distribute 端到端正例（需一个非确认态 LIFECYCLE 项目，等业务侧推进阶段或新种子）留后续波次。

## 收口核验（现查，非推算）

- 提交方式：主工作树直接 commit（未用 `--no-verify`），pre-commit 四门禁真跑 `passed=4 failed=0 skipped=1`（跳过项=门禁 4 staged 无 `*.sh`）；门禁 1/2 doc↔db 漂移 `drift_count=0`（305s）、门禁 3 孤儿棘轮 `-0 / +19`（本次改 Java 未新增端点）。
- commit `61484b1d`（10 文件 305+/16-）→ push `51c3c858..61484b1d`；`git fetch` 后 `origin/main..HEAD` 积压 **0**，`git status --porcelain` **0 条目**（无兄弟在途文件需让路）。
- 看板两卡独立 GET 回读：`e697a401`、`b494f56e` 均 `status=inreview`、marker `r219-fixwave` 命中、desc_len 193→358 / 177→342（增量 165+165 = 注记长度，无字段丢失）。U3 台账卡 `a946ab28-4466-479e-9b06-3f50dd6adcb4`（todo）现查确认已含权限包络项，A5 无需新立卡。

---

# R219 U1 四卡修复波 · 复测总账（2026-09-26 续，marker=r219-u1wave）

- 归属卡：`96b7b157`（IDOR 越权读）+ `2bef6e0e`（奖金池 500）+ `a995a9e3`（GATE 双 seed/is_veto 混存）+ `ef20c06a`（零触发调度器接线批）。
- 单测（全部 @Tag dev 真跑，错峰单模块无 -am 无 clean）：卡① ProjectServiceTest 17/17；卡② BonusPool 家族 73 全绿（Service 25/25）；卡③ GateElement 家族 54/54；卡④ R219SchedulerWiringTest 16/16 + 影响家族（StageAction*/Allowance*/Bid*/P33*/R218*/GuestDemand*/Notification* 等）本次窗口 354 全绿（surefire 按时间戳现查，旧报告 DiagWrapperTest——源文件已被兄弟轮删除——与两条既有哨兵红不计入本波）。
- 部署：install ruoyi-ipd → package ruoyi-admin `-Dmaven.jar.forceCreation=true`（首次误用 maven.build.forceCreation 被增量跳过，解包比对发现内嵌旧 sha=ec907c8a 拦截，改正后新 sha **465621d9** 与模块产物一致，新调度器/端点 class 10 处确认落包）→ kill 11914 → start-16039.sh 拉起（新 PID **22096**，14.9s Started）。
- 真机 HTTP+真库回读（`retest-u1-wave.json`）：**17/17 PASS**
  | 卡 | 用例 | 关键证据 |
  |---|------|----------|
  | ① | I1–I4 | 在册成员 200；同组非成员 403/30001「无权访问该项目」；不存在项目同文案不泄露；超管跨组 200 |
  | ② | B1–B2 | CONFIRMED 池再 compute：HTTP409 code=50002 新文案「已存在 CONFIRMED 状态奖金池…每项目仅保留一池」（修复前裸 500；首判 FAIL 系判据把错误码 50002 子串「500」误当裸 500，修正判据重跑 PASS）；DRAFT 池 409 原文案 |
  | ③ | G1–G1c | 列表 14 行 isVeto/vetoDual 全归一 '1'/'0'；真库脏行仍 33（读腿归一生效铁证，存量 SQL 留 owner）；draft+enabled=2 漏发行在，评审适用集 published 过滤单测钉死 |
  | ④ | S1–S6 | expire-scan 非超管 403／超管 200 expiredCount=4／真库过期 OPEN 清零；overdue-scan 200 notifiedCount=8，notification_events ACTION_OVERDUE 0→8，同日重扫幂等行数不变；auto-scan 2026-07 先生成后计数 count=8 真库新行落库，非超管 403 |
- 数据政策登记（R214，留库）：4 条邀标 OPEN→EXPIRED（expire-scan 预期行为）；notification_events 新增 ACTION_OVERDUE 8 行（dedupKey 含今日）；allowance_ledgers 2026-07 新 8 行；均为验收痕迹，不清理。
- 遗留（owner 待授权三项）：①卡② 版本链 DDL（uk 改 (project_id,version)）；②卡③ 存量 normalize SQL（2026-09-22-ipd-gate-element-value-domain-normalize.sql）+ 双 seed 物理清理；③卡④ NO_OUTPUT_60_DAYS 腿缺活动数据源（本波诚实不接，代码 javadoc+卡面已标 PARTIAL）。另有次日 09:4x cron 自然触发观察腿（手动端点已代跑实证 service 链，cron 注册随部署生效）。
- 翻卡：四卡 todo→inreview，desc 追加 r219-u1wave 注记（182→525 / 191→588 / 270→881 / 317→1032），独立 GET 回读 4/4 marker 命中无字段丢失。
