# 看板 todo 卡梳理与登录真路径修复（2026-09-07 晚）

独立会话产线：fresh 拉看板（127.0.0.1:62250 / ruoyi-ai 项目）→ 逐卡对照卡面注记、镜像、commit 证据 → 前端真路径修复与验证。本会话未翻任何看板卡、未改 SSOT 镜像（镜像正被兄弟会话修改中）、未 commit。

## 一、看板 fresh 总账（GET /api/tasks?project_id=01dcf15c…）

total 396 = done 305 / todo 28 / inprogress 10 / inreview 8 / cancelled 45。
对照 W28-3 收尾时（total 397 / done 342 / todo 2）：**37 张 done 回退成了 todo**。

## 二、根因：镜像双源失真 → sync 批量打回

- 镜像追加段有 42 处「todo | done」收口登记（例：`开发计划-看板镜像.md` line 520 P4-1.3、line 569 P4-3）；
- 同文件**主表状态列**这些卡仍是 ⬜（抽查 line 55 P2-2 / 59 P2-6 / 61 P2-8 / 62 P3-1 / 68 P3-7 / 71 P4-3 全 ⬜）；
- 看板按主表 sync/重建后，这批卡被打回 todo——done 342→305 与此吻合。

**结论**：直接 PUT 看板翻卡会被下次 sync 再打回。恢复入口是先把镜像主表状态列与追加段对齐（manage.py set 路径，主协调/owner 职责），再同步看板。

## 三、28 张 todo 卡三分类

**A. 真 todo（卡面与状态一致，维持）**：QA-08、QA-07、QA-06（三者均有主协调"维持 todo"驳回注记）、OPS-06（0 commit 失真卡）、P3-8.3、P3-3.3、P1-10.2（19:32 主协调"维持 todo"）、P3-8 / P3-3 / P1-10 汇总（等上述子卡）、P4-4 + P4-4.1、P4-5 + P4-5.1、P2-7 汇总（2.7.1 inreview）。

**B. 业务已收口但被 sync 打回（镜像追加段有登记、主表未跟上，共 7 张）**：
P4-3（子卡 P4-3.1 done）、P4-1.3（223df5f6 测试 commit 真）、P3-1（4 子卡全 done）、P2-8（子卡 done）、P2-6（2 子卡全 done）、P2-2（3 子卡全 done）、P3-7（子卡 P3-7.1 done）。

**C. 失真注记（「翻 done」判定与 fresh 证据矛盾，维持 todo 正确）**：
- DEF-5：注记"翻 done（feat+test 落地）"引 6a2b1703 → fresh 核实为纯 docs commit（决策索引+owner 必看），DB 级 REVOKE/逐表授权修复未落地；
- SEC-04：注记引 3e1babbb → 09-05 docs commit（P0-9.1 验收），阻塞未解；
- P3-4 汇总：注记"子卡全 done"但 P3-4.1 实为 inreview；
- P4-1 汇总：注记"全 done"但 P4-1.3 实为 todo（B 类已单列）；
- P4-2 汇总：注记"全 done"但 P4-2.3 实为 inprogress；
- P1-10 汇总：注记"2 子卡全 done"但 P1-10.2 实为 todo。

## 四、前端真路径修复（ruoyi-ipd-web，均未提交）

1. **sysadmin 登录报「身份信息格式异常」**（上轮会话"100% 可用"的假绿之一；2026-09-06 傅志谦同款）：后端 Jackson 全局 NON_NULL 吞掉 null 键 → sysadmin 无组时 /auth/me 无 groupId 键 → 前端 parseIdentity 严格校验报警。按 2026-09-06 既定决策（memory 98838d6b）修**后端**：`IpdAuthController.PersonView.groupId` 加 `@JsonInclude(ALWAYS)`（编译验证 BUILD SUCCESS，重跑避开兄弟并发假红）；前端**保持严格不放宽**（报警器语义），回归用例改为"键缺失必须抛错"。**生效条件：后端重启窗口**（16039 为共享实例未重启，重启前 sysadmin 浏览器登录仍会报错，ipd-admin 等有组账号不受影响）。
2. **空凭据守卫**：login.vue submit 前拦截，提示「请输入用户名和密码」，不再把空密码打到后端换误导文案。
3. **demo 密码缺失可见化**：dev 下未配置 VITE_IPD_DEMO_PASSWORDS 时登录页黄字提示 + console.warn（不用全局 throw，避免没配 .env 的会话整体起不来）。
4. **dev 账号单一信源**：`docs/dev-accounts.md`（4 账号/角色/personId；密码不入库，指引导 .env.development.local）。
5. **E2E 冒烟脚本**：`scripts/ipd-smoke.mjs`（零依赖；顺带证伪上轮假端点 workbench/today——envelope code=404，真端点为 /api/v1/workbench/summary）。

## 五、验证证据（fresh）

- vitest（vitest.ipd.config.mts）：auth.test.ts + login.test.ts + auth-refresh 相关共 3 文件 **103 用例全绿**；`pnpm run check:type` 零诊断。
- 冒烟脚本 **5/5 PASS**：SPA 非空（3288 字节含 #app）、登录 code=0+traceId、/auth/me、/workbench/summary 全走 15666 代理。
- 真浏览器（真实点击输入）：ipd-admin 登录成功跳 /ipd/workbench（待办 7 真实数据，截图 logs/login-success-workbench.png）；sysadmin 修复需后端重启窗口生效（前端严格校验报警器工作正常）。
- 后端 `mvn -o -pl ruoyi-modules/ruoyi-ipd -DskipTests compile` BUILD SUCCESS（首跑 ERROR 为兄弟并发构建瞬时假红，重跑即绿）。

## 六、待主协调 / owner

1. B 类 7 张：镜像主表状态列对齐追加段后 sync，看板 done 总账才会真实回升；
2. C 类失真注记卡：按卡面"前态记录"惯例处置，DEF-5 的 DB 级修复、QA 系列依赖链按原卡硬门槛推进；
3. 本报告及改动未 commit，等 owner 指令：前端仓 5 处（login.vue / auth.ts 注释与严格校验 / 两个测试文件 / docs+脚本 2 个新文件）；后端仓 1 处（IpdAuthController.PersonView 注解，仅编译验证未部署）。

---

## 七、终态执行报告（2026-09-07 23:15，owner 指令"全部都做"）

### 7.1 提交（均已 commit，未 push）

| 仓 | commit | 内容 |
|---|---|---|
| ruoyi-ipd-web | `7448451` | 7 文件：login.vue 守卫 / auth.ts 报警器注释 / 2 测试文件 / vite 代理 16049→16039 / dev-accounts.md / ipd-smoke.mjs |
| ruoyi-ai | `112e0057` | fix: PersonView.groupId @JsonInclude(ALWAYS) |
| ruoyi-ai | `db89a683` | docs: 本报告首版 |
| ruoyi-ai | `169e1259` | 镜像主表 7 行对齐（git apply --cached 隔离，他人在途 +84 行追加未纳入） |

### 7.2 看板恢复（终态 7 张，fresh 核验后从 9 缩到 7）

- 翻 done：P4-1.3（依赖 P4-1.1/P2-4.1/OPS-05 fresh 全 done + 223df5f6 真测试）→ 连带 P4-1 汇总；P2-2/P2-6/P2-8/P3-7/P4-3 汇总（子卡 fresh 全 done）。看板 PUT 后独立 GET 回读全 done，done 303→310。
- **不翻 P3-1 / P3-1.3**：其收口前提「依赖 P1-9.2 done（21:20 登记）」与看板实态不符（P1-9.2 现 **inprogress**）——fresh 证据优先于历史登记，镜像登记与看板反向失真的又一实证。
- **不翻 P4-5**：子卡 P4-5.1 为真 todo，与「P4 阶段 10 张细卡全 done」登记矛盾。

### 7.3 后端 16039 切换真库（重大事实修正）

- 旧 16039（PID 69535，dev profile）经 processlist 实证连 **docker MySQL 3306 的 `ruoyi-ai` 库**（错库）：docker 实例里同时存在 `ruoyi-ai`(137 表) 与 `ipd_dev`(142 表) **双库副本**，persons 均有 ipd-admin——**此前在该实例上的全部"登录成功"验证（含昨晚浏览器验证）均为错库验证**。
- 已按 owner 待办执行切换：kill 69535 → 以 16040 样板（fat jar + `ipd-local,dev` + external config）重启 16039，命令行追加 `--spring.data.redis.port=6379`（external config 指 16379 已无监听；redisson 段无显式 address，继承 spring.data.redis）。
- 新实例（PID 20242）验证：登录 ipd-admin code=0，**person.groupId 键存在（值 900001）**；jar 内字节码 javap 确认 ALWAYS 注解在位；冒烟 5/5（走 15666 代理全链路）。
- **真库（13306 mysqld，非 docker）实态**：persons 含 2 个 SUPER_ADMIN（系统管理员 id 2096266884…6782 / 傅志谦 9110001）**group_id=NULL 且 ACTIVE**——本修复对真库是现实态刚需，非防御性。

### 7.4 前端 vite 恢复

- 15666 原 vite（PID 1752）已处卡死态（kill -15 无效，最终 kill -9）；nohup 直起 vite 会因无活 pty 卡 read syscall（AGENTS.md 已知坑的深化：node 直起也不够，**必须带活终端**）。现以后台终端（pty）直起，SPA 200（0.017s）。
- `.env.local` 的 VITE_IPD_DEMO_PASSWORDS 已更新为真库 ipd-admin 凭据（等号分隔格式，gitignored，值未落任何日志/记录）；重启后浏览器验证密码自动填充生效。

### 7.5 真浏览器终验

密码自动填充（10 字符）→ 登录成功 → `/ipd/workbench` 渲染 40+ 项目真实数据（待办 7/临期 3/未读 8、任务队列、治理待办），零报错。

### 7.6 遗留 / 待 owner

1. **真库超管密码无信源**：系统管理员/傅志谦的密码不在 config 任何文件中（Ipd@123456 实测 10001）。需 owner 提供或授权重置，才能对「SUPER_ADMIN + groupId=null」路径做 HTTP 实登录验证（当前由 jar 字节码 + 前端契约用例 + ipd-admin 真库登录三元证据支撑）。
2. P1-9.2 inprogress 与镜像 done 登记矛盾待主协调裁决；P4-5 汇总登记矛盾待修。
3. 16040（备用真活）仍在跑，fat jar 已被本轮 package 替换，下次重启会带上新 ruoyi-ipd（HEAD+修复），方向正确无需干预。
4. 两仓 commit 均未 push。
