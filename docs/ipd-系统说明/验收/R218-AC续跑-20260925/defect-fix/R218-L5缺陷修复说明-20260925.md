# R218 波次 L5 缺陷修复车道产出（2026-09-25）

- **车道授权**：本波次唯一 Java 写入者；写范围严格限定卡1/卡2 涉及文件；0 git commit、0 mvn clean、0 -am 全量构建、0 触碰 16039 进程与 jar。
- **环境**：`export PATH="$HOME/tools/maven/bin:$PATH"; export JAVA_HOME="$HOME/tools/jdk-17/Contents/Home"`（Maven 3.9.11 / OpenJDK 17+35），仅在仓库根执行 `mvn -o -pl ruoyi-modules/ruoyi-ipd -Dtest=<类> test`。
- **TDD 纪律**：两卡契约测试均先在**未修改实现代码**上跑出红（原始输出+时间戳见 `evidence/`），修到绿后留档。新测试仅用既有 `@Tag("dev")` 惯例（Surefire `<groups>${profiles.active}</groups>` 默认 dev profile 过滤，无另造花样）。
- **工作树状态**（git status 实录，Java 改动与测试全部留存未 commit）：
  ```
   M ruoyi-modules/ruoyi-ipd/src/main/java/org/ruoyi/ipd/config/IpdSchedulingConfig.java
   M ruoyi-modules/ruoyi-ipd/src/main/java/org/ruoyi/ipd/service/DeletionRequestServiceImpl.java
   M ruoyi-modules/ruoyi-ipd/src/main/java/org/ruoyi/ipd/service/GuestDemandService.java
  ?? ruoyi-modules/ruoyi-ipd/src/main/java/org/ruoyi/ipd/controller/GuestDemandOverdueScanController.java
  ?? ruoyi-modules/ruoyi-ipd/src/main/java/org/ruoyi/ipd/service/GuestDemandOverdueScheduler.java
  ?? ruoyi-modules/ruoyi-ipd/src/main/java/org/ruoyi/ipd/service/executor/RequirementSoftDeleteExecutor.java
  ?? ruoyi-modules/ruoyi-ipd/src/test/java/org/ruoyi/ipd/service/R218AcProd09WiringTest.java
  ?? ruoyi-modules/ruoyi-ipd/src/test/java/org/ruoyi/ipd/service/R218AcReq09DeletionChainTest.java
  ```

---

## 卡1（U1 高）看板 e256007b / AC-REQ-09：需求池删除申请接入双层审核链

### 根因
R218 归因 DEF-A 证实的**三分量缺失**（POST 删除申请对 entityType=requirements 返回 400「不支持的 entity_type: requirements」）：
1. `DeletionRequestServiceImpl.SUPPORTED_ENTITY_TYPES` 白名单 = projects/products/persons/cert_templates/gates，缺 `requirements`；
2. 全仓仅 5 个 `SoftDeleteExecutor`，无 `RequirementSoftDeleteExecutor` → 即使放白名单，终审 `DeleteAuditService.approveAndExecute` 仍会 500「不支持的 entity_type」；
3. `entityExists()`（R217 拍板C 防僵尸申请）与 `resolveScope()`（W5-E-2.2 IDOR 归属校验）switch 均缺 requirements 分量（fail-closed）。

### 改动文件清单
| 文件 | 改动 |
|---|---|
| `service/DeletionRequestServiceImpl.java` | 白名单补 `requirements`；可选注入 `RequirementMapper`（仿 certTemplateMapper 先例，**不动 9 参构造签名**，存量 DeletionRequestServiceTest 零破坏）；`entityExists` 补 requirements 分支（软删行经 @TableLogic 过滤 selectById=null 视为不存在）；`resolveScope` 补 requirements 分支（需求经 productId 上溯所属产品的项目/组归属，与 products 分支同口径；productId=NULL 的「其他/不确定」需求非超管 fail-closed 拒绝） |
| `service/executor/RequirementSoftDeleteExecutor.java`（**新增**） | 照 Gate/Project 既有模式：显式 `LambdaUpdateWrapper UPDATE del_flag='0'→'1'`（@TableLogic 实体 updateById 静默失效的 R216 实测教训同款防御）；`isDeleted` 先行判定幂等 NOOP；rows≠1 抛错防假删。经 Spring `List<SoftDeleteExecutor<?>>` 注入**自动注册**进 DeleteAuditService，无需改该文件（写范围最小化） |
| `test/.../R218AcReq09DeletionChainTest.java`（**新增**） | 7 例契约：①submit(requirements)→LEADER_REVIEW ②未知类型仍 400（防扩表过度）③双层链复用 submit→leaderDecision→adminDecision→approveAndExecute（**复用既有实现，未另起炉灶**）④不存在实体→NOT_FOUND 零写入 ⑤本组组长可发起/外组组长 FORBIDDEN ⑥approveAndExecute 显式 SET del_flag+DELETE_EXECUTE 审计同事务 ⑦已删/不存在→DELETE_NOOP 不重复 UPDATE |

双层审核链语义确认：需求池删除与其余 5 类实体完全同链——DRAFT→**LEADER_REVIEW（组长初审，2 工作日）**→**ADMIN_REVIEW（超管终审，2 工作日）**→DELETED（原子软删）/REJECTED/WITHDRAWN，期限/升级/守卫（preCheck/postCommit）全部复用。

### 验证证据（evidence/ 原始输出）
- **红**：`card1-red-run-raw.txt`，2026-09-25T20:13:53-0700——`submitRequirementsEntersDualReviewChain` 在未修实现上抛 `IpdBusinessException: 不支持的 entity_type: requirements`（DeletionRequestServiceImpl.java:464，与看板 400 错误逐字一致，自证复现）。
- **绿**：`card1-card2-green-run-raw.txt`，2026-09-25T20:18:51-0700——R218AcReq09DeletionChainTest **7/7 绿**；同趟回归 DeleteAuditServiceTest 13/13、DeletionRequestServiceTest 25/25、P413AcceptanceTest 9/9 绿，合计 59/59 BUILD SUCCESS。
- **扩面回归**：`regression-run-raw.txt`，20:19:19 趟暴露 1 红 `ServiceBareClockGuardTest`（指向 `DeliverableService:133` 裸 `new Date()`）——**非本车道文件**（git status 证实；该文件系今日 08:23 兄弟卡 R217/P1-4.2 commit 07e68ef6 入库的存量红，见「未尽风险」）。剔除该类重跑 12 个相关类 **78/78 绿**（同文件第二段，BUILD SUCCESS）。

### log.md 第十七轮误标✅勘误（供主会话统一登记，本车道未改 log.md）
- **位置**：`docs/ipd-系统说明/log.md:1064`（2026-09-05 16:03 第十七轮 P0-6.1 集成真闭环段）。
- **误标事实**：该轮以**通用 entityType**（e2e 证据链未标注 requirements；当时白名单根本不接收 requirements，物理上不可能验证过需求池实体）走通删除双层链后，将 `AC-REQ-09 ✅ 需求池双层（组长初审+超管终审）真实业务流验证` 记为通过——**属误标**：AC-REQ-09 的被验对象是「需求池记录」，通用实体走通不等于 requirements 分量接通。
- **佐证链**：R218 归因报告 DEF-A「附加发现」条目（ac-领域-归因.md:31）；本轮修复红跑复现的 400 文案与该缺口自洽。
- **建议勘误文案**：`log.md:1064 AC-REQ-09 ✅ → 勘误为 ❌（2026-09-25 R218 归因确诊）：第十七轮仅以通用 entityType 验证双层链，requirements 实体分量从未接通（400 不支持），R218 L5 车道修复后待 16039 部署复测再翻 ✅`。

### 部署后待复测断言（主会话 HTTP 腿，16039 换装新 jar 后）
1. `POST /api/v1/deletion-requests {entityType:"requirements", entityId:2103338131276259329, reason/snapshot}` → 200，status=LEADER_REVIEW，leaderDueAt=+2 工作日。
2. 组长初审（本组组长凭证）`POST /{id}/leader-decision?approve=true` → 200 ADMIN_REVIEW；外组组长凭证初审 → 403。
3. 超管终审 `POST /{id}/admin-decision?approve=true` → 200 status=DELETED 且 executedAt 非空；真库 `requirements` 该 id `del_flag='1'`；`audit_logs` 有 `DELETE_EXECUTE`（entityType=requirements）行。
4. 幂等腿：对已软删实体再走一遍申请+终审 → 终态 DELETED 且审计为 `DELETE_NOOP`，无重复 UPDATE。
5. 防僵尸腿：超管对不存在 requirement id 发起 → 404 NOT_FOUND「目标实体不存在: requirements/xxx」（R217 语义在新分量上生效）。
6. 回归腿：projects/products 等原 5 类删除链抽一类复测不变红。

---

## 卡2（U2 中）看板 f62ab684 / AC-PROD-09：待指派超5工作日提醒零触发接线

### 根因
R218 归因 DEF-B 确诊的**双重偏差**：
1. **零触发**：`GuestDemandService.notifyOverdueUnassigned()`（:354-371）全仓唯一引用=定义处；11 个 @Scheduled 类无一包装、无 Controller 端点；真库 `audit_logs action LIKE '%overdue_unassigned%'`=0 行（从未真实运行）。单测 P413 直接调 service 方法断言返回值，不覆盖触发路径，掩盖「实现完成但入口不可达」。
2. **通知对象偏差**：方法体只写 audit 不 publish 真通知（注释自称「桥接见后续 P2-4.1」——桥接未发生）；且注释口径=通知超管，AC 文本=提醒**产品组组长**。

### 改动文件清单
| 文件 | 改动 |
|---|---|
| `service/GuestDemandService.java` | 可选注入 `NotificationService`+`PersonMapper`（仿 certTemplateMapper 先例，不动 4/5 参构造，P413 存量测试零破坏）；`notifyOverdueUnassigned` 每条逾期需求 audit 后 `publishDaily` 真通知：**接收人=该产品组组长**（productId→products.group_id→persons GROUP_LEADER 未软删，GateReviewService.collectLeaders 同源口径）；组无在任组长/productId=NULL「其他」需求 → 升级在任超管（ACTIVE SUPER_ADMIN，HandoverOverdueScanner 兜底同口径）；**工作日口径**沿用既有 `subtractBusinessDays`（逐日回退跳周六日，明确注释未计法定节假日=既有登记限制）。事件类型本地常量 `DEMAND_OVERDUE_UNASSIGNED`（避免跨文件改 Types 目录）；publishDaily dedupKey 含自然日=同日重扫幂等不重发、次日可再提醒（KPI/移交催办同款语义） |
| `service/GuestDemandOverdueScheduler.java`（**新增**） | `@Scheduled(cron="0 40 9 * * ?")` 每日 09:40 触发扫描（照 KpiSharedDeadlineScheduler 惯例；09:00/05/10/15/20/25/30/35 已占用，09:40 无冲突） |
| `config/IpdSchedulingConfig.java` | 错峰登记表注释追加本 job 行（该文件注释即调度惯例的登记簿，@EnableScheduling 已在主树生效） |
| `controller/GuestDemandOverdueScanController.java`（**新增**） | `POST /api/v1/guest-demands/overdue-scan`，`permission.requireAdmin()`（仅超管手动触发，LegacyScanController 同型），返回 `{notifiedCount}` —— 验收复测与 ops 补扫兜底 |
| `test/.../R218AcProd09WiringTest.java`（**新增**） | 5 例契约：①调度器存在且 @Scheduled 方法实际委托 service（反射 Class.forName+构造注入 mock 验证，红阶段可编译、以行为失败自证）②手动端点存在且映射路径断言 ③扫描命中→publishDaily 给组长 777+audit 不丢 ④无组长→升级 ACTIVE 超管 900 ⑤协作者未装配优雅降级（仅 audit 不 NPE，保护 5 参构造存量测试） |

### mock 合法性自查（WB-17-1 教训 / mock合法性与已知死路登记-20260908.md 规约）
- 测试数据组合 `status=SUBMITTED + productId 有值 + marketPmId/rdPmId 均空 + createTime>5 工作日` = 真库写入路径可产出：游客选 ACTIVE 产品提交→`resolveDualPm` 命中 `project-no-pm` 分支（产品 1:1 项目无在职 MARKET_PM/RD_PM）→需求原样入库不路由（GuestDemandService.submit:129 真实现路径）。
- `persons(personType=GROUP_LEADER, groupId=产品组, del_flag=0)` 与 `ACTIVE SUPER_ADMIN` 均为真库在库人员形态（dev-accounts/种子数据同源）。
- 未造「不可能状态」：未使用 ACCEPTED 态超时、未用已路由需求冒充待指派等假绿组合。

### 验证证据（evidence/ 原始输出）
- **红**：`card2-red-run-raw.txt`，2026-09-25T20:15:13-0700——未修实现上 4 红 1 绿：`ClassNotFoundException: GuestDemandOverdueScheduler`、`ClassNotFoundException: GuestDemandOverdueScanController`、publish 契约×2 报「契约缺失：GuestDemandService 未装配 NotificationService」（与归因 DEF-B 三条取证逐条对偶自证）；⑤降级用例当时即绿（作回归守卫保留）。
- **绿**：`card1-card2-green-run-raw.txt`，2026-09-25T20:18:51-0700——R218AcProd09WiringTest **5/5 绿**，日志实录 `GuestDemandOverdueScheduler: AC-PROD-09 待指派超时扫描完成`；同趟 P413AcceptanceTest 9/9 绿（旧构造路径不破坏）。

### 部署后待复测断言（主会话腿）
1. **调度腿**：部署次日 09:40 后查 `audit_logs action='overdue_unassigned'` 从 0 行变为 ≥ 逾期存量数（真库现存 requirements id=2103338131276259329 若仍 SUBMITTED+超期应命中）；`notification_events` 出现 `event_type='DEMAND_OVERDUE_UNASSIGNED'`、`receiver_id=该产品组组长`、dedupKey 含当日 yyyyMMdd。
2. **手动腿**：超管凭证 `POST /api/v1/guest-demands/overdue-scan` → 200 `{notifiedCount:N}`；同日二次调用 notifiedCount 同值但组长收件箱**不新增**行（publishDaily 同日幂等）；非超管凭证 → 401/403。
3. **对象对齐腿**：notification_events receiver 与 `products.group_id→product_groups 组长` 一致（AC 文本口径），不再仅超管注释口径。
4. **降级腿**：组无在任组长存量（如有）→ receiver 落在 ACTIVE SUPER_ADMIN 人员，不静默。
5. **回归腿**：需求池提交/查询码链路（P4-1.1/1.2）HTTP 抽测不变红。

---

## 未尽风险（两卡合并登记）
1. **兄弟卡存量红**：`ServiceBareClockGuardTest` 现对 `DeliverableService:133` 裸 `new Date()` 红——非本车道写范围（git status 证实未触碰），系 R217/P1-4.2（commit 07e68ef6，今日 08:23 入库）引入的白名单缺口，建议主会话转该卡车道处置或登记白名单豁免。
2. **16039 仍跑旧 jar**：本车道交付=代码+单测红→绿证据+待复测清单；HTTP/e2e 腿全部留给主会话部署（重新打包+轮换进程）后执行。
3. **工作日口径限制**：`subtractBusinessDays` 跳周末不跳法定节假日（既有工具口径，注释已明确）；国庆类长假会推迟提醒触发，与 P4-1.3 复核记录登记口径一致，未擅自扩面。
4. **扫描谓词未扩**：`notifyOverdueUnassigned` 仍只扫 `status=SUBMITTED`（P4-1.3 原语义）；`routeDualPm` 回退路径理论上可产生 UNASSIGNED 态需求，若验收裁决认定 UNASSIGNED 亦属「待指派」，需另卡扩谓词（本卡守写范围不并包）。
5. **多组长/多超管**：组内多名 GROUP_LEADER 时逐人发布（receiver 维度天然去重）；超管单在任不变式由 ZK-IPD §九保证，若违例存量存在则逐人知会（保守不静默）。
6. **log.md:1064 勘误未落笔**：按卡面纪律仅产出勘误文案（见卡1 段），待主会话统一登记。
