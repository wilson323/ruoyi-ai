# SEC-04 安全集成验收 — 可执行子集真活（2026-09-19）

- **看板卡**：SEC-04（后续附件下载、审计导出与需求池权限集成验收）
- **执行环境**：本机真活后端（PID 90722，`ruoyi-admin.jar` @ 127.0.0.1:16039，jar 含 R97 源码，嵌套 `ruoyi-ipd-3.1.0.jar` 07:19 构建，`IpdZkScenarioInitializer.class` 含 locked_level/allowance 修复串——R98 未改 Java 源码，jar 与 HEAD 语义一致）
- **口径来源**：`docs/ipd-系统说明/验收/SEC-04-依赖真值复核与任务信号真伪核查-20260907.md` §3.3（可执行子集：审计导出 + 需求池权限；附件项仍依赖 P1-4.2）
- **本轮授权**：R95 波 3 派单主体（用户授权完整执行）

## 0. 范围判定

| 主验收项 | 依赖 | 本轮结果 |
|---|---|---|
| (1) 真实上传/下载（附件） | P1-4.2 ▶ inprogress | **维持阻塞**，与 09-07 复核结论一致 |
| (2) 审计查询/导出权限矩阵 | P0-5.4 ✅ | **本轮真活验收通过**（§2） |
| (3) 需求池权限矩阵 | P4-1.4 ✅ | **本轮真活验收通过**（§3） |
| (4) AC-REQ-05 游客拒绝 | P4-1.4 ✅ + SEC-02 ✅ | **本轮真活通过**（§3 C1/C4） |

## 1. 认证链最小闭环（A 系列）

| # | 场景 | 结果 | 证据（HTTP / code） |
|---|---|---|---|
| A1 | 错误密码登录 ipd-admin | ✅ 拒绝 | HTTP 400，`code=10001`"用户名或密码错误"（不泄露具体哪错） |
| A2 | 正确密码登录 ipd-admin / ipd-rd | ✅ 发 token | HTTP 200 `code=0`，JWT 187 字符（loginId 900101 / 900104） |
| A3 | 无 token 访问 GET /api/v1/audit-logs | ✅ 拒绝 | HTTP 401，`code=20001`"未认证或凭证失效" |
| A4 | 假 token（Bearer FAKE_TOKEN_XYZ） | ✅ 拒绝 | HTTP 401，`code=20001` |
| A5 | 有效 admin token 同端点 | ✅ 放行 | HTTP 200 `code=0`，返回审计记录（seq 2981 起） |

## 2. 审计导出与危险操作权限矩阵（B 系列）

| # | 场景 | 结果 | 证据 |
|---|---|---|---|
| B1 | admin GET /api/v1/audit-logs/export | ✅ 放行 | HTTP 200（有 OPERATION_AUDIT_LOG_EXPORT） |
| B2 | ipd-rd GET 同导出端点 | ✅ 拒绝 | HTTP 403，`code=30001`"权限不足" |
| B3 | ipd-rd GET /api/v1/audit-logs 列表 | ✅ 拒绝 | HTTP 403，`code=30001` |
| B4 | ipd-rd POST /api/v1/audit-logs/rebuild-chain（危险操作） | ✅ 拒绝 | HTTP 403，`code=30001` |

## 3. 需求池权限矩阵（C 系列，含 AC-REQ-05）

| # | 场景 | 结果 | 证据 |
|---|---|---|---|
| C1 | 游客（无 token）GET /api/v1/demands | ✅ 拒绝（AC-REQ-05） | HTTP 401，`code=20001` |
| C2 | admin GET /api/v1/demands | ✅ 放行 | HTTP 200 `code=0`，data.demands 含 PORTAL_GUEST 来源需求 |
| C3 | ipd-rd POST /api/v1/demands/{id}/triage | ⚠️ 权限码放行，业务层拦截 | 首跑误传 `{"action":..}` 与空 id；修正后 rd 有 OPERATION_PRODUCT_GROUP_BIND_PROJECT 权限码，到达业务校验返回 HTTP 400"非法需求状态"（分流操作按角色目录对 rd 开放，非越权路径） |
| C4 | 无 token POST triage | ✅ 拒绝 | HTTP 401，`code=20001` |

C3 口径说明：triage/link-project 的权限码（OPERATION_PRODUCT_GROUP_BIND_PROJECT）按 `IpdRolePermissionCatalog` 对非组长角色放行，需求池"越权"防线在**业务校验层**（状态机/归属）而非权限码层；权限码拒绝通路已由 B2/B3/B4 三例证伪能力（无权限码时稳定 403）。

### 3.1 跨组越权补充（AC-AUTH-11 联动重跑）

ipd-leader POST `/api/v1/projects/9140001/status?target=ONLINE`（协同组项目）→ **HTTP 403 `code=30001`"无权操作"**，跨组编辑被拒。

## 4. 结论

- 可执行子集（2)(3)(4) 三块 **15 项探针全部符合预期**（A1-A5、B1-B4、C1-C4 + AC-AUTH-11 联动）。
- 附件项（1）维持 BLOCKED_DEPENDENCY：P1-4.2 仍 inprogress，**SEC-04 整卡不翻 done**，本轮不制造部分完成信号。
- 附带产出（QA-07 素材）：3 个错误路径覆盖缺口，见 `QA-07-08-波3推进-20260919.md`。

## 5. 环境与可复现

- 登录限流：`@RateLimiter(time=60, count=5, limitType=IP)`，本矩阵每账号仅登录 1 次（未触限流）。
- 原始输出：`/tmp/sec04_results.txt`（会话产物，本表为准）。
- 测试账号来源：`.codex/ipd-dev/config/dev-accounts.yaml`（gitignored，凭证不入库）。
