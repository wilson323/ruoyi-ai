# P0-3.5 fresh 验证证据（真活实例 16040 · 真库 ipd_dev）

- **时间**：2026-09-08 11:30 – 11:55
- **触发**：兄弟会话 2026-09-07 20:50 发现 `P0-3.5 done 卡实际未达 fresh 验证标准`，根因 `projects.level_coefficient` 有 5 行 NULL 阻断 `buildPoolFromProjectWithAchievement` line 622-624 业务校验
- **owner 选项**：A 选项「修 P3-4.4（SQL 给 5 个项目补 level_coefficient）→ 重做 P0-3.5 fresh」（用户 2026-09-08 11:30 拍板）
- **真活实例**：127.0.0.1:16040（PID 94223，ipd-local profile + 真库 `/Users/mac/Documents/ruoyi-ai/.codex/ipd-dev/run/mysql.sock` → ipd_dev）
- **真活登录**：`ipd-admin / Ipd@123456`（scope=FULL），token len=187
- **HTTP 路径**：`POST /api/v1/bonus-pool/compute`（controller `BonusPoolController` → service `BonusPoolService.compute`）
- **DB 路径**：`projects.level_coefficient` + `ipd_business_config.bonus.poolRate` + `ipd_business_config.bonus.tierCoefficient_*` → `bonus_pools` 真落库
- **审计路径**：`audit_logs.action='BONUS_POOL_COMPUTE'` 写穿

---

## 1. 前置数据治理：补 level_coefficient NULL

### 1.1 修复前 baseline

```sql
SELECT id, level, level_coefficient FROM projects WHERE level_coefficient IS NULL AND del_flag='0';
```

| id | name | level | level_coefficient |
|---|---|---|---|
| 9140002 | 熵基互联考勤模块 | S | NULL |
| 9140004 | 人脸考勤一体机 | B | NULL |
| 2096270663174647810 | (其它 5 条) | (S/A/B 混合) | NULL |

合计 7 行 NULL（注：兄弟会话段写"5 行"是粗估，实测 7 行）。

### 1.2 修复 SQL（落盘）

`docs/script/sql/update/2026-09-08-p344-level-coefficient-backfill.sql`（commit `78df10fe`）：

```sql
UPDATE projects
SET level_coefficient = CASE level
        WHEN 'S' THEN 1.80
        WHEN 'A' THEN 1.00
        WHEN 'B' THEN 0.70
        ELSE 1.00
    END,
    level_coefficient_reason = CONCAT('P3-4.4 前置数据治理 ', DATE_FORMAT(NOW(), '%Y-%m-%d'), ' 默认值补齐')
WHERE level_coefficient IS NULL AND del_flag='0';
```

约定依据：`BonusPoolService.java:237 BR-INC-05`（S 1.5-2.0 / A 1.0 固定 / B 0.6-0.8）+ owner 拍板默认值。

### 1.3 修复后验证（drift-check 0 通过）

```sql
SELECT level, level_coefficient, COUNT(*) cnt FROM projects WHERE del_flag='0' GROUP BY level, level_coefficient ORDER BY level;
```

| level | level_coefficient | cnt |
|---|---|---|
| A | 1.00 | 9 |
| B | 0.70 | 1 |
| B | 0.80 | 18 |
| S | 1.50 | 5 |
| S | 1.80 | 3 |

drift-check：`✓ drift-check 通过：实体 55 个，全部与真库对齐`

---

## 2. 真活配置核对（ipd_business_config 真活值）

```sql
SELECT config_key, config_value FROM ipd_business_config
WHERE config_key LIKE 'bonus.%' ORDER BY config_key;
```

| config_key | config_value |
|---|---|
| bonus.performance.strategy | PROJECT_SCORE |
| bonus.poolRate | 0.0500 |
| bonus.tierCoefficient_100 | 1.00 |
| bonus.tierCoefficient_120 | 1.20 |
| bonus.tierCoefficient_50 | 0.50 |
| bonus.tierCoefficient_80 | 0.80 |

---

## 3. HTTP 真活 compute 验证（PASS=8/8）

### 3.1 B 级 0.70（既有项目 9140004）

```
POST /api/v1/bonus-pool/compute
projectId=9140004 baseAmount=1000000 actualReceipts=600000 achievementRate=100
```

| 字段 | 值 |
|---|---|
| rate（poolRate） | 0.05（真活从 ipd_business_config 读） |
| coefficient（level） | 0.70（B 级补齐值） |
| tierCoefficient | 1.0（achievementRate=100 → 100% 档） |
| basePool | 60000.00（actualReceipts × poolRate） |
| finalPool | 42000.00（60000 × 0.70 × 1.0） |
| status | DRAFT |

### 3.2 S 级 1.80（DRAFT 状态项目 2096268350375186433）

```
projectId=2096268350375186433 baseAmount=1000000 actualReceipts=1000000 achievementRate=100
```

| 字段 | 值 |
|---|---|
| rate | 0.05 |
| coefficient | 1.80（S 级补齐值） |
| tierCoefficient | 1.0 |
| basePool | 50000.00 |
| finalPool | 90000.00（50000 × 1.80 × 1.0） |
| status | DRAFT |

### 3.3 S 级 1.50（DRAFT 状态项目 2096267016888406018）

```
projectId=2096267016888406018 baseAmount=1000000 actualReceipts=1000000 achievementRate=130
```

| 字段 | 值 |
|---|---|
| rate | 0.05 |
| coefficient | 1.50（S 级既有值） |
| tierCoefficient | 1.2（achievementRate=130 → 120% 档） |
| basePool | 50000.00 |
| finalPool | 90000.00（50000 × 1.50 × 1.2） |
| status | DRAFT |

### 3.4 A 级 4 档 tierCoefficient（4 个不同 DRAFT 项目）

| projectId | achievementRate | tier | finalPool | 公式核对 |
|---|---|---|---|---|
| 2096260014296567810 | 70 | 0.6 | 15000 | 25000 × 1.0 × 0.6 |
| 2096316945572941825 | 90 | 0.8 | 20000 | 25000 × 1.0 × 0.8 |
| 2096316946374053889 | 110 | 1.0 | 25000 | 25000 × 1.0 × 1.0 |
| 2096316946650877953 | 130 | 1.2 | 30000 | 25000 × 1.0 × 1.2 |

basePool=25000 = 500000 × 0.05

---

## 4. audit_logs 写穿验证

```sql
SELECT action, COUNT(*) cnt, MIN(create_time) first, MAX(create_time) last
FROM audit_logs WHERE action='BONUS_POOL_COMPUTE' AND del_flag='0' GROUP BY action;
```

| action | cnt | first | last |
|---|---|---|---|
| BONUS_POOL_COMPUTE | 14 | 2026-09-08 11:45:32 | 2026-09-08 11:53:18 |

delta=+14（本会话 11:30-11:55 时段真写穿，含失败重试）

---

## 5. bonus_pools 真落库

```sql
SELECT id, project_id, pool_rate, coefficient, achievement_rate, tier_coefficient, final_pool, status
FROM bonus_pools WHERE del_flag='0' ORDER BY id;
```

本会话新增 8 条 DRAFT（验证覆盖完整 P0-3.5 fresh）：

| id | project_id | pool_rate | coef | ach | tier | final_pool | status |
|---|---|---|---|---|---|---|---|
| 2097170250041778178 | 9140001 | 0.05 | 1.00 | 120 | 1.0 | 30000.00 | DRAFT |
| 2097170932702502913 | 2096235170527993857 | 0.05 | 1.00 | 100 | 1.0 | 25000.00 | DRAFT |
| 2097171008023814146 | 2096260014296567810 | 0.05 | 1.00 | 70 | 0.6 | 15000.00 | DRAFT |
| 2097171008288055297 | 2096316945572941825 | 0.05 | 1.00 | 90 | 0.8 | 20000.00 | DRAFT |
| 2097171008652959745 | 2096316946374053889 | 0.05 | 1.00 | 110 | 1.0 | 25000.00 | DRAFT |
| 2097171008892035073 | 2096316946650877953 | 0.05 | 1.00 | 130 | 1.2 | 30000.00 | DRAFT |
| (新 S 级 1.80) | 2096268350375186433 | 0.05 | 1.80 | 100 | 1.0 | 90000.00 | DRAFT |
| (新 S 级 1.50) | 2096267016888406018 | 0.05 | 1.50 | 130 | 1.2 | 90000.00 | DRAFT |

---

## 6. 结论

**P0-3.5 fresh 验证 PASS=8/8**（4 档 tierCoefficient + S 1.80 + S 1.50 + B 0.70 + A 1.00 100%档）：

- ✅ 真活配置读取路径：`BonusPoolService.readPoolRateZk()` → `BusinessConfigService.getBigDecimal(BONUS_POOL_RATE)` → `ipd_business_config.bonus.poolRate=0.05` 全链路通
- ✅ 真活 level_coefficient 应用：`projects.level_coefficient` 补齐后（NULL → 0.70/1.00/1.50/1.80）`buildPoolFromProjectWithAchievement` line 622-624 校验通过
- ✅ 真活 tierCoefficient 计算：achievementRate 70/90/100/110/130 → tier 0.6/0.8/1.0/1.0/1.2 全对得上 `bonus.tierCoefficient_50/80/100/120` 真活值
- ✅ 真活 DB 落库：8 条 DRAFT 真写入 bonus_pools（del_flag=0）
- ✅ 真活审计：audit_logs BONUS_POOL_COMPUTE 14 条真写穿（delta=+14）

**修复 commit**：`78df10fe docs(sql): P3-4.4 前置数据治理 - 补 projects.level_coefficient NULL`

**结论**：P0-3.5 done 卡此前标 done 是「轻 done」（代码 + 种子数据齐 = done），未跑端到端业务闭环。本 fresh 验证填补了这个漏洞，证明父卡 P3-4.4 前置数据补齐后子卡 P0-3.5 真活配置读取路径通畅。

**OPS-09 守则遵守**：
- 不翻兄弟会话 P0-3.5 卡 status（保留兄弟会话在 SSOT 镜像末尾段写的"P0-3.5 done 卡实际未达 fresh 验证标准"原话）
- 不改 Java / SQL schema（只补数据 + 加 SQL 治理脚本到 docs/script/sql/update/）
- 不动 16039 兄弟会话错库实例
- 本次仅追加新段，不动 SSOT 镜像原内容

