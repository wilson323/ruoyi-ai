# R219 台账缺陷修复波·收口记录（a946ab28 / AC-LEDGER · 2026-09-26）

> 本波唯一 Java 写入者：R219 主协调延续会话。范围＝R218-AC 续跑四车道分诊产物（缺陷台账）可自主落地项。
> 纪律：错峰单模块 `mvn -o -pl ruoyi-modules/ruoyi-ipd -Dtest=<类> test`（不带 -am/clean）；新测试仅用既有 `@Tag("dev")`；
> 凭证仅内存不打印；seed/真机腿走 `.codex/ipd-dev/config/mysql-client.cnf` socket（MySQL 8.0.46@13306，ipd_dev）。

## 一、已修复并端到端验证（5 个 commit，全部 push origin/main=224d8044）

| 批 | commit | 缺陷 | 落点 | 单测证据 | 真机/真库证据 |
|---|---|---|---|---|---|
| 1 | `43fd16d4` | ⑧ 局部 ExceptionHandler rethrow 塌 500 → 就地 403 包络 | DeletionRequestController | Sec03 12/12 | — |
| 1 | `43fd16d4` | ④ disableIfAllCleared 守卫接受 ACTIVE/FROZEN 双态（resign 后全清真禁用） | HandoverService | Handover 6/6、P411 13/13 | — |
| 1 | `43fd16d4` | ⑮ lookupPmPair BOTH 补研发 PM（双 PM 共同担责 ×2 不漏发） | NegativeFeedbackService | P382 16/16 | — |
| 1 | `43fd16d4` | ⑦ 待指派口径收口（无 UNASSIGNED 态，删 or 死守卫防 SQL 优先级误更他行） | 需求/待指派 SQL | R218AcProd09 5/5 | — |
| 2 | `10b3ad8f` | ⑪ budgetTokens 配置面丢弃 | AiModelSaveReq + AiModelConfigService（create 落键/validate 非负/update 三态合并 null 沿用·0 显式覆盖） | P421 14/14（+2 契约） | — |
| 3 | `c291baa8` | ③ 通用 PUT /system-configs 直写绕过 KPI 共担 30% 下限（lane3 D-1） | SystemConfigServiceImpl.update 键级值域守卫（对齐 GateEngine A 级键先例，只验不改写） | P033 16/16（+2 契约） | **PUT kpi.sharedWeight=0.1 → HTTP 400 code=10001「不得低于 30%」，本体值仍 0.4 未落库** |
| 4 | `43789d1c` | ① AC-CFG-01 系数面值 7 处硬编码 | KpiSharedCollectionService.readK04Weight 接 `system_configs.kpi.k04Weight`；ProjectService 奖金池面值收敛到 BonusPoolService 单一源 + 3 参配置化重载；S/B 系数区间与文案改常量拼装 | 12 类 164 用例全绿 | **seed `docs/script/sql/update/ipd_r219_k04_weight_seed_20260926.sql` 已 apply，回验行 id=1948092601 value=0.05 存在** |
| 5 | `224d8044` | ② AC-CFG-02 双配置源漂移：PUT /system-configs 只写 system 行、实际生效源 ipd_business_config 不变（黑洞） | SystemConfigServiceImpl setter 注入 IBusinessConfigService；DUAL_SOURCE_KEYS={gate.signDeadlineDays,bonus.poolRate} 写代理同事务收敛两源；同值判定改数值语义；短路分支内自愈；Controller 短路前复核生效源 + GET/PUT 回显 source | R219DualConfigSourceSyncTest 7 用例；P032/P033/Cache 回归全绿 | **PUT gate.signDeadlineDays=5 → sys/biz 同步收敛到 5，回显 source=BUSINESS_CONFIG；restore=3 干净** |

- 累计回归（本会话复跑）：批 2/4 消费配置面 P421=14 / K04Config=2 / K04Collect=8 / BonusPool=25 / ProjectService=17 / P121=7 全绿；接口新增方法 `mvn test-compile` 全模块通过，无其他实现受累。
- 部署一致性：install ruoyi-ipd → 强制重打 ruoyi-admin.jar（`47c1cd5b68311b8bd85579c288e6af8a9798cbade8b1ca78e85f9020e2e425`），内嵌 ipd 类经 javap 确认含 ②⑪ 新符号；16039 已换装该 jar 起新进程。

## 二、本轮登记不落地（边界锁定，待协调会话在 a946ab28 卡面逐项登记处置）

| 缺陷 | 分类 | 不落地理由 | 建议出口 |
|---|---|---|---|
| 游客 withdraw/supplement 无路由 | 需新增端点 | 撞门禁 3 API 契约孤儿棘轮 R212；`ratchet-data-guard.cjs` 阻断 agent 直接编辑 baseline/白名单 | 白名单登记须挂卡人审后落 |
| 产品 status 口径错位（mk_product 默认 IN_RD，游客需求 GuestDemandService 要求 ACTIVE） | 产品裁决项 | 改默认值 / 改校验口径 / IN_RD 本身正确——属业务语义裁决，非工程可自决 | 产品确认后单独立卡 |
| 50001 错误码位（属线契约码值变更） | 线契约破坏性变更 | 改码值影响既有前端/消费方，须显式裁决 | 登记待裁决，本轮不改码值 |
| 前端 i18n 未收口 | 前端仓独立 | 产物在 `ruoyi-ipd-web`，非本后端仓范围 | 前端仓单独立项处置 |
| 津贴绑定名额死锁 / MISSED_WINDOW 双停 | 产品裁决项 | 涉及绩效/津贴业务规则裁决 | 登记待专项 |

> 说明：上述登记项的具体台账序号（⑤⑥⑨⑩⑬⑭ 等）以 a946ab28 卡原文为准；本记录按缺陷内容描述、不凭序号记忆硬映射，避免编号撞车漂移。

## 三、状态

- 代码：6 个 commit（43fd16d4/10b3ad8f/c291baa8/43789d1c/224d8044/e246527d）全部 push，工作树 clean（local==origin==e246527d，本条更正原误记的 224d8044）。
- 真库：kpi.k04Weight seed 已 apply；gate/bonus 双源键复测后已复原（gate.signDeadlineDays=3/3）。
- 真机：②③ 已在换装后的 16039 上 HTTP+DB 复测通过（见上表）。
- 看板：a946ab28 已于 2026-09-26 走 HTTP 外部直投卡协议翻 `todo → inreview`（LIST 取基文 + desc 追加 `[r219-ledger-close-wave]` 注记 + PUT + 独立 GET 回读核验：status=inreview、desc_len 1186、marker 在位）。§二 登记项的处置仍待 owner 在卡面裁决。
- 工具链路根除（2026-09-26 复盘）：本会话曾误报「看板改不了」。实为 Qoder 与 Claude Code 的 MCP 注册表彼此独立——`zker_vibe_kanban` 只登记在 `~/.claude.json`，Qoder 侧 `~/.qoder-cn/settings.json` 从未有过该条目。已实证容器内 `/opt/vibe-kanban-mcp` stdio 握手可用（v0.0.168）并登记进 Qoder 配置（备份 `settings.json.bak-r219-mcp`，重启 Qoder 后生效）；本次卡面翻转走 manage.py 同源 HTTP API 通道闭环，不依赖 MCP 即时可用。
