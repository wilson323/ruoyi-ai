# 实体↔真库 drift 根治轮验收报告（2026-09-07）

## 1. 触发与授权

用户指令：「基于系统性梳理全局项目，深度思考反思根源性原因，充分利用蜂群模式根源性进行修复」。

前置发现（本会话前段，fresh 证据）：
- 看板 396 张卡中 45 张「status ≠ done 但 description 末段判 done」失真候选；
- `P2-7.4` 卡 done 但真库 `handover_records` 缺列、0 行——Mock 单测绿不等于真业务通；
- 根因定性：**仓库无 Flyway/Liquibase，`docs/script/sql/update/**` 全靠人工 apply，无「实体↔真库」比对门禁**，导致「SQL 已 commit ≠ 对象已生效」系统性积压。

## 2. 根因分析（四层）

1. **工具层**：无迁移框架，无 drift 门禁——写了 SQL 文件即视为完成，无人回读 information_schema。
2. **流程层**：done 判定只看「commit 在 + 单测绿」，Mockito stub 绕过真 SQL，DDL 漏迁移不可见（P2-7.4 同款坑第二撞）。
3. **协同层**：多会话共工，A 会话 apply、B 会话不知情；快照分钟级过期（本轮实测：盘点间隙兄弟会话 apply 了 10 张表，132→138 表）。
4. **数据层**：积压迁移含非幂等语句，重放会被中段报错卡死（既有记忆教训重放非幂等脚本）。

## 3. 实施记录（时序）

| 时点 | 动作 | 证据 |
|---|---|---|
| 盘点1 | 实体↔真库首扫（驼峰推导版）报 23 异常 | 含假阳性（未处理 @TableField 显式映射/exist=false） |
| 盘点2 | 修正尺子重扫 → 8 实体异常 | 假阳性排除：StageAction/CertTemplate 恢复清白 |
| 交叉验证 | docker 通道 vs socket 通道比对 | 同库确认（handover_records 0 行一致） |
| 盘点3 | 68 迁移文件积压审计矩阵 | 缺表 7 / 缺列 12 文件 / 无 SQL 2 实体 |
| 兄弟 apply | 手快照过期：handover 6 列 + 10 表落库 | archived_at 出现、表数 132→138 |
| fresh 重扫 | 剩 8 实体异常 | 见 §4 |
| 我方 apply | drift-backfill SQL（6 处幂等回填）+ 兄弟 p133/p256 | 回读全绿 |
| 防线 | `check-entity-db-drift.py` 门禁落盘 | exit 0，55 实体对齐 |
| 验证 | mvn 单测 37/37 绿 + HTTP 探针 code:0 | §5 |

## 4. 我方交付物（新文件，未动兄弟在途文件）

- `docs/script/sql/update/2026-09-07-ipd-drift-backfill-entity-gap.sql`
  - correction_logs 建表（对齐 CorrectionLog 实体 12 列）
  - kpi_rule_snapshots 建表（对齐 KpiRuleSnapshot 实体 9 列）
  - contributions 补 person_id / market_contribution_rate
  - negative_feedbacks 补 6 决策/解除列（triggered_by/decided_by/decided_at/lifted_by/lifted_at/decision_comment）
  - project_scores 补 dimension_1..5（@TableField 显式下划线映射）
  - sop_templates 补 7 元数据列（与兄弟 P1-3.3 instances 表配套）
  - 幂等：information_schema 判存在守卫 + IF NOT EXISTS 双保险
- `docs/script/sql/check-entity-db-drift.py`：可复跑门禁（--docker / --cnf 双通道，exit code 可接 CI）

兄弟会话在途文件（本会话只执行未修改）：`2026-09-07-ipd-p274-handover-deadline-fields.sql`（M）、`2026-09-07-ipd-p133-sop-template-instances.sql`（??）、`2026-09-07-ipd-p256-post-launch-reviews.sql`（??）。

## 5. 验证证据（fresh，全部本会话实测）

1. **门禁**：`python3 docs/script/sql/check-entity-db-drift.py --docker ruoyi-ai-mysql --db ipd_dev`
   → `✓ drift-check 通过：实体 55 个，全部与真库对齐`，exit 0
2. **SQL 回读**：correction_logs=1 / kpi_rule_snapshots=1 / contributions+rate=2 / negative_feedbacks.6cols=6 / project_scores.dim5=5 / sop_templates.7cols=7（幂等重放亦通过）
3. **mvn 单测**（错峰单模块，-o -pl 不带 -am 不带 clean）：
   `Tests run: 37, Failures: 0, Errors: 0, Skipped: 0`（P274 12 + P271 10 + P382 15），BUILD SUCCESS
4. **HTTP 真探针**（16039 运行态，防假 200 看业务包络）：
   `GET /api/v1/public/products` → `{"code":0,"message":"ok","data":[真数据]}` ✓
5. **多会话隔离**：git status 兄弟在途 M/?? 文件全程未修改；SQL 只执行内容未改写文件。

## 6. 边界与残留

- 本会话**未 commit**（保留同目录未提交工作，等 owner 指令）；
- 68 个迁移文件的 DML seed 类（INSERT/UPDATE）未批量执行——有数据重复风险，需逐文件按唯一键审查后另批处理；
- 45 张「desc 判 done 但 status 未翻」失真候选未逐一处理，属看板治理轮（OPS-09/AUD-GOV-01 泳道），本报告只交付 §1 前置发现清单；
- 真 HTTP 端到端业务闭环（登录→业务写→读回）仍受后端重启窗口约束，本轮只做只读探针。

## 7. 防线固化建议（给 owner）

1. 把 `check-entity-db-drift.py` 接入验收纪律：任何 DDL 变更卡 done 前必须 exit 0；
2. 新迁移文件强制幂等模板（information_schema 守卫），参考 drift-backfill 文件写法；
3. done 判定硬门槛升级：commit 在 + DDL 回读在 + 真业务（HTTP 或真库）通，三者缺一不可。
