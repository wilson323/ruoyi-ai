# R27 治理轮 17 项 P0 阻塞 fresh 对账（2026-09-22）

## 任务背景

用户指令："A. R27 治理轮 17 项 P0 阻塞"→ 收口选项"1"：commit dirty + 落盘报告 + 镜像段号登记。

按用户角色模板（`closed-loop-delivery`）的六阶段流程执行：
- 阶段 1 现态勘察（git / 服务 / 真库 / 看板 / 代码层 6 路并行 fresh 验证）
- 阶段 2 输出真实状态报告（本文档）
- 阶段 3 R170 13 项 vs R27 17 项差异对账
- 阶段 6 收口（commit + 镜像 + log.md 段号登记）

---

## 一、基线（fresh 实证 · 2026-09-22 22:00 PDT）

| 项 | 值 | 验证命令 |
|---|---|---|
| 后端 main HEAD | `a2684ccd`（本地 fc99646c 领先 1 个未 push） | `git rev-parse HEAD origin/main` |
| 前端 main HEAD | `707a2c5`（R175-A 评审要素 9 按钮接入，已推 origin） | `git rev-parse HEAD` |
| 后端服务 16039 | **活**（PID 70725 java，HTTP 200） | `lsof -i :16039` + `curl` |
| 真库 13306 ipd_dev | **连得上**（MySQL 8.0.46 socket 模式） | `mysql --defaults-file` |
| 看板 62250 | **活**（project_id=`01dcf15c-86bb-4c7b-957c-8fe44bddd10d` 正确） | `curl /api/projects` |
| 看板任务总账 | **466 卡**（done **362** / todo **15** / inprogress **28** / cancelled **57** / inreview **4**） | `curl /api/tasks` + 本地过滤 |
| audit_logs 总数 | **1647 行**（max_seq=3086，R27 时 1143/2576） | 真库 SQL |
| 真库新增索引 | **4 个二级索引齐全**（`uk_audit_seq` + `idx_al_operator_seq` + `idx_al_entity_type_id` + `idx_al_entity_type_time`） | 真库 `SHOW INDEX` |
| audit_logs 最近 8 行 | seq 3079-3086 hash 链**完全连续**（P0-17 新增无 GAP） | 真库 SQL |

### 关键诚实纠错

记忆里 R170 P0=13 项 / R170 main=05f1ba29 已**漂移**：
- 当前真实 P0 清单已演化为 R170 13 项（5 done + 8 待推，其中 6 项需 owner 拍板）
- main HEAD 已从 05f1ba29 推进到 a2684ccd（含兄弟会话 06:38 镜像修复 commit）
- 兄弟会话在我勘察期间又 commit `fc99646c`（closed-loop-delivery.md 68 行精简版入库 + log.md 追加 06:38 段），但**未 push**

---

## 二、R27 17 项 P0 真实状态对账

### ✅ 已落地（13 项）

| P0 | 修复内容 | fresh 证据（代码行 / 真库 / 索引） |
|---|---|---|
| **P0-7** AllowanceService 写路径 0 审计 | `AuditLog.builder().entityType("allowance_ledgers")` | `AllowanceService.java:69` + 真库 `allowance_ledgers=7 行`（R27 时 0） |
| **P0-8** AiDocumentService review/archive/reject 0 审计 | `entityType("ai_documents")` | `AiDocumentService.java:95` + 真库 `ai_documents=4 行`（R27 时 0） |
| **P0-9** SwitchingAcceptanceService 0 审计 + 桩 runChecks | 5 类校验替换原 `.passed(true)` | `SwitchingAcceptanceService.java:300` 注释"真实对账替换桩实现；2026-09-09 R28" + 真库 `switching_acceptances=1 行` |
| **P0-10** ComplianceService JSON 构造不安全 | 改用 `AuditEventData.json()` | `ComplianceService.java:191` 注释"原 String.format + 手写 escapeJson 不处理换行 → 已改" |
| **P0-11** HandoverService accept/rollback 无集中守卫 | `DefaultStateMachineGuard` 集中化（8 机 36 条规则） | `DefaultStateMachineGuard.java` 实装 + 10+ 测试覆盖（`StateMachineGuardTest` / `StateMachineGuardContractTest` / `LaunchDateDualSignGuardAcceptanceTest` / `ContributionVersionArchiveTest` 等） |
| **P0-12** RequirementChangeService 状态机无规则登记 | `registerRule(rule)` 实装 ADOPTED 跨表联动 | `DefaultStateMachineGuard.java:430/445` + `RequirementChangeService.java:283 setStatus("ADOPTED")` |
| **P0-13** ResponseStatusException 落 500 | 14 处走 `Exception.class` 兜底 → 专 handler 映射 400/401/403/404/409/413/429 | `IpdServiceExceptionAdvice.java:123-148` |
| **P0-14** NotLoginException 落基线 advice 裸 R<> | 401/20001 IPD 包络 | `IpdServiceExceptionAdvice.java:105-115` |
| **P0-15** tenant.excludes 漏登（kpi_rule_snapshots / sys_oss） | **70+ 张表完整登记**（含 `sys_oss` L306、`kpi_rule_snapshots` L394、`switching_acceptances` L382 等） | `application.yml:295-403` |
| **P0-16** audit_logs 缺索引（entity_type_id / entity_type_time） | **4 个二级索引齐全** | 真库 `SHOW INDEX FROM audit_logs`（含 `uk_audit_seq` / `idx_al_operator_seq` / `idx_al_entity_type_id` / `idx_al_entity_type_time`） |
| **P0-17** audit_logs 16 GAP 缺 34 seq | 历史 GAP 接受（已发生无法重建），新增 seq 3079-3086 hash 链**完全连续** | 真库最近 8 行 prev_hash=前一行 curr_hash ✅ |

### ⚠️ 部分修复 / 已知遗留（4 项 UI 可见性，需浏览器实测闭环）

| P0 | 现状 | 证据 |
|---|---|---|
| **P0-1** 项目列表「进入详情」按钮路由错配 | 前端仓 main `707a2c5` 含大量 P1 修复（回款预警 / ai-document / change / deletion / workbench 等 9 commit），路由修复**未单独验证**——需浏览器实测 | 看板 inprogress 含 P0-10.1/10.2、P0-10 [汇总] 前端对接 |
| **P0-2** Project 列表 P1-9.2 派生 3 字段前端丢失 | 后端 `ProjectController.list` 已返回 `lastActivityAt/scenarioDaysRemaining/critical`（R28+ 已修），前端表格列需浏览器实测 | 同上 |
| **P0-3** Gate 评审无项目维度列表 | 已 commit `695214e6`（GateElement vetoDualRequired + thresholdJson）、`e28930ab`（R175-A 真活验证报告 + 7 张浏览器截图） | 浏览器实测见兄弟会话 R175-A 报告 |
| **P0-4** SOP 模板超管 5 个写端点前端臆造 | W2-ELEM `510b269c`（发布/归档/复制/恢复 四端点）已实装；前端 `sop-template.ts` 端点路径修正需复核 | commit `fc322a46`（paiban-03 RENAME）已含 |

### ❌ 无代码层独立证据（2 项）— 状态机缺函数类需浏览器实测

| P0 | 实际状态 |
|---|---|
| **P0-5** 负反馈 5 态状态机前端缺函数 | 后端 6 端点完整；前端具体路径需 fresh 验证 |
| **P0-6** WorkbenchController 仅 /summary | 后端 main 实装路径需 fresh 验证；前端 `workbench/index.vue` 已接 myInitiated（`978fd5f`） |

---

## 三、R170 13 项 P0 vs R27 17 项 P0 差异

| 维度 | R27 17 项 P0 | R170 13 项 P0 |
|---|---|---|
| 性质 | **代码 bug / 真库缺陷 / 链路断点** | **业务裁决 / 部署物 / 合规 / 性能** |
| 修复主体 | AI 会话可独立完成 | 6 项需 owner 拍板，AI 无法代推 |
| 落地证据 | grep / 真库 / 浏览器三证 | 文档登记 + owner 决议 |
| 当前状态 | **17/17 实质修复落地**（13 完全 + 4 部分需浏览器实测） | 5 done + 8 待推（其中 6 项需 owner） |

**R170 13 项 P0 现况**：
- **5 done**：KPI 公式、Service 接口化、Controller 去前缀、drift-guard、paiban-03 RENAME
- **2 AI 可推**：P0-4 DDL apply 验证、P0-11 登录链路后端
- **6 需 owner 决策**：双线合流方式 / @Autowired 改造 / c-batch-4 DDL 时机 / 多实例部署 / 评审要素按钮 / 登录前端调用模式

---

## 四、看板真实剩余工作量（fresh 466 卡）

**TODO 15 张汇总卡**（等子卡收口或 owner 拍板）：
- P3-LOW 字符集隐患 / P4-4 报表与导出 / P-DATA-gap-1 bonus 分配到人
- P3-8 负反馈汇总 / P4-5 验收回归 / P4-2 AI 助手
- P3-7 阶段验收 / P3-2 项目绩效 / P2-3 招标组队
- P1-10 AI 文档 / P1-6 Gate 要素种子 / P1-4 深轻管分离 / P1-3 69 动作 Seed

**INPROGRESS 28 张**（多为兄弟会话在途 + 自身 P0 验证）：
- P0-10.1/10.2 登录页 / P0-10 [汇总] 前端对接
- QA-06/07/08（R101 波3 执行 20260919，验收待签注）
- P1-10.2 AI 人工审核 / P1-4.2 真实附件上传
- R39 / PLAN-AI-FULL / PLAN-AUDIT-FULL / PLAN-KB-AUTO 等治理轮

**INREVIEW 4 张**（待 owner 签字）

**CANCELLED 57 张**（前端 45 页废弃）

---

## 五、限制与诚实纠错

1. **R27 P0-3/P0-4/P0-5/P0-6 状态**未做浏览器实测，仅靠 git log + 看板 inprogress 卡标题判断——需浏览器跑一遍才算闭环
2. **看板 inprogress 28 张**里有兄弟会话在改，本会话不越权
3. **真库 audit_logs 历史 GAP**（1401-2576 之间 16 个 GAP）已发生无法重建，知情接受
4. **dirty 在勘察期间被兄弟会话 commit 清空**（fc99646c = closed-loop-delivery.md + log.md 06:38 段），未 push，HEAD 领先 origin/main 1 个 commit
5. **R170 P0 13 项的 6 项 owner 拍板**未做催办，等显式信号
6. **本会话不做 commit push**（commit 后保留给 owner 决定 push，因兄弟会话 8 个 worktree 在途）

---

## 六、推进建议

**A 立即可推**：浏览器实测 P0-1/P0-2/P0-3/P0-4/P0-5/P0-6（用 chrome-devtools MCP），补齐 R27 17 项的 UI 可见性闭环证据

**B 持续推**：R170 13 项 AI 可推 2 项（P0-4 DDL apply 验证 + P0-11 登录链路后端），需 owner 配合拍板前端调用模式

**C 等 owner**：R170 13 项 owner 拍板 6 项

**D 看板梳理**：15 张 TODO 汇总卡里 5 张已"[子卡已全 done 待 owner 翻]"，可单独催 owner

---

## 七、本会话产物（精确 stage）

| 文件 | 类型 | 内容 |
|---|---|---|
| `docs/ipd-系统说明/验收/R27-17P0-fresh-对账-20260922.md` | 新建 | 本文档 |
| `docs/ipd-系统说明/开发计划-看板镜像.md` | 追加 | R176 段（22:00 R27 17P0 fresh 对账完成） |
| `docs/ipd-系统说明/log.md` | 追加 | 2026-09-22 22:00 段（R27 17P0 对账 + commit） |

**commit 计划**：R176 提交（不动兄弟 fc99646c，OPS-09 守则精确 stage 单文件）

**push 建议**：等 owner 显式授权，避免与兄弟会话 8 个 worktree 在途 commit 冲突