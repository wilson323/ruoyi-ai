# WB-17-1 工作台 taskType 17 类字段映射 spec 模板（owner 填写用）

- 车道：R218-WB171（2026-09-25）；卡：e30c86a2-2263-49ca-ad4d-218c644ccb08
- 规格权威源：`docs/开发说明/spec/batch-01-pages-01-12.md` 页03 §3「🔴 17 类 taskType」枚举行（L165 附近，本文档现查摘录）
- 证据规约：所有表名/列/行数=2026-09-25 `mysql --defaults-extra-file=.codex/ipd-dev/config/mysql-client.cnf ipd_dev` 现查（SHOW TABLES / SHOW COLUMNS / SELECT COUNT 只读）；类名/端点=磁盘源码现查；「规格待补」=现查后仍无依据、须 owner 拍板项
- 状态图例：✅已投递（有聚合器+真活验证）｜🟦表在码缺（真库已建表、Java 零实体）｜⚠️锚缺（表在、投递锚列/态不在）｜🟥表缺（真库无表）

## 一、17 类逐类映射表（已实现 9 类）

| # | taskType | 数据来源表（真库✓） | 聚合逻辑 pending_expr（契约登记 yaml 摘录） | 跳转路由（前端 ipd.ts 现查） | 优先级口径 | 原型页引用 | 状态 |
|---|---|---|---|---|---|---|---|
| 1 | stage_sign | stage_actions ✓ | `status IN (NOT_STARTED,IN_PROGRESS,DELAYED)`，ownerRole 匹配（WorkbenchPolicy.isMine） | `/ipd/projects/:projectId/actions/:actionId`（ipd.ts L149） | dueDate 逾期=high（聚合器统一规则） | spec 页03 §5②；StageSignAggregator @Order(1) | ✅ |
| 2 | key_gate | gate_reviews ⋈ gates ✓ | `decision IS NULL` + `gates.status='PENDING'`；期限 signDueAt 回退 gate.signDueAt（BR-GATE-04） | `/ipd/projects/:id/gates`（ipd.ts L94 IpdProjectGates） | 同上 | spec 页03 §5②；KeyGateAggregator @Order(3) | ✅（⚠️死路 A4：decision IS NULL 无写入生产者，真活 GR- 卡恒空，见契约 yaml L53） |
| 3 | key_gate_arbitration | gate_arbitrations ⋈ gates ✓ | `decision IS NULL` + `gates.status='REJECTED'` + round 一致性；arbitratorId 全局直查 | 规格待补（现无专属页；建议同 gates 页，owner 定） | 无期限列恒 normal | spec 页03 §5②；KeyGateArbitrationAggregator @Order(4) | ✅（2026-09-08 真活 9/9 PASS，契约 yaml L66） |
| 4 | deletion_review | deletion_requests ✓ | `status IN ('LEADER_REVIEW','ADMIN_REVIEW')` 按角色分流（超管→ADMIN/组长→LEADER） | `/ipd/deletion/review`（ipd.ts L402） | adminDueAt/leaderDueAt | spec 页03 §5② + 页04；DeletionReviewAggregator @Order(2) | ✅ |
| 5 | handover | handover_records ✓ | `status IN ('DRAFT','CONFIRMED')`；from/to 当事人双视角投卡 | `/ipd/handover`（ipd.ts L285） | 无期限恒 normal | spec 页03 §5③（HandoffWorkbench）；HandoverAggregator @Order(5) | ✅ |
| 6 | contribution_confirm | contributions ✓ | `status='SUBMITTED'` + leaderId 匹配 | `/ipd/incentive/contribution`（ipd.ts L363） | 恒 normal | spec 页03；ContributionConfirmAggregator @Order(6) | ✅（⚠️死路 A3：SUBMITTED 行 leader_id 恒 NULL，真活 CT- 卡恒空，契约 yaml L102） |
| 7 | strategic_change | launch_date_change_requests ∪ coefficient_change_requests ✓ | LD `status='PENDING_SECOND'`+confirmerId / CC `status='PENDING_LEADER'`+leaderId | `/ipd/projects/:id/changes`（ipd.ts L99 IpdProjectChanges） | 恒 normal | spec 页03 §5②；StrategicChangeAggregator @Order(8) | ✅（⚠️死路 A1/A2：在途行锚字段恒 NULL，契约 yaml L114） |
| 8 | closeout | project_score_tasks ✓ | `targetType IN ('SELF_SCORING','LEADER_REVIEW') AND status='PENDING'` + personId | `/ipd/kpi/project-score`（ipd.ts L320） | dueAt 逾期=high | spec 页03；CloseoutAggregator @Order(7) | ✅ |
| 9 | kpi_fill | kpi_records ✓ | `status='EDITING' AND period=当月` + personId（⚠️DDL 注释 default 'DRAFT' 与代码 EDITING 漂移，契约 yaml L138） | 按 kpiType 分流：SHARED→`/ipd/kpi/shared`（L313）其余→`/ipd/kpi/functional`（L305） | 月度截止不读（kpi.monthlyDeadlineDay 配置） | spec 页03 §5②；KpiFillAggregator @Order(9) | ✅ |

## 二、未实现 8 类映射表（卡面「缺表 4 + 口径 4」→ 现查修正分类见状态列）

| # | taskType | 数据来源表（真库现状） | 聚合逻辑 pending_expr | 跳转路由 | 优先级口径 | 原型页引用 | 状态与 owner 待填空白 |
|---|---|---|---|---|---|---|---|
| 10 | waiver_review | gate_waivers ✓已建（0 行；SHOW COLUMNS 现查：三签列 market/rd/super _id+_decision 齐 + due_at） | 规格待补。表锚可派生：`joint_decision IS NULL AND status<>'DRAFT'`？三签人各自待办= `{market\|rd\|super}_decision IS NULL AND {…}_id=当前人`（14 问 Q4 三签，**owner 拍板投递语义与 status 值域**） | 规格待补（前端无 waiver 路由，ipd.ts 现查零；需新建页或挂 gates 页） | due_at 逾期=high？待拍板（Q2 乙已加 due_at 列） | spec 页03 §5②；14问 Q1-Q5（WorkbenchService.java L65 引）；缺表4类-14问澄清-20260908.md | 🟦表在码缺：Java 实体/Mapper/写入 API（发起豁免申请端点）全零（grep 现查仅 javadoc 提及） |
| 11 | rd_replacement | rd_replacements + rd_replacement_approvals ✓已建（各 0 行；status 默认 PENDING_SELF、approvals 表 decision NOT NULL） | 规格待补。四步链（Q8）投递锚推测=`status=当前步 AND 审批人=当前人`，但 approvals.decision NOT NULL 意味着「待审占位行」无法落库（与 key_gate 死路 A4 同构！）——**owner 须拍板：approvals 预落行 decision 改可空 或 以主表 status+step 派生** | 规格待补（前端无路由） | due_at（主表有列）待拍板 | spec 页03 §5②；14问 Q6-Q8 | 🟦表在码缺：实体/Mapper/发起与审批端点全零 |
| 12 | retirement_review | product_retirements ✓已建（0 行；rd/super 双级审批列 + readiness_* 齐） | 规格待补。可派生 `status='PENDING_RD_LEADER' AND rd_leader_id=当前人` ∪ `status='PENDING_SUPER?' AND super_admin_id=当前人`（**status 值域只默认了 PENDING_RD_LEADER，第二态名 owner 拍板**） | 规格待补（前端无路由；products/manage 页? owner 定） | due_at 有列待拍板 | spec 页03 §5②；14问 Q9-Q11（正名=产品退市） | 🟦表在码缺：实体/Mapper/端点全零 |
| 13 | capacity_approval | multi_project_capacity_approvals ✓已建（0 行；group/super 双级 + valid_until + consumed_*） | 规格待补。可派生 `status='PENDING_GROUP_LEADER' AND group_leader_id=当前人` ∪ 超管态（**≥3 触发即自动建单？申请端点语义 owner 拍板**，Q13） | 规格待补（前端无路由；spec 页03 §5③ 原型组件 `<CapacityApprovalPanel>` 存在，Vue 未建页） | due_at 有列待拍板 | spec 页03 §5②③；14问 Q12-Q14 | 🟦表在码缺：实体/Mapper/端点全零 |
| 14 | receipt_review | receipt_ledgers ✓已建（2 行）但**无 review 语义列**（SHOW COLUMNS 现查：无 status/reviewer/review_*） | 规格待补（R217 现查：该表实为销售回款台账，「收据待审」单据态不存在）。两方案：A 扩列（DDL 草稿段 D-1）/ B 新建评审单表——**owner 二选一** | 规格待补（候选挂 `/ipd/performance`（ipd.ts L261）或 `/ipd/projects/:id/overview`，owner 定） | 规格待补 | spec 页03 §5② | ⚠️锚缺：见 DDL 草稿段 D-1 |
| 15 | change_implementation | **change_implementations 🟥真库无表**（SHOW TABLES LIKE 现查空；spec batch-03 页26 L88 明确引用 `change_implementations.owner_id` 为权限锚） | 规格待补（可派生：`owner_id=当前人 AND status IN (NOT_STARTED,IN_PROGRESS)`，值域 owner 拍板，DDL 草稿段 C 已按 spec 剧本预铺） | `/ipd/projects/:id/changes`（ipd.ts L99，变更域闭环面板页26 L35-36） | 规格待补（spec 未给期限） | spec batch-03 页26 §4-§5（L88/L90/L101）| 🟥表缺 + 实施链 API（POST /:id/implementation 等 4 端点，spec L90-94）后端全零（RequirementChangeController 现查仅 6 端点无 implementation/verify/close/baseline/evidence） |
| 16 | change_verify | 同 #15（change_implementations 的 market/rd _decision 列承载，DDL 草稿段 C 并入方案）| 规格待补（spec L137「两名责任PM分别收到 change_verify 任务」→ `{market\|rd}_decision IS NULL AND {market\|rd}_pm_id=当前人`，待 owner 确认） | 同 #15 | 规格待补 | spec batch-03 页26 L133-138 | 🟥表缺（随 #15 同批） |
| 17 | bonus_lock | bonus_pools ✓已建（20 行：DRAFT 16/CONFIRMED 1/DISTRIBUTED 3，SELECT 现查）但状态机**无「待锁定」前置态**（BonusPoolService.java L57 + DefaultStateMachineGuard.java L156 现查：DRAFT→CONFIRMED 由 freeze 主动触发） | 规格待补。owner 二选一：①新增 PENDING_LOCK 态+生产者（契约 yaml L176 警示：无生产者即违 mock 规约）②以「DRAFT 且进入分配窗口」派生投递 | `/ipd/incentive/bonus-pool`（ipd.ts L355） | 规格待补 | spec 页03 §5② | ⚠️态缺：无 DDL 需求，纯口径拍板（DDL 草稿段 E 登记） |

## 三、页03 非 taskType 附属缺口（同卡验收范围，现查登记）

| 项 | spec 依据 | 现状（现查证据） | owner 待办 |
|---|---|---|---|
| saved bucket 收藏 | 页03 §3 `saved` 字段 + §5⑥ saved_items 表 | 🟥真库无 saved_items 表；后端 grep saved-items 零命中；前端 `/api/saved-items` 无对应 | 拍板 DDL 草稿段 B + 是否本期实施 |
| GET tasks?type= 过滤 | 页03 §4 🔴 v3 缺 | WorkbenchController 现查仅 3 端点（/summary /my-initiated /my-pending-approvals），无 type 过滤、无 /workbench/tasks | 按 controller 评审方案 C 拍板（卡描述 L「推荐方案 C」） |
| no-output-alerts | 页03 §4 🔴 + BR-INC-11 | 后端零端点（grep no-output 仅 AllowanceService 内部 noOutput60Days 参数，非 HTTP 面）；数据源可派生 kpi_raw_records/kpi_records 口径**待 owner 给规则** | 给「60 天无产出」判定口径 + 端点归属（performance 域?） |
| 统一通知 read-all | 页03 §4 🔴 | ✅已存在：NotificationController `POST /api/v1/notifications/read-all`（L76 现查）——spec「v3 缺」标注已过时，仅需前端接线（另卡） | 无（登记勘误） |
| pendingType 细分 | 页03 §3 �� | ✅已实现：WorkbenchService L144-151（17 键预置 0） | 无 |

## 四、owner 必填空白汇总（本模板 ⬜ 项）
1. #10-13 四类的：投递 pending_expr 终版 / status 值域 / 跳转页面（建页或挂靠）/ 发起与审批端点契约（这 4 类是「表就绪、业务链路从零」，字段映射依赖 API 设计先行）
2. #11 rd_replacement_approvals.decision NOT NULL 与待审占位行的矛盾裁决（改列 or 改派生口径）
3. #14 receipt_review 方案 A（扩列）vs B（新表）
4. #15/16 change_implementations 列值域终版（DDL 草稿段 C 内 ⚠️ 注释项：status 值域、due_at 来源、双 PM 并入 vs 拆表）
5. #17 bonus_lock 投递口径（新增态 vs 派生）
6. saved_items target_type 值域 + 唯一约束
7. no-output-alerts 的 60 天口径与端点归属
