# WB-17-1 真库缺表现状核对表 + 可行性调研（R218 车道，2026-09-25）

- 卡：e30c86a2-2263-49ca-ad4d-218c644ccb08（WB-17-1，status=inprogress，现查 API）
- 真库通道：`mysql --defaults-extra-file=/Users/mac/Documents/ruoyi-ai/.codex/ipd-dev/config/mysql-client.cnf ipd_dev`（只读 SELECT/SHOW，本会话零 DDL/DML）
- 代码证据根：`ruoyi-modules/ruoyi-ipd/src/main/java/org/ruoyi/ipd/`（WorkbenchController.java / service/WorkbenchService.java / workbench/*Aggregator.java）

## 〇、总结论（先行）
1. **卡面「缺表 4 类」前提已过时**：gate_waivers / rd_replacements / rd_replacement_approvals / product_retirements / multi_project_capacity_approvals 五表 **2026-09-25 现查全部存在于真库**（0 行；来源=2026-09-08-ipd-c-batch-4-tables-draft.sql，R12.1 拍板后 apply；WorkbenchService.java L64 javadoc「未 apply」为过时登记，建议下轮随代码修正——本车道不改 Java）。
2. 修正后**真·表级缺口 = 3 张**：`change_implementations`、`change_implementation_evidence`（spec batch-03 页26 L50/L88 引用）、`saved_items`（页03 §5⑥）。DDL 草稿已落 `docs/script/sql/update/20260925-wb171-draft-missing-tables.sql`（未执行，待 owner 拍板 + DBA 窗口）。
3. 「缺表 8 类」实为三种性质：🟦表在码缺 ×4（waiver/rd_replacement/retirement/capacity）、⚠️锚/态缺 ×2（receipt_review/change…实施域若走扩列方案；bonus_lock）、🟥表缺 ×2（change_implementation/change_verify）。
4. 已实现 9 类的**工作台侧投递代码全部在盘**，但其中 3 类 4 卡存在「写入侧死路」（契约登记 yaml A1-A4 已登记，非本卡引入）——真库有行也投不出卡，修死路属于「不缺表类」的真实施工量。

## 一、真库缺表现状核对表（spec 需要的表 × 真库是否存在 × 证据）

| # | taskType | spec 需要的数据源表 | 真库存在？ | 行数（现查） | 证据命令 |
|---|---|---|---|---|---|
| 1 | stage_sign | stage_actions | ✅ | —（在投，R6 真活 7 卡） | SHOW TABLES 2026-09-25 |
| 2 | key_gate | gate_reviews + gates | ✅ | 死路A4 | SHOW TABLES；契约yaml L53 |
| 3 | key_gate_arbitration | gate_arbitrations | ✅ | 表现状3行全已裁→R6 后 openArbitration 已修（契约yaml L60 ALLOCATE 预落 NULL 行，真活 9/9 PASS L66） | SHOW TABLES |
| 4 | deletion_review | deletion_requests | ✅ | 25 in-flight（卡描述）| SHOW TABLES |
| 5 | waiver_review | gate_waivers | ✅（卡面说缺，**已不存在此缺口**） | 0 | SHOW TABLES + SHOW COLUMNS 2026-09-25（35 列含三签+due_at） |
| 6 | handover | handover_records | ✅ | 有（R6 HD-999031 造数验证过） | SHOW TABLES |
| 7 | rd_replacement | rd_replacements (+approvals) | ✅ 双表均在 | 0 / 0 | SHOW COLUMNS：approvals.decision NOT NULL（隐患见模板 §二#11） |
| 8 | contribution_confirm | contributions | ✅ | 死路A3 | 契约yaml L102 |
| 9 | receipt_review | receipt_ledgers | ✅ 表在、**review 锚列不在** | 2（source=RECEIPT） | SHOW COLUMNS：无 status/reviewer/review_* |
| 10 | retirement_review | product_retirements | ✅ | 0 | SHOW COLUMNS 33 列 |
| 11 | strategic_change | launch_date_change_requests + coefficient_change_requests | ✅ 双表 | 死路A1/A2（R12.1 已修 1 行 confirmer_id 回填） | SHOW TABLES + 卡 R12.1 段 |
| 12 | capacity_approval | multi_project_capacity_approvals | ✅ | 0 | SHOW COLUMNS 32 列 |
| 13 | kpi_fill | kpi_records | ✅ | 有 | SHOW TABLES |
| 14 | change_implementation | change_implementations | ❌ **缺表** | — | `SHOW TABLES LIKE 'change_implementations'` 返回空；spec 页26 L88 引用该表为权限锚 |
| 15 | change_verify | 同上（或 evidence 表） | ❌ 缺表（change_implementation_evidence 亦无） | — | SHOW TABLES LIKE 空 |
| 16 | bonus_lock | bonus_pools | ✅ 表在、**「待锁定」态不在** | 20（DRAFT16/CONFIRMED1/DISTRIBUTED3） | SELECT status 分组现查；BonusPoolService.java L57 |
| 17 | closeout | project_score_tasks | ✅ | 有（R6 PS-999051 验证） | SHOW TABLES |
| 附 | saved bucket（页03）| saved_items | ❌ **缺表** | — | SHOW TABLES LIKE 'saved_items' 空 |

**净结论：17 类 taskType 真·缺表 = 2 类（#14/#15 同域）+ 1 张页03附属表（saved_items）；其余 15 类表全部在真库。**

## 二、9 类不缺表（已实现投递）逐类可行性调研

现有端点（WorkbenchController.java 现查全量，共 3 个）：
- `GET /api/v1/workbench/summary?projectId=`（L40-44）→ stats{pending,overdue,unread,completed,myInitiated,pendingType17键} + tasks[] + deletionPending + currentAdvance
- `GET /api/v1/workbench/my-initiated?personId=`（L51）/ `GET /api/v1/workbench/my-pending-approvals?personId=`（L63）（R27 P0-6，3 业务单据聚合）

**共性缺口（9 类共享一次施工，非逐类）**：
| 需新增端点 | 依据 | 改动量 | 风险 |
|---|---|---|---|
| `GET /api/v1/workbench/tasks?bucket=&type=&limit=&projectId=` | spec 页03 §4 🔴 type 过滤；卡内 controller 评审「推荐方案 C」 | 4-6h（新增 Controller 方法 + Service 过滤复用 aggregators；含删 inbox 死代码 WorkbenchService.java L241 区段核验） | 低——不动聚合器；响应契约新增，旧 /summary 保留兼容 |
| `GET /api/v1/performance/no-output-alerts` | spec 页03 §4 🔴 + BR-INC-11 | 3-4h | **口径未定**（60 天无产出判定规则 spec 未给，owner 必须先补，见模板 §四.7）|

**逐类现状与剩余工作量**：
| taskType | 投递代码（现查） | 真活验证 | 需新增端点 | 剩余改动量 | 风险 |
|---|---|---|---|---|---|
| stage_sign | StageSignAggregator @Order(1) | R6 PASS | 无（挂公共过滤即支持） | 0h | 无 |
| key_gate | KeyGateAggregator @Order(3) | 卡死路A4：GR- 恒空 | 修写入侧非端点：GateCreationService 预落 decision=NULL 行（对齐 arbitration 2026-09-08 修法） | 4-6h | Gate 域兄弟会话在途，需 allowedPaths 认领 |
| key_gate_arbitration | @Order(4) | R6 后修复 PASS 9/9 | 无 | 0h | 已闭环（先例可复制到 key_gate） |
| deletion_review | @Order(2) | R6 admin 25 卡 | 无 | 0h | 无 |
| handover | @Order(5) | R6 HD- 造数 PASS | 无 | 0h | 无 |
| contribution_confirm | @Order(6) | 卡死路A3：CT- 恒空 | submit 路径回填 leader_id（或投递锚改项目组长关系表派生，owner 选） | 3-4h | incentives 域兄弟在途 |
| strategic_change | @Order(8) | 死路A1/A2：LD-/CC- 恒空（R12.1 仅修 1 行存量数据，代码 propose 路径仍不写锚字段） | propose 时写 confirmer_id/leader_id（同 A4 修法系） | 4-6h | 低 |
| closeout | @Order(7) | R6 PS- PASS | 无 | 0h | 无 |
| kpi_fill | @Order(9) | R6 KF- PASS | 无 | 0.5h（kpi_records DDL 注释 default 'DRAFT'→EDITING 勘误，归 DDL 窗口顺带，非本卡） | 注释漂移不挡落库（契约yaml L138） |

9 类小计：**≈12-17h**（公共过滤端点 4-6h + 死路修复 7-10h + 勘误 0.5h），无建表需求。

## 三、未实现 8 类逐类调研（前提修正后的真实缺口）

| taskType | 现有端点（现查） | 需新增 | 改动量估计 | 风险 |
|---|---|---|---|---|
| capacity_approval | 业务端点零（表在码缺）；workbench 侧 /summary 已含其 pendingType 键 | 实体+Mapper 1h；发起/组长批/超管批 3 端点 + 状态机 6h；CapacityApprovalAggregator+测试 3h；前端 CapacityApprovalPanel 对应 Vue 页（spec 页03 §5③ 原型有组件、路由无）4-6h | **14-16h** | 中：≥3 触发自动化（Q13）落点未定——申请单由谁创建必须先拍板，否则聚合器建了恒 0 行（违 mock 规约②） |
| waiver_review | 零 | 同上结构（三签链 +1 端点） | **15-17h** | 中高：三签 joint_decision（Q4乙）与 gate 域耦合；前端页完全未建 |
| retirement_review | 零 | 同上结构 + readiness 校验 + products 扩列 retired_at/retirement_locked（DDL 草稿联动，Q11） | **16-18h** | 中高：退市语义与 deletion_requests 分工（Q6甲独立两链）需写入侧同步 |
| rd_replacement | 零 | 双表实体/Mapper + 四步链（含本人确认步）4 端点 + 原子转移 + 聚合器 + 前端页 | **20-24h** | **高：rd_replacement_approvals.decision NOT NULL 阻断「待审占位行」**（现查 SHOW COLUMNS），与 key_gate 死路 A4 同构——DDL 层先裁决，否则写完必恒空 |
| receipt_review | ReceiptLedgerController 3 端点（POST / POST /{pid}/refunds / GET by-project） | 方案A：DDL 扩列 0.5h + 评审端点 2 + 聚合器 3 → **5-6h**；方案B：新表+全链 **10-12h** | 低-中 | 「收据待审」业务语义本身待 owner 确认（表实为回款台账，R217 注记）——可能整类口径是 spec 误置 |
| change_implementation | RequirementChangeController 6 端点（POST/submit/sign/get/list/open），**无 implementation/baseline/evidence** | DDL apply（草稿段 C）+ 实体/Mapper + spec 页26 L88-92 的 3-4 端点 + 聚合器 | **10-12h（与 verify 同表共享后）** | 表未建即一切为零；前端路由 /ipd/projects/:id/changes 已存在可挂 |
| change_verify | 同上 | 同表双 PM 验证列（草稿段 C 已含）+ verify/close 2 端点 + 聚合器 | **6-8h（并入 #15 施工）** | 双 PM 验证驳回→in_progress 回退态机需登记 DefaultStateMachineGuard |
| bonus_lock | BonusPoolController 8 端点（含 freeze/distribute） | 派生口径：仅新增 BonusLockAggregator（DRAFT+窗口条件）+ 测试 **2-3h**；新增态方案：状态机+生产者+调度 **6-8h** | **2-8h（口径二选一）** | 低；真库 20 行 DRAFT 即在途存量，投出即有真卡（17 类中唯一「立即见数」类） |

## 四、建议施工切片（按改动量升序；先做投产比最高、可验证性最强的）

> 前置总闸门（所有切片共享）：①公共 `GET /workbench/tasks?type=` 端点（切片 0）；②owner 填空白清单（见模板 §四）——凡 ⚠️ 口径未拍的类禁止开工（R217 纪律：不发明新机制、不擅定字段）。

| 切片 | 内容 | 量 | 为什么先做 |
|---|---|---|---|
| S0 | /workbench/tasks?type= 过滤端点 + inbox 死代码清理核验 | 4-6h | 纯消费侧，解锁前端 17 类 tab 渲染，零业务拍板依赖 |
| S1 | bonus_lock（派生口径，待 owner 点头即做）+ 9 类死路修复包（key_gate A4 / contribution A3 / strategic A1-A2，复用 arbitration 已验证修法） | 9-18h | 全为「代码在盘、改写入路径/加只读聚合器」，无 DDL 窗口依赖；bonus_lock 立即见真卡 |
| S2 | capacity_approval 全链（4 类表在码缺中最简：两级审批 vs waiver 三签） | 14-16h | 表已就绪；依赖 owner 拍板 ≥3 触发生产者 |
| S3 | waiver_review + retirement_review（同构两链，可并行兄弟卡） | 31-35h | 依赖 DDL 无新增（表在），依赖前端建页设计 |
| S4 | change_implementation + change_verify 同批（先 DDL 草稿段 C apply，后实体/端点/聚合器） | 16-20h | 唯一依赖新表 apply 的批；DBA 窗口对齐 |
| S5 | receipt_review（先口径裁决再施工）+ saved_items/no-output-alerts（页03 附属，口径待补） | 5-6h + 口径 | 最可能「spec 误置/口径不存在」，放最后防返工 |

## 五、红线遵守声明
- 本会话未改任何 Java 代码 / 看板 / 镜像 / log.md；无 git 写操作；真库仅 SHOW/SELECT。
- 产出物 3 件：本调研文档、17 类映射模板（同目录）、DDL 草稿 `docs/script/sql/update/20260925-wb171-draft-missing-tables.sql`（含「待 owner 拍板 + DBA 窗口 apply，未执行」头注）。
- 证据命令可复跑：SHOW TABLES / SHOW COLUMNS / SELECT … GROUP BY（ipd_dev，只读）；grep/find 命令见各表「证据」列。
