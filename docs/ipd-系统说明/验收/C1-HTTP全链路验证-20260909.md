# C1 HTTP 全链路验证报告（R25，2026-09-09 10:04-10:12）

> 对象：fix/p0-r2-round1-20260909 分支（d76a6086 接手链 + 939c4704 状态机修复 + c51e705b audit 补提）@ 80b9c540。
> 方式：独立构建 → 独立实例 16089（不碰 16039）→ 一次性测试账号冒烟 → 账号即删、实例即关。

## 1. 构建与启动
- `mvn -o -pl ruoyi-admin -am package -DskipTests`：BUILD_EXIT=0（wt-p0r2 工作区，错峰单实例构建）。
- 首次启动失败：Redis 16379 拒连——`logs/java-p274.args` 是旧参数（16379 为历史 socat 端口），本机 Docker Redis 实际在 **6379**（16039 现连 6379 ESTABLISHED 实证）。
- 修正：argfile 方式复刻全量 -D 参数（`sed 16039→16089, 16379→6379`），`java @/tmp/c1-16089.args -jar ruoyi-admin.jar` → 23.4s 启动成功。凭据全程不经命令行。

## 2. 冒烟战果（9 端点全真活）
| # | 端点 | 结果 |
|---|---|---|
| 1 | POST /api/v1/auth/login | code=0 拿 token（BCrypt+会话链通） |
| 2 | GET /api/v1/auth/me | code=0 会话自证 |
| 3 | GET /api/v1/handovers/inbox | code=0（574 行 HandoverService 接手主链真活） |
| 4 | GET /api/v1/audit-logs | code=0 返回 5 行（audit 包补提后真活+有数据） |
| 5 | GET /api/v1/deletion-requests/archive | code=0 |
| 6 | GET /api/v1/deletion-requests/overdue-admin-review | code=0（升级端点真活） |
| 7 | GET /api/v1/bonus-pool/list | HTTP 400 code=10001"缺 projectId"——参数校验业务响应，端点活 |
| 8 | GET /api/v1/deletion-requests（POST 域） | 405=路径注册证明（首轮探测方法错，非故障） |
| 9 | 首轮 404（handover/bonus-pools 等错路径） | 全部为探测姿势错，log 4 处 ERROR 均对应 404/405 日志，运行期零应用级错误 |

## 3. 测试账号纪律
- 造号：persons id=99990001 / username=c1-smoke-20260909 / SUPER_ADMIN / must_change_pwd=0，bcrypt 密码仅存于临时进程。
- 冒烟后 `DELETE FROM persons WHERE id=99990001` 删除 1 行实证清理，共享库无残留。

## 4. 结论
fix 分支接手链（Handover 574 行 / 审计 AOP / audit 包 / 状态机守卫域端点）**HTTP 全链路真活**，C1 关闭。
遗留：端口文档建议——`.codex/ipd-dev/config/application-ipd-local.yml` 的 `spring.data.redis.port: 16379` 与 Docker Redis 实际 6379 不符（16039 靠旧 args 之外的方式连通），建议 owner 顺手改成 6379 消除下一人踩坑。
