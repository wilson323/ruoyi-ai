# AUD-02 全局依赖审计 — 基线重跑（2026-09-19）

- **看板卡**：AUD-02（全局依赖审计，基线 2026-09-07 已写，R93 HEAD 后待重跑）
- **基线 HEAD**：`50450cbb`（2026-09-07，见 `AUD-02-全局依赖验收-20260907.md`）
- **本轮 HEAD**：`3d8cddf7`（R98）
- **方法**：`mvn -o dependency:tree -Dscope=runtime`（仓库根，39 模块全绿 BUILD SUCCESS）+ `git diff 50450cbb..HEAD -- 'pom.xml' '**/pom.xml'` 全仓 POM 对比

## 1. 结论速览

| 检查项 | 基线（09-07） | 本轮（09-19） | 判定 |
|---|---|---|---|
| langchain4j.version（stable BOM） | 1.17.2 | 1.17.2 | ✅ 对齐保持 |
| langchain4j.beta.version | 1.17.2-beta27 | 1.17.2-beta27 | ✅ 对齐保持 |
| langchain4j.community.version | 1.17.0-beta27 | 1.17.0-beta27 | ✅ 对齐保持 |
| grpc.version | 1.62.2 | 1.62.2 | ✅ 对齐保持 |
| 版本升级 | — | **0** | ✅ |
| 依赖移除 | — | **0** | ✅ |
| dependency:tree 冲突（omitted for conflict） | — | **0** | ✅ |

## 2. 依赖树实测版本（当前 HEAD，runtime scope）

- `langchain4j` / `langchain4j-core` / `langchain4j-anthropic` / `langchain4j-http-client(-jdk)`：**1.17.2**
- `langchain4j-agentic` / `langchain4j-experimental-skills-shell` / `langchain4j-document-parser-apache-tika`：**1.17.2-beta27**
- `langchain4j-community-dashscope` / `langchain4j-community-zhipu-ai`：**1.17.0-beta27**
- `io.grpc:grpc-{api,context,core,netty-shaded,protobuf,protobuf-lite,stub,util}`：**全部 1.62.2**（与父 POM 钉版一致，Milvus SDK 冲突未复现）

三 BOM 家族与 gRPC 在依赖树实际解析层面零漂移（不仅 POM 声明对齐，传递解析也无 conflict/omitted）。

## 3. 基线 → HEAD 的 POM 变更全量清单（9 文件，+92/-7）

唯一语义性新增依赖（3 项，全在 ruoyi-ipd）：

| 模块 | 新增 | 版本/scope | 关联 |
|---|---|---|---|
| ruoyi-modules/ruoyi-ipd | `dev.langchain4j:langchain4j-open-ai` | `${langchain4j.version}` = 1.17.2，跟随 BOM | R95 波次新功能引入 |
| ruoyi-modules/ruoyi-ipd | `org.ruoyi:ruoyi-common-sse` | 内部模块 | SSE 契约门禁配套 |
| ruoyi-modules/ruoyi-ipd | `org.ruoyi:ruoyi-system` | 内部模块 | 系统服务依赖 |

测试性新增（不进 runtime 树）：`spring-boot-starter-test` 加到 ruoyi-common-chat / log / mybatis / sse / web / websocket、ruoyi-aiflow、ruoyi-system 等 8 处（test scope）；内部模块引用 `ruoyi-common-web` 等。

-7 行全部来自 `ruoyi-modules/ruoyi-ipd/pom.xml` 的 surefire `<testExcludes>` 临时排除清理（W20-B 兄弟会话在途测试排除被移除——修复项，非依赖变更）。

## 4. 与基线报告的关系说明

09-07 基线报告（`AUD-02-全局依赖验收-20260907.md`）的主体是 devTag 全量测试基线（679 run / 13F / 148E）+ SQL 输出 + 看板 check，**未含依赖树快照**；本轮补齐依赖维度，方法为 git POM diff（可复现、不受本地仓库演进影响）替代"重出基线树"（不可行：远端仓库版本随时间漂移，旧树无法忠实重现）。

## 5. 建议（不阻塞）

1. langchain4j-open-ai 引入后，`langchain4j.version` 升级窗口需连带回归 ruoyi-ipd 的 OpenAI 调用链（当前三 BOM 对齐约束见 AGENTS.md）。
2. 依赖树 0 冲突是好状态，保持"升级必跑 dependency:tree 对比"的惯例即可。
