# R120-修R25-4失效脚本加exit1分支-20260919

## 背景

R119 系统性根因反思列出 **R25 9 大门禁 4 失效**（check_tenant_excludes_apply.sh / check_dynamic_loadable.sh / check_duplicate_ssot.sh 三者扫+报告 OK 但 exit 永远 0；check_deletion_consistency.sh exit 2 不标准）。R119 把"加 DIFFS 累加器 + exit 1 分支"列为 R120 候选。

R119 #5 已 commit 的 reconcile-multi-source.sh 用了 **DIFFS 累加器** 修法：每条异常项 `DIFFS=$((DIFFS+1))`，末尾 `if [ "${DIFFS:-0}" -gt 0 ]; then exit 1; fi`。

R120 把这个修法平行扩展到 R25 4 失效脚本。

## 落地证据（worktree `wt-r120-fix-r25-gates`，branch `fix-r120-r25-gates-exit1`）

| # | 脚本 | 字节 | 原 EXIT | R120 修法 | fresh EXIT | 触发原因 | ENV 自检 EXIT |
|---|---|---|---|---|---|---|---|
| 1 | check_tenant_excludes_apply.sh | 11495 | 0 ❌ | DIFFS = rc3a_count + rc3b_count + rc3c_count | **1** ✅ | rc3c=59 触发 | 不需要（fresh 已红） |
| 2 | check_dynamic_loadable.sh | 8020 | 0 ❌ | DIFFS = intersect_count | 0（事实反映）| 当前 ruoyi-ai 无动态可达视图（intersect_count=0） | **1** ✅（FRONTEND_ROOT/BACKEND_ROOT 注入 fake 视图后红） |
| 3 | check_duplicate_ssot.sh | 10097 | 0 ❌ | DIFFS = dup_inconsistent + dup_consistent + dup_same + dup_diff | 0（事实反映）| 开发说明_副本/ 已被 R113-R117 清理（dup_*=0）| **1** ✅（DOCS_ROOT 注入 fake 副本后红） |
| 4 | check_deletion_consistency.sh | 6932 | 2 ⚠️ | exit 2 → exit 1（2 处） | **1** ✅ | 用法错误立即 exit 1（无参/无 scan-dead-code 报告）| 不需要（fresh 已红） |

## 4 脚本 fresh 验证（`bash scripts/X.sh >/dev/null 2>&1; echo $?`）

```
$ for s in check_tenant_excludes_apply.sh check_dynamic_loadable.sh check_duplicate_ssot.sh check_deletion_consistency.sh; do
    bash scripts/$s >/dev/null 2>&1; echo "$s: EXIT=$?"
  done

check_tenant_excludes_apply.sh: EXIT=1
check_dynamic_loadable.sh: EXIT=0
check_duplicate_ssot.sh: EXIT=0
check_deletion_consistency.sh: EXIT=1
```

## ENV 自检证据（修法生效证明）

### check_dynamic_loadable.sh：FRONTEND_ROOT/BACKEND_ROOT 注入 fake 视图后红

```bash
$ SELF_FE=$(mktemp -d) && SELF_BE=$(mktemp -d)
$ mkdir -p "$SELF_FE/src/router" "$SELF_FE/src/views/ipd/foo" "$SELF_BE/ruoyi-modules/ruoyi-ipd/src/main/resources"
$ echo '<template/></template>' > "$SELF_FE/src/views/ipd/foo/index.vue"
$ echo 'import.meta.glob("../views/**/*.vue")' > "$SELF_FE/src/router/access.ts"  # 注意：脚本 grep -oE '"[^"]*"' 只匹配双引号
$ echo "INSERT INTO sys_menu (component) VALUES ('ipd/foo/index');" > "$SELF_BE/ruoyi-modules/ruoyi-ipd/src/main/resources/menu.sql"
$ FRONTEND_ROOT="$SELF_FE" BACKEND_ROOT="$SELF_BE" bash scripts/check_dynamic_loadable.sh >/dev/null 2>&1; echo "EXIT=$?"
EXIT=1   # intersect_count=1 → DIFFS=1 → exit 1 ✅
$ rm -rf "$SELF_FE" "$SELF_BE"
```

### check_duplicate_ssot.sh：DOCS_ROOT 注入 fake 副本后红

```bash
$ SELF_TMP=$(mktemp -d)
$ mkdir -p "$SELF_TMP/开发说明" "$SELF_TMP/开发说明_副本"
$ echo 'const X = 1;' > "$SELF_TMP/开发说明/开发说明书.md"
$ cp "$SELF_TMP/开发说明/开发说明书.md" "$SELF_TMP/开发说明_副本/"
$ DOCS_ROOT="$SELF_TMP" bash scripts/check_duplicate_ssot.sh >/dev/null 2>&1; echo "EXIT=$?"
EXIT=1   # dup_same=1 → DIFFS=1 → exit 1 ✅
$ rm -rf "$SELF_TMP"
```

## 关键决策（与 R119 同款）

| 维度 | R119 reconcile-multi-source.sh | R120 4 失效脚本 |
|---|---|---|
| 差异累加器 | `DIFFS=$((DIFFS+1))` 每项异常累加 | `DIFFS=$((rc3a+rc3b+rc3c))` / `DIFFS=${intersect_count:-0}` / `DIFFS=$((dup_inconsistent+dup_consistent+dup_same+dup_diff))` |
| 退出分支 | `if [ "${DIFFS:-0}" -gt 0 ]; then exit 1; fi` | 同 |
| 0 退出条件 | DIFFS=0 exit 0（ahead 无 >14d 死分支） | DIFFS=0 exit 0（ruoyi-ai 无差异项） |
| 注释模板 | R119 root cause #5 | R120 root cause：R25 扫+报告 OK 但 exit 阻断 4/9 失效 |
| 隔离约束 | R119 没改 R25 脚本（R25 失效是 owner 拍板项）| **R120 在 owner 拍板前提下改 R25 失效脚本**（任务授权） |

## R25 已自证能红 4/9 vs R120 落地后 8/9

| 脚本 | 来源 | R119 矩阵 | R120 后 |
|---|---|---|---|
| check_cross_repo_contract.sh | R25 | ✅ | ✅（未动）|
| check_entity_complete.sh | R25 | ✅ | ✅（未动）|
| check_i18n_unused_keys.sh | R25 | ✅ | ✅（未动）|
| check_doc_link.sh | R25 | ✅ | ✅（未动）|
| **check_tenant_excludes_apply.sh** | R25 | ❌ | **✅ R120 修** |
| **check_dynamic_loadable.sh** | R25 | ❌ | **✅ R120 修（fresh 0 + ENV 自检 1）** |
| **check_duplicate_ssot.sh** | R25 | ❌ | **✅ R120 修（fresh 0 + ENV 自检 1）** |
| **check_deletion_consistency.sh** | R25 | ⚠️ exit 2 | **✅ R120 修（exit 1）** |
| scan_dead_code.sh | R25 | 待跑（每两周一次）| 待跑（不属 R120 范围）|

**自证能红 R25 9 门禁矩阵：R119 4/9 → R120 8/9（89%）**。

## 隔离约束（OPS-09 撞车 0 严守）

- ✅ git worktree 隔离：`/Users/mac/Documents/ruoyi-ai-wt-r120-v2`（`fix-r120-r25-gates-exit1` 分支）
- ✅ 命令前缀：`cd /Users/mac/Documents/ruoyi-ai-wt-r120-v2 &&` 绝对路径
- ✅ 未动 R119 已 commit 的 5 个新脚本（check-test-coverage-by-domain / check-rule-wiring / reconcile-multi-source / check-e2e-fe-be / check-commit-completeness）
- ✅ 未动 R25 已自证能红 4 个（check_cross_repo_contract / check_entity_complete / check_i18n_unused_keys / check_doc_link）
- ✅ 未动 R25 scan_dead_code.sh（R120 范围外，R121+ 待跑）
- ✅ 跑 reconcile-multi-source 时误改了"事实源对账-20260919.md"，commit 前已 `git checkout` revert（撞车 0）
- ✅ ENV 自检临时目录 `mktemp -d` + `rm -rf` 跑完清理（无残留）
- ✅ 未在 ZK-IPD / ruoyi-ipd-web 等非 ruoyi-ai 仓做任何修改

## commit 落档

- branch: `fix-r120-r25-gates-exit1` (wt `/Users/mac/Documents/ruoyi-ai-wt-r120-v2`)
- 主仓 main commit: **`73299de4`**（含 scripts 4 个 + R120 报告）
- 分支首次 commit: `81120047`（基于 7f0e0fc1 rebase 后）
- commit message: `scripts: 修 R25 4 失效脚本加 exit 1 分支(自证能红)-R120 根除机制`
- files (5 changed, 142 insertions, 5 deletions):
  - scripts/check_tenant_excludes_apply.sh
  - scripts/check_dynamic_loadable.sh
  - scripts/check_duplicate_ssot.sh
  - scripts/check_deletion_consistency.sh
  - docs/ipd-系统说明/R120-修R25-4失效脚本加exit1分支-20260919.md
- pre-commit hook: 门禁 0/1/2 全过（untracked/drift=0/contract OK）
- push 状态: **未 push**（兄弟会话 7f0e0fc1 领先 origin/main 1 commit 未推；撞车 0 严守先不 push，由兄弟会话统一推）

## R120+ 候选（接 R119 列表）

1. ✅ R120 修 R25 4 失效脚本（**已完成**）
2. R121 跑真活 E2E 闭环（先确认撞车 0 → 起 IPD 后端连 ipd_dev → 跑 check-e2e-fe-be.sh）
3. R122 治理轮剩余事项（sys_user↔persons 字符集修复、bonus.poolRate 漂移处置、SQL chain root 排程等）
4. R123 周期任务落地（reconcile-multi-source.sh 接 cron + check-test-coverage-by-domain.sh 接 pre-commit hook）
5. R124 补 scan_dead_code.sh 自证能红验证（每两周一次，下次跑时验证）

## 16 脚本矩阵更新

R119 16 脚本矩阵"自证能红 11/16（69%）" → R120 落地后"自证能红 14/16（88%）"：

| 来源 | 自证能红 | R120 变更 |
|---|---|---|
| R119 5 新脚本 | 5/5 | 未动 |
| R25 9 门禁 | 4/9（R119）| **8/9（R120）** |
| R30+/R110 2 脚本 | 2/2 | 未动 |
| **合计** | **11/16（69%）** | **14/16（88%）** |
