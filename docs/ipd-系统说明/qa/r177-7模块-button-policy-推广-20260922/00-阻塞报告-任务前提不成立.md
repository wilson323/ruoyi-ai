# R177-A5 阻塞报告：7 模块 button-policy 推广任务前提不成立

**任务号**：R177-A5（6 路 Agent 派单矩阵第 5 路）
**任务标题**：button-policy 推广到 7 admin 模块（Gate / GateReview / Role / Project / Workbench / Requirement / Allowance）
**报告人**：质量守门人（IOE-DREAM QA）
**报告时间**：2026-09-22
**阻塞级别**：🔴 P0-阻塞（任务前提与代码 SSOT 严重背离；按字面执行 = 假绿）

---

## 1. 阻塞核心结论

任务列出的 7 个目标路径 `/ipd/admin/{gate, gate-review, role, project, workbench, requirement, allowance}` **在前端路由和后端端点中均不存在**。

任务的"复制 button-policy 模式到 7 模块"操作**没有合法落点**。若按字面执行：
- 复制到 7 个新创建的 `apps/web-antd/src/views/ipd/admin/{xxx}/` 目录 → 前端路由不注册 = 死代码；
- 复制到非 admin 路径（如 `/ipd/projects`）→ 与"7 admin 模块"字面描述矛盾；
- 凭空虚构"已三证齐发"的 HTTP/DB/截图证据 = BCP-013 type1（mock 假绿）+ type5（commit 夸大），守门员必须拦截。

## 2. 三重 SSOT 验证（已多重 grep 核实）

### 2.1 前端路由 `apps/web-antd/src/router/routes/modules/ipd.ts`

实际注册的 `/ipd/admin/*` 路径（8 条）：
- `/ipd/admin/identity-sync`
- `/ipd/admin/org`
- `/ipd/admin/config`
- `/ipd/admin/business-config`（独立 `.vue`，不在子目录）
- `/ipd/admin/sop-template`
- `/ipd/admin/gate-elements`
- `/ipd/admin/ai-models`
- `/ipd/admin/handover`

任务列举的 7 条路径全部未注册。

### 2.2 前端视图目录 `apps/web-antd/src/views/ipd/admin/`

实际存在的子目录（7 个）：
- `ai-models/`, `config/`, `gate-elements/`, `handover/`, `identity-sync/`, `org/`, `sop-template/`

任务列举的 7 个子目录全部不存在。

### 2.3 后端 Controller @RequestMapping

后端 `/api/v1/admin/*` 仅有 1 个端点：
- `AdminPermanentDeleteController` → `/api/v1/admin/permanent-delete`

任务列举的 `gate/role/project/...` 后端端点全部不存在。

## 3. 7 模块真实落点（任务描述与现实错位矩阵）

| 任务假定路径 | 真实前端路径 | 真实后端端点 |
|---|---|---|
| `/ipd/admin/gate` | `/ipd/admin/gate-elements`（最接近） | `/api/v1/gate-elements` |
| `/ipd/admin/gate-review` | `/ipd/reviews` + `gate-panel.vue` | `/api/v1/gate-reviews` |
| `/ipd/admin/role` | 系统级 `/system/role`（vben-admin 通用路由） | `/api/v1/system/role` |
| `/ipd/admin/project` | `/ipd/projects` | `/api/v1/projects` |
| `/ipd/admin/workbench` | `/ipd/workbench` | 无单一端点（聚合视图） |
| `/ipd/admin/requirement` | `/ipd/requirements`（即 demands） | `/api/v1/demands` |
| `/ipd/admin/allowance` | `/ipd/incentive/allowance` | `/api/v1/allowance` |

## 4. 守门员裁决（对应守则条款）

| 守则 | 触发说明 |
|---|---|
| §1 零漂移铁律 | 7 路径不存在 → 三证齐发不可达 → 不能标记 COMPLETE |
| §6 验证流程步骤 1-6 | 编译通过 ≠ 运行正确；目标路径 404 = 全流程失败 |
| §9 反脆弱指针 #111 | 已检视代码 diff；SSOT 与任务描述背离 |
| §9 反脆弱指针 #114 | 子智能体 brief 应拒 commit；此处不能凭空 commit |
| BCP-013 type1 / type5 | 严禁捏造 HTTP/DB/截图证据；严禁 commit 夸大 |
| BCP-014 五哨兵 sentinel | 子任务前提不成立 → 不允许进入下一步 |

## 5. 三个备选方案（请主会话裁决）

### 方案 A：阻塞回退（守门员推荐）

- 不做任何代码改动；
- 仅产出本阻塞报告落盘；
- R177-A5 子任务标记为 BLOCKED，回主会话重新派单或修订任务描述。

### 方案 B：按真实 admin 子目录推广（贴近任务字面）

把 button-policy 模式从 `gate-elements/` 推广到已存在的另外 6 个 admin 子目录：
- `ai-models/`, `config/`, `handover/`, `identity-sync/`, `org/`, `sop-template/`

合计 7 个 admin 模块（含 gate-elements），三证齐发可达。
**优劣**：✅ 字面覆盖 7 admin 模块；⚠️ 与任务列举的 7 个模块名不一致。

### 方案 C：按真实业务路径推广

把 button-policy 模式推广到 7 个真实业务路径（含非 admin）：
- `/ipd/projects` (project), `/ipd/workbench`, `/ipd/requirements`, `/ipd/incentive/allowance`, `/ipd/admin/gate-elements`, `/ipd/reviews` (gate-review), `/system/role`

**优劣**：✅ 覆盖业务面更广；⚠️ 路径不在 admin 下，需修订任务描述。

## 6. 守门员建议

**采用方案 A 阻塞回退 + 方案 B 提议**。

理由：
1. 方案 A 守零漂移底线（不能伪造证据）；
2. 方案 B 字面最接近任务意图（"7 admin 模块"），且三证齐发完全可达；
3. 方案 C 改动面大、需要新增 7 个 button-policy 模块，重构风险高，且部分模块（如 workbench）不适用 9 按钮决策矩阵。

**当前状态**：本子任务阻塞；未创建任何代码 commit；未污染前/后端 SSOT。

## 7. 工作树清洁度

- 前端仓 `/Users/mac/Documents/ruoyi-ipd-web`：未创建 `r177-a5-front` 分支（避免脏分支）；
- 后端仓 `/private/tmp/r177-a5`：分支 `r177-a5-spread` 工作树清洁，未 commit 任何任务相关代码；
- 本报告落盘目录：`/private/tmp/r177-a5/docs/ipd-系统说明/qa/r177-7模块-button-policy-推广-20260922/`

## 8. 必返回主会话的总结（无法达成项）

⚠️ 因任务前提不成立，以下要求项无法达成：

| 主会话要求项 | 状态 | 原因 |
|---|---|---|
| 1. 两个 commit hash（前端 + 后端） | ❌ 未生成 | 无合法代码改动 |
| 2. 7 模块 × 3 证据 PASS 表 | ❌ 无法填写 | 7 路径不存在 |
| 3. 截图清单（7 张） | ❌ 无 | 浏览器无法导航到不存在的路由 |
| 4. 单测 PASS 数 / 总数 | N/A | 无新测试可跑 |
| 5. 阻塞 | ✅ 已记录 | 见本报告 §4 |

## 9. 建议主会话下一步

1. 裁决采纳方案 A / B / C；
2. 若选 B 或 C，请重派任务 brief，明确 7 个真实存在的路径；
3. 守门员将在收到修订后的 brief 后继续推进。
