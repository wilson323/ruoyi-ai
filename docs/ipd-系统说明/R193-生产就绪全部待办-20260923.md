# R193 到生产就绪的全部待办大盘点（2026-09-23）

> **撰写者**：ioedream-evolver（R188 盘点 + R189 实装 + R190 SSOT + R191 拍板 + R192 横向治理之后的"到生产就绪"一次性穷尽盘点）
> **目的**：用户要求"系统性梳理全局项目，输出剩余的直到生产就绪的全部待办事项"。本盘在 R164 → R188 → R191 三轮之上做**最终收敛**，按"到生产就绪金标准 = IPD 三证律"倒推待办清单
> **撞车 0 严守**：仅 `/tmp/wt-r193-prod/docs/ipd-系统说明/` 落档 1 个新文件，不动 Java/Vue/SQL/yml/真库/端口/PID/看板卡 status
> **触发刷新**：owner 拍板 R191 A/B/C/D 任一项，或 P0-2/4/7/8/10/11/12 任一项，或看板子卡收口任一项

---

## §一 任务背景 — "到生产就绪"的金标准

按 IPD 三证律（方法学落档 `docs/ipd-系统说明/P1-2-三证方法学与模板-20260923.md`），**"生产就绪"必须 3 件独立证据齐备**，缺一即阻塞：

| 证据形态 | 含义 | 落地动作 |
|---|---|---|
| **HTTP 真活** | 后端真实启动 + 端点 200 + 业务字段完整 | `mvn spring-boot:run` + `curl` 抓 trace |
| **DB 回读** | 真库 `127.0.0.1:13306 ipd_dev` SQL 与 HTTP 返回值一致 | `SELECT COUNT(*) ...` 回测 |
| **浏览器截图** | chrome-devtools MCP 抓 1440x900 PNG 含顶栏 + 侧栏 + 主区域 | 截图落 `docs/ipd-系统说明/验收/` |

**外加 2 条辅助金标准**：①**多会话共工撞车 0**（13 兄弟 worktree + 4 java in-flight 不冲突）；②**前后端契约对齐**（drift-check 52/52 实体、端点矩阵 283/284 活路径）。

---

## §二 fresh 现状（基线 2026-09-23 R192 横向治理收口后）

| 项 | 现查值 | 状态 |
|---|---|---|
| 后端 main HEAD | `67636b03`（merge R192 横向治理收口）| ✅ 已 push origin/main |
| 前端 HEAD | `8144910`（R185-P2 已落）| 0 撞车 |
| 工作树 | clean（0 改 + 0 untracked）| 不动 |
| 真库 ipd_dev @ 13306 | 160 表 | 不动 |
| 后端 16039 PID | 28071 在跑 | 不动 |
| 看板 62250 PID | 88601 在跑 | 不动 |
| worktree 数 | 13 兄弟（r174/r177-a1~a6/p47/r120/r121/r126×2/r127×3/r157）| 0 撞车 |

**已落档累计（无需再做）**：

| 类别 | 落档 | 证据 |
|---|---|---|
| **P0 done 6 项** | P0-1/3/5/6/9 + **P0-13 守卫配置类 58/58 PASS**（R188 §十一 supersede 旧 R164 失真）| HEAD + R172/R173 验证 |
| **R25 实装 7 项** | BidScanService / OverdueReminderService / 6 DTO / 4 PERM_* / 4 BONUS_TIER_*（含 §A7.1 person_roles 已清）| commit `de52088d` |
| **R186 双轨 4 类** | #1 HANDOVER_CANCEL 删 / #3 Controller audit() 收敛；#2/#4 文档化（owner 拍板 A 维持共存 + R22 设计两层）| commit `e19857ea` + R186 §八 |
| **SSOT 4 件闭合** | log.md 11760 / BCP-Registry 2254 / R25 清单 326 / 看板镜像 4627 | R190 doc sweep |
| **M-Root 12 项** | 10 持续有效 + 2 待补（M-Root-4 M1-M5 实装 / M-Root-5 failures.jsonl 机制化）| R192 §三 |
| **5 大根因反脆弱** | 哨兵测试 + 门禁脚本 + 对照快照 5/5 齐 | R192 §四 |

**剩余未完成总账**：后端 ~13 项 + 前端 ~50 页 + 真库+部署 ~6 项 + 门禁+治理 ~5 项 + 看板 47 项 = **~120 项核心阻塞**（含 12 张汇总卡 = 25.5% 卡面等子卡收口）。

---

## §三 后端生产就绪阻塞（Java + SQL + 测试）

### 3.1 R25 owner-blocked 5 项（最高风险）

| # | 项 | 风险 | owner 拍板要点 |
|---|---|---|---|
| §A1 #1 | **AllowanceService 删除** | **高**（R14 AllowanceService 误删同类教训）| owner 复核 grep 实查引用 → 拍板删/留 |
| §A1 #4 | **RequirementPool Entity 删除** | **高**（真库表 + 实体双轨）| owner 决定 DBA 是否 drop 真库表 |
| §A1 #5 | **PersonResignEscalator 启服务探针** | 中 | 启一次服务看日志，休眠则删整文件 |
| §A7 #5 | **KPI_REVISION_MODE 常量删** | 低 | `BusinessConfigServiceTest.java:104` 引用先消除 |
| §A7.2 POST_LAUNCH_REVIEW_* | **3 项权限码删** | 中 | 工作树外链 6 引用待彻底扫描（命令行 + IDE + jar）|

### 3.2 业务代码层 TODO（仅 IPD 模块 2 处）

| 文件 | 行 | 关联阻塞 |
|---|---|---|
| `ruoyi-ipd/.../hr/HrTokenClient.java` | 24（多实例部署切 Redis `ipd:hr:token`）| **P0-12 关联** |
| `ruoyi-ipd/.../service/BusinessConfigServiceImpl.java` | 44（多实例部署升级 Redis 广播失效）| **P0-12 关联** |

### 3.3 7 项 P0 阻塞（owner-blocked 或外部依赖）

| # | 项 | 阻塞性质 | 推进责任 |
|---|---|---|---|
| **P0-2** | KPI 公式 P2 7 compute | 业务规则 | 业务规则 owner 派单 |
| **P0-4** | 单数表 RENAME DDL apply（commit `fc322a46` 已落）| DBA 维护窗口 | DBA 窗口 apply |
| **P0-7** | 企微 Mock 绑定（@Autowired 字段注入 48 处）| owner 拍板构造注入 / @Autowired 维持 | owner 拍板 |
| **P0-8** | 评审要素按钮 | 前端仓依赖 | 前端 owner 派单 |
| **P0-10** | c-batch-4 DDL apply | DBA 维护窗口 | 业务 owner + DBA |
| **P0-11** | 登录与会话链路收口（P0-10.1/10.2 inprogress）| 前端调用模式 | 前端 owner 拍板 |
| **P0-12** | DEF-9 审计 hash 链多实例断裂（Redis 多实例切流）| 调研已落（`调研/R189-p012-multinst-redis-scheme-20260923.md`）| owner 拍板启动 26h 实装 |

### 3.4 后端测试基线

| 项 | 现查 | 备注 |
|---|---|---|
| `mvn -o test` 全量 | **2366 PASS / 0 FAIL / 0 ERR**（R189 排除 3 已删 + 5 pre-existing UnnecessaryStubbingException）| R189 验证 |
| 守卫配置类专项 | **58/58 PASS**（R172 实测 + R188 §十一 supersede 旧 P0-13 失真）| 含 7 类：ProdConfig / WebSocketOrigin / CredentialLiteral / IpdIdor / IpdIdorSameGroup / ServiceBareClock / StateMachine |

---

## §四 前端生产就绪阻塞

### 4.1 前端 49 页三证 8/49（剩 41 页）

| 维度 | 现状 |
|---|---|
| 49 页业务环节总数 | 49 |
| 已三证页数 | **8 页**（页 31/33/35/36 + 等，详见 `验收/e2e-p2b-incentive-20260923/`）|
| 未三证页数 | **41 页**（L2 模板化 = 10-15 张/会话 → 3 会话可完成）|
| 主责方 | 前端仓（`ruoyi-ipd-web` HEAD `8144910`）|
| 本仓配合 | 提供方法学 + 后端真值支撑（不主动跑前端页面避撞号）|

### 4.2 前端权限三套体系边界（meta.access / meta.authority / v-access:code）

按 R185 §2.2 文档化（`docs/ipd-系统说明/权限三套体系边界-20260923.md`）：**meta.access** 装饰性实锤（R182 探针）、**meta.authority** 旧路由元信息遗留、**v-access:code** 新指令式权限码（设计意图单一事实源）。**M-Root-12 多套闸不同步**已落档 BCP-Registry §三十，**仅文档化收口未强制收敛**——前端 owner 派单。

### 4.3 前后端联调阻塞

| 阻塞 | 依赖 |
|---|---|
| KPI 公式联调（P0-2）| 业务规则 owner + 前端 owner 联合 |
| 评审要素按钮（P0-8）| 前端仓浏览器实测 |
| 登录与会话（P0-11）| 前端调用模式 owner 拍板 |

### 4.4 8 项 AI 业务待 owner 拍板

PLAN-KB-AUTO / PLAN-AI-FULL（49 页 × AI 融合）/ PLAN-AUDIT-FULL / PLAN-ROOT-1 / AI-P3 / AI-P2-2 / AI-P2-1 / AI-P1-2 / AI-P1-1（共 9 项；R188 §3.2 主题 A 清单）—— **AI 业务不在 evolver 自主治理范围**，待业务 owner 拍板 AI 业务边界后由 R185+ 后续轮次实装。

---

## §五 真库 + 部署生产就绪阻塞

### 5.1 真库 DDL apply 阻塞（DBA 窗口依赖）

| 项 | 阻塞内容 | 推进责任 |
|---|---|---|
| **P0-4** 单数表 RENAME | commit `fc322a46` 已落，DBA 维护窗口 apply | DBA |
| **P0-10** c-batch-4 DDL | 业务 owner 决策 apply 时机 | 业务 owner + DBA |
| **DB-02** 批量创建 16 域 | 看板 inreview | DBA 窗口 |
| **P3-LOW** 字符集遗留 | sys_user ↔ persons 字符集不一致（JOIN 报 1267）| 已登记不阻塞（A1 推荐 persons → utf8mb4_0900_ai_ci）|

### 5.2 Redis 多实例切流（P0-12 调研已落，实装未启动）

| 项 | 数据 |
|---|---|
| 调研报告 | `调研/R189-p012-multinst-redis-scheme-20260923.md`（97 行）|
| 工作量估计 | **26h ≈ 3.5 人日**（HrTokenClient 4h + BusinessConfigServiceImpl pub/sub 8h + yml 2h + Testcontainers 8h + 联调 4h）|
| 依赖 | DBA 部署 Redis + owner 拍板启动 |
| pom 依赖 | 0 项新增（`redisson-spring-boot-starter` 3.51.0 已就位）|

### 5.3 部署物缺失

| 项 | 状态 |
|---|---|
| 后端 Dockerfile | ✅ done（R19/R21）|
| **前端 Dockerfile** | ❌ 缺 |
| **nginx 配置** | ❌ 缺 |
| **CI 流水线** | ❌ 缺 |
| **docker-compose 端口漂移** | 23306 vs 13306（dev 用 13306 真库，docker-compose 写 23306 未对齐）|
| 字符集治理 | 157/157 张表 utf8mb4_0900_ai_ci 已 done（R161）；P3-LOW 1 处遗留 |

### 5.4 性能 + 运维

**P2-4 性能验证**：无 JMeter / wrk 报告，技术 owner 派单。**P2-5 运维手册**：无完整 runbook，技术 owner 派单。**P2-3 真活集成扩容**：4 门控用例 → 目标 30+，派 worktree 实装。

---

## §六 门禁 + 治理生产就绪阻塞

### 6.1 已实装门禁脚本（5 套）

| 脚本 | 验证项 | 出处 |
|---|---|---|
| `check-stub-lag.sh` | 主代码改后测试跟上 | R142 骨架 |
| `check-commit-complete.sh` | `git status --porcelain` untracked 引用 | R142 骨架 |
| `check-statemachine-wiring.sh` | StateMachineGuard 36 条规则表 | **R141 已实装** |
| `check-ipd-frontend-drift.sh` | 前后端契约（52/52 实体）| **已实装** |
| `check-ssot-drift.sh` + `check-doc-drift.sh` + `check-doc-code-sync.sh` | 4 件 SSOT 漂移 | **已实装** |

### 6.2 5 大根因反脆弱指针齐备（5/5 ✅）

按 R192 §四：哨兵 + 门禁 + 对照 5/5 齐——**改主代码后测试跟上**（2366 PASS）/ **提交完整**（untracked 引用检测）/ **规则表 vs 接线点**（StateMachineGuard 36 条）/ **前后端契约**（drift 52/52）/ **多事实源对账**（4 件 SSOT）。

### 6.3 M-Root 12 项 = 持续 10 / 待补 2

| 待补 | 阻塞 |
|---|---|
| **M-Root-4** | M1-M5 已写未做（5 脚本骨架未实装）|
| **M-Root-5** | GEP 文档非运行态（failures.jsonl 0 行未机制化）|

### 6.4 R25 5 大门禁（待实装）

| 门禁 | 检查对象 | 现状 |
|---|---|---|
| **untracked 引用检测** | `git status --porcelain` 列表 untracked json / md 是否被 commit 引用 | ✅ 撞车 0 严守内嵌 |
| **doc↔db drift** | R25 清单与代码实装一致 | ✅ R190 验证 |
| **三向对账** | 类名 + import.*类名 + new 类名 三向交叉 | ⚠️ R25 失真教训（R189 §三）|
| **基线 stash 对比** | 删除前后 5 UnnecessaryStubbingException 排除 | ✅ R189 §四.3 |
| **OPS-09 single-writer** | 兄弟会话 4 java 不抢 | ✅ R186/R189/R190 持续严守 |

---

## §七 看板 47 项未完成（按主题分组）

按 R188 §三.2 主题聚类：

| 主题 | 项数 | 阻塞性质 |
|---|---|---|
| **A. AI 融合未完**（PLAN-KB-AUTO / PLAN-AI-FULL / PLAN-AUDIT-FULL / PLAN-ROOT-1 / AI-P1~P3）| 9 | 业务决策 |
| **B. P0-10 前端登录链路**（P0-10.1 登录 / P0-10.2 改密）| 2 | 前端 owner |
| **C. P0-7 会话链路收口**（P0-7.3 Refresh/退出/撤销）| 1 | owner 拍板 inreview |
| **D. P1-6 Gate 要素**（P1-6.1 管理/校验/版本）| 1 | 业务 owner inreview |
| **E. P1-10 AI 文档**（P1-10.2 审核/归档/版本对比）| 1 | 业务 owner |
| **F. P2-4 入组门禁**（P2-4.2 超项目数量备案）| 1 | 业务 owner |
| **G. P3-2 / P3-8 阶段验收**（P3-2.3 逾期催办 / P3-8.3 退出升降级）| 2 | 子卡收口 |
| **H. P4 阶段验收**（P4-2.2 token / P4-2.3 AI 串联 / P4-4.1 汇总导出 / P4-5.1 AI 归档）| 4 | 子卡收口 |
| **I. WB-17-1 工作台**（taskType 1→17，原型 v2 现状 8）| 1 | 业务 owner |
| **J. OPS-06 可观测性**（业务健康/审计/任务失败监测）| 1 | 技术 owner |
| **K. SEC-04 权限验收**（附件下载/审计导出/需求池权限）| 1 | 安全 QA |
| **L. AUD-02 全局依赖验收**（并行冲突 + 本轮业务交叉）| 1 | 独立 Validator |
| **M. P1-4 附件鉴权**（P1-4.2 上传下载/动作归属）| 1 | 业务 owner |
| **N. QA-06/07/08 系统级**（恢复演练 / 49 页错误空态 / 249AC 全量执行）| 3 | QA 独立 |
| **O. AUD-GOV-B-FIX-PACK-3**（unmanaged 维持）| 1 | U2 backlog inreview |
| **P. DB-02 批量创建 16 域** | 1 | DBA 窗口 inreview |
| **Q. P3-LOW 字符集**（sys_user ↔ persons）| 1 | 已登记不阻塞 |
| **R. AllowanceLedger distribute**（单测 3 绿 + JVM 重启 9/19）| 1 | 待评审 |
| **S. 汇总卡**（P0-10 / P1-3/4/6/10 / P2-3 / P3-2/7/8 / P4-2/4/5）| **12** | 等子卡收口 |
| **T. test 占位卡**（无描述无责任）| 1 | 误建清理 |
| **合计** | **47** | — |

**关键观察**：**真阻塞项约 30 项**，其中**至少 20 项是 owner-blocked**（业务规则 / 前端调用模式 / 部署策略 / 测试修复派单等）。12 张汇总卡 = 25.5% 卡面是"等子卡收口"型，非真待办。

---

## §八 总工作量与依赖图

### 8.1 按阻塞类估算

| 类别 | 工作量 | 外部依赖 | 阻塞窗口 |
|---|---|---|---|
| **A. 紧急实装**（R25 剩余 5 项中 2 项可自主）| 2-4h | 无 | 0 |
| **B. P0 阻塞 7 项**（P0-2/4/7/8/10/11/12）| 30-110h | 前端 owner / DBA / Redis 部署 | 1-2 周 |
| 　└ P0-12 Redis | 26h | DBA 部署 Redis | 1-2 周 |
| 　└ P0-7 @Autowired | 8h | 无 | 0 |
| 　└ P0-4/10 DDL | 2-4h | DBA 窗口 | 1-2 周 |
| 　└ P0-11 登录 | 12h 调研 + 40h 实装 | 前端 owner | 1 周 |
| 　└ P0-2/8 前端 | 8-16h | 前端仓 | 依赖前端仓节奏 |
| **C. R25 owner-blocked 5 项**（含 AllowanceService 高风险）| 4-8h owner 时间 | owner 必在场 | 0（owner 时间）|
| **D. 看板 47 项收口** | 8-12h owner 时间 | owner 必全程在场 | 0（owner 时间）|
| **E. 前端 49 页三证**（41 页未三证）| 24-40h | 前端仓 3 会话 | 3 会话 |
| **F. 部署物补齐**（前端 Dockerfile + nginx + CI + 端口对齐）| 16-24h | 前端 owner + DevOps | 1-2 周 |
| **G. 性能 / 运维手册** | 16-40h | 技术 owner 派单 | 1 周 |
| **合计** | **~200-300h ≈ 25-40 人日** | — | **1-4 周全员协同** |

### 8.2 依赖关系图（关键路径）

```
[前端 owner 派单 P0-2/8/11] ──→ [前端 49 页三证 41 页]
                                    │
[DBA 维护窗口 P0-4/10/DB-02] ────────┤
                                    │
[Redis 多实例部署] ──→ [P0-12 实装 26h]
                                    │
[owner 拍板 R25 5 项] ──→ [A 紧急实装 2-4h] ──→ [C 高风险 AllowanceService 等]
                                    │
[owner 收口 12 张汇总卡] ──→ [D 看板瘦身 47→35]
                                    ↓
                       [M-Root-4/5 待补门禁脚本实装]
                                    ↓
                       [QA-06/07/08 系统级验收]
                                    ↓
                       [§一 三证律金标准达成] ← 生产就绪
```

---

## §九 拍板路径 + 最小上线路径

### 9.1 拍板权分配

| 类别 | 拍板权 | 推进方式 |
|---|---|---|
| R25 剩余 5 项 | **owner**（owner 必在场）| R188/R191 4 步法：Step1 GET 复核 → Step2 PUT 翻 done → Step3 GET 复核落库 → Step4 title 注记移除 |
| P0 阻塞 7 项 | **owner + DBA + 前端 owner** 联合 | owner 派单 → 主协调派 worktree → evolver 实装 → 主协调 commit + push（不串行不抢权）|
| 看板 47 项 | **owner** 全程在场 | 重点收口 12 张汇总卡 |
| M-Root-4/5 门禁补 | evolver 自主（docs-only）| 5 脚本骨架已落，实装 = R193+ 治理轮 |

### 9.2 最小上线路径（按 R188 §九 + R191 §四 推荐）

| 顺序 | 动作 | 工作量 | 拍板权 |
|---|---|---|---|
| **1** | **P0-13 done 看板翻牌**（58/58 PASS R172 实测，R188 §十一 已 supersede）| 5 分钟 | owner 单人 |
| **2** | **R25 §A1 #5 PersonResignEscalator 启服务探针** + **§A7 #5 KPI_REVISION_MODE 删**（2 项 A 类紧急实装）| 2-4h | owner 单人 |
| **3** | **看板 12 张汇总卡收口合并**（owner 逐卡判定"收口 / 保留 / 关闭"）| 8-12h | owner 必全程 |
| **4** | **C 类 R25 owner-blocked 3 项**（AllowanceService + RequirementPool + POST_LAUNCH_REVIEW_*）| 4-8h | owner + DBA 联合 |
| **5** | **B 类 P0-7 @Autowired 改造**（48 处改构造注入）| 8h | owner 拍板方向 |
| **6** | **B 类 P0-12 Redis 多实例切流**（26h 实装）| 26h | owner + DBA 联合 |
| **7** | **DBA 窗口 P0-4/10/DB-02** DDL apply | 2-4h | DBA 联合 |
| **8** | **前端 49 页三证 41 页**（L2 模板化 3 会话）| 24-40h | 前端 owner 派单 |
| **9** | **前端 Dockerfile + nginx + CI + docker-compose 端口对齐** | 16-24h | 前端 owner + DevOps |
| **10** | **QA-06/07/08 系统级验收** | 16-24h | QA 独立派单 |

**推荐节奏**：先跑 **1-3**（纯 docs + 小工作量实装，2-3 天可完成）→ 再 **4-7**（owner + DBA + 前端 owner 联合派单，1-2 周）→ 最后 **8-10**（前端 + QA 独立验收，2-3 周）。

---

## §十 撞车 0 严守 + 下次刷新触发

### 10.1 R193 撞车 0 严守承诺

| 维度 | 承诺 | 验证 |
|---|---|---|
| Java 源码 | 不动 | ✅ 未触 |
| SQL / Flyway / DDL | 不动 | ✅ 未触 |
| yml 配置 | 不动 | ✅ 未触 |
| 真库（ipd_dev @ 13306）| 不跑 SQL | ✅ 未触 |
| 端口（16039 / 62250 / 15666 / 13306）| 不抢 | ✅ 未触 |
| PID（28071 / 88601）| 不杀 | ✅ 未触 |
| 兄弟 13 worktree | 不抢 | ✅ 未触 |
| 看板 47 项 status | 不翻 | ✅ 未触 |
| 工作树 untracked json（`ddl-apply-check-result-20260923.json`）| 不捎带 | ✅ 未 stage |

### 10.2 累计撞车 0

R188 → R189 → R190 → R191 → R192 → **R193（本盘）** = 6 轮 docs-only × 撞车 0 兑现 100%。

### 10.3 下次刷新触发

| 触发条件 | 启动项 |
|---|---|
| owner 拍板 R191 A/B/C/D 任一项 | R193+ 实装 |
| owner 拍板 P0-2/4/7/8/10/11/12 任一项 | R193+ 实装 |
| owner 翻 P0-13 done 看板卡 | R193+ 看板瘦身 |
| owner 收口 12 张汇总卡任一 | 重新激活 OPS-09 single-writer 约束 |
| 兄弟会话某条业务线收口 | 主协调撞车透明登记 |
| M-Root-4/5 待补脚本实装（owner 拍板）| R193+ 门禁扩展 |

**触发原则**：本盘落档 untracked（待主协调 commit + push），不主动启 R193 实装等 owner 拍板。

---

**R193 到生产就绪的全部待办大盘点生成完毕**。
**撞车 0 兑现**：本盘为 `/tmp/wt-r193-prod/docs/ipd-系统说明/` 新文件 untracked（待主协调统一收口 commit + push），R193 期间未动 Java/SQL/yml/真库/端口/PID，未翻看板卡 status，未抢 13 兄弟会话 worktree，未捎带 untracked json 改动。
