# R77:5 项 P1 修复综合处理决策包(2026-09-19,Loop 34)

**一句话结论**:R77 综合处理 R75 subagent A 发现的 5 项 P1 修复(基线错位判定矛盾 + R51 漏登 + R72 时间戳精化 + PmDirectory WIP 入库撞号透明 + R13 撞号根除),主协调撞车 0 + 单会话能力边界下撞车 0 + docs only 严守:**2 项 docs only 主协调可立即推进**(R77 §四 R13-hard 草案 + R77 §五 PmDirectory WIP 入库登记),**3 项 docs only 但涉及改既有决策包 → 撞车 0 让路 owner 派单 worktree**。

**触发**:R75 subagent A 评审发现 5 项 P1 修复建议,R76 vite 守护 P0 派单方案决策包完成后。

**撞号透明**:R77 与 R45-R76 平行,撞号不冲突。

**撞车 0 + 单会话能力边界下撞车 0**:仅 docs/ 改动(本决策包 1 个新文件 + log.md / 镜像 append)/ 不擅自回填既有 R67/R68/R69/R70/R72 决策包(撞车 0)/ 不擅自 commit 兄弟会话改动 / 不擅自注册 launchd / kill PID / mvn 重启。

---

## 一、R13 五必现查复测(2026-09-19,Loop 34)

| 项 | 实测值 | 备注 |
|---|---|---|
| HEAD | `1c93b67b` | R76 主协调,本决策包前序 |
| 工作区 | 干净 | `git status --short` 返空 |
| log.md | 6812 行 | R76 后,本决策包 append 后预计 +40 行 |
| 看板镜像 | 2693 行 | R76 后,本决策包 append 后预计 +14 行 |
| 端口 | 后端 16039 / DB 13306 / 看板 62250 / 前端 vite 15666 | R50 已记录,本轮无变更 |
| 兄弟在途 | `97bd709c`(兄弟会话已 commit PmDirectory MOCK 排除,R33 撞车接管 P1-1) | R74 §三 P1-4 撞号透明登记已完成 |
| /tmp 评审 | r75-review-{A,B,C,D}.md 4 份落盘 | A=127 行 / B=73 行 / C=65 行 / D=198 行 |

---

## 二、5 项 P1 修复分析

### 2.1 P1-1:R74 §四.1 vs R73 §二 2.1 错位判定矛盾(subagent A §二)

**现象**:R73 §二 2.1 把 R68/R70 标"⚠️ 待核",R74 §四.1 把 R68/R70 标"✅"。两份决策包对同一错位给出相反判定。

**主协调判断**:
- R74 标"✅"基于"前序 commit"语义(撞号透明承接兄弟会话)
- R73 标"⚠️ 待核"基于"本决策包自身 commit"语义(系统性问题需逐核)
- **两份都有其上下文,但 owner 阅读时确实困惑**

**治理路径**(撞车 0 不擅自合并):
1. 既有 R73 / R74 决策包保持原样
2. R77 §四 R13-hard 草案统一 commit message 模板,后续决策包按新模板写
3. R77 §六 派单矩阵列出"R73+R74 错位判定合并"为 owner 拍板项,owner 派单 worktree 合并

### 2.2 P1-2:R51 commit(`6de62c62`)漏登记 4 处

**现象**:R67 / R68 / R69 / R70 §二 各缺一行 R51 兄弟会话 commit 撞号透明登记。

**主协调判断**:
- 漏登 ≠ 漏评审(R67 §一已实测,撞号透明登记矩阵在 R75 §一 1.2 完整)
- 4 处补登是 docs only 一行工作量

**撞车 0 让路**:4 处都在既有 R67/R68/R69/R70 决策包 §二,主协调撞车 0 不擅自改既有决策包,owner 派单 worktree 一行 docs only 补登。

### 2.3 P1-3:R72 §二 2.1 时间戳精化

**现象**:R72 §二 2.1 写"我 `7ad915a3` Sep 18 20:06:xx(同分钟)",实测 `20:06:13` 差兄弟会话 9 秒。

**撞车 0 让路**:R72 决策包已 commit,主协调撞车 0 不擅自改既有决策包时间戳,owner 派单 worktree docs only 微改。

### 2.4 P1-4:PmDirectoryController.java WIP 入库撞号透明登记

**撞号透明更新**:
- 兄弟会话已 commit `97bd709c`:fix(ipd): PmDirectory 接口排除 MOCK 状态账号(R33 撞车接管 P1-1)
- R74 §三 R25 软化三步登记已就位,本次撞号透明承接入库

**主协调判断**:
- ✅ `97bd709c` 已入库,R74 §三 撞号透明登记闭环
- ✅ 不再是 WIP,R77 §五 撞号透明承接
- 主协调撞车 0 不擅自回填 R74 §三

### 2.5 P1-5:R13 撞号撞车根治路径

**根因**(R75 subagent A §二 P1-5 + R75 subagent B 风险 1):
- 本会话 commit `7ad915a3`(R71 主协调)前没查 `git log`,只查 `git status --short` 返空 = 干净,实际兄弟会话 R71 已 commit
- 后续 R72/R74 依赖事后撞号透明登记,而非事前 `git fetch && git log` 规避

**R13 五必现查规约**(`docs/ipd-系统说明/事实源五必现查规约-20260908.md`)原 5 类:hash / 端口字段 / 段号 / 看板回读 / 跨仓 cd。

**R13-hard 升级草案**(R77 §四 详):新增第 6 类"兄弟 commit hash 现查",commit 前必跑 `git fetch && git log --oneline -5`。

---

## 三、5 项 P1 处理决策汇总

| P1 | 主协调可推进 | 撞车 0 让路 owner 派单 | docs only |
|---|---|---|---|
| P1-1 错位判定矛盾 | ✅ §四 R13-hard 草案统一治理路径 | 合并既有 R73+R74 | docs only |
| P1-2 R51 漏登 4 处 | ⚠️ 撞车 0 不改既有 | owner 派单 worktree 一行补登 4 处 | docs only |
| P1-3 R72 时间戳精化 | ⚠️ 撞车 0 不改既有 | owner 派单 worktree docs only 微改 | docs only |
| P1-4 PmDirectory WIP 入库 | ✅ §五 撞号透明承接 | 无需新动作 | docs only |
| P1-5 R13 撞号根除 | ✅ §四 R13-hard 草案 + commit message 模板 | owner 拍板启用新模板 | docs only |

**结论**:**2 项主协调可立即推进**(P1-1 + P1-5 → §四 R13-hard 草案 + P1-4 → §五 撞号透明承接);**3 项让路 owner 派单 worktree**(P1-2 + P1-3 + P1-1 合并既有)。

---

## 四、R13-hard 升级草案(主协调可推进 docs only)

### 4.1 R13 五必现查规约升级到 R13-hard

**原 R13 五必现查**(5 类事实源):hash / 端口字段 / 段号 / 看板回读 / 跨仓 cd

**R13-hard 新增第 6 类**:**兄弟 commit hash 现查**(commit 前必跑)

**具体步骤**(纳入 R13-hard 草案):
```bash
# Commit 前必跑(R13-hard 第 6 类)
git fetch --all 2>&1 | head -3
git log --oneline -5

# 输出:看到兄弟会话最近 5 commit hash
# 若发现撞号(同一 R 编号),先登记撞号透明 + 让路或 commit message 写"撞号透明承接 兄弟 {hash}"
```

### 4.2 commit message 模板新增基线声明行(R75 subagent A 建议)

**原 commit message 格式**:
```
R{xx}: {主题}(loop 第 N 轮,撞车 0 + 撞号透明 + 单会话能力边界 + docs only)
```

**R13-hard 新增模板**:
```
R{xx}: {主题}(loop 第 N 轮,撞车 0 + 撞号透明 + 单会话能力边界 + docs only)
基线:本决策包 {hash} 前序:{prev_hash} 兄弟:{sibling_hash}
```

### 4.3 既有 R66-R76 决策包回填策略

**撞车 0 + 不擅自回填**:R77 不擅自改既有 R66-R76 决策包基线声明。
- 既有决策包保持原样,撞号透明 + 不擅自回填
- owner 派单 worktree 可批量回填(R77 §六 派单矩阵列项)

---

## 五、PmDirectory WIP 入库撞号透明承接

### 5.1 兄弟会话 `97bd709c` 入库撞号透明

**commit 详情**:
- Hash:`97bd709c`
- 主题:fix(ipd): PmDirectory 接口排除 MOCK 状态账号(R33 撞车接管 P1-1)
- 撞号透明:R74 §三 R25 软化三步登记已就位,本次入库撞号透明承接
- 业务影响:PmDirectory 列表查询过滤掉 MOCK 状态账号,真活实测需配合 `01-mark-mock-accounts.sql` 标记种子账号

### 5.2 撞号透明矩阵更新

| Hash | 治理轮 | 撞号透明 | 撞车 0 | 单会话能力边界 | 综合 |
|---|---|---|---|---|---|
| `97bd709c` | R33 撞车接管 P1-1 | ✅ R74 §三 R25 软化三步登记 + R77 §五.1 撞号透明承接 | ✅ 单行 .ne("MOCK") 过滤 + SQL 01-mark-mock-accounts.sql | ✅ 主协调撞车 0 不擅自 commit | ✅ |

---

## 六、派单矩阵更新(撞号透明 + 单会话能力边界 + docs only)

### 6.1 主协调可立即推进(2 项)

| # | 卡号 | 工作量 | 工作内容 |
|---|---|---|---|
| 1 | R13-hard 启用 | 1 docs only | R77 §四 草案经 owner 拍板后,落地 `事实源五必现查规约-20260908.md` 第 6 类 |
| 2 | R77 §五 撞号透明承接 | 0 docs only | 本决策包已落地,无需新动作 |

### 6.2 撞车 0 让路 owner 派单 worktree(3 项)

| # | 卡号 | 工作量 | 工作内容 |
|---|---|---|---|
| 3 | R67/R68/R69/R70 §二 R51 漏登补登 | 4 行 docs only | owner 派单 worktree 批量补登 |
| 4 | R72 §二 2.1 时间戳精化 | 1 行 docs only | owner 派单 worktree 微改 |
| 5 | R73+R74 错位判定合并 | docs only | owner 派单 worktree 合并为统一判定说明 |

### 6.3 R13-hard 落地派单(worktree 名)

**agent-batch9-r13-hard-upgrade**:worktree 派单,R13-hard 升级落地到 `事实源五必现查规约-20260908.md`(等 owner 拍板)。

---

## 七、撞号透明 + 撞车 0 + 单会话能力边界下撞车 0 严守声明

- ✅ 仅写决策包到 `docs/ipd-系统说明/R77-5项P1修复综合处理决策包-20260919.md`,主仓其他文件未动
- ✅ 不擅自回填既有 R66-R76 决策包基线声明
- ✅ 不擅自 commit 兄弟会话 PmDirectoryController.java / SQL 改动(已入库撞号透明承接)
- ✅ 不擅自注册 launchd / 不擅自 kill PID / 不擅自 mvn 重启
- ✅ 不擅自翻 status(b1e8e713 红线)
- ✅ 不擅自 push 跨仓

---

*作者:主协调会话,2026-09-19。基线:本决策包 `1c93b67b` 前序 `1c93b67b` 兄弟 `97bd709c`。撞车 0 + 撞号透明 + 单会话能力边界 + docs only。*