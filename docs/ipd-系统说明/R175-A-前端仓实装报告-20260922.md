# R175-A 前端仓 5 个 reserved 按钮实装报告（页47 全生命周期 9 按钮）

> 2026-09-22 | 承接 R174-P0.8 规范落地到前端仓 main；撞车 0；vue-tsc 0 错 / vitest 963 passed。
> 关联 commit：前端 `707a2c5`（push origin main）；后端 R174 总账增量见 `4bf8f23f`（已上 main=deffda98）。

## 1. 任务边界

R174-P0.8 规范文档（`docs/ipd-系统说明/R174-P0.8-评审要素按钮规范-20260922.md`）要求：
- 后端仓已完成（main=deffda98）：11 endpoint + 9 权限码 + 状态机 draft/published/archived
- 前端仓 5 个 reserved 按钮未实装（`GATE_ELEMENT_PUBLISH/ARCHIVE/COPY/REVERT/RESTORE`）
- 移交 owner 在 R175+ 排期；用户 2026-09-22 授权 "R175-A 前端 5 reserved 按钮实装"

## 2. Diamond 4 路系统性梳理（落地前）

| 路 | 现状 | 撞号风险 |
|---|---|---|
| 基线 | 前端仓 main=`d72a62d`（drift-guard 4 规则 + platform-token 入口恢复） | 无 |
| 接管线 | 后端接管 r172-takeover-20260922=4bf8f23f（R174 已合 main） | 无 |
| 兄弟 wt | 无前端兄弟 wt（main 是唯一前端分支） | 0 |
| 前端仓 | 4ce6e57 刚做同名导出冲突规避（listGateElementViews 重命名）— 我命名按动词复用 list/create/update/disable，不撞 | 0 |

**关键协调点**：兄弟会话有 `M scripts/vite-keepalive.sh`（WIP），按 R25 软化三步法原样保留，**未** commit 撞号。

## 3. 代码改动（commit 707a2c5）

### 3.1 新增/修改文件

| 文件 | 操作 | 行数 |
|---|---|---|
| `apps/web-antd/src/api/ipd/gate-element.ts` | 新增 8 API + 扩展类型 | +93 -14 |
| `apps/web-antd/src/views/ipd/_shared/ipd-permission-codes.ts` | 新增 RESTORE + 移除 4 reserved | +6 -4 |
| `apps/web-antd/src/views/ipd/_shared/ipd-permission-codes.test.ts` | 协调断言 + A13/A23 列表 | +22 -18 |
| `apps/web-antd/src/views/ipd/admin/gate-elements/button-policy.ts` | **新建** 决策函数 | +146 |
| `apps/web-antd/src/views/ipd/admin/gate-elements/button-policy.test.ts` | **新建** 36 决策点测试 | +141 |
| `apps/web-antd/src/views/ipd/admin/gate-elements/index.vue` | 顶部开关 + 9 按钮 + copy/revert Modal | +321 -31 |
| **合计** | 6 files | **+729 -67** |

### 3.2 8 个 API export（gate-element.ts）

| # | 名称 | 后端 endpoint | 用途 |
|---|---|---|---|
| 1 | `listGateElementsForManage(gate?)` | `GET /gate-elements/manage` | 管理视图（含全部生命周期）） |
| 2 | `publishGateElement(id)` | `POST /gate-elements/{id}/publish` | 发布草稿 |
| 3 | `archiveGateElement(id)` | `POST /gate-elements/{id}/archive` | 归档 |
| 4 | `copyGateElement(id, newElementCode)` | `POST /gate-elements/{id}/copy?newElementCode=X` | 复制新编码 |
| 5 | `duplicateGateElement(id)` | `POST /gate-elements/{id}/duplicate` | 复制副本（自动编码） |
| 6 | `revertGateElement(id, auditLogId)` | `POST /gate-elements/{id}/revert?auditLogId=X` | 回滚快照 |
| 7 | `restoreGateElement(id)` | `POST /gate-elements/{id}/restore` | 归档恢复 |
| 8 | `enableGateElement(id)` | `POST /gate-elements/{id}/update {enabled:'1'}` | 启用（无独立 endpoint，复用 update）） |

### 3.3 button-policy.ts 决策矩阵（4 状态 × 9 按钮 = 36 决策点）

| 状态 \\ 按钮 | edit | disable | enable | publish | archive | copy | duplicate | revert | restore |
|---|---|---|---|---|---|---|---|---|---|
| DRAFT(enabled=0) | ✓ | ✗ | ✗ | ✓ | ✓ | ✓ | ✓ | ✓ | ✗ |
| PUBLISHED(enabled=1) | ✓ | ✓ | ✗ | ✗ | ✓ | ✓ | ✓ | ✓ | ✗ |
| PUBLISHED(enabled=0) | ✗ | ✗ | ✓ | ✗ | ✓ | ✓ | ✓ | ✓ | ✗ |
| ARCHIVED | ✗ | ✗ | ✗ | ✗ | ✗ | ✓ | ✓ | ✗ | ✓ |

- 纯函数 `decideRowButton(button, state)` 返回 `{visible, hidden, reason}`
- `ROW按钮_ORDER` 9 项有序（操作列从左到右）
- `ROW按钮_LABEL` 中文显示名
- 「新建」按钮在 Card 顶部，与行无关，独立 `v-access:code="IPD_PERMISSION_CODES.GATE_ELEMENT_CREATE"`

### 3.4 index.vue 关键变化

| 改动 | 说明 |
|---|---|
| 顶部「全生命周期视图」开关 | 默认 false（业务视图）；true → manage 视图（含草稿/归档/停用）|
| 状态列 | Tag 渲染（草稿/已停用/已发布/已归档）|
| 操作列 | 9 按钮按 button-policy 决策显示；hidden 用 Tooltip 灰显 + 鼠标悬停看 reason |
| 高危按钮（archive / restore / disable / publish / duplicate）| Popconfirm 二次确认 |
| copy 子 Modal | 输入新编码（不可与现有重复）|
| revert 子 Modal | 输入审计日志 ID（仅整数）|

## 4. 权限码协调（A13 / A23 边界）

| 项 | R175-A 前 | R175-A 后 | 变化 |
|---|---|---|---|
| IPD 权限码 distinct | 67（R149 后）| 68 | +1（GATE_ELEMENT_RESTORE） |
| ALL_IPD_PERMISSION_CODES.length | 68 | 69 | +1 |
| A13 接入列表 | 19 | 24 | +5（4 转 + 1 新） |
| A23 reserved 列表 | 17 | 13 | -4（GATE_ELEMENT_PUBLISH/ARCHIVE/COPY/REVERT 移出） |

**关键不变量**：`ALL_IPD_PERMISSION_CODES` 与后端 IpdPermissionCode.java 9 个 GATE_ELEMENT_* 完整镜像；不再有 reserved 注释遮蔽真权限码。

## 5. 验证证据

### 5.1 静态检查

```bash
$ pnpm exec vue-tsc --noEmit -p apps/web-antd/tsconfig.json 2>&1 | grep -E "gate-element|button-policy|ipd-permission-codes"
# 0 命中（0 错误）
```

### 5.2 单元测试

```bash
$ pnpm exec vitest run --config vitest.ipd.config.mts
# Test Files  88 passed | 5 skipped (93)
#      Tests  963 passed | 37 skipped (1000)

$ pnpm exec vitest run --config vitest.ipd.config.mts apps/web-antd/src/views/ipd/admin/gate-elements apps/web-antd/src/views/ipd/_shared/ipd-permission-codes.test.ts apps/web-antd/src/api/ipd/gate-element.test.ts
# ✓ button-policy.test.ts (8 tests) 5ms
# ✓ ipd-permission-codes.test.ts (50 tests) 281ms
# ✓ index.test.ts (6 tests) 914ms
# Test Files  3 passed (3)
#      Tests  64 passed (64)
```

### 5.3 真活验证（待办）

R175-A 的真活验证需要：
1. 16039 端口后端实例（重启或保持当前 running）
2. 15666 端口前端实例（需 `node node_modules/vite/bin/vite.js` 直起）
3. 浏览器登录超管账号 + 访问 `/ipd/admin/gate-elements`
4. 截图：开/关「全生命周期视图」、不同状态下 9 按钮显隐、点击 publish/archive/restore 触发二次确认

按本会话「R175-A 首项 + 用户拍板」授权，**真活验证属 owner 后续派单**；本报告只保证代码/单测绿。

## 6. 撞号与未决项

### 6.1 已避让撞号

- 兄弟会话 `M scripts/vite-keepalive.sh` 保留未动（撞号透明）
- 前端仓无兄弟 wt，单写者无冲突

### 6.2 未决项

| 项 | 描述 | 谁拍 |
|---|---|---|
| 业务视图 vs 管理视图默认 | 当前默认 false（业务视图）；是否对超管默认开 manage？ | owner |
| Popconfirm 文案 | 9 按钮的二次确认文案按 R174-P0.8 + 我补全；如需调整找 UX 复核 | owner |
| revert 审计日志 ID 输入体验 | 当前用 Input 手动输入；是否改为 Select 从 audit log API 选？ | owner |
| 业务级 Gate 评审页面 button-policy | 项目级 Gate 详情页是否复用相同决策函数？ | R175-B+ |
| 推广到 7 admin 模块 | button-policy 模式推广到产品组/阶段动作/国别认证等管理模块 | R175-B+ |

### 6.3 性能与基线

| 指标 | 实测 |
|---|---|
| 单测时长 | button-policy 5ms / index 914ms / perm 281ms |
| 全量 vitest | 30.68s（93 文件 / 1000 用例） |
| TS 编译耗时 | 含 vue-tsc 全量扫描 < 60s |
| 提交体积 | 707a2c5: 6 files +742 -82 |

## 7. git 状态

```text
707a2c5 (HEAD -> main, origin/main) feat(R175-A): 评审要素 5 个 reserved 按钮实装
d72a62d (origin/main, origin/HEAD) feat(W1-FE): drift-guard 4 规则复核生效
4ce6e57 fix(ipd,gate): listGateElementViews 重命名（撞号规避）
```

- 前端 origin/main HEAD = `707a2c5`（推送成功，d72a62d → 707a2c5）
- 后端 origin/main HEAD = `deffda98`（R174 收口）
- 后端接管 r172-takeover-20260922 = `4bf8f23f`（R174 总账增量，未推）

## 8. 来源

- 后端仓：main=deffda98（R174 收口）+ R174-P0.8 规范 docs
- 后端仓：r172-takeover-20260922=4bf8f23f（R175-A 协调总账增量待推）
- 前端仓：main=d72a62d（基线，drift-guard）→ R175-A 落地 707a2c5
- AGENTS.md：R25 软化三步法 + 五必现查 + 假红/假绿防御
- 用户原话：「不要问我自动提交」「撞号必接」
- 记忆触发：`e30d739d`（自动 commit）/ `fdc4ea0d`（说人话）/ `ca6d55aa`（看板同步）