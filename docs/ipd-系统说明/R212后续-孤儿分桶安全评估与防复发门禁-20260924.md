# R212 后续：孤儿端点分桶 · 安全评估 · 防复发门禁（2026-09-24）

> **性质**：R212 闭环梳理的后续跟进轮。三路专业智能体并行执行（PM 分桶 / QA 守门人复核+门禁设计 / 安全评估），本轮主会话只做固化、勘误、建卡登记。全程只读探针 + docs 写入 + 看板建卡；Java/Vue/SQL/yml 零改动；真库零写；16039 零请求。
> **交付物落盘**：`验收/R212-孤儿跟进-20260924/`（5 个文件：分桶表 / 安全评估 / 门禁草案 / 复核记录 / 现查 JSON）。

---

## 一、口径勘误：门禁孤儿 98 → 96（非劣化，单向好转）

- 现查重跑（14:43:51Z 主会话独立复跑；14:13:06Z QA 守门人首跑两次集合对称差 0）：**orphan_endpoints=96 · field_mismatches=15 · orphan_paths=0**，exit 0。
- 消亡 2 条 = `GET /api/v1/kpi/functional-metrics`、`GET /api/v1/kpi/functional-metrics/codes`；归因：前端提交 `997fbc7`（`kpi.ts:196/201/206` 补 ipdGet/ipdPut），**不是**后端删端点。兄弟 merge（`523e4670..HEAD` 25 提交）中被扫 controller 目录仅 `IpdAuthController.java` 变更且新增 `@*Mapping`=0 → 结构上不可能产生新孤儿，实测排除。
- **0921 差集链修正**：105（0921 基线）→ 98（R212 报告）→ **96（现查）**。canonical 主文与 V2 指针文中「98」为 13:01Z 时点值，按撞车 0 不改写他会话正文，以本文件为最新口径。

## 二、交付一：96 条孤儿证据化分桶（PM 智能体，全表见 `r212-orphan-buckets.md`）

| 桶 | 条数 | 定性 |
|---|---:|---|
| **A 真缺口（该接没接）** | **47** | 16 个业务域，后端 Service 已实现 + 原型有产品对应 + 前端无调用 |
| **B 内部/运维专用（不该接）** | **27** | @Scheduled 手动触发 13、hr-sync/person-sync 运维 10、调度项、owner 已裁决预留（d81af12c 合规 4、产品规则关闭 2） |
| **C 存疑/疑似废弃** | **13** | stub / 旧接口保留 / 变体冗余 / 无产品对应 |
| **C-FP 门禁误报** | **9** | 前端实际已接线（模板串拼接成静态匹配盲区），与看板 `67896f31 [AUD-TRI-R2]`「9 处平反」吻合 |

**A 类 16 域**（净新建卡建议 ≈13 张，2 域挂既有 todo 卡 P2-3 `7ad0b052` / P1-3 `53ecc543`，切换验收 5 条按 owner 裁决 d81af12c 归 P3-7.1 联测不建卡）：Gate评审 5 · 项目配置 7 · 奖金池 3 · 回款台账 3 · KPI配置 3 · KPI共担 3 · 移交 3 · 上市复盘 3 · 工作台 2 · 人员 2 · SOP 2 · 登录扫码 1 · 需求变更 1 · 产品解绑 1（强证据举例：`login.vue:178` 占位"暂未开放"、`changes.vue:20` 注释宣告契约未兑现、batch-01:465 明文"缺失接口"）。

**5 条无法定性待 owner 裁决**（详见分桶文件附录）：permanent-delete/audit、persons/{id}/rehire、P0升级链 list/resolve、负反馈 3 变体（[P3-8] 98db7e00 todo 在用裸端点时需回补 A）、ai-copilot/chat（R183 三卡在建，属"规划中未接"）。

## 三、交付二：安全暴露面评估（agency 安全审计，全表见 `r212-orphan-security.md`）

**分级：高 7 · 中 14 · 低 75**。全局兜底结论：`IpdWebSecurityConfig.java:42-60` 登录拦截器 + `SaInterceptor(isAnnotation)` 覆盖 `/api/v1/**`，匿名豁免仅 4 条路径模式 → **严格"无鉴权裸奔"孤儿 = 0**；但豁免路径本身藏着本轮最高危发现。

**"高"档 7 条**：
1. 🔴 **`POST /api/v1/auth/wecom/qr-login`**（认证绕过/账号接管级）：匿名端点，仅凭请求体 `wecomUserId` 查库即签发 scope=FULL JWT——无口令、无 OAuth code 换验、**无 @RateLimiter**（对照 `/login` 有 5次/60s）；代码自称"Mock"但无 profile/开关，**当前恒定启用**（`IpdAuthController.java:81-88` + `IpdAuthService.java:75-100`）→ 立即下线或改造。
2. `POST /api/v1/projects/{projectId}/members`：仅 `requireProjectCreator()` 角色门、无组归属比对 → 跨组绑定成员并触发评级快照/津贴基数锁定（财务影响）。
3. `POST /api/v1/stage-actions/instantiate`：服务方法无 actor 形参 → 任意内部 PM 可向任意项目批量物化动作。
4. `POST /api/v1/stage-actions/ensure-bio-compliance`：同上，跨组挂载 C12 合规动作。
5. `POST /api/v1/gates/{gateId}/observers/invite`：有角色复检但无 gate 归属校验 → 任一组长可向他组 gate 邀列席并触发通知。
6. `POST /api/v1/projects/{id}/cert-items/sync`：权限仅角色级、operatorId 只用于审计 → 跨组写认证清单。
7. `POST /api/v1/public/demands`：匿名写入属 P4-1.1 设计公开，补偿控制已核实存在（honeypot + 同 IP 10次/时），**需确认生产限流非 InMemoryHourRateLimiter**。

**附带发现**：孤儿清单 `(类级/兜底)` 标签系统性名实不符——52 条标"类级"，实际 39 个孤儿 Controller 类级 @Sa 注解为 0（20 条实为方法级权限码、32 条完全无注解仅靠登录兜底 + 方法体 `permission.requireXxx()`）。权限注解与磁盘差异 0 条（兄弟未改动）。

## 四、交付三：孤儿"只减不增"防复发门禁草案（QA 守门人，全文见 `r212-gate-design.md`）

要点：新增后端端点提交时必须①同轮有前端调用，或②登记内部/运维白名单（path/reason/owner_card/review_date/expire/evidence），否则 FAIL。
- 影响面实测=零（仓库内无 CI/hook 实际 exec 靶脚本），单跑 0.067s 可进 pre-commit。
- **口径地雷**：orphan 按 canonical path 去重（285 mapping → 96 条）→ 白名单/baseline 主键必须是 path。
- baseline 棘轮 + **硬闸：磁盘 count > `git show HEAD:baseline` count 即 FAIL**（删基线重生绕不过）；白名单卡号须在看板镜像可命中、expire 过期回落 violation、reason 禁泛词（六道防伪）。
- **待 owner 拍板 4 项**：① exit code 语义（现状 1=P0孤儿/2=环境错误，推荐位掩码新增 4=新孤儿未白名单，照搬需求书会静默反转）；② 96 条是否全量塞 baseline 豁免；③ method 维度是否收紧；④ FE 侧 orphan_paths 是否同接。拍板后另立卡实施（S1-S5 拆解含"临时加 @GetMapping 必红"自证验收）。

## 五、本轮看板建卡（4 张，POST + 独立 GET 回读；看板 477→481）

| 卡 | 状态 | 内容 |
|---|---|---|
| `9d50c5fd` | todo | [SEC-R212-A][U0高] qr-login 匿名签发 FULL JWT 缺陷，下线或改造（开关+限流+真实接入前移除） |
| `dbe1b6a7` | todo | [SEC-R212-B][U1高] 孤儿写端点跨组越权 5 条打包 + public/demands 生产限流核实 |
| `b4be8fa5` | todo | [ORPHAN-A][U2中] 孤儿 47 条真缺口 16 域接线汇总伞卡（13 卡拆分建议待 owner 拍板） |
| `7b76b7cd` | todo | [API-GATE-RATCHET][U2中] 孤儿只减不增门禁实施（§四 4 项拍板前置） |

## 六、复算命令

```bash
# 门禁现查（本轮 96/15/0，exit 0）
cd /Users/mac/Documents/ruoyi-ai && IPD_FE_API_DIR=/Users/mac/Documents/ruoyi-ipd-web/apps/web-antd/src/api/ipd \
  node scripts/check-api-contract-fe-be.mjs --json | python3 -c "import json,sys;d=json.load(sys.stdin);print(len(d['orphan_endpoints']),len(d['field_mismatches']))"
# 分桶/安全/门禁草案/复核记录
ls docs/ipd-系统说明/验收/R212-孤儿跟进-20260924/
```

## 七、撞车 0 声明与限制

- 仅写 `docs/ipd-系统说明/`（本文件 + 验收目录 + log.md + 镜像追加）与看板建卡；兄弟在途文件（BonusPoolService 等）零触碰；canonical R212 主文与 R213 计划仅追加一行指针，未改其正文数字。
- 分桶以 `/tmp/r212_l1_table.md` 96 行为准（对应 13:0xZ 现查）；本文件 §一记录其后口径已再消亡 0 条、稳定 96。
- 安全评估为静态审计，未对 16039 发任何请求；"中"档 14 条的服务层归属校验未逐一验证（评估者已在报告 §6 声明局限）。
- A 类 47 条的接线优先级排序属产品决策，本轮不排。
