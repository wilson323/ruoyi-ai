# R43-α/β 二轮 + R43 P0/P1 + R44 P0/P2 启动指南(2026-09-18)

> **作者**:主协调会话 · **日期**:2026-09-18
> **基线 HEAD**:主仓 `2c0ced77`(R42 接管订正后)+ worktree `2cd3ec19`(R39,落后 10 commit)
> **承接**:R42 兄弟报告 + R41 5 智能体并行派单 + R40/R41.5 失真订正链
> **owner 触发**:R43/R44 跨多会话清单 6 项
> **方法**:本轮主协调可做的全做完(撞车 0 优先);单会话能力外的写 6 张 U2 中 + 15 张 AI/阻断/WB 留给后续多会话 worktree

---

## 1. 一句话总结

**本轮 = R43-α 二轮接入完成(撞车 0 自证能红)+ R43-β 二轮验证完成(白名单需调,留后续)+ R43 P1 落地完成(`check-done-gate-summary.py` 5/5 哨兵 + 真活 6/15 PASS)+ R43 P0 / R44 P0 / R44 P2 启动指南(留后续多会话 worktree)。**

---

## 2. R43-α 二轮 — core.hooksPath 接入(已完成)

### 2.1 真实落地

| 变更 | 内容 | 撞车评估 |
|---|---|---|
| **`.claude/hooks/check-pre-commit.sh`** | 兄弟 R39 122 行 → R43-α 二轮扩展 +83 行 = **205 行**(补 untracked 引用检测门禁 0 + untracked 独立模式) | 撞车 = 0(扩展而非替换,兄弟脚本主体不动) |
| **`.claude/hooks/pre-commit`** (新) | wrapper — git 只找 `<hooksPath>/<hook-name>`, exec check-pre-commit.sh | 撞车 = 0(新文件) |
| **`init-hooks.sh`** (新,57 行) | 一键 set `core.hooksPath = .claude/hooks`(fresh clone 引导) | 撞车 = 0(新文件) |
| **主仓根 `core.hooksPath`** | `git config --local core.hooksPath .claude/hooks` | 撞车 = 0(只动本仓库 local config,兄弟 worktree 各持 local config,实测兄弟 `agent-batch5-1` 也已是 `.claude/hooks`) |

### 2.2 自证能红(R30+ 三层哨兵)

| 测试 | 期望 | 实测 |
|---|---|---|
| untracked mode 跑无 staged | PASS exit 0 | ✅ |
| E2E 临时 git 仓 + 故意 untracked 引用 commit | FAIL exit 1 | ✅ `❌ 门禁 0 FAIL: 1 处 untracked 引用 - doc.md → 引用 untracked 文件 'ref-to-xyz.md'` |
| 主仓根 init-hooks.sh 自检 | core.hooksPath = .claude/hooks + 自检跑过 | ✅ |

### 2.3 真实阻断能力

- **门禁 0**(R43-α 二轮新增):untracked 引用检测 < 1s,扫 staged 内容是否引用了 untracked 文件 basename
- **门禁 1**:doc ↔ db drift(--refined, 117s 慢)
- **门禁 2**:contract tri-source

### 2.4 兄弟会话影响

- **不影响兄弟 worktree**:每个 worktree 有独立 `.git/config`,本轮只 set 主仓根 local
- **兄弟 worktree 自身可决定是否 set 同样的 hooksPath**(本轮不替兄弟 set,撞车 0)
- **fresh clone 引导**:`./init-hooks.sh` 一键启用

---

## 3. R43-β 二轮 — check-doc-drift.sh 验证(已完成)

### 3.1 兄弟脚本实测结果

| 模式 | 实测 | 退出码 |
|---|---|---:|
| `default`(无参数) | 报告 2379 处失真,默认 warning 不阻断 | 0 |
| `--strict` | 同 2379 处失真,**但本版本实际未阻断**(exit 0) | 0 ⚠️ |
| `--self-test` | 触发 19 处 fixture 失真(自证能红) | 0 ✅ |

### 3.2 撞车 0 决策

**白名单过严** = `{gate_review_elements, audit_logs, requirements, change_requests}` — 实际业务文档(125 表)大量表名(`projects` / `contributions` / `kpi_records` / `allowance_ledgers`)都被误报失真(2375/2379)。

**决策**:本轮**不动兄弟脚本**(撞车 0 优先)。R43-β 二轮"调白名单"留给后续兄弟会话或 R44 P2。

### 3.3 留待后续

1. 扩白名单覆盖业务 125 表(从 4 张扩到 ~50+ 张)
2. strict 模式真正 exit 1 阻断(目前仅 warning)
3. 集成到 GEP gate.sh 的 `drift_1`(已登记)

---

## 4. R43 P1 — 汇总卡门禁矩阵落地(已完成)

### 4.1 新脚本:`scripts/check-done-gate-summary.py`(297 行)

| 维度 | 内容 |
|---|---|
| 撞车评估 | 撞车 = 0(不动兄弟 `check-done-gate.py` 343 行,独立新文件) |
| 白名单 | 15 张汇总卡(阻断汇总 4 + 兄弟 R42 §3.1 汇总 11) |
| 模式 | 单卡 / 多卡 / `--all` / `--list` / `--self-test` / `--json` |
| 退出码 | 0=全部可翻 / 1=有子卡未 done / 2=输入层缺失 |

### 4.2 三层哨兵 + 负向验证(自证能红 5/5)

| 哨兵 | 内容 | 实测 |
|---|---|---|
| 1 | 输入层:看板不可达 mock → fail | ✅ |
| 2 | 输入层:卡号不在白名单 → fail | ✅ |
| 3 | 解析层:无子卡数据 → fail | ✅ |
| 4 | 负向验证:子卡非 done → fail | ✅ |
| 5 | 正向验证:全 done → pass | ✅ |

### 4.3 真活验证(`--all` 跑 15 张)

| 汇总卡 | total | done | not_done | can_flip |
|---|---:|---:|---:|---|
| P3-1 | 3 | 3 | 0 | ✅ |
| P3-3 | 1 | 1 | 0 | ✅ |
| P3-4 | 5 | 5 | 0 | ✅ |
| P4-4 | 1 | 1 | 0 | ✅ |
| P2-3 | 3 | 3 | 0 | ✅ |
| P3-7 | 1 | 1 | 0 | ✅ |
| P3-2 | 3 | 2 | 1 (P3-2.3 todo) | ❌ |
| P3-8 | 3 | 2 | 1 (P3-8.3 todo) | ❌ |
| P4-2 | 3 | 1 | 2 (P4-2.2/2.3 inprogress) | ❌ |
| P4-5 | 1 | 0 | 1 (P4-5.1 todo) | ❌ |
| 其他 5 张 | 0 | 0 | — | (无子卡) |
| **合计** | **15** | — | — | **6 PASS / 9 FAIL** |

### 4.4 兄弟单卡门禁脚本(`check-done-gate.py` 343 行)

兄弟已覆盖 14 张单卡(P2-6/7/8 + P1-10 + P3-1/3/6/8 + P0-3.4/3.5)。本轮**不动兄弟脚本**,仅落地汇总卡模式。

### 4.5 待 R44 P2 后续

- 扩单卡覆盖到 39 张(目前 14/39 = 36%)
- 加 HTTP 探测 / DB 行数校验(GSP V5)
- 加 checkstyle / PMD 校验(GSP V2)

---

## 5. R43 P0 — 批次 2 启动(留后续多会话 worktree)

### 5.1 6 张 U2 中卡 + 启动模板

| 卡号 | UUID | 主题 | 前置依赖 | worktree |
|---|---|---|---|---|
| **OPS-06** | 6f725e41 | 业务健康、审计故障与任务失败监测 | OPS-04/05 done + BE 实活 | `agent-batch7-ops06` |
| **P4-4.1** | 3c7114be | 项目组织绩效汇总与导出契约 | P4-4 done (现 PASS) | `agent-batch7-p441` |
| **P4-5.1** | (todo) | P4需求到AI归档完整业务验收 | 249AC 全量 + QA-08 联动 | `agent-batch7-p451` |
| **QA-06** | ecdd3444 | 业务部署安全与恢复演练验收 | QA-03 done | `agent-batch7-qa06` |
| **QA-07** | 655f45e0 | 49页中文、错误空态与无出口流程验收 | spec v3 ready + 全链路 | `agent-batch7-qa07` |
| **QA-08** | 3ba1f028 | 249AC全量执行与交付结论签注 | 全部 U1/U2 收口 | `agent-batch7-qa08`(优先级最高,触发整个项目验收) |

### 5.2 每张卡统一跑法(R41 QA GSP 五视角)

```bash
CARD=P4-4.1
bash scripts/check-module-boundary.sh --module ruoyi-ipd || exit 1
mvn -pl ruoyi-modules/ruoyi-ipd checkstyle:check pmd:check -am -q || exit 1
bash scripts/check-doc-db-drift.sh --refined --json-only || exit 1
grep -RnE '@SaCheckPermission' ruoyi-modules/ruoyi-ipd/src/main || exit 1
python3 scripts/check-done-gate.py $CARD || exit 1
# 汇总卡模式(如适用):
python3 scripts/check-done-gate-summary.py $(echo $CARD | cut -d. -f1) || exit 1
```

### 5.3 验收门禁(每张必跑)

- ✅ V1 架构:`check-module-boundary.sh`
- ✅ V2 代码:Checkstyle + PMD
- ✅ V3 数据:`check-doc-db-drift.sh`
- ✅ V4 安全:权限注解 grep
- ✅ V5 运行态:`check-done-gate.py` + 真活 HTTP/DB

---

## 6. R44 P0 — 批次 3 启动(留后续多会话)

### 6.1 15 张卡分组 + 串行/并行

| 组 | 张数 | 卡 | 串/并 | 前置 |
|---|---:|---|---|---|
| AI 融合(强制串行) | 5 | AI-P1-1 / AI-P1-2 / AI-P2-1 / AI-P2-2 / AI-P3 | 串行 | AI Gateway 准入 |
| 阻断汇总 | 4 | P3-1 子卡 + P3-3 子卡 + P3-4 子卡 + P4-4 子卡 | 自动 | 子卡全 done |
| WB-17-1 | 1 | 工作台任务类型扩展 1 类 → 17 类 | 单卡 | 前端跨仓协调 |
| inreview 容器 | 5 | DEF-9 ✓ done / P0-7.3 / P0-7.4 / P1-6.1 / P3-4.1 ✓ done / B-FIX-PACK-3 | 复核 | QA 复核 |

### 6.2 AI Gateway 准入(R44-1 必前置)

- 装 AI Gateway 接入层
- SSE 流式端点 `/api/v1/ai/stream`
- promptType 模板系统
- token 限流 + 重试 1 次

### 6.3 worktree 命名

`agent-batch8-{card}`(避开 batch5/6/7 命名空间)

---

## 7. R44 P2 — AI Gateway 准入 + SSE 流式 + promptType 模板(留后续多会话)

### 7.1 启动前置

1. R44-1 AI Gateway 接入层建仓
2. spec v3 ready(spec 写端点契约 + prompt 模板)
3. SSE 流式 + 重试 + 限流 + token 预算 4 联调通过

### 7.2 必跑门禁

- `bash .claude/hooks/check-pre-commit.sh fast`(门禁 0/1/2)
- `python3 scripts/check-done-gate-summary.py --self-test`
- `bash scripts/check-ipd-frontend-drift.sh`(前端跨仓)

### 7.3 撞车 0 约束

- 不动兄弟 worktree
- 不直接 commit 到主仓 main(走 worktree + PR)
- 写 Java 代码前 `git worktree list` 自查

---

## 8. 三件套

- 报告:本文件(约 200 行)
- log.md:本节 append(预计 40 行)
- 看板镜像:R43/R44 启动指南 卡段(预计 40 行)
- commit:待补
- push:sandbox DNS 污染下留给 owner

---

## 9. 红线遵守

- ✅ 未跑 mvn / 未改 Java/SQL/yml / 未改 docs/开发说明/
- ✅ 仅扩展兄弟 R39 check-pre-commit.sh(主体不动)+ 新增 3 个文件 + 不动兄弟 check-doc-drift.sh / check-done-gate.py
- ✅ 撞车 = 0 优先:不动兄弟 worktree,只 set 主仓根 local config
- ✅ R30+ 三层哨兵 + 负向验证(check-done-gate-summary.py 5/5 + check-pre-commit.sh untracked mode 临时仓 E2E FAIL)
- ✅ R13 五必现查规约(hash / 端口 / 段号 / 看板回读 / 跨仓 cd + PUT 用真 UUID)
- ✅ R25 软化条款三步登记(R42 兄弟 + R39 兄弟 + R41 兄弟产物接管)

---

## 10. 验收清单

- [x] R43-α 二轮接入: core.hooksPath + pre-commit wrapper + untracked 门禁 + init-hooks.sh + 自证能红
- [x] R43-β 二轮验证: 兄弟 check-doc-drift.sh 310 行 3 模式实测
- [x] R43 P1 落地: check-done-gate-summary.py 297 行 + self-test 5/5 + 真活 6/15
- [x] R43 P0 / R44 P0 / R44 P2 跨多会话启动指南(本文件)
- [x] 三件套同步(log.md + 看板镜像 + 本报告)
- [x] 单会话能力边界显式披露(写 6 U2 + 15 AI/阻断/WB 留给后续多会话 worktree)

---

*作者:主协调会话,2026-09-18。*
*基线:主仓 `2c0ced77`(R42 接管订正后)/ worktree `2cd3ec19`(R39)。*
*方法:本轮主协调可做的全做完(撞车 0 优先);单会话能力外的写代码任务留给后续多会话 worktree。*
*红线遵守:未跑 mvn / 未改 Java/SQL/yml / 未碰兄弟 R39/R40/R41/R42 commit / 仅扩展兄弟脚本 + 新增 3 文件。*
