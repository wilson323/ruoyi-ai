# Pointer #133 — BCP 飞轮齿位

> **类型**：飞轮层
> **创建时间**：2026-09-20
> **基线**：R131 落地后（R131 §四 飞轮 + R131 §二.2.3 触发态）
> **严重度**：🟢
> **触发**：`.harness/evolve/flywheel.sh`（H-13 候选）每 N 分钟扫 17 根指针
> **修复路径**：飞轮调度器 = 17 根指针 × 3 元组 × N 分钟扫

## Why（为何需要）
R131 §四 飞轮设计要求指针驱动：每个指针的"触发"映射到一个 BCP（Bash/Commit/Push）三联操作，飞轮 = 周期扫描 + 命中即执行。但当前是文档形态，未运行态。BCP 齿位 = 把"指针 → 工具 → 操作"三元组对齐齿轮，确保飞轮每转一圈都能咬合。

## How（如何触发）
```bash
# 飞轮扫描（H-13 候选）
bash .harness/evolve/flywheel.sh scan
# 触发态：每 30 分钟 cron 调一次 → 命中指针触发 BCP → 写 log.md
```

## Link（关联）
- #129-#132 + #134/#135（飞轮扫描对象 = 17 根指针全集）
- R131 §四 飞轮
- R131 §二.2.3 触发态

## 自证能红
故意改 cron 频率到 1 分钟 → 跑 → 应输出"BCP 命中指针 #NNN → 操作 XYZ"
